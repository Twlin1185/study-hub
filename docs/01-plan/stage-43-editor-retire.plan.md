# Stage 43 — 구 편집기 퇴역 실행(N-2) + 노트 v2 정식 승격 + 철저 검증 (v2.0.x 핵심 → **v2.01.1**)

> 상태: **구현·자동 검증·문서(묶음 D) 완료(2026-09-01)** — **잔여 = V-2·V-3 브라우저 실측(에이전트 — Chrome 확장 미연결로 미수행) + DoD 7 사용자 실사용(PC+폰) → 게이트 통과 시 v2.01.1 발행(§7)**. 경위 = §8 완료 기록.
> **버전 영향: 핵심**(CHANGELOG 규약 ③ — 기능 퇴역 + 새 기능 표면 승격. 게이트 통과 시 **v2.01.1** 발행 = 2.00.1 → MM+1·PP=1).
> 생성 경위: 사용자 원문(2026-08-24) — *"기존 노트기능을 제거하고 새로 만든 노트기능을 이식해서 완성하고
> 철저한 검증을 진행한 후에 2.0.1버젼으로 가는거야."* → **해석 ⓐ로 확정**(구 편집기 = DocEditor 소스 폼·
> `MarkdownFieldEditor`·`EditablePreview`·퇴로 토글 — stage-38 규약 C 대상 정의. 근거: 이후 문서인
> stage-index 43행 제목("구 편집기 퇴역 + 새 노트 편집기 완성")과 stage-45 억제기 판정표가 이 해석을 전제로
> 편성됐고 그 v2.00.1 게이트를 사용자가 통과시켰다 — 2026-09-01 편성 시 확정). 실행 조건(stage-40·41 중
> 추가 수정사항 미발견)은 사용자가 stage-44·45 종결 후 **"다음 = stage-43"으로 확정하며 충족 처리**.
> 번호는 stage-42(반입 결함 4건 선행)에 밀려 42→43 이월(별지 머리말 정본).
> 정본 포인터: 퇴역 판정·대상 정의 = `stage-38-mobile-v2-closeout.plan.md` 규약 C·말미 G-10(본 문서 §3에
> 전사) · N-2 경위 = `editor-v2.plan.md` 머리말·§9 R38 행 · 승격 표면 = screens §5.16(편성 추기 = Design
> v1.52 · 구현 후 실측 재개정 = v1.53 예정) · 문서 규약 소멸 6건 = `stage-45-docs-index-version-scheme.plan.md`
> 억제기 판정표(이관 의무 명기 — 본 문서 §3.3이 이행 정본).

## 1. 범위 (3축)

| 축 | 내용 |
|---|---|
| ① 퇴역 | 구 편집기 코드 제거 — DocEditor 소스 폼(textarea) 경로·`MarkdownFieldEditor`·`EditablePreview`·퇴로 토글(`docEditorPreference`) 전부(§3.1 대조표) |
| ② 승격 | 노트 v2(§5.16 실험실 격리 표면)의 정식 표면 승격 — 사이드바·모바일 드로어 노출·베타 딱지 해제·설정 실험실 카드 제거(§2 규약 B·C) |
| ③ 철저 검증 | 빌드·테스트 전체·불변 검사·브라우저 실사용(진입점 전수·미전환 문서·노트 루프) + 사용자 실사용(PC+폰) — §4 묶음 V·§6 DoD |

**백엔드 코드 diff 0이 목표다** — 서버 계약(§4.28·§4.29)은 무변, 백엔드 묶음은 무접촉 검증으로 구성.

## 2. 확정 규약 (착수 전 결정 — 위임 판정, 이 문서가 정본. 최종 재론 경로 = DoD 7 사용자 실사용)

- **A. 퇴역 범위 = stage-38 규약 C 대상 정의 그대로**(§3.1 전사 + 2026-09-01 코드 실존 확인). 확장·축소 금지 —
  대조표에 없는 파일을 지우거나 비대상을 건드리면 중단·보고.
- **B. 승격 노출 방식**: **데스크톱 사이드바에 '노트' 항목 1개 + 모바일 좌측 드로어에 '노트' 항목 1개**
  (`Layout.tsx` — 드로어는 "향후 항목이 필요해지면 늘린다"로 예비된 자리 · FB-8 정합). **하단 탭바 5개
  불변**(F38·F39 관례 — 탭 추가 금지). `/notes`·`/notes/:id` **lazy 청크 유지**(R37 — 사이드바 링크는
  라우트 진입일 뿐 초기 청크에 편집기를 싣지 않는다). 홈 대시보드 무변.
- **C. §5.16 승격 전제의 부분 개정**(G-9/N-1 기록 "정식 승격 = 검색(FTS)·인쇄·백업 UX 통합 선행 필요"의
  번복 절차): 3건 중 **백업 = 기충족**(VACUUM INTO가 DB 전체 포함 — 코드 0) · **전역 검색(FTS) 통합·인쇄
  통합 = v2.x 백로그로 이월**하고 승격 차단 조건에서 해제한다. 사유: ① 승격 자체가 사용자 확정(원문 —
  N-1 ⓒ "노출 현상 유지"의 상위 결정) ② 노트 자체 검색(§4.28 ④ LIKE)이 기존재해 실사용 공백이 아니다
  ③ 전역 검색·인쇄의 노트 수요는 실측 0 — YAGNI. 이월 등재는 별지 §13 관례(코드 0·등재만, D-5에서 수행).
- **D. 미전환·변환 불가 문서의 퇴로 소멸 후 처리**(규약 C 비대상 "§4.29 하위 호환 계약 — 실행 시점 재론"의
  재론 종결): **서버 계약(§4.29 — content 단독 문서 수용) 무변**. 클라이언트는 종전대로 미전환 문서를
  메모리 변환해 새 편집기에 올리되, **변환 불가(미지원 보고 발생) 시 = 본문·해설 편집 진입 차단 + 명시
  안내**(읽기·나머지 폼 필드 편집은 가능 — 조용한 변형 0). 근거: s35-realdoc 실문서 폴백 0/440(검토자
  재실행 재확인) — 실표본 0인 경로에 구 편집기를 존치할 이유가 없다. 차단이 실제 발생하면 **강등이 아니라
  보고 항목**(사용자 보고 후 후속 판단).
- **E. FB-14 편입 · FB-15 관찰만**(별지 §13 — stage-43 범위 판단 명기): **FB-14**(콜아웃 자식 그룹 시각
  소속 CSS 매칭 0 — main 기존·stage-41 실측)는 "철저 검증"에서 콜아웃 시각 확인이 통과 불가한 상태라
  **이 단계에 편입**(columns 전례 `:has(…)` 선택자 치환 — variant 포함 5규칙·CSS만). **FB-15**(React
  #185 1회 관찰·재현 미특정)는 **수정 착수 없음** — V-2 검증 중 재현 시 보고만.
- **F. localStorage 잔재**: `docEditorPreference` 키는 모듈 삭제 후 읽는 코드 0 — **정리 코드 금지**(YAGNI·
  잔존 무해). stage-39 스냅샷 키(`studyhub.note/doc.snapshot.<id>`)는 현행 기능 — 무접촉.
- **G. 신규 의존 0**(D10 — GPL 금지 불변) · **DDL 0**(notes·documents 스키마 무변 — 불변 규칙 6) ·
  색상 토큰만(불변 규칙 5).

## 3. 퇴역 대조표 (정본)

### 3.1 코드 퇴역 대상 (규약 C 대상 정의 전사 + 2026-09-01 실존 확인)

| 대상(규약 C) | 실존 위치(확인됨) | 처리 |
|---|---|---|
| DocEditor **소스 폼(textarea) 경로** | `components/DocEditor.tsx` — `isDocBlockEditorEnabled` 분기 + `MarkdownFieldEditor` 렌더 2곳(본문·해설) | 분기 제거 — 새 편집기(`DocFormBlockFields` lazy) 상시화. **DocEditor 파일 자체는 존치**(아래 3.2) |
| `MarkdownFieldEditor`(S26~S30 방언 폼 편집기) | `components/MarkdownFieldEditor.tsx` | **파일 삭제** |
| `EditablePreview`(F55/S27 블록 편집·S30 리치 표면 — 내부 `wysiwyg` 퇴로 플래그 포함) | `components/EditablePreview.tsx` | **파일 삭제**(S30 퇴로 플래그도 동반 소멸) |
| 퇴로 토글 `docEditorPreference` | `editor2/lib/docEditorPreference.ts` + `pages/Settings.tsx`(`DocBlockEditorToggle`) + `pages/DocumentDetail.tsx`(`useDocBlockEditorEnabled`) | 모듈 삭제·토글 UI 제거·분기 제거 |
| 잔존 참조·주석 | `api/uploads.ts` 주석 등 — 구현 시 grep 전수 | 정리(참조 0이 DoD 5) |

### 3.2 비대상 — 존치 (규약 C 전사. 접촉 금지)

- **`MarkdownView`**(리더) — D5 재보류 확정(블록 네이티브 리더는 v2.x 후보) · Markdown **프로젝션**
  (저장 동반 계약 §4.28 ③·§4.29).
- **§4.29 미전환 하위 호환 계약(서버)** — 규약 D로 재론 종결·무변.
- **DocEditor 폼 껍데기** — 제목·type·`choices`·정답·난이도·태그·스타일 필드(F37 공용 폼 — D7 1차 확정
  "필드 유지"의 숙주. 새 편집기 본문·해설 위젯이 그 안에 들어앉는다).
- **editor2 계열 전부**(`DocFormBlockFields`·`DocBlockEditor`·notes 표면·어댑터·변환기) — 이번 승격 대상.
- `DocStyleFields`(F53)·stage-39 [저장]/[취소]·스냅샷 계약.

### 3.3 문서 규약 소멸 6건 (stage-45 판정표 이관 의무 — 본 절이 소멸 계획 정본. 이행 = 체크리스트 묶음 D)

| # | 소멸 규약 | 현 위치 | 소멸 계획 |
|---|---|---|---|
| ① | 구 편집기 무접촉·개선 금지(stage-36 규약 A) | `stage-36-surface-unify.plan.md` §규약 A·"하지 않는 것" + `editor-v2.plan.md` §9 R38 행의 "무접촉 유지" 문구 | 퇴역 실행 자체가 규약을 무의미화 — **R38 행 완결 추기**(D-1)로 소멸 선언. stage-36 문서 원문은 무수정(완료 이력 보존) |
| ② | 무기한 병존 금지(R38 · R41 대응 ① 게이트 상한) | `editor-v2.plan.md` §9 R38·R41 행 | R38 행에 "stage-43 퇴역 실행 완료 — 병존 종결·리스크 소멸" 추기(D-1) · R41 대응 ① 병존 상한 종결 추기(D-2) |
| ③ | 드래프트 격리 D9 문구 | `editor-v2.plan.md` §10 D9 행·§12 본문 | D9 행에 상태 1줄 추기 — "격리 종료(stage-43): 노트 v2 정식 승격·구 편집기 퇴역으로 개발 방식 목적 달성"(D-3). **§12 본문 재작성은 하지 않는다**(ev2 본문 재편 시 — stage-45 명기·§5 범위 밖) |
| ④ | 노트 정식 메뉴 미노출(검증 전 데이터 유입 억제) | screens §5.16 "노출 방식" · `editor-v2.plan.md` §9 R41 대응 ⑤ | §5.16을 승격 계약으로 실측 재개정(D-5 — Design v1.53) · R41 ⑤ 해제 추기(D-2). 편성 추기(v1.52)는 본 지시서 생성과 함께 기반영 |
| ⑤ | N-2 보류·예정(조건부) 딱지 | `editor-v2.plan.md` 머리말·§9 R38 행 | 완료 추기 — "N-2 실행 완료(stage-43 · 일자)"(D-1·D-7) |
| ⑥ | M31~M36 로드맵 게이트 문구(전 게이트 종결 — 사망 문구) | `editor-v2.plan.md` §8 로드맵 초안 표 | §8 머리에 1줄 선언 추기 — "전 단계 게이트 종결(v2.00.0)·구 편집기 퇴역 완료(stage-43) — 이 표는 이력"(D-4). 행 원문 무수정 |

## 4. 체크리스트 (묶음 = sonnet 구현 에이전트 단독 수행 단위. 권장 순서 G → F → B·V → D)

### 묶음 B — 백엔드 (무접촉 검증 — 서로 독립·프론트 묶음과 병렬 가능)

- [x] B-1. **계약 무변 스모크** — §4.28(notes CRUD·`content_blocks`+`content` 동반)·§4.29(documents 블록
      저장·미전환 content 단독 수용)·resolve-embeds 봉인(불변 규칙 1 — 정답·해설 필드 부재) 응답 확인.
- [x] B-2. `scripts/run-tests.ps1` **전체** 통과(611+ 기준 무회귀) — 변환 코퍼스(s30·s35-realdoc·s41 계열) 무변.
- [x] B-3. `scripts/invariant-scan.ps1` **PASS**(신규 위반 0).
- [x] B-4. **백엔드 diff 0 확정** — `git diff -- backend` 빈 결과. diff 발생 시 즉시 중단·보고(이 단계에
      백엔드 변경 권한 없음).

### 묶음 G — 프론트 ①: 노트 v2 정식 승격 (규약 B·C)

- [x] G-1. **사이드바 '노트' 항목 + 모바일 드로어 '노트' 항목**(`Layout.tsx` — 탭바 5개 불변·드로어는
      항목 1개 추가만·전면 확장 금지). 라우트 lazy 무변(R37).
- [x] G-2. **베타 딱지 해제** — `/notes` 목록 제목 "노트 (베타)" → "노트" · 안내 문구("시험용 저장소…")를
      정식 문구로 갱신 · `App.tsx` 격리 주석 갱신("진입은 설정 실험실 카드뿐" 서술 소거).
- [x] G-3. **초기 청크 실측** — 빌드 후 증감 기록(노트 편집기·Mantine이 초기 청크에 새로 편입되지 않았음을
      수치로 — R37 한도 준수).

### 묶음 F — 프론트 ②: 구 편집기 퇴역 (§3.1 대조표 — G-1 이후 착수 권장: 노트 진입점 대체 확보 후 카드 제거)

- [x] F-1. **DocEditor 분기 제거** — `isDocBlockEditorEnabled` 판정·`MarkdownFieldEditor` 렌더 경로 제거,
      `DocFormBlockFields`(lazy) 상시화. **변환 불가 시 규약 D 차단+안내** 구현(조용한 변형 0 — 저장
      경로에 미변환 본문이 실리지 않음을 확인).
- [x] F-2. **`MarkdownFieldEditor.tsx`·`EditablePreview.tsx` 파일 삭제** + import·타입·주석 잔존 참조
      정리(`frontend/src` grep 0건 — DoD 5).
- [x] F-3. **퇴로 토글 전면 제거** — `editor2/lib/docEditorPreference.ts` 삭제 · `Settings.tsx`
      `DocBlockEditorToggle` + **실험실(베타) 카드 자체 제거**(내용물 = 토글+노트 링크뿐 → 전부 대체됨.
      F38 6그룹 수 불변 — 그룹 ⑥ 안의 카드 1개 감소) · `DocumentDetail.tsx` `useDocBlockEditorEnabled`
      분기 제거. localStorage 정리 코드는 만들지 않는다(규약 F).
- [x] F-4. **FB-14 수정**(규약 E) — `notes.css` 콜아웃 자식 그룹 5규칙을 columns 전례
      `.bn-block:has(> :not(.bn-block-group) [data-content-type='callout']…) > .bn-block-group` 형태로
      치환(variant 3규칙 포함) — 브라우저에서 매칭 성립 실측.
      *(부기 2026-09-01: 형제 결합자 4규칙 `:has()` 치환 + head 둥글림 1규칙 자손 결합자 완화로 구현.
      같은 날 V-2 ⓓ 실측으로 매칭 성립 전건 확인(§8 7항) — 검토 경미 ②의 "자식 0 콜아웃" 우려는
      BlockNote content model상 도달 불가(자식 ≥1 강제·빈 콜아웃 Backspace = 블록 삭제)로 해소·원복 불요.)*

### 묶음 V — 철저 검증 (구현 후 · 서버 구동 금지 — 브라우저는 사용자가 띄운 `localhost:8000`만)

- [x] V-1. `npm run build` 성공(성공/실패만 보고) + G-3 청크 수치.
- [x] V-2. **(2026-09-01 실측 완료 — 확장 복구 후 재시도·ⓐ~ⓕ 전건 통과·신규 결함 0. §8 7항)** **브라우저 실사용(PC)** — ⓐ 문서 편집 진입점 6곳 전수(stage-36 F-2 표 재실행 — 토글 부재
      상태에서 신규 작성·모달·창·커리큘럼·탐색·문서 상세 각 편집→저장→재진입 확인) ⓑ 미전환 문서
      편집(메모리 변환 경로)·저장 후 왕복 무손실 ⓒ 노트 — 사이드바 진입·[새 노트]·자동저장·[저장]/[취소]·
      스냅샷 복구 ⓓ 콜아웃 시각 소속(F-4)·columns·툴바 회귀 ⓔ 콘솔 관찰 — FB-15(React #185)·FB-11 ②
      (코드 블록 언어 에러) 재현 시 **보고만**(수정 착수 금지).
- [x] V-3. **(2026-09-01 실측 완료 — iframe 390px 에뮬·ⓖ~ⓘ 전건 통과. §8 7항)** **모바일 에뮬(390px)** — 드로어 '노트' 진입·편집 표면 적층·도킹 툴바·탭바 5개 불변 확인.
      실기기는 DoD 7(사용자).

### 묶음 D — 문서 (병존 규약 소멸 §3.3 + 재개정 — 구현·검증 종료 후)

- [x] D-1. `editor-v2.plan.md` §9 **R38 행 완결 추기**(§3.3 ①②⑤ — 퇴역 실행 완료·병존 종결·무접촉 규약
      소멸·N-2 종결).
- [x] D-2. 같은 §9 **R41 행 추기**(§3.3 ②④ — 대응 ① 병존 상한 종결·⑤ 정식 메뉴 미노출 해제).
- [x] D-3. 같은 §10 **D9 행 상태 1줄 추기**(§3.3 ③ — §12 본문 무접촉).
- [x] D-4. 같은 §8 머리 **이력 선언 1줄 추기**(§3.3 ⑥).
- [x] D-5. **설계 실측 재개정 = Design v1.53**(색인 헤더 갱신 포함) — screens **§5.16**(승격 계약: 노출
      방식·베타 문구 해제·규약 C 개정 사유 기록) · **§5.3**(편집 진입 분기 — 퇴로 토글 서술 소거·변환 불가
      차단 계약) · **§5.11**(설정 — 실험실 카드 제거). + 별지 §13에 "노트 전역 검색(FTS)·인쇄 통합 =
      v2.x 후보" 이월 등재(규약 C — 코드 0).
- [x] D-6. **매뉴얼 갱신**(`docs/manual/user-manual.html`) — 노트 정식 안내·구 편집기/퇴로 토글 서술 제거.
- [x] D-7. `editor-v2.plan.md` **머리말 완료 추기**(N-2 실행 완료·stage-43) + 본 문서 완료 기록·체크박스.

## 5. 이 단계에서 하지 않는 것

- **백엔드·스키마**: 서버 코드 변경 0 · DDL 0(notes·documents 무변 — 불변 규칙 6) · notes→documents 이관
  (N-1 ⓐ/ⓑ — v2.x 캡처 계획서 시점 재론 불변).
- **v2.x 이월분**: D8 휴지통·고아 이미지 정리 구현(결정만 확정된 상태 유지) · D11-ⓐ 참조 복사(v2.x 이월)·
  ⓑ 양방향 편집(착수 금지) · 캡처 파이프라인(별지 §5.5 — 설계만 존재) · FB-7(YouTube 재생·영상 저장) ·
  FB-11 ⓑ 구문 강조·복사 버튼 · 노트 복제(FB-2 잔여) · **노트 전역 검색(FTS)·인쇄 통합**(규약 C — 백로그
  이월·등재만) · 블록 네이티브 리더(D5 재보류 불변).
- **FB-8 전면 드로어 확장**(홈·커리큘럼 등) — 이번에는 '노트' 1항목 추가만. 전면 확장은 별도 편성.
- **FB-15·FB-11 ② 수정** — 관찰·보고만(규약 E).
- **`editor-v2.plan.md` 본문(§1~§14) 분해·재편** — stage-44가 "퇴역 후 재론"으로 남긴 것. **이번 범위
  아님으로 판단**(사유: 코드 퇴역·검증 stage에 문서 대수술을 섞으면 검증 실패 시 되돌림 범위가 커진다 —
  묶음 D는 규약 소멸 추기만. 재론 = v2.01.1 발행 후 별도 사소 stage 후보).
- **신규 의존 추가 0**(D10 — GPL 금지·public 저장소).

## 6. DoD (완료 정의)

**자동 검증(에이전트 수행):**
1. `npm run build` 성공 + 초기 청크 R37 한도 내(수치 기록).
2. `run-tests.ps1` 전체 통과(무회귀) · `invariant-scan.ps1` PASS.
3. **백엔드 diff 0**(`git diff -- backend` 빈 결과).
4. 브라우저 확인(V-2·V-3) — 진입점 6곳·미전환 문서·노트 루프·FB-14 매칭 전건 통과.
5. **잔존 참조 0** — `frontend/src`에서 `MarkdownFieldEditor|EditablePreview|docEditorPreference` grep 0건
   (dist 제외 — 토큰 규약 1).
6. 문서 묶음 D 전건(§3.3 소멸 6건 이행 포함) + Design v1.53 반영.

**사용자 실사용 확인(PC+폰 — 게이트 본체):**
7. ⓐ 문서 편집 — 대표 진입점(문서 상세·신규 작성·커리큘럼 모달)에서 편집→저장→재진입, 미전환 문서 포함
   ⓑ 노트 — 사이드바/드로어 진입·작성·[저장]/[취소] ⓒ 폰 실기기 — 드로어 진입·편집·저장
   ⓓ **치명 결함 0 회신**(구 편집기로 되돌리고 싶은 상황 발생 시 즉시 보고 — 퇴로가 없으므로 치명 발견
   시 수정이 발행에 선행).

**게이트**: 1~6 전건 + 7 치명 0 → **v2.01.1 발행**(§7). 치명 발견 시 발행 보류·수정 선행.

## 7. 완료 시 절차 (발행 — CHANGELOG 규약 ⑤)

1. `docs/03-release/CHANGELOG.md` 1항목(stage-43) 추가.
2. 루트 `VERSION` = `2.01.1`.
3. `stage-index.md` 43행 갱신(완료·일자·산출 버전 v2.01.1).
4. `CLAUDE.md` 머리 버전 줄 1곳(경위 전재 금지).
5. git tag `v2.01.1`(머지 커밋 대상 — v2.00.0 전례).

## 8. 완료 기록 (구현·검증·문서 경위 정본 — 게이트 판정·발행은 아래 "잔여" 종결 후 추기)

**경위 (2026-09-01)**

1. **편성**: 지시서 생성(같은 날) — 해석 ⓐ 확정·실행 조건 충족 처리·확정 규약 A~G·§3 대조표·§3.3 소멸 계획 확정(머리말 참조).
2. **묶음 G·F 구현 완료(frontend-dev)**: 파일 삭제 3건(`MarkdownFieldEditor.tsx`·`EditablePreview.tsx`·`editor2/lib/docEditorPreference.ts`) · `DocEditor.tsx` `isDocBlockEditorEnabled` 분기 제거 — `DocFormBlockFields`(lazy) 상시화 · **변환 불가 시 규약 D 차단 + 읽기 전용 배너 구현** · `Layout.tsx` 사이드바 '노트'(📓) + 모바일 드로어 '노트' 항목(탭바 5개 불변·lazy 무변) · `Settings.tsx` 실험실(베타) 카드 전체 제거(그룹 6개 불변) · `DocumentDetail.tsx` 분기 제거 · 베타 딱지 해제(목록 제목·탭 제목·격리 주석) · **F-4 = FB-14 콜아웃 치환 — 형제 결합자 4규칙 `:has()` + head 1규칙 자손 결합자 완화**(규약 E — 5규칙 전부 `:has()`는 아님·실측 잔여, 유보 ② 참조).
3. **묶음 B·V-1 검증 — 자동 전건 통과**: `npm run build` 성공 · **초기 청크 index.js −56,068B(−54.8KB)·css −368B — R37 증가 0(순감소)**(G-3·V-1) · `run-tests.ps1` **611 passed**(무회귀 — B-2) · `invariant-scan.ps1` **PASS**(B-3) · **백엔드 diff 0**(B-4) · DoD 5 grep(`MarkdownFieldEditor|EditablePreview|docEditorPreference`) **0건**. **B-1 계약 스모크 통과**(임시 포트 8917 — 종료·리스너 부재 확인 완료): §4.28 notes CRUD 왕복(동반 계약 `projection_required`·블록 스키마 검증 발동 확인) · §4.29 문서 상세 `content`+`content_blocks`+`blocks_version` 응답 · resolve-embeds 응답 스키마에 answer·explanation 부재(불변 규칙 1 봉인).
4. **V-2·V-3 브라우저 실측 = 1차 미수행 → 같은 날 완료(7항)**: 1차 시도는 Claude Chrome 확장 미연결(원인 = Chrome 미실행 — 사용자 재시작 후 복구)로 미수행. 사용자 회신 후 재시도해 전건 통과(아래 7항).
5. **묶음 D 문서 전건 이행(D-1~D-7)**: `editor-v2.plan.md` §9 R38·R41 완결 추기 + §10 D9 격리 종료 1줄 + §8 머리 이력 선언 + 머리말 N-2 실행 완료 추기 + §13 이월 등재(전역 검색(FTS)·인쇄 통합 = v2.x 후보 — 규약 C·코드 0) · **Design v1.53**(screens §5.16 승격 계약 재개정·§5.3 퇴로 서술 이력화+변환 불가 차단 계약·§5.11 실험실 카드 제거 · 색인 헤더 승급 — v1.49 괄호 줄은 `docs/04-archive/design-changelog.md` 맨 위로 원문 이동) · 매뉴얼(`user-manual.html`) 노트 정식 안내(화면 지도 행·4장 소절·설정 6그룹)·구 편집기/퇴로 토글/실험실 카드 서술 제거. §3.3 소멸 6건 전건 이행.

**유보 2건 (보고만 — 후속 판단)**

- ① `RichBlockEditor.tsx`가 `EditablePreview` 삭제로 고아화 — §3.1 대조표 밖이라 규약 A대로 **존치**(삭제 여부는 후속 판단).
- ② FB-14 5규칙 중 head 둥글림 1규칙은 소스 추적상 원래 정상 추정(형제 결합자 4규칙만 실제 깨짐) — 5규칙 모두 치환했고 실측 판정은 잔여(V-2 ⓓ).

6. **Opus 검토(stage-reviewer) = 조건부 통과 — 치명 0·중요 0·경미 4**(2026-09-01): DoD 자동 항목(1·2·3·5·6) 전건 실충족 재확인(재빌드·재테스트·대조표·규약 A~G·§3.2 무접촉·조용한 변형 0 코드 추적 포함). 경미 = ⓐ 규약 D 차단 경로의 "기존 편집기로 여는 중…" 문구 2곳(`DocFormBlockFields.tsx:108`·`DocBlockEditor.tsx:99` — 1렌더 노출) + 동류 주석 5곳 ⓑ FB-14 head 규칙 자손 결합자 완화가 규약 E 형태와 다르고 자식 0 콜아웃에서 신규 시각 결함 가능성(실측 전 판정 불가 — `>` 원복 또는 `:has` 조건화 권고) ⓒ F-4 체크박스 선행(→ 같은 날 [ ]+부기로 정정) ⓓ 고아 모듈 2개(`RichBlockEditor.tsx`+전용 의존 `markdown/richSurface.ts`) 후속 등재 부재. 미확인 의심 1(변환 불가 문서 잠금 저장 시 §4.29 ④ 전환 해제 — 기존 동작과 동일·회귀 아님·실표본 0/440). **경미 4건의 수정 여부 = 사용자 결정**(워크플로 규약 — 보고만).

7. **V-2·V-3 브라우저 실측 완료 — 전건 통과·신규 결함 0**(2026-09-01 · 임시 서버 8917(스크래치 DB) — 종료·리스너 부재 확인): ⓐ 진입점 6곳 전수 통과(전부 블록 편집기·textarea 소스 폼 0·미전환 `/docs/1` 잠김 배너 없이 정상 전환·저장 왕복 반영) ⓑ 미전환 `/docs/3`(표·아래첨자 포함) 왕복 무손실 ⓒ 노트 루프(사이드바 진입·베타 딱지 없음·자동저장·[저장]/[취소] — 취소 = 문서화된 스냅샷 복원 동작) ⓓ **FB-14 매칭 성립 전건 확인**: `:has()` 4규칙 실매칭(배경·좌보더 4px·둥글림·variant 색 적용) + head 규칙 정상 적용. **경미 ② 해소** — "자식 0 콜아웃"은 BlockNote content model상 도달 불가(자식 ≥1 강제·빈 콜아웃 Backspace = 블록 자체 삭제) → 완화 무해·원복 불요 판정. 관찰 1(기존 제한·결함 아님): 기본 variant의 자식 그룹 좌보더 색이 `--border`로 남는 것은 `notes.css` 주석에 명시된 알려진 제한 ⓔ 콘솔 무재현(FB-15·FB-11 ② 미발생) ⓕ 설정 6그룹·실험실 카드 부재 · V-3(iframe 390px 에뮬): ⓖ 드로어 '노트' 진입 ⓗ 편집 표면 적층·도킹 툴바 ⓘ 탭바 5개. **DoD 4 충족 — 자동 검증 6항 전건 충족.**

**잔여**: DoD 7 사용자 실사용(PC+폰·치명 0 회신) + 경미 처분 결정(① 차단 경로 문구·주석 잔존 ④ 고아 모듈 등재 — ② 실측으로 해소·③ 정정 완료) → 게이트 통과 시 §7 발행 절차(v2.01.1 — CHANGELOG·VERSION·stage-index·CLAUDE.md 버전 줄·tag) + 머지.
