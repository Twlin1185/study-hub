# 문서 작성·편집 재편(에디터 v2) 계획서 — v2.00.0 프로젝트

> **상태: 초안(2026-08-15) → 결정 현황(2026-08-15 3차 — M31 집중 분석 종료).** D2 **확정·게이트 완전 종결**(위임 판정 — 잔여 통과 조건이던 한글 IME 실기기 실측이 2026-08-15 사용자 실측(PC+폰, 체크리스트 6항 이상 없음·치명 결함 없음)으로 해소 → **게이트 5조건 전건 합격·재론 없음**, `editor-v2.m31-analysis.md` §7) · D1 방향 지지(최종 확정 M34 유지) · D3·D4·D5·D9·D10 확정 / D7·D8 대기. M31 분석 정본 = `editor-v2.m31-analysis.md`(stage-31 산출물). **M32(변환 계층) 구현 완료(2026-08-16)** — 지시서 = `stage-32-transform-layer.plan.md`.
> **M33(노트 베타 표면) 착수(2026-08-16)** — **stage-33(저장·표면 골격·코어 블록) + stage-34(방언 이식·참조 칩·이미지·붙여넣기 · M33 게이트)로 분할**(이후 M34=stage-35 · M35=stage-36 · M36=stage-37). 착수 시 확정분: **참조 칩 UX ①~④**(§5.4) · **M32 전달 사항 ⓐ~ⓕ 전건**(§5.4 말미) · `notes` DDL(마스터 §6.2 등재 · 기능 번호 **F57** · 리스크 **R41**) · 계약 정본 = 설계 **§4.28(S33)** + **screens §5.16**.
> **→ stage-33 완료·검토 통과(2026-08-16)** — `notes` 저장(DDL·API 5개) + 표면 골격 + 코어 어댑터 + 왕복 스크립트. 잔여 = DoD 10(한글 IME 1차 실측 — 사용자). **부수 확정: §5.4 말미에 "M33 어댑터가 확인한 코어 범위 제약 7종" 신설**(코어 CommonMark/GFM인데 BlockNote 코어가 담지 못하는 표현의 소유 stage 배정 — 3건은 **사용자 판정 대기**, R34 경로). **stage-34(방언 이식 · M33 게이트) 착수 가능.**
> **→ stage-34 구현 완료 · M33 게이트 부분 통과(2026-08-16)** — 방언·참조 칩·수식·이미지·붙여넣기 전건 이식,
> **패리티 59항목/167건 실패 0** · 신규 의존 **1건**(`@blocknote/math-block@0.54.0` — MPL-2.0, 전이 GPL 0 · D10 정합) ·
> 초기 청크 +0.22 kB · 백엔드·DDL·API diff 0.
> **→ M33 게이트 종결 = 통과(2026-08-17)** — 착수를 막던 두 조건이 모두 해소됐다:
> ⓐ **DoD 12(PC+폰 실사용 · 한글 IME 2차 관문 — R35) = 사용자 확인완료.** 이행 중 결함 2건(U-1 조합 중
> undo 무반응 · U-2 제목 `Ctrl+Z`가 본문을 되돌림 = **사용자가 치명으로 판정**)은 **전건 수정 완료**.
> ⓑ **판정 대기 3종(R34) = 판정 완료.** 사용자가 판정을 위임 → **실문서 실측**(`study.db` 224건: 폴백 3건·
> 1.3% · 전부 `listItem:spread` 단독 · `groupBreak`/`table:align`은 실문서 0건)을 근거로
> **"손실 수용하지 않음 — 사이드카 흡수"** 확정. 폴백 문서 3건 → 0건, 조용한 손실 0을 동시 충족.
> ⇒ **M34(stage-35) 착수 가능.**
> 상세 정본 = `stage-34-notes-dialect.plan.md` "완료 기록"(프로젝션 손실 목록 초안 9항목 = M36 입력).
> **→ M34(stage-35) 착수(2026-08-18)** — 지시서 = **`stage-35-documents-blocks.plan.md`**. 착수 시 확정분:
> **documents DDL 3컬럼**(`content_blocks`·`explanation_blocks`·`blocks_version` — 마스터 §6.2 등재·**R42**·
> Alembic은 구현분) · 계약 정본 = 설계 **§4.29(S35)** + **screens §5.3 S35** · 핵심 규약 = 동반 규칙 2쌍 ·
> **전환 해제**(블록 미동반 기록 = 3컬럼 NULL — 이중 저장 드리프트 차단) · 지연 전환(열람 = 메모리만·저장 = 확정) ·
> 리더 `{w=}` 반영(M33 이월분 — "리더 무변경"의 명시 예외 1건) · stage-34 인계 9항목 전건 소화(대응표 = 지시서).
> **D1 최종 확정 절차 = 지시서 "게이트/판정 규칙"이 정본**(게이트 충족 시 Claude 위임 판정·근거 기록).
> notes는 실험실 격리 유지(정식 노출 재론 = M36 — screens §5.16 조항 이행).
> **→ stage-35 구현 완료·검토 통과(2026-08-18)** — B-1~B-5·F-1~F-8·G-9 전건. 실측 = 신규 `s35-doc-roundtrip`
> **3055건 실패 0** · 폴백 0/226 · 회귀 세트 무변(s34-paste 95·parity 193·s33 997·s32 751·s30 364/365 아티팩트 1) ·
> 초기 청크 +2.58 kB · 신규 의존 0. 검토 2회전에서 중요 2건(D-1 문서 경계 저장 오염·D-8 언마운트 flush 본문
> 유실) 수정 확인 — "게이트 진행 가능". **잔여 = DoD 11(사용자 실사용 = M34 게이트 본체) → 그 후 D1 확정 기록.**
> 상세 정본 = `stage-35-documents-blocks.plan.md` "완료 기록".
> **→ M34 게이트 종결 = 통과 · D1 확정(2026-08-19)** — DoD 11 사용자 확인(퇴로 토글 OFF 복귀 · ON/OFF 저장분
> 표시 일관 · 치명 결함 없음)과 **"실사용 중 발견분은 후속 수정" 방침 수용**. **D1 확정: 저장 소스 오브 트루스 =
> 블록 JSON**(전환 문서 한정 · Markdown = 파생 프로젝션 · 미전환 퇴로 불변 — 근거·경위 정본 = stage-35 "완료 기록").
> 실사용 피드백 5건은 §13(FB-1~5 · D11 신설 · FB-1은 수정 완료 PR #51). **⇒ M35(stage-36) 착수 가능.**
> **→ 실사용 피드백 5건 접수·등재(2026-08-19)** — **§13 신설**(결함 1(노트 단건 캐시 미갱신 — 별도 수정 중) ·
> 제안 3 · 질문 1) · **D11 신설**(선택형 "참조 복사" — ⓐ 참조로 붙여넣기 = M35 후보 / ⓑ 양방향 편집 =
> v2.x 보류 권고·임의 착수 금지). FB-3은 **기존 M35 범위 확인**(§7.1 7번 — 답 = 예정됨) · FB-5는 M35 편집
> 표면 통합의 **우선순위 상향 신호** · FB-2는 M35 후보 백로그. **코드 0 — 배정·권고까지만(새 확정 없음).**
> **→ M35 착수(2026-08-19) — stage-36 + stage-37로 분할**(M33 분할 전례 — 위험 축이 다르다: 전반부 =
> 기존 표면·저장 계약의 확장 / 후반부 = 신규 블록·신규 방언 문법. **이후 M36 = stage-38**. 대응표 새 정본 =
> `stage-36-surface-unify.plan.md` "범위 분할"). **stage-36(전반부)** = 편집 표면 통합(진입점 전수 6곳 —
> FB-5 이행·개조 지점은 공용 DocEditor 분기 1곳·**POST 블록 확장 = api §4.29 ⑦ S36**) + **D7 1차 확정 이행**
> (choices·메모 = 필드 유지 + 인라인 리치 렌더 — §10) + **FB-3 문서 임베드 리치 카드**(resolve-embeds 봉인
> 응답만 — R20 정합) + UX 마감(슬래시·드래그·표 열 정렬 UI·undo·**찾기/바꾸기 자체 구현**·콜아웃).
> **stage-37(후반부·M35 게이트)** = 신규 커스텀 블록 3종(TOC = `::toc` · 웹 임베드 = `::web` + **api §4.30
> 메타 조회 선확정**(SSRF 가드 — R39) · 이미지 크롭 = §4.27 재사용·신규 해시 파일) + D8 alt/크기 충족 확인.
> **사용자 대기 존치**: D8 고아 정리(불변 규칙 4 재해석 — 선택지는 stage-36 문서) · D11-ⓐ(권고 등재만 —
> 확정 전 착수 금지) · FB-2 [저장] 버튼(§5.16 번복 절차). **FB-2 복제 = 보류 판정**(M36 notes 거취와 재론 — §13).
> 설계 Design v1.41(api §4.29 ⑦·§4.30·screens §5.3 S36).
> **→ stage-37 착수 문서화(2026-08-21)** — M35 후반부 지시서 **`stage-37-custom-blocks.plan.md`** 생성
> (신규 커스텀 블록 3종 확정 규약 A~F + D8 alt/크기 충족 확인 + **M35 게이트 = 그 문서 말미** — 전·후반부
> 합산 판정). 착수 확정분: `::toc` = **저장 데이터 0**(파생 렌더 — 자동 갱신 공짜·비정규형은 폴백 보존) ·
> `::web` = url·title 2속성 정규형(메타 캐시는 블록 prop — **프로젝션 비대상 = 명문화 강등**·M36 손실
> 목록 기재 예약) · **iframe 실임베드 = 편집 표면 한정 — 리더·인쇄는 항상 카드**(§5.4 비고 괄호 ↔ api
> §4.30 ③의 충돌을 §4.30 ③ 우선으로 정돈 — 비고에 추기·완화 재론은 별도 착수 전 결정) · 크롭 = 자체
> 호스팅 `/images/`만·GIF 제외·원본 MIME 유지 · SSRF 이행 = 기존 `services/net_safety.py` 재사용(백엔드
> httpx 미사용 실측 — 신규 파이썬 의존 0) · `BLOCK_SCHEMA_VERSION = 1` 유지(가산 확장 — 마이그레이션 0).
> stage-36 검토 잔여 이월 = beforeunload(F-7)·찾기 패널 한글 IME(DoD 9). **D8 고아 정리·D11-ⓐ =
> 미편입 확정**(범위 편성 시점 2026-08-21 기준 사용자 판정 미도착 — stage-36 게이트 규칙 이행·중간 편입
> 금지). 설계 Design v1.42(api §4.30 헤더 착수 문구·screens §5.3 S37 신설).
> **→ stage-37 구현 완료(2026-08-21)** — B-1·B-2(§4.30 이행 — urllib 실측 확정·pytest 13) +
> F-1~F-9(스키마·변환기·어댑터·TOC/웹 임베드 편집 표면·리더 카드·이미지 크롭·beforeunload·D8 확인표
> ⓐ~ⓓ 전건 충족·수정 0) + G-10 전건. 실측 = 신규 **s37 191 실패 0** · 회귀 전건 무변(s35-doc-roundtrip
> 3055·s33 999(+어서션 2)·s36 40(+슬래시 전수표 5)) · `run-tests` **552**(+13) · invariant PASS ·
> 초기 청크 **+2.9KB**(한도 5) · 신규 의존 0 · 신규 엔드포인트 1(§4.30뿐). 프로젝션 손실 목록 10항
> (메타 캐시 1항 기재 완료 — stage-34 문서 말미). 상세 정본 = `stage-37-custom-blocks.plan.md`
> "완료 기록". **검토(Opus) = 조건부 통과(2026-08-21 — 치명/중요 0·경미 3건 후속 권고·재량 판단 3건
> 수용). 잔여 = DoD 9(사용자 실사용) → M35 게이트 판정(그 문서 말미).**
> **→ M36(stage-38) 문서 묶음 개정(2026-08-22 — 문서만·코드 0·DDL 0·api 문서 무접촉)**: ① **§5.5 캡처
> 인터페이스 설계 확정판**(`IngestSource` 계약·블록 공통 `meta.provenance` 스키마 예약 — 구현은 v2.x·
> §4.x 번호 예약 없음) ② **§14 신설 = 프로젝션 손실 목록 정본**(stage-34 초안 10항 승격 + 상태 대조
> (해소 3·부분 해소 1·유지 6) + R34 손실 규칙 명문화) ③ **D5 재론 = 재보류 확정**(§10 — MarkdownView
> 단독 유지·기술 위임 판정) ④ **notes 거취 M36 재론 기록**(R41·§13 FB-2 — **N-1 ⓒ 전제**: 존치·노출
> 현상 유지·복제 = v2.x 후보. 사용자 결정 N-1 도착 시 그 결정으로 대체) · Design v1.43(screens §5.16).
> 정본 = **`stage-38-mobile-v2-closeout.plan.md` 규약 D~G**.
> 마스터 계획(`study-app.plan.md`) 말미 버전 정의가 예고한 "다음 거대 프로젝트 = 문서 작성·편집 기능 재편"의
> 별지 계획서다. 확정되는 결정·로드맵·DDL은 관례대로 마스터 계획(§5 기능 목록·§6.2 DDL·로드맵)에 반영한 뒤 착수한다.

---

## 1. 목적·배경

- **지시(2026-08-15, 사용자)**: 문서 작성·편집 기능을 대폭 업그레이드한다. **완전 모듈식** 설계를 고려하고,
  목표 수준은 **Notion·Word급 UX·편의성**. 장기적으로는 휴대폰에서 강의 중 **녹음·메모·필기·사진 촬영**을
  받아 **LLM으로 정제해 문서 중간에 삽입**하는 캡처 파이프라인까지 수용해야 한다.
- 이 문서의 역할: ① 현행 기준선과 기존 결정의 경계를 정리하고 ② 전략 후보를 비교해 권고안을 제시하며
  ③ 목표 아키텍처·범위·로드맵 초안을 세워 **사용자 확정을 받을 결정 목록(§10)** 으로 수렴시킨다.
- 완료 시 **v2.00.0** 출시(마스터 계획 버전 정의 참조).

## 2. 현행 기준선 (2026-08-15 실측 — v1.90.1)

코드·문서 조사 결과 요약. 상세 근거는 각 stage 문서·설계 문서가 정본.

| 축 | 현행 |
|---|---|
| 저장 모델 | `documents.content`·`explanation` = **Markdown 평문 TEXT**. 블록 모델·HTML 저장 없음. `choices`는 JSON 배열 문자열, `style`은 JSON(폰트·크기·배경) |
| 편집기 | **에디터 라이브러리 의존 0** — 자체 구현. `MarkdownFieldEditor`(1008줄, 툴바·단축키·표면 추상화 `EditSurface`) + `EditablePreview`(S27 블록 소스 편집) + `RichBlockEditor`(S30 contentEditable 리치 표면, react-markdown 파이프라인 재활용) |
| 리치 표면 범위 | 문단·헤딩·목록·인용(코퍼스 86.6%)만. 표·코드·수식·콜아웃·이미지는 소스 표면 착지 |
| 의도적 보류(M30 2·3단계) | **블록 간 구조 조작**(Enter 분할·Backspace 병합·Tab 들여쓰기·드래그 재배열)·**자체 undo 스택**·**모바일·터치 마감**·표/수식 시각 편집 |
| 그 외 갭 | `title`·`choices`·오답노트 메모·`local_note`는 평문 input/textarea. 읽기 전용 화면(문서 상세·퀴즈·복습 등 7곳) 인라인 편집 0. 고아 이미지 정리·alt/크기 문법 유예(stage-29) |
| 검증 자산 | 왕복 직렬화 하네스 `frontend/scripts/s30-roundtrip.mjs`(365 표본) — v2 마이그레이션 검증에 재활용 가능 |
| 렌더 | `MarkdownView` 앱 전체 유일 렌더러(remark/rehype 파이프라인 + F52 방언 + F43 참조/임베드) |

**진단**: 자체 구현 노선(S26~S30)은 "Markdown 소스를 지키면서 부분 리치 표면"까지는 도달했으나,
Notion급(블록 조작·드래그·슬래시 메뉴·표/수식 시각 편집·안정된 undo·모바일)은 **전부 보류 목록에 있다**.
이 보류분을 자체 구현으로 완주하는 것은 사실상 ProseMirror를 재발명하는 일이며, R29(왕복 손실)·R31(IME)
리스크가 기능마다 곱으로 늘어난다.

## 3. 기존 결정과의 관계 (경계 정리 — 번복이 필요한 것과 아닌 것)

| 기존 결정 | 상태 | v2에서의 처리 |
|---|---|---|
| F43 ⓐ 원시 HTML 렌더(rehype-raw) 기각 | **불번복** | **유지.** 블록 모델을 도입해도 원시 HTML은 렌더하지 않는다 |
| F43 ⓑ-1 HTML 저장 기각 | **불번복** | **유지.** 후보안 B의 저장 형식은 HTML이 아니라 **구조화 블록 JSON**이다. 다만 "저장 소스 = Markdown 단일"이라는 전제 자체는 바뀌므로 **명시 개정 필요(D1)** |
| F43 ⓑ-2 turndown류 DOM 스크래핑 역직렬화 기각 | 유지 | **유지.** 직렬화는 편집기 모델→변환기 경유(DOM 스크래핑 아님) |
| R32 편집기 의존 도입(2026-08-15 종결) | 재개 조건부 | **재개 등재가 이 문서의 역할.** 종결문의 재개 조건은 "자체 구현이 실사용에서 실패"였으나, 실제 재개 사유는 **목표 수준의 상향**(부분 리치 표면 → Notion급 전면 재편)이다 — 실패가 아니라 목표 변경임을 정직하게 기록한다(D3). 재개 시 필수 조건 3건: (a) 자체 문법 전수 왕복 표본 테스트 = DoD (b) 렌더는 공용 `MarkdownView` 단독 유지 (c) 번들 증가 실측 기록 — **전부 로드맵 DoD에 편입(§8)** |
| F42/R19 "포맷당 파서 1개" | 유지 | **유지가 설계 제약.** 후보안 B에서도 Markdown 파서는 remark 1개 — 편집기 내장 Markdown 파서(markdown-it류)를 쓰지 않고 **mdast↔블록 자체 변환기**로 우회한다(§5.3, D4) |
| 불변 규칙 5(색 토큰) | 유지 | 편집기 테마는 `tokens.css` 토큰으로 결선(CSS 변수 오버라이드) — 다크 모드 공짜 원칙 유지 |
| 불변 규칙 6(스키마 변경 절차) | 유지 | DDL은 확정 후 plan §6.2 갱신 + Alembic 세트로만(§6) |
| S30 퇴로 플래그(`wysiwyg?`) | 유지 | 전환기 내내 **기존 편집기(S26~S30 표면) 전체가 퇴로** — 플래그로 신·구 편집기 전환(§8 게이트) |

## 4. 전략 후보 비교

### 후보 A — 자체 구현 계속 확장 (M30 2·3단계 완주 + α)
Markdown 단일 소스 유지. `RichBlockEditor`에 블록 분할/병합·드래그·자체 undo 스택·표/수식 편집기를 자체 구현.
- 장점: 기존 결정 전부 무번복. 의존 0 유지. 기존 코드 연속.
- 단점: **자체 undo 스택 + 블록 간 구조 조작 + IME 안전**의 동시 성립은 ProseMirror가 수년 걸려 푼 문제군이다.
  기능마다 R29·R31이 곱으로 증식. 표·수식·다단·중첩 블록으로 갈수록 Markdown 왕복 계약이 병목.
  **Notion급 도달 비용이 세 후보 중 최대, 도달 확률은 최저.**

### 후보 B — 블록 편집 엔진 도입: **BlockNote** (권고 — 채택 판정은 M31 집중 분석 후 위임, §10 D2)
ProseMirror/Tiptap 위에 Notion형 UX(슬래시 메뉴·드래그 핸들·블록 중첩·표·이미지 블록·undo·기본 모바일
대응)를 기본 내장한 오픈소스. 2026-08 현재 활발히 릴리스 중(v0.54, 2026-08-13).
**정정(M31 실측 — 2026-08-15)**: 수식(KaTeX)·다이어그램(mermaid)은 코어 미내장이나 v0.54.0에서 **무료 MPL
별도 패키지**(@blocknote/math-block·diagram-block)로 출시 — 무료 범위에서 충족(성숙도는 M32~M33 검증).
찾기/바꾸기·목차(TOC)는 부재 — M35 자체 구현(상세 = `editor-v2.m31-analysis.md` §2).
**라이선스(2026-08-15 확인 — 초안의 "MIT" 표기는 오류, 정정)**: 코어 패키지(@blocknote/core·react 등)는
**MPL-2.0**(파일 단위 카피레프트 — 앱 코드 전염 없음, 의존성 사용에 제약 없음). **XL 패키지**(@blocknote/xl-*:
다단 레이아웃·PDF/DOCX 내보내기·AI)는 **GPL-3.0 듀얼**(GPL 비호환 시 상용 구독).
**전제(2026-08-15 사용자 고지): 본 앱 저장소는 GitHub public — 소스 공개 = 배포로 간주하는 보수적 해석을
채택한다.** 따라서 코어(MPL)는 공개 저장소 의존으로 문제없으나, **XL(GPL)은 앱 라이선스가 GPL-3.0 호환이
아닌 한 편입 불가.** → **D10 확정(2026-08-15 사용자): 라이선스가 걸리는 패키지는 도입하지 않는다 — 해당
기능(다단 등)은 후순위로 미뤄 자체 커스텀 블록으로 구현한다.** **1차 범위 = 코어(MPL)만.**
커스텀 블록/인라인 스펙을 TypeScript로 정의 가능 — F52 방언(형광펜·스포일러·글자색)·F43 참조 칩·콜아웃을
커스텀 스펙으로 이식한다.
- 장점: Notion급 UX의 대부분을 **검증된 엔진에서 상속**(undo = ProseMirror history, IME = ProseMirror의 성숙한 처리).
  구현량이 "엔진 제작"에서 "블록 스펙 정의 + 변환기"로 줄어든다. 모듈식(블록 스펙 = 모듈)이 프레임워크 차원에서 성립.
- 단점: **0.x 버전**(마이너 브레이킹 체인지 리스크 — 버전 고정 + 어댑터 계층으로 완충). 번들 증가(실측 필요, R32-c).
  테마 토큰 결선 작업. 저장 모델 전환(§5.2)이 수반.

### 후보 C — Tiptap 직접 조립
BlockNote가 서 있는 기반을 직접 조립. 자유도 최대, 구현량은 A와 B 사이(슬래시 메뉴·드래그 핸들·블록 UX를 직접 제작).
- 평가: B가 한계에 부딪히면 **같은 기반(Tiptap/ProseMirror) 위에서 하강 이주가 가능**하므로, C는 1차 선택이 아니라
  **B의 퇴로**로 두는 것이 합리적.

### 권고
**B(BlockNote) 채택, C를 구조적 퇴로로, 전환기 내내 기존 편집기(A의 산출물)를 기능 플래그 퇴로로 유지.**
근거: ① 목표가 Notion급으로 상향된 이상 "엔진을 만들 것인가, 쓸 것인가"의 문제이고, 개인 학습 앱에서 엔진 제작은
목적 전도다 ② 장기 캡처 파이프라인(§7.3)은 블록 단위 메타데이터(출처·시각·정제 이력)를 요구하는데 이는
블록 모델의 자연 속성이다 ③ R32가 경고한 "파서 2개"는 자체 변환기(D4)로 회피 가능해졌다.

## 5. 목표 아키텍처 — 완전 모듈식

### 5.1 계층 구조

```
[입력 소스]                [콘텐츠 코어]                    [소비 표면]
 키보드/붙여넣기 ─┐   ┌─ 블록 스키마 레지스트리 ─┐   ┌─ 에디터 뷰(BlockNote)
 이미지 업로드   ─┼─→ │  (블록 타입 = 모듈 단위)  │ ←─┼─ 리더 뷰(MarkdownView, 전환기)
 (v2.x) 녹음     ─┤   │  블록 JSON 문서 트리      │   ├─ 퀴즈/인쇄/플래시카드
 (v2.x) 사진/필기─┘   └─ 변환 계층(mdast↔블록)  ─┘   └─ LLM(반입·해설·정제)
        ↑ LLM 정제 파이프라인(IngestSource 인터페이스)
```

### 5.2 콘텐츠 코어 — 저장 모델 (D1)

- **소스 오브 트루스 = 블록 JSON**(`documents.content_blocks`): BlockNote 스키마 그대로가 아니라
  **앱 소유의 중립 블록 스키마**(버전 필드 포함)로 저장 — 엔진 교체(B→C) 시에도 저장분이 인질이 되지 않는다.
- **Markdown 프로젝션 유지**(`documents.content` 존치): 저장 시 블록→방언 Markdown을 **결정적으로 파생**해 함께 기록.
  파생 전용(역방향 편집 없음)이므로 이원화 드리프트가 원리상 없다. 용도: ① 전환기 리더 뷰(`MarkdownView` 무변경 —
  R32-b 충족) ② LLM 입출력(반입·해설 생성은 Markdown이 강함) ③ 검색(FTS)·인쇄 ④ 백업 가독성.
- Markdown으로 표현 불가한 블록(다단 등)은 프로젝션에서 **명시 규칙으로 강등**(예: 다단→순차 나열)하고
  프로젝션 손실 목록을 문서화한다. 1차 범위에서는 방언이 표현 가능한 블록 팔레트로 제한하는 안도 가능(D6).
- `explanation`도 동일 구조(`explanation_blocks` + 프로젝션). `choices`는 §7.2 참조.

### 5.3 변환 계층 (D4)

- **Markdown→블록**: 기존 remark 파이프라인(F52 방언 플러그인 포함)으로 mdast 생성 → **mdast→블록 변환기**(신규, 앱 소유).
  마이그레이션·LLM 산출물 반입·붙여넣기 공용. **Markdown 파서는 여전히 remark 1개** — F42/R19 원칙 유지.
- **블록→Markdown**: 프로젝션 직렬화기(신규, 앱 소유). DoD = 전 문서 코퍼스 왕복 검증
  (`s30-roundtrip.mjs` 코퍼스 재활용: Markdown→블록→Markdown 동형성) — R32-a 충족.
- **HTML 붙여넣기**: 기존 `htmlPasteMarkdown` 화이트리스트 계약(R30) 유지 — HTML→방언 Markdown→블록 경로로 통일.

### 5.4 블록 스키마 레지스트리 — 모듈 단위

블록 타입 하나 = 모듈 하나(스펙 파일 1개): `{ 데이터 스키마, 에디터 뷰, Markdown 프로젝션 규칙, (선택) 리더 뷰 }`.

| 묶음 | 블록 타입 | 비고 |
|---|---|---|
| 코어(엔진 내장) | 문단·헤딩·목록(중첩)·인용·코드·표·수식·이미지(**크기 조절** 포함)·**가로 구분선** | BlockNote 코어(MPL) 기본 + 설정 |
| 방언 이식(커스텀) | 콜아웃(F52)·인라인: 형광펜/밑줄/글자색/스포일러(F52)·참조 칩 3종(F43)·**문서 임베드(F43 트랜스클루전의 리치 표면 계승)** | 인라인 스펙 + 커스텀 블록 |
| 신규 커스텀(2026-08-15 사용자 요구) | **목차(TOC) 블록**(문서 헤딩 자동 수집·클릭 이동·자동 갱신) · **이미지 자르기(크롭)**(편집 UI에서 크롭 → 결과는 **신규 해시 파일로 저장**, 원본 파일 불변 — 불변 규칙 4 준수. **경로 확정(2026-08-19, M35 착수)**: 크롭은 클라이언트(canvas)에서 수행하고 결과를 **기존 `POST /api/uploads`(§4.27) 재사용**으로 저장 — 신규 엔드포인트 0·해시 파일명·중복 병합 자동) · **웹사이트 임베드**(화이트리스트 iframe(YouTube 등) + 그 외 URL은 북마크 카드 폴백 — R39 · **메타 조회 = api §4.30 신설(S37)**) | 각 블록의 Markdown 프로젝션 강등 규칙 명문화(§5.2): ~~TOC→생략 또는 링크 목록, 웹 임베드→URL 링크~~ → **프로젝션 확정(2026-08-19, M35 착수 — 잠정안 개정·이행은 stage-37)**: **TOC = `::toc` · 웹 임베드 = `::web{url=… title=…}` leaf directive 방언**(F52/F43 directive 패턴 계승 — 변환기 왕복 + 리더(remarkStudy) 렌더: TOC = 헤딩 앵커 링크 목록·web = 북마크 카드(화이트리스트만 iframe·**인쇄는 항상 카드** — **표면 정돈(2026-08-21, stage-37 착수)**: 이 괄호의 압축 표현과 api §4.30 ③("인쇄·프로젝션(리더) 렌더에서 iframe 금지")이 충돌 — **§4.30 ③ 우선**으로 확정한다. 즉 **iframe 실임베드는 편집 표면 한정, 리더·인쇄는 항상 카드**(리더는 퀴즈·학습·인쇄·임베드가 공유하는 표면이라 R39 표면 최소화 정합 — 완화 재론은 별도 착수 전 결정. 이행 정본 = `stage-37-custom-blocks.plan.md` 규약 B))). 개정 근거: **D1 확정으로 프로젝션은 리더 소비가 본질**이 됐다 — "생략"은 전환 문서 열람에서 목차가 사라지고, "URL 링크"는 열람 요구(임베드가 보이는 것)를 못 채우며, 둘 다 재전환(md→블록) 시 블록성이 소실된다(왕복 비동형). directive면 왕복 동형 + 리더 렌더가 동시에 성립한다. 신규 방언 없는 기존 문서 렌더 diff 0이 계약(`{w=}` 전례), 이미지 크기→방언 확장 표기(stage-29 "계획서 먼저" 유예 조건을 본 계획서가 충족. **표기 확정(M32, 2026-08-15): 이미지 직후 부속 표기 `{w=<px>}` — 예 `![캡션](/images/x.png){w=400}`. `h=`는 예약(비율 유지 원칙이라 1차 미사용). remark 플러그인 추가 없이 변환기가 image 노드+후행 텍스트를 병합 해석/직렬화하며, 이스케이프한 `\{w=…}`는 평문으로 보존한다. 리더(`MarkdownView`/`remarkStudy`) 반영은 D9에 따라 M33~M34로 이월**), 크롭→파일 자체 반영이라 손실 0 |
| 자체 구현·후순위(D10) | **세로 단나누기(다단 레이아웃)** — XL(GPL) 대신 자체 커스텀 블록 | 공개 저장소 전제로 GPL 의존 불가(§4 B). 프로젝션 강등 = 순차 나열 |
| XL(도입 금지 — D10) | PDF/DOCX 내보내기·AI 확장 등 @blocknote/xl-* 전체 | GPL-3.0 — 필요 기능은 자체 구현·후순위 원칙 |
| 도메인(신규) | 문제 블록(지문+보기+정답/해설 접힘)·개념 카드 | v2.00.0 범위는 D6에서 확정 |
| 캡처(v2.x 예약) | 녹취 정제 블록·사진(OCR) 블록·필기 블록 | **스키마 예약만** — `meta.provenance`(출처·시각·정제 모델·원본 파일 참조) 필드를 블록 공통 메타로 지금 설계. **설계 확정판 = §5.5(2026-08-22, stage-38 G-6)** |

**참조 칩 UX 재설계 요구(2026-08-15 사용자 실기기 피드백 — M33 방언 이식 설계 시 확정, 구현 보류)**:
M31 PoC의 칩 버튼은 스펙 성립 증거일 뿐 UX가 아니다(대상 하드코딩 + 선택 텍스트를 대체하며 삽입 —
"글자가 사라지면서 참조 문서 7이 되는" 현상). 재설계 시 확정할 것:
① **대상 선택** — 삽입 시 어떤 문서/문제/개념을 참조할지 고르는 피커 필수(검색·최근 문서. 기존 F43 참조
문법의 대상 해석 규칙 계승). 하드코딩·무피커 즉시 삽입 금지.
② **선택 텍스트 보존** — 텍스트를 드래그한 상태의 삽입이 선택 영역을 파괴하지 않아야 한다. 원칙:
칩 삽입은 **커서 위치 삽입**으로 한정하고, 선택 상태에서는 (a) 선택 텍스트를 칩 라벨로 승계하거나
(b) 선택 해제 후 커서 삽입 중 택일 — M33 설계에서 결정.
③ **라벨 규칙** — 기본 라벨 = 대상 문서 제목(대상 변경·제목 변경 시 추종 여부 포함), 사용자 지정 라벨
허용 여부, 칩 클릭 시 동작(이동 vs 미리보기 — 기존 F43 동작과의 일관성)을 함께 확정.
④ **문법 왕복** — 칩의 Markdown 프로젝션은 기존 F43 참조 문법으로 강등(§5.2) — 위 결정과 모순되지
않는지 M32 변환기 설계에서 교차 검증.
→ **교차 검증 결과(M32, 2026-08-15): 구조적 모순 없음.** 블록 스키마의 `refChip`은 별칭을 `label?`로 두어
**추종형(`label` 부재 = 제목 문자열을 저장하지 않음 → ③ 제목 변경 추종 가능)** 과 **사용자 지정 라벨형
(`[[DOC-0012|라벨]]`)** 을 구분 저장하며, 선택 텍스트 승계안 ②-(a)는 지정 라벨형으로 표현된다.
앵커 칩·문서 임베드도 각각 왕복 성립(검증 스크립트 계열 ④). 즉 스키마는 ①~③의 어느 UX 결정도 막지 않는다.
**단, ②-(a)는 라벨 문자 도메인 안에서만 성립한다** — 아래 "M32 → M33 전달 사항" ⓒ 참조.

**→ 참조 칩 UX 재설계 확정(M33 착수 시점, 2026-08-16 — 구현은 stage-34. 화면 표현 = screens §5.16)**:
- **① 대상 선택 = 피커 필수(확정)** — 진입은 툴바 [참조] + 슬래시 `/참조`·`/임베드`·`/앵커`. 3종 공용 인라인
  팝오버가 **`GET /api/search?q=`**(item에 `doc_no`·`title` 포함) 검색 + 최근 열람 문서를 제시한다(**신규 API 0** — 읽기 전용 재사용이라
  `documents` 무접촉 계약과 정합). **하드코딩·무피커 즉시 삽입 경로 0.** **앵커 칩은 현재 노트의 heading
  목록에서만 고른다**(자유 입력 금지 — ⓕ target 도메인 문제를 원천 차단. 도메인 밖 제목은 사유와 함께 비활성 노출).
- **② 선택 텍스트 = (a) 승계 채택 · 실패 시 (b) 폴백(확정)** — 선택 없으면 커서 삽입 + 추종형 칩. 선택이 있으면
  그 텍스트를 **라벨로 승계**해 지정 라벨형(`[[DOC-0012|라벨]]`)으로 치환한다(사용자 불만이던 "글자가 사라지는"
  현상의 해소 지점). 라벨 도메인(ⓒ) 위반이면 **삽입을 강행하지 않고** 선택을 유지한 채 사유 + 라벨 입력란 +
  [라벨 없이 삽입](= 선택 해제 후 커서 삽입 = (b))을 제시한다. **텍스트를 조용히 지우거나 바꾸는 경로 0.**
- **③ 라벨·클릭 동작(확정)** — 기본은 **추종형**(라벨 문자열 미저장 → 대상 제목 변경 자동 추종. 제목 조회는
  `POST /api/documents/resolve-embeds`(§4.19) 배치 + 세션 캐시). 사용자 지정 라벨 허용 + [제목 추종으로
  되돌리기]로 복귀 가능. 미해결·삭제 대상은 자리표시자 칩(F43 문구 계승). **클릭은 표면에 따라 다르다**:
  **편집 표면 = 팝오버**([문서 열기]·[라벨 편집]·[대상 변경]·[삭제]) / **읽기 표면(프로젝션 리더·기존 문서 화면)
  = 기존 F43 동작 그대로 이동**(리더 무수정이므로 자동 충족 — D9). 편집 중 클릭이 라우팅되면 편집이 끊긴다는
  것이 분기 근거이며, 두 표면의 성격 차이로 일관되게 설명된다.
- **④ 문법 왕복 = 위 M32 교차 검증 결과 그대로**(추종형 `[[DOC-0012]]` · 지정 라벨형 `[[DOC-0012|라벨]]` ·
  앵커 `[[#절 제목]]` · 임베드 `![[DOC-0007]]`) — **UX 확정이 스키마·문법을 바꾸지 않았다.**

**M32 → M33 전달 사항 (변환 계층이 확인한 미해결 제약 6건 ⓐ~ⓕ — M33 방언 이식·에디터 표면 설계의 입력)**:
ⓐ **한 run에 마이크로 마크 2개 이상 금지** — `remarkStudy`는 `++`/`==`/`||` **안쪽을 다시 파싱하지
않으므로**(`==**x**==`의 `**`는 평문) 밑줄+형광처럼 겹친 서식은 방언이 표현할 수 없다. Markdown 원본에서는
발생 불가하고 에디터에서만 만들 수 있으므로 **M33 표면이 그 조합을 차단**해야 한다(변환기는 CommonMark
마크가 항상 바깥을 감싸도록 `MARK_ORDER`를 고정해 둔 상태).
ⓑ **inlineImage의 BlockNote 어댑터 결정 필요** — 앱 스키마는 문단 안 이미지를 인라인으로 담지만
(무손실 왕복에 필요) BlockNote 이미지는 **블록 전용**이다. 블록 승격 vs 원자 인라인 커스텀 스펙 중 택일.
ⓒ **참조 칩 라벨 문자 도메인** — 라벨은 `]`·`|`·개행을 구조적으로 담을 수 없고, 그 전에 remark-parse가
먼저 줄을 훑으므로 **인라인 문법을 여는 문자**(`*`·`` ` ``·`~`·`$`·`:t[`류 directive·강조가 되는 위치의 `_`)가
있으면 칩이 붕괴하거나 사라진다(가장자리 공백은 파서가 버린다). ②-(a)로 선택 텍스트를 승계할 때
**치환·거절 정책을 M33에서 확정**할 것(M32 검증 스크립트 계열 ⑥이 도메인을 기계로 고정해 두었다).
ⓓ **빈 문단·공백만 있는 문단은 Markdown 투영에서 소멸한다** — 빈 줄은 블록 구분자일 뿐이라 담을 내용이
없다(M32 검증 계열 ④가 `[]`로 고정). 에디터에서 빈 문단은 흔하므로 M33은 ① 저장 시 제거 ② 블록 JSON
쪽에만 남기고 프로젝션에서만 생략 중 택일해야 한다(현 변환기는 ②에 해당 — 블록은 보존, 투영만 생략).
ⓔ **블록 메타데이터 도메인 — 값·키·이름 3축**(전부 remark-directive 실측): **값**에는 `"`를 넣을 수단이
없고(인용 안에서도 이스케이프 불가) 개행도 헤더 줄을 끊는다 · **키**는 글자·숫자·비ASCII·`-` `_` `.` `:`만
받고 나머지 ASCII 구두점·공백이 들어가면 **그 directive의 속성 블록 전체가 무효**가 된다 · **이름**은
글자·숫자·비ASCII·`-` `_`만 받고 `.`·`:`이 섞이면 **블록이 통째로 문단으로 붕괴**한다. 코드 펜스 정보
문자열에는 백틱·개행이 들어갈 수 없다. 변환기가 표현 가능한 형태로 수렴시키되(표현 불가 키만 제거 ·
`"` 제거 · 개행 접기 · 이름이 비면 `note`) **수렴은 값 변경**이므로 M33 UI가 애초에 이 도메인만 입력받는
편이 낫다. 라벨·정보 문자열의 **내부** 공백·탭은 파서가 보존하므로 접지 않는다(가장자리만 다듬는다).
ⓕ **참조 칩 `target` 문자 도메인 — ⓒ의 라벨과 별개다**(M32 3차 검토 지적). M32 검증 계열 ⑥이 기계로
고정한 것은 **라벨** 도메인뿐이고, `target`에 `]`·`|`·개행이 들어가면 칩이 **평문으로 강등**된다(텍스트
자체는 보존되므로 손실은 아니다). 특히 **앵커 칩 `[[#절 제목]]`의 target = 헤딩 텍스트**라 사용자가 쓴
제목이 그대로 들어온다 — ⓒ의 "치환·거절 정책"에 **target도 포함**해 M33에서 확정할 것.

**→ M32 전달 사항 ⓐ~ⓕ 확정(M33 착수 시점, 2026-08-16 — 규약 정본은 `stage-33`·`stage-34` 지시서의 "확정 규약")**:

| # | M33 결정 | 근거 | 이행 stage |
|---|---|---|---|
| ⓐ | **마이크로 마크 3종(`++`·`==`·`\|\|`)은 상호 배타 — 새로 적용하면 기존 것을 대체**(라디오 동작). CommonMark 마크와의 조합은 자유(변환기가 `MARK_ORDER`로 바깥에 둔다). 강제 지점 3곳 = 툴바 커맨드 래퍼 · 역방향 방어적 정규화(2개 이상이면 우선 1개만 + 집계) · 붙여넣기 접기 | 적용을 무시(차단)하면 사용자는 버튼이 고장 난 것으로 읽는다. 대체는 결과가 즉시 보이고 되돌리기 쉽다. 안내 토스트는 만들지 않는다(툴바 active 표시로 충분) | stage-34 |
| ⓑ | **inlineImage = 원자 인라인 커스텀 스펙**(`createReactInlineContentSpec`, content:"none"). **블록 승격 기각.** 단 **새로 삽입하는 이미지는 언제나 블록 이미지**이고 인라인형은 **기존 데이터 수용 전용**(보존·표시·삭제만) | 블록 승격은 문단을 쪼개 **왕복 무손실을 깬다** — M32가 inlineImage를 스키마에 넣은 이유가 그 손실 방지였다. 원자 인라인은 M31 스파이크(refChip)에서 성립 확인됨 | stage-34 |
| ⓒ | **거절 정책(치환 금지)** — 도메인 밖 문자는 몰래 지우지 않고 사유를 보여 사용자가 고치게 한다. **유일한 자동 정리 = 가장자리 공백 트림**(파서가 어차피 버리는 자리 = 표기 정규화). 판정은 **`editor2/schema/refDomain.ts`의 `isSafeRefText(s)` 단일 함수**로 하고 **M32 검증 스크립트(계열 ⑥)가 이 함수를 import**하도록 전환한다(도메인 단일 출처) | 치환은 "조용한 본문 변형 0"(M32 규약 A-3) 계약과 정면 충돌한다. 검증 스크립트와 UI가 각자 도메인을 들고 있으면 반드시 갈라진다 | stage-34 |
| ⓓ | **빈 문단은 블록 JSON에 보존, 프로젝션에서만 생략(현 변환기 동작 ②를 확정 채택)** — 저장 시 제거하지 않는다. **예외 1건 = 문서 말미 연속 빈 문단 트림(1개만 유지)** | 블록 JSON이 소스 오브 트루스이고, 사용자가 만든 빈 줄을 저장 때마다 지우면 캐럿이 사라지는 파괴적 편집이 된다. 말미만 트림하는 이유는 BlockNote가 구조상 말미 빈 문단을 유지해 **저장할 때마다 누적**되기 때문이다. 결과로 "블록→md→블록"은 빈 문단만큼 비대칭이며 이는 손실이 아니라 프로젝션의 정상 동작이다(계약으로 명문화) | stage-33 |
| ⓔ | **UI가 애초에 도메인 밖 입력을 받지 않는다** — 콜아웃 variant = **고정 목록만**(note·warn·tip·fold·hide) · 콜아웃 title = `isSafeRefText` 재사용 검증 · **`{key=value}` 자유 편집 UI 없음**(기존 attrs는 보존만) · 코드 블록은 **언어 드롭다운만**(info 자유 입력 없음, 기존 값은 보존만) | M32가 경고한 대로 **수렴은 값 변경**이다. 입력 경로 자체를 도메인 안으로 좁히는 것이 유일하게 무손실인 길이며, 부수적으로 "블록이 통째로 문단으로 붕괴"하는 이름 도메인 사고가 원천 소멸한다 | 코드 블록 = stage-33 · 나머지 = stage-34 |
| ⓕ | **target도 같은 도메인 함수로 판정**(라벨과 별도 함수를 만들지 않는다 — 금지 집합이 실질적으로 같다). doc/embed target은 **피커가 고른 `doc_no`**(`DOC-\d{4,}`)라 자유 입력 경로가 없고, anchor target은 **현재 노트 heading 선택 + 필터**. 역방향(반입 데이터)의 도메인 밖 target은 **M32 변환기의 기존 평문 강등을 그대로 수용**(텍스트 보존 = 손실 0) — 어댑터가 새 규칙을 만들지 않는다 | 위험 입력의 유일한 유입구가 "사용자가 쓴 헤딩 제목"이므로, 피커에서 거르면 신규 생성 경로의 문제는 사라진다. 함수 분리는 필요가 실측되기 전까지 하지 않는다(YAGNI) | stage-34 |

**M33 어댑터가 확인한 코어 범위 제약 7종 (stage-33 실측 2026-08-16 — 소유 stage 배정. 전달 사항 ⓐ~ⓕ와 별개)**

stage-33 어댑터(`editor2/adapter/toBlockNote.ts`)가 **미지원으로 보고**하는 사유 중 아래 7종은
**방언이 아니라 순수 CommonMark/GFM**이다. 즉 "코어 블록만 다룬다"는 stage-33 범위 안에 있어야 할
표현인데 **BlockNote 0.54 코어 모델이 담지 못하는** 것들이며, stage-34의 방언 범위에도 screens §5.16의
단계별 기능 범위 표에도 없어 **소유자가 없었다**(검토 지적 2026-08-16). 여기서 소유를 확정한다.

- **노트는 신규 저장소라 지금은 무해**하다(BlockNote로 만든 문서에는 이 값이 생기지 않는다). 단
  **붙여넣기 경로**(HTML→방언 Markdown→`markdownToBlocks`)로는 유입될 수 있고, 무엇보다
  **M34(stage-35) `documents` 탑재 시점에 느슨한 목록·정렬 있는 표를 가진 기존 문서 다수가 걸린다**.
- 흡수 경로 후보는 두 가지다 — ⓐ **잔여 속성 사이드카**(어댑터가 BN이 못 담는 속성을 **블록 id 키**로
  보관했다가 역변환에서 복원. 편집으로 블록이 사라지면 함께 사라지는 것이 자연스럽다) ⓑ **원자 인라인
  커스텀 스펙**(M32 전달 사항 ⓑ의 inlineImage와 같은 메커니즘). **내장 블록 스펙 재정의는 1차 후보가
  아니다**(내장 UI 재구현 비용 — 필요가 실측되기 전까지 하지 않는다).
- **"사용자 판정 대기"로 분류된 것은 임의로 손실 수용으로 확정하지 않는다** — R34 경로다.
  판정 시한 = **stage-35(M34) 착수 전**(그 단계가 기존 문서를 실제로 태우는 시점).

| # | 어댑터 보고 코드 | 정체(코어 문법) | BlockNote 0.54 코어의 수용 가능성 | 소유 |
|---|---|---|---|---|
| 1 | `listItem:spread` | CommonMark **느슨한 목록**(항목 사이 빈 줄 = 항목 내용이 문단으로 감싸짐) | 목록 블록에 loose/tight 개념 자체가 없다. 값 보존은 사이드카ⓐ로만 가능하고, **편집으로 항목이 늘거나 갈라지면 새 항목의 값이 미정**이며 화면에는 차이가 보이지 않는다 → **모델상 흡수 불가** | **사용자 판정 대기(R34)** — 대안 = "느슨/조밀 구분 소실 수용(프로젝션을 조밀로 통일)" vs "해당 문서는 읽기 전용 폴백 유지". stage-34는 **명시 보고를 유지**만 한다 |
| 2 | `listItem:groupBreak` | **인접한 두 목록의 경계**(마커 교체로 표기 — M32가 손실 방지로 신설한 필드) | 코어는 연속 항목을 한 목록으로만 표현하고 **경계를 만들 UI가 없다**(신규 생성 불가). 기존 값 보존은 사이드카ⓐ로만 | **사용자 판정 대기(R34)** — 1번과 같은 시한·같은 선택지 |
| 3 | `table:align` | **GFM 열 정렬**(`:---:`) | `tableContent`에 정렬 자리가 없다(`columnWidths`·`headerRows`뿐). 표는 블록이라 **열 정렬 배열을 사이드카ⓐ로 보존하는 것 자체는 가능**하나 **열 추가·삭제 시 인덱스가 어긋난다**. 정렬 **편집 UI**는 M35 범위 | **stage-34 = 열 수 불변 조건의 왕복 보존 시도**(편집 UI 없음) · **정렬 편집 UI = stage-36(M35)** · **열 증감 시의 손실 수용은 사용자 판정 대기(R34)** |
| 4 | `inline:hardBreak` | CommonMark **경성 줄바꿈**(`\` 또는 행말 두 공백 = `<br>`) | 코어는 텍스트 안 `\n` 1종뿐이라 soft/hard가 합쳐진다. **원자 인라인 스펙ⓑ로 표현 가능** — 단 새로 만드는 수단은 없다(Shift+Enter = softBreak) | **stage-34**(흡수) — **기존 데이터 수용 전용**(보존·표시·삭제만, 신규 생성 UI 없음 = 규약 D inlineImage 선례). **붙여넣기로 실제 유입되는 항목**이라 우선순위가 높다 |
| 5 | `link:title` | CommonMark 링크 **title**(`[x](url "제목")`) | 코어 링크는 `{href, content}`뿐. 링크를 커스텀 원자 인라인으로 대체하면 내장 링크 UX를 잃는다 → **사이드카ⓐ로 왕복 보존**(편집 UI 없음) | **stage-34**(흡수) — 붙여넣기 유입 가능 |
| 6 | `image:title` / `image:height` | 이미지 **title** · **높이**(M32에서 `h=`는 **예약**이며 1차 미사용 — 현재 생성 경로 0) | 이미지는 **블록**이라 사이드카ⓐ 키(블록 id)가 안정적이다. `caption`↔`alt`·`previewWidth`↔`width`는 이미 매핑됨 | **stage-34**(흡수 — 보존 전용) |
| 7 | `block:meta` | 블록 공통 `meta`(provenance — **v2.x 캡처 파이프라인 예약 필드**, 현재 생성 경로 0) | 편집 UI가 필요 없고 **값 보존만** 하면 되므로 사이드카ⓐ로 충분 | **stage-34**(흡수 — 보존 전용) |

- **stage-34가 흡수를 시도했다가 불가로 판명되면** 임의 강등 금지 — 표본 수와 사유를 정리해 **사용자 보고**한다
  (stage-34 게이트 규칙 "왕복에서 조용한 손실 발견" 행 = R34 경로). 어느 경우에도 **조용한 손실 0**이 계약이다.

**→ 이행 결과(stage-34 완료 2026-08-16 — 상세 정본 = `stage-34-notes-dialect.plan.md` "완료 기록 → 규약 I 처리 결과")**

- **흡수 4종 전건 이행 · 미지원 보고 소멸**: 4번 `inline:hardBreak` = **원자 인라인ⓑ**(단 **편집기 타입명은
  `lineBreak`** — 코어 PM 스키마에 이미 `hardBreak` 노드가 있어 이름이 겹치면 되읽기에서 무조건 `\n`으로
  접힌다는 실측 때문. 앱 스키마 이름은 `hardBreak` 그대로) · 5번 `link:title`·7번 `block:meta` = **사이드카ⓐ**
  (블록 id 키. `link:title`은 content 인덱스 대신 **href를 보조 키**로 둬 편집에 강하게 했다 — 같은 블록·같은
  href에 서로 다른 title이면 마지막 값이 남는다) · 6번 `image:title`/`height` = **내장 image 스펙 prop 확장**
  (`codeBlockSpecWithInfo` 전례 — ⓐⓑ 어느 후보도 아니지만 내장 UI 재구현이 0이라 "재정의 비용" 우려가
  성립하지 않는다. **보존 전용, 편집 UI 없음**).
- **사이드카는 메모리 전용**(매 로드마다 `content_blocks`에서 재유도) — `fromBlockNoteBlocks`를 사이드카 없이
  부르는 **새 호출부가 생기면 흡수 4종이 조용히 사라진다**. **M34(stage-35) `documents` 저장 경로 결선 시 필수 확인.**
- **판정 대기 3종은 그대로 잔존**(1·2·3번 — 코퍼스 표본 4·6·1건). 3번 `table:align`은 **열 수 불변 조건의
  사이드카 보존까지 이행**했으나 **미지원 보고는 유지**한다(열 증감 시 미복원). **사용자 판정 시한 = stage-35
  착수 전** — 미판정 시 해당 문서는 읽기 전용 폴백이다.
- **신규 사유 1종 추가**: `inline:mathStyles`(인라인 수식에 걸린 서식 — 채택한 `@blocknote/math-block`의 `math`
  스펙 propSchema가 비어 담을 자리가 없다). 코퍼스 표본 **0건** · 툴바 입력은 원자 가드로 차단 ·
  흡수하려면 자체 수식 스펙 전환이 필요하다(**프로젝션 손실 목록 1번 = M36 입력**).

### 5.5 입력 파이프라인 인터페이스 (v2.00.0은 설계만, 구현은 v2.x)

> **설계 확정판(2026-08-22, stage-38 G-6 — M36 "캡처 인터페이스 설계 반영"의 이행. 정본 = 이 절 +
> `stage-38-mobile-v2-closeout.plan.md` 규약 E).** 구현 코드 0 · DDL 0(provenance는 블록 JSON 내부
> 필드 — 마스터 §6.2 무접촉) · **api 문서 무접촉**(신규 API 명세는 v2.x 구현 계획서 소관 — §4.x 번호
> 예약도 하지 않는다·YAGNI).

- **수명주기(불변 골격)**: `IngestSource(원본 획득) → 원본 보관(sources/ 하위 — 불변 규칙 4·백업 F27
  자동 포함) → LLM 정제 잡(비동기 — 기존 잡 패턴(§4.24 관례) 재사용 전제) → 블록 초안(각 블록에
  meta.provenance 동반) → 삽입 리뷰 UI(검토 후 확정 삽입)`. **자동 삽입 금지** — 정제 산출물은 반드시
  리뷰를 거친다(LLM 산출물 검토 관례 — F45 수신함 정신).
- **`IngestSource` 인터페이스(설계 수준 계약)**: `kind`(`recording`|`photo`|`memo`|`handwriting`) ·
  `original_ref`(`sources/` 하위 원본 경로 — 원본 불변·수정/삭제 코드 금지) · `captured_at`(획득 시각) ·
  `status`(`stored → refining → drafted → inserted | discarded` — 수명주기와 1:1). **소스 종류 추가 =
  인터페이스 구현 추가일 뿐** 블록 스키마·저장 계약 무변(§5.4 "캡처(v2.x 예약)" 행과 상호 참조 —
  확장 지점은 여기 하나다).
- **블록 공통 `meta.provenance` 스키마 예약(정본)**:
  `{ source: 'capture'|'import'|'llm', kind?, captured_at?, model?, original_ref? }` — 전 필드 선택적·
  미지 필드 보존. **보존 경로는 이미 실측 완료**: 사이드카ⓐ 보존(§5.4 코어 범위 제약 7번 행 — stage-34
  흡수·보존 전용) + [메타 새로고침] 스프레드 병합(stage-37 검토 경미-3 수정분)이 provenance 생존을
  보장한다. **현재 생산 경로 0** — 생산은 v2.x 캡처 구현부터(그 전까지 어떤 코드도 이 필드를 만들지
  않는다 — `blocks.ts` 주석 추기도 구현 시점).
- **착지 표면 = notes 우선**(§12-5 — 강의 노트가 캡처의 자연스러운 1차 착지·N-1 ⓒ 존치 전제와 정합),
  문서(documents) 삽입은 리뷰 UI 경유. 휴대폰은 기존 홈 네트워크 접속(R12 유지) + PWA로 커버 —
  별도 앱 없음.

## 6. 데이터 모델 영향 (초안 — 확정 시 plan §6.2 + Alembic)

```sql
-- documents 확장 — **확정·정식 등재 완료(2026-08-18, stage-35 착수): 정본은 마스터 계획 §6.2**(아래는 초안 흔적이며 정본이 아니다).
-- 확정본과의 차이: **blocks_version INTEGER 컬럼 추가**(notes 전례 준용 — 전환 판정·지연 마이그레이션 SQL 조회의
-- 단일 기준. NULL = 미전환) · **전환 해제 규약** 신설(블록 미동반 content/explanation 기록 = 같은 트랜잭션에서
-- 3컬럼 NULL — 설계 §4.29 ④). 계약 = 설계 §4.29(S35) · 리스크 = 마스터 R42.
ALTER TABLE documents ADD COLUMN content_blocks TEXT;      -- 블록 JSON (버전 필드 포함), NULL = 미전환 문서
ALTER TABLE documents ADD COLUMN explanation_blocks TEXT;  -- 동일
-- content·explanation 컬럼은 존치 — 프로젝션 저장소로 역할 전환 (렌더·LLM·검색·백업 호환)

-- 노트(베타) — **확정·정식 등재 완료(2026-08-16): 정본은 마스터 계획 §6.2**(아래는 초안 흔적이며 정본이 아니다).
-- 확정본과의 차이: title·content에 NOT NULL DEFAULT '' 부여 · **blocks_version을 컬럼으로 승격**(JSON 안 version의
-- 사본 — 서버가 JSON을 해석하지 않는 계약과 M34 지연 마이그레이션의 SQL 조회를 양립시키기 위함) ·
-- 인덱스 ix_notes_active_updated(is_active, updated_at DESC) 추가. 계약 = 설계 §4.28(S33).
CREATE TABLE notes (
  id             INTEGER PRIMARY KEY,
  title          TEXT,
  content_blocks TEXT NOT NULL,   -- 앱 중립 블록 JSON (blocks_version 포함)
  content        TEXT,            -- Markdown 프로젝션 (파생 전용)
  is_active      INTEGER DEFAULT 1,
  created_at     DATETIME, updated_at DATETIME
);
```
- 마이그레이션: 일괄 변환하지 않고 **열람·편집 시 지연 전환**(lazy) + 배치 변환 스크립트 별도 제공 —
  변환기 검증(§5.3 DoD) 통과 후에만. 미전환 문서는 기존 편집기로 그대로 열린다(퇴로 겸용).
- `blocks_version` 필드로 스키마 진화 대비. 백업(F27)은 무변경(같은 DB 파일).

## 7. 범위 계층

### 7.1 v2.00.0 포함 (확정은 D6)
1. BlockNote 기반 새 편집기(패리티: 현행 문법·기능 전부 신 편집기에서 가능) + 퇴로 플래그
2. Notion급 UX: 슬래시 메뉴·블록 드래그·중첩·표/수식 시각 편집·안정 undo·찾기/바꾸기
3. 저장 모델 전환(블록 JSON + Markdown 프로젝션) + 지연 마이그레이션
4. 편집 표면 통합: 제목·본문·해설을 한 문서 표면으로, `choices`·오답노트 메모 리치화(D7)
5. 모바일 편집 마감(폰에서 작성·편집이 실용 수준 — 캡처 파이프라인의 전제)
6. 이미지 잔여분: alt/크기, 고아 이미지 정리(stage-29 유예분 — D8)
7. **기능 요구(2026-08-15 사용자 지시)**: 목차(TOC) · 가로 구분선 · 이미지 크기 조절+자르기 ·
   프로젝트 내 타 문서 임베드(F43 계승·리치 표면 격상) · 웹사이트 임베드(화이트리스트 iframe + 북마크 카드 —
   stage-29 제외 항목의 "계획서 먼저" 조건 충족) — 블록 매핑은 §5.4.
   **← 실사용 피드백 FB-3(2026-08-19, §13)**: "임베드한 문서 내용이 편집 표면 안에서 보이는가"가 곧
   이 항목의 "문서 임베드 리치 표면 격상"이다 — **M35(stage-36) 이행 예정**(질문에 대한 답 = 예정됨).
   **세로 단나누기(다단)는 D10에 따라 자체 구현·후순위**(v2.00.0 필수 범위에서 제외, 백로그 등재)

### 7.2 명시 제외 (v2.00.0에서 하지 않는 것)
- 실시간 협업(Yjs)·멀티 커서 — 개인 앱, 수요 없음. 엔진이 지원하므로 문을 닫지는 않되 로드맵 없음
- 원시 HTML 렌더·HTML 저장 (불번복 유지)
- 외부 사이트 임베드(iframe)·동영상 업로드 (stage-29 제외 계승 — 별도 계획서 선행)
- 문서 간 동시 편집 충돌 해결(단일 사용자 전제 유지)

### 7.3 v2.x 이후 (이번에 설계 제약만 반영)
- 캡처 파이프라인 구현(녹음→전사→정제, 사진→OCR, 필기) — **블록 공통 `meta.provenance`와
  `IngestSource` 인터페이스만 v2.00.0 설계에 포함**하고 구현은 후속 계획서로
  **← 설계 반영 완료(2026-08-22, stage-38 G-6 — §5.5 확정판)**

## 8. 로드맵 초안 (M31~M36 — 확정 시 마스터 계획 로드맵에 등재)

| M | 내용 | 게이트/DoD 핵심 |
|---|---|---|
| M31 | **설계·PoC·집중 분석 게이트**: BlockNote 스파이크 — 방언 커스텀 스펙 3종(형광펜·스포일러·참조 칩)·한글 IME 실측·토큰 테마 결선·번들 실측(R32-c)·**대량·장문 성능 실측**(문서 수천 건 목록/검색 표시 + 장문 문서 편집 반응성 — D1 조건) → 산출물 = **가능성·한계 집중 분석 보고서** | **게이트(위임 판정 — D1·D2)**: 분석 결과 채택이 압도적으로 유리하면 Claude 판단으로 D1·D2 확정, 아니면 후보 C 전환 판단 또는 사용자 재보고. 설계 문서(§4.x·§5.x) 개정. **← ✅ 2026-08-15 완전 종결(stage-31): D2 확정·D1 방향 지지, 보고서 = `editor-v2.m31-analysis.md`. 사용자 IME 실기기 실측(PC+폰, PoC 체크리스트 6항) 이상 없음으로 잔여 조건 해소 — 게이트 5조건 전건 합격** |
| M32 | 변환 계층: 앱 블록 스키마 확정 + mdast↔블록 변환기 + 왕복 코퍼스 검증 | 365 표본 왕복 동형성(R32-a). **← ✅ 2026-08-16 구현 완료(stage-32, 검토 1~3차 지적 전건 반영): 스키마 `frontend/src/editor2/schema/blocks.ts`(정본) + 변환기 `editor2/transform/` + 검증 `s32-roundtrip-blocks.mjs` **723건 전건 통과**(≥365) · s30 회귀 365 무변 · 실문서 436텍스트 정규형 동등·고정점 100%(fallback 2노드 = `:2018`류 비-`t` 인라인 directive) · 초기 청크 변동 0(어떤 화면에도 미import). 동형 정의·블록 팔레트·이미지 크기 표기 확정은 지시서 `stage-32-transform-layer.plan.md` "확정 규약"이 정본. 프론트 전용 — DDL 0·API 0·백엔드 diff 0·신규 의존 0** |
| M33 | **노트(베타) 표면**(D9): 신규 `notes` 저장 위에 새 편집기 1차 — 코어 블록 + 방언 이식 + 이미지 + 붙여넣기. **기존 편집기 파일·documents 무접촉**. **← 착수 2026-08-16 · stage-33 + stage-34로 분할**(근거·M↔stage 대응표 = `stage-33-notes-surface.plan.md` "범위 분할". 이후 M34=stage-35 · M35=stage-36 · M36=stage-37). **stage-33** = `notes` 저장(DDL 1건 — 마스터 §6.2 등재·Alembic은 구현분) + API 5개(설계 **§4.28** S33) + 표면 골격(lazy 라우트 2개·화면 = screens **§5.16**) + **코어 블록 어댑터**(앱 블록↔BlockNote **순수 JSON 변환**) + 왕복 스크립트. **stage-34** = 방언 이식(F52 인라인·콜아웃) + **참조 칩 UX ①~④**(§5.4 확정분) + 수식 + 이미지 3진입점(§4.27 재사용) + 붙여넣기 + 패리티. **M32 전달 사항 ⓐ~ⓕ 전건 확정 완료**(§5.4 말미 표). 기능 번호 **F57** · 리스크 **R41**. **← ✅ stage-33 완료·검토 조건부 통과 → 조건 해소(2026-08-16)**: `notes` DDL/Alembic(`630a4c2531e8`) + API 5개 + 표면 골격 + 코어 어댑터 + s33 왕복 스크립트. 실측 = API 스모크 30항목 전건 · s32 723/723 · s30 365/365 · `run-tests.ps1` 528 passed · **초기 청크 증가 0**(index JS 1,615.59→1,607.15 kB(min)) · 신규 의존 0 · 허용 접촉점 5곳 밖 diff 0. 잔여 = DoD 10(한글 IME 1차 실측 — 사용자) · **코어 범위 제약 7종**(§5.4 말미 표 — stage-34/사용자 판정 배정). 상세 정본 = `stage-33-notes-surface.plan.md` "완료 기록". **← ✅ stage-34 완료(2026-08-16) · ✅ M33 게이트 종결 = 통과(2026-08-17)**: F52 방언 전항 + 참조 칩 UX ①~④(피커·팝오버·추종 라벨) + 수식(**`@blocknote/math-block@0.54.0` 채택 — MPL-2.0 · 전이 GPL 0건 · 신규 의존 총 1건**, 상한 2) + 이미지 3진입점 + 붙여넣기 + **규약 I 흡수 4종 왕복 보존**(hardBreak = 원자 인라인 `lineBreak` · link title·block meta = **사이드카** · image title/height = 내장 스펙 prop 확장). 실측 = **패리티 59항목/167건 실패 0**(실제 `noteSchema` 적재 경유) · 붙여넣기 52건 실패 0 · s33 왕복 **961건 실패 0** · s32 **751**(기존 723 무변) · s30 365 무변 · `run-tests.ps1` 528 passed · invariant PASS · **초기 청크 +0.22 kB** · **백엔드·DDL·API diff 0**. **게이트 종결(2026-08-17)** = ⓐ **DoD 12(PC+폰 실사용 · 한글 IME 2차 관문 — R35) 사용자 확인완료**(이행 중 결함 U-1·U-2 **전건 수정** — U-2 = 제목 `Ctrl+Z`가 본문을 되돌리던 조용한 손실 경로, 사용자가 치명으로 판정) · ⓑ **판정 대기 3종(R34) 판정 완료** — 사용자 위임 + **실문서 실측**(`study.db` 224건 중 폴백 3건 = 1.3%, 전부 `listItem:spread` 단독 · `groupBreak`/`table:align` 실문서 0건)을 근거로 **"손실 수용 안 함 — 사이드카 흡수"** 확정(폴백 3건 → 0건). 잔여 = 신규 미지원 사유 1종 `inline:mathStyles`(표본 0건). **프로젝션 손실 목록 초안 9항목 = M36 입력.** 상세 정본 = `stage-34-notes-dialect.plan.md` "완료 기록" | **stage-33**: Alembic 왕복 · API 스모크 · 어댑터 왕복 전건 · 초기 청크 증가 ≤5KB · IME 1차 실측 **← DoD 1~9 충족(10은 사용자 이행 미완)** / **stage-34(= M33 게이트)**: 현행 기능 패리티 체크리스트 + **노트 실사용**(IME 2차 관문·왕복 무손실) **← ✅ DoD 1~12 전건 충족 = 게이트 통과. M34(stage-35) 착수 가능** |
| M34 | **documents 탑재 + 저장 전환**: 노트 베타 검증 통과가 전제(게이트). DDL·Alembic·지연 마이그레이션·프로젝션 저장, 리더/퀴즈/인쇄/LLM 무변경 확인 — **D1 최종 확정 시점**. **← 착수 2026-08-18(stage-35)** — 지시서 = **`stage-35-documents-blocks.plan.md`**(확정 규약 A~H = 전환 판정(`blocks_version IS NOT NULL`)·동반 규칙 2쌍·**전환 해제**·사이드카 결선 필수·지연 전환 수명주기·퇴로 토글(기본 ON)·리더 `{w=}` 반영(명시 예외 1건)·notes 격리 유지가 정본). DDL 3컬럼 = 마스터 §6.2 등재(**R42**) · 계약 = 설계 **§4.29(S35 — 신규 엔드포인트 0)** + screens **§5.3 S35**. **← ✅ 구현 완료·검토 통과(2026-08-18)**: Alembic `9b1e0f7c4a2d`(소급 0) + §4.29 전건(스모크 13 + 검토 계약 테스트 18) + 전환 해제 4경로 전수 결선 + 새 편집기 진입 분기·저장 파이프라인(사이드카 결선) + 배치 스크립트(드라이런 기본) + 리더 `{w=}`(별도 커밋 격리). 실측 = s35-doc-roundtrip **3055건 실패 0**·폴백 0/226·회귀 무변·초기 청크 +2.58 kB·`run-tests` 528·invariant PASS. 검토 2회전 — 중요 D-1·D-8 수정 확인, 경미 6건 잔존(기능 영향 0 — 완료 기록 참조). **← ✅ M34 게이트 종결 = 통과 · D1 확정(2026-08-19 — DoD 11 사용자 확인: 퇴로 토글 복귀·ON/OFF 저장분 표시 일관·치명 결함 없음 · "실사용 중 발견분은 후속 수정" 방침 수용)**. 상세 정본 = `stage-35-documents-blocks.plan.md` "완료 기록" | 미전환 문서 완전 호환(퇴로) — **판정 절차·D1 위임 판정 조건 = 지시서 "게이트/판정 규칙" ← ✅ 이행 완료** |
| M35 | Notion UX 마감: 슬래시·드래그·표/수식 편집·undo·찾기/바꾸기 + 편집 표면 통합(choices 등) + **신규 커스텀 블록(목차·웹 임베드·이미지 크롭 — §7.1 7번)**. **← 실사용 피드백 접수(2026-08-19 — §13)**: FB-3(임베드 내용 표시 = 이 행의 리치 표면 격상 — 기존 범위 확인·이행 예정) · FB-5(새 편집기 적용 범위를 문서 편집 전반으로 = **편집 표면 통합의 우선순위 상향 신호** — 착수 시 편집 진입점 전수 체크리스트 고정) · FB-2(노트 [저장]/[취소]·복제 = 후보 백로그 — [저장] 상시 버튼은 §5.16 확정의 개정 절차 필요) · **D11-ⓐ("참조로 붙여넣기") 후보 편입 검토**. **← 착수 2026-08-19 — stage-36 + stage-37로 분할**(전반부 = 표면 통합·D7 1차·FB-3·UX 마감 / 후반부 = 신규 커스텀 블록 3종 + **M35 게이트**. 근거·대응표 새 정본 = **`stage-36-surface-unify.plan.md` "범위 분할"** · 이후 **M36 = stage-38**). 착수 시 확정분: **D7 1차 확정**(필드 유지 + 인라인 리치 — §10) · **POST 블록 확장 = api §4.29 ⑦(S36·신규 엔드포인트 0)** · **웹 임베드 메타 조회 = api §4.30 선확정(S37·신규 엔드포인트 1 — SSRF 가드·R39)** · **TOC/웹 임베드 프로젝션 = directive 방언 확정**(§5.4 비고) · screens §5.3 S36 · Design v1.41. 사용자 대기 존치 = D8 고아 정리·D11-ⓐ·FB-2 [저장] 버튼(FB-2 복제는 보류 — §13). **← ✅ stage-36 구현 완료·검토 통과(2026-08-20 — Opus 2회전: 중요 2건(미저장 손실 새 탭 차단·렌더 diff 실측 수용 정정) 처리 확인 · DoD 1~8 전건 = stage-37 착수 조건 성립)**: B-1·B-2(POST 블록 동반 — PATCH 검증 공용 헬퍼화·계약 pytest 11) + F-1~F-9(DocEditor 통합 분기 1곳 = 진입점 ②~⑥ 전수·태생 전환 · choices/메모 인라인 리치 렌더 8곳 · docEmbed 리치 카드(봉인 응답만·중첩 1단) · 슬래시 전수표 26항·표 열 정렬 UI(세션 소유+사이드카 직렬화)·찾기/바꾸기 자체 PM 플러그인·콜아웃 마감). 실측 = 신규 `s36-editor-ux` 35/35 · s35-doc-roundtrip 3055 실패 0 · 회귀 전건 무변 · `run-tests` **539**(+11) · invariant PASS · **초기 청크 +3.72 kB(min)** · 신규 의존 0·DDL 0·신규 엔드포인트 0. **통합 후 클라이언트 강등 경로 = 퇴로 토글 OFF 1개로 수렴**(stage-35 규약 F 알려진 문제 해소). 상세 정본 = `stage-36-surface-unify.plan.md` "완료 기록"(검토 결과 포함). **← ✅ stage-37(후반부) 구현 완료(2026-08-21)**: 신규 커스텀 블록 3종(TOC `::toc` 저장 데이터 0·파생 렌더 / 웹 임베드 `::web` + §4.30 이행(urllib·`net_safety` 재사용·가드 전건) — iframe = 편집 표면 한정·YouTube 화이트리스트·리더/인쇄 항상 카드 / 이미지 크롭 = §4.27 재사용·원본 무접촉·undo 1단위) + D8 alt/크기 확인표 ⓐ~ⓓ 전건 충족(수정 0) + beforeunload 이월분. 실측 = 신규 s37 **191 실패 0** · 회귀 전건 무변 · `run-tests` **552**(+13) · invariant PASS · 초기 청크 +2.9KB · 신규 의존 0 · 신규 엔드포인트 1. 프로젝션 손실 목록 10항(메타 캐시 기재). 검토(Opus) = **조건부 통과(2026-08-21)** — 치명/중요 0·경미 3건(후속 권고 — 리더 표시 정확성 계열)·재량 판단 3건 수용. 상세 정본 = `stage-37-custom-blocks.plan.md` "완료 기록". **← ✅ M35 게이트 종결 = 통과(2026-08-22 — Claude 위임 판정·실용 수용 기조. 치명 0 — 실사용 발견분 U-6 수정·머지 완료(PR #58) · 잔여 확인 항목은 후속 수정 경로 존치. 판정 정본 = stage-37 말미 "M35 게이트 판정") → M36(stage-38) 착수 조건 성립** | 실사용 확인(**게이트 = stage-37 말미** — 전·후반부 합산 판정) **← ✅ 게이트 종결 = 통과(2026-08-22)** |
| M36 | 모바일 마감 + 캡처 인터페이스 설계 반영 + 구 편집기 퇴역 판단 → **v2.00.0** (**지시서는 stage-38** — M35 분할로 2026-08-19 재조정) | 폰 실기기 DoD·프로젝션 손실 목록 문서화 |

- 단계 문서(stage-31~)는 관례대로 **착수 시 생성**. M31 게이트 통과 전에는 M32 이후에 착수하지 않는다.
- 검증·리뷰 체계(래퍼 스크립트·invariant-scan·stage-reviewer)는 기존 그대로.

## 9. 신규 리스크 (**마스터 계획 §15 등재 완료 2026-08-16** — R33~R40은 색인 행 1줄 + 이 표가 정본, R41은 마스터에도 전문 등재)

| 안 | 리스크 | 대응 초안 |
|---|---|---|
| R33 | BlockNote 0.x 브레이킹 체인지 | 버전 고정(lockfile) + 앱 소유 중립 블록 스키마(§5.2)로 저장분 절연 + 어댑터 1곳 경유 |
| R34 | 프로젝션 손실(블록→Markdown 강등) | 손실 규칙 명문화 + 왕복 코퍼스 DoD + 1차 블록 팔레트 제한 옵션(D6). **경로 추가(2026-08-16, stage-33 실측)**: 손실은 프로젝션(블록→md)뿐 아니라 **엔진 모델 방향(앱 블록→BlockNote)에서도 발생**한다 — **코어 CommonMark/GFM인데 BlockNote 0.54 코어가 담지 못하는 7종**(느슨한 목록·인접 목록 경계·표 열 정렬·경성 줄바꿈·link title·image title/height·block meta). 소유 stage 배정과 판정 절차는 **§5.4 말미 "M33 어댑터가 확인한 코어 범위 제약"** 표가 정본이며, 그중 **느슨한 목록·인접 목록 경계·표 열 증감 시 정렬**은 **사용자 판정 대기(임의 손실 수용 금지)** · 판정 시한 = **stage-35(M34) 착수 전**(기존 문서를 실제로 태우는 시점). **← 손실 규칙·목록 정본 = §14(M36/stage-38 G-7 — 2026-08-22 문서화 완료: 초안 10항 승격 + 손실 3분류·신규 손실 기재 절차 명문화)** |
| R35 | 한글 IME(ProseMirror 기반이라 자체 구현보다 유리하나 실측 전 확정 금지) | M31 게이트에 실기기 실측 포함(R31 계승). **M31 문헌 실측(2026-08-15)**: v0.51~0.54에서 CJK 수정 물결 5회 반영 — v0.54 고정 필수. 잔존 이슈 1679(조합+Enter 음절 소실, P4)·모바일 터치 미성숙 — 실기기 체크리스트 6항이 PoC에 내장. **실기기 실측 완료(2026-08-15 사용자 — PC+폰 6항 전건 이상 없음·1679 미재현) → M31 게이트 조건 해소.** 유지되는 전제: 0.54.0 정확 고정 · M33 노트 베타 실사용 = 2차 관문 · 모바일 터치는 M36 마감 게이트 |
| R36 | 마이그레이션 손상 | 지연 전환 + 전환 전 원본 보존(`content` 무수정) + 배치 변환은 검증 통과 후 |
| R37 | 번들·오프라인(PWA) 비대 | R32-c 실측 기록 + 코드 스플리팅(편집기 청크 분리). **M31 실측(2026-08-15)**: 지연 청크 분리 시 초기 청크 +0.4KB(min)·편집 진입 시 gzip ≈307~415KB 1회 로드 — 수용 판정(분석 보고서 §5.2) |
| R38 | 전환기 편집기 2개 병존의 유지비 | 병존 기간을 로드맵으로 상한(M36에서 퇴역 판단) — 무기한 병존 금지 |
| R39 | **웹사이트 임베드** — 다수 사이트가 X-Frame-Options/CSP로 iframe을 차단하고, 외부 콘텐츠 로드는 보안·프라이버시 표면을 연다 | iframe은 **도메인 화이트리스트**(YouTube 등)만 허용, 그 외 URL은 북마크 카드(제목·썸네일) 폴백. 임베드 블록은 앱 컴포넌트지 원시 HTML이 아니다(F43 ⓐ 불번복 유지). 오프라인 시 카드 캐시 표시. **경로 추가(2026-08-19, M35 착수)**: 북마크 카드 메타 확보 = **서버측 fetch 엔드포인트 신설(api §4.30 — S37)**이며 인증 없는 로컬 서버가 사용자 지정 URL을 대신 fetch하므로 **SSRF 표면**이 새로 열린다(R12 연장 — 내부망 프로브 차단) → **가드 전건을 계약으로 명시**(스킴 2종만·해석 IP 사설/루프백/링크로컬 차단·리다이렉트 매 홉 재검증·본문 512KB/5초 상한·HTML 파싱 후 폐기 — 정본 = §4.30 ②). 캐시는 클라이언트가 블록 prop에 저장(재조회는 명시 조작만·오프라인 = 저장분 텍스트 카드 강등) |
| R40 | **공개 저장소 라이선스**(2026-08-15 사용자 고지 — 저장소 GitHub public) — 소스 공개 = 배포로 간주. GPL 의존성 편입 시 앱 전체 라이선스 문제 발생 | **D10 확정으로 원천 차단**: 라이선스 걸리는 패키지 도입 금지, 해당 기능은 자체 구현·후순위. 신규 의존 추가 시 라이선스 확인을 절차화(MPL·MIT·Apache-2.0·BSD 계열만) |
| R41 | **`notes` 테이블 신설(M33/stage-33) — Stage 1 이후의 스키마 변경**: 저장소가 `documents`와 둘이 되어 노트가 검색(FTS)·인쇄·퀴즈·백업 UX에서 빠진 채 늘어나고, 베타에 데이터가 쌓인 뒤 M34에서 구조를 바꾸면 **두 번째 마이그레이션 대상**이 된다. 복구(휴지통) UI도 없다 | **마스터 계획 §15 R41에 정식 등재(2026-08-16 — 불변 규칙 6 절차)**. 대응: ① 병존 상한을 게이트로 고정(M34 = documents 전환 판정 · M36 = 구 편집기 퇴역 — R38과 같은 정신) ② `blocks_version` **컬럼**으로 스키마 진화 대비(SQL 조회 가능) ③ 백업 자동 포함(VACUUM INTO — 코드 개정 0)·물리 삭제 코드 0 ④ 복구는 `?include_inactive=1`로 확인, 휴지통 UI는 실수요 확인 후 계획서 먼저 ⑤ **정식 메뉴 미노출**(설정 실험실 카드 1개 — screens §5.16)로 검증 전 데이터 유입 억제. **← M36 재론 종결 기록(2026-08-22, stage-38 G-9 — N-1 ⓒ 전제·사용자 결정 미도착 시 기록)**: notes = **존치**(§12-5 기조 — v2.x 캡처 착지 표면)·**노출 현상 유지**(실험실 카드), 정식 승격(검색·인쇄·백업 통합 선행)·documents 이관은 **v2.x 캡처 계획서 시점 재론**(정본 = stage-38 규약 D. N-1 도착 시 그 결정으로 대체) |

## 10. 착수 전 결정 (사용자 확정 대기)

| # | 결정 | 권고안 |
|---|---|---|
| D1 | 저장 소스 오브 트루스를 블록 JSON으로 전환(Markdown은 파생 프로젝션으로 역할 변경) — "Markdown 단일 소스" 전제의 명시 개정 | **조건부 위임(2026-08-15 사용자)** → **M31 판정(2026-08-15): 방향 지지** — 실문서 436텍스트 전건 변환 성공(실패 0)·크기 중앙값 ×2.54(5천 건 외삽 +3.7MB)·6k 블록 단타 28.6ms(분석 보고서 §5.4~5.5). 최종 확정은 설계대로 **M34 게이트**. 노트(베타)는 신규 저장이라 이 결정과 무관하게 블록 JSON으로 시작. **확정 절차 명문화(2026-08-18, stage-35 착수)**: M34 게이트 조건(DoD 자동 검증 전건 + 사용자 실사용 확인) 전건 충족 시 **Claude 위임 판정으로 확정 기록**(판정 주체·일자·근거 실측치를 남긴다) — 조건·기록 양식 = `stage-35-documents-blocks.plan.md` "게이트/판정 규칙"이 정본. 미충족·치명 결함 시 임의 확정 금지(사용자 보고). **← ✅ 확정(2026-08-19 — M34 게이트 종결, Claude 위임 판정)**: 저장 소스 오브 트루스 = 블록 JSON · Markdown은 파생 프로젝션(**전환 문서 한정** — 미전환 문서 퇴로 불변). 근거 실측치·판정 경위 정본 = stage-35 "완료 기록 → M34 게이트 판정 → D1 확정 기록" |
| D2 | 편집 엔진 = BlockNote(후보 B), Tiptap 직접 조립(C)은 구조적 퇴로 | **조건부 위임(2026-08-15 사용자)** → **✅ 확정(2026-08-15 — M31 위임 판정, Claude)**: 게이트 5조건 중 4건 실측 합격 + 후보 A·C 대비 전 축 우위(분석 보고서 §7). **✅ 잔여 조건 해소 = 한글 IME 실기기 실측 완료(2026-08-15 사용자 — PC+폰, 체크리스트 6항 이상 없음·치명 결함 없음) → 게이트 완전 종결·재론 없음.** 버전 0.54.0 정확 고정, 라이선스는 MPL 패키지만(§4 B·D10) |
| D3 | R32 재개 등재 — 재개 사유를 "목표 수준 상향(사용자 지시)"으로 기록하고 필수 조건 3건을 로드맵 DoD에 편입 | **✅ 확정 2026-08-15** — 절차 결정이라 D2 판정 결과와 무관하게 유효 |
| D4 | Markdown 파서는 remark 1개 유지 — 편집기 내장 Markdown 변환 대신 자체 mdast↔블록 변환기 | **✅ 확정 2026-08-15(기술 위임)** |
| D5 | 전환기 렌더는 `MarkdownView` 단독 유지(프로젝션 소비), 블록 네이티브 리더 뷰는 M36 이후 재론 | **✅ 확정 2026-08-15(기술 위임)** **← M36 재론(2026-08-22, stage-38 규약 G — 기술 위임 판정·Claude): 재보류 확정** — `MarkdownView` 단독 유지·블록 네이티브 리더 뷰는 **v2.x 후보**(임의 착수 금지·실수요 실측 후 별도 착수 전 결정). 근거 4건: ① 프로젝션 소비 무결(왕복 3055 실패 0·폴백 0/226·손실은 §14 전수 관리 — 리더 표시 실질 영향 항목 0) ② 리더 = 퀴즈·학습·인쇄·임베드 전 화면 공유 표면(교체 파급 최대인데 실수요 실측 0) ③ 초기 청크 표면(BlockNote 렌더 도입 = R37 정면 충돌) ④ 재론 트리거 = **블록 전용 표현(예: provenance)의 리더 수요 실측**(v2.x 캡처 구현 시점이 자연스럽다) |
| D6 | v2.00.0 범위 = §7.1의 1~7 전부 vs 축소(예: 5 모바일을 v2.1로) · 1차 블록 팔레트를 방언 표현 가능 범위로 제한할지 | **§7.1 전부 + 팔레트 제한 없이, 강등 규칙 명문화** — 기능 요구 7번은 2026-08-15 사용자 지시로 편입(다단만 D10에 따라 후순위 백로그) |
| D7 | 편집 표면 통합의 깊이 — `choices`를 구조화 블록(문제 블록)으로 재설계 vs 필드 유지 + 인라인 리치만 | **1차는 필드 유지 + 인라인 리치, 문제 블록은 M35에서 재론** → **✅ 1차 확정(2026-08-19 — M35 착수, 기술 위임 판정·Claude)**: **필드 유지 + 인라인 리치**로 확정 이행(stage-36 규약 C — 저장 = 기존 문자열 필드 무변·편집 = 기존 폼 + 방언 문법·**렌더만 확장**(지점 전수표는 stage-36 완료 기록)·채점 봉인 무접촉 = 서버 diff 0). 근거: 저장 계약·불변 규칙 1을 건드리지 않고 요구(보기 강조 표현)를 충족하며, 표면 통합 일정 위험이 최소다. **문제 블록 구조화(재론 후반전)는 v2.x 백로그로 이월** — 실수요(보기별 이미지·복합 지문 등)가 실측되면 별도 착수 전 결정으로 |
| D8 | stage-29 유예분(고아 이미지 정리·alt/크기) 편입 여부 | **편입(M35)** — 단 고아 정리는 불변 규칙 4 재해석 필요(업로드 이미지는 `sources/` 원본인가?)로 별도 확정 → **분리 처리(2026-08-19 — M35 착수)**: **alt/크기 = 편입 유지·사실상 충족**(새 편집기 코어가 caption↔alt·크기 핸들↔`{w=}`를 이미 수용 — 잔여 확인은 stage-37 항목). **고아 이미지 정리 = 사용자 확정 대기 존치(임의 확정 금지)** — 선택지 3안(ⓐ 원본 간주 = 정리 금지·현황 표시만 / ⓑ 파생물 재해석 = 2단계 정리 + 불변 규칙 4 명시 개정 / ⓒ 정리 안 함·용량 실측 후 재론 = **권고**)은 `stage-36-surface-unify.plan.md` "하지 않는 것"에 등재. 판정 전 관련 코드 0. **← ✅ alt/크기 종결(2026-08-21 — stage-37 F-8 실측)**: 확인표 ⓐ caption↔alt 왕복 · ⓑ 크기 핸들↔`{w=}` 왕복(이스케이프 평문 보존 포함) · ⓒ 리더 `{w=}` 폭 반영 · ⓓ 크롭 후 ⓐⓑ 유지 — **전건 충족·미충족 0·수정 0**(근거 실측 = stage-37 "완료 기록" F-8 표). stage-29 유예분 중 alt/크기는 종결 — **고아 이미지 정리만 사용자 확정 대기로 존치** |
| D9 | 개발 방식 = **드래프트 격리**(사용자 제안 2026-08-15): 원본 편집기·documents 무접촉으로 노트(베타) 표면에서 새 편집기를 완성 → 검증 후 main 기능으로 탑재 | **✅ 확정 2026-08-15** — 상세는 §12. 단 git 전술은 장기 브랜치가 아니라 **소단위 PR 지속 머지(코드는 플래그·베타 표면 뒤 비활성)** — git 운용 위임 범위 |
| D10 | 라이선스가 걸리는 패키지(XL/GPL 등)의 처리 — 저장소가 GitHub public이라 소스 공개 = 배포로 간주(R40) | **✅ 확정 2026-08-15(사용자)**: 도입하지 않는다 — 해당 기능은 **후순위로 미뤄 자체 구현**(다단이 첫 대상). 신규 의존은 MPL·MIT·Apache-2.0·BSD 계열만 |
| D11 | **선택형 "참조 복사"·양방향 임베드 편집**(2026-08-19 실사용 피드백 — §13 FB-4): 복사→붙여넣기 기본은 일반 붙여넣기 유지, 사용자가 선택하면 "참조 복사" — ⓐ **참조로 붙여넣기**: 붙여넣을 자리에 **문서 임베드 블록 삽입**. 원본 수정 → 붙여넣은 쪽 자동 반영(단방향)은 **기존 F43 트랜스클루전 성질로 이미 성립** — 신규 저장 구조 0, 삽입 진입 UX 1개뿐 ⓑ **양방향 편집**: 임베드된 내용을 그 자리에서 수정하면 **원본 문서가 갱신** | **권고(등재만 — 확정 아님·사용자 확정 대기)**: **ⓐ = M35(stage-36) 후보** — 임베드 리치 표면 격상(§7.1 7번)과 같은 표면 위이고 피커 확정(§5.4 ①)과 정합·비용 저렴. **ⓑ = v2.x 이후 보류 — 임의 착수 금지**: 장점 = "한 내용은 한 곳"의 완성형이나, ① 편집 표면(타 문서 블록의 교차 편집·중첩 임베드) ② 저장 모델(부분 저장이 §4.29 동반 규칙·전환 해제와 충돌 — 전환/미전환 혼재 문서) ③ 정합(순환 참조·같은 화면 이중 표시 동기화) ④ 유출면(R20 임베드 해석 봉인 계약과 편집 경로)에 걸치는 다층 결정이라 **별도 착수 전 결정 + 리스크 등재 선행 필수**. ⓐ만으로도 요구의 절반(원본→사본 방향)은 충족된다 |

## 11. 검증 전략

- 왕복 동형성: `s30-roundtrip.mjs` 코퍼스(365) → Markdown→블록→Markdown 확장판이 M32 DoD
  (**M33 확장**: `s33-adapter-roundtrip.mjs` — 앱 블록↔BlockNote 어댑터를 **순수 JSON 변환**으로 만들어
  DOM 없이 md→블록→BN→블록→md 왕복을 검증한다. stage-33은 코어 범위, stage-34는 전 팔레트가 대상)
- 패리티: 현행 문법·기능 체크리스트(F52 방언 전항·F43 참조 3종·수식·표·이미지 3진입점)를 신 편집기에서 재검증
- 실기기: 한글 IME(조합 중 서식·확정·blur)·모바일 터치는 매 단계 실측 — 추측 확정 금지(R31 계승)
- 불변 규칙 기계 검사(`invariant-scan.ps1`)·테스트 래퍼 등 기존 체계 유지

## 12. 개발 방식 — 드래프트 격리 (D9, 확정 2026-08-15)

사용자 제안("완전히 드래프트해서 원본을 건드리지 말고 노트 기능을 완성하고 main에 탑재")의 이행 규칙.

1. **코드 격리**: 새 편집기 코드는 격리 디렉터리(가칭 `frontend/src/editor2/`)에만 둔다.
   기존 편집기 파일(`MarkdownFieldEditor`·`EditablePreview`·`RichBlockEditor`·`DocEditor`)과
   기존 렌더러(`MarkdownView`)는 **무수정** — v1.90.1 편집 경험은 탑재(M34) 전까지 1바이트도 변하지 않는다.
2. **표면 격리**: 진입점은 신규 **"노트(베타)"** 표면(라우트/패널)뿐. 저장도 신규 `notes` 테이블(§6)로
   documents와 완전 분리 — 실데이터 무위험 상태에서 실사용 검증(IME·모바일·왕복·UX)을 쌓는다.
   **M33 착수 시 구체화(2026-08-16)**: 라우트 = `/notes`·`/notes/:id`(둘 다 lazy) · 진입 = **설정 화면의
   실험실(베타) 카드 링크 1개**(사이드바·하단 탭바 **무변경** — 베타를 상시 메뉴에 올리지 않는다) ·
   **허용 접촉점 전수 = App.tsx 라우트 2줄 + 설정 카드 1개 + `models.py`/`main.py`/Alembic 리비전**
   (목록 정본 = `stage-33-notes-surface.plan.md` "격리 계약" — 그 밖의 기존 파일 diff는 롤백 사유).
3. **탑재 게이트(M34)**: 노트 베타에서 패리티 체크리스트 + 실사용 확인이 통과해야 documents에 연결한다.
   그 시점이 D1(저장 전환)의 최종 확정 시점이며, 실패 시 노트 베타만 남기고 documents는 현행 유지(무손실 철수).
4. **git 전술(위임 범위)**: 수개월짜리 장기 드래프트 브랜치는 쓰지 않는다 — main과의 드리프트·거대 일괄 머지·
   워크트리 소실 전례의 리스크가 크다. 대신 **stage 단위 소 PR을 계속 main에 머지**하되, 새 코드는 베타 표면
   뒤에서 비활성이므로 "원본 무접촉"이라는 격리 목적은 앱 수준에서 그대로 성립한다. main은 항상 안정.
5. **노트 기능의 겸용 가치**: 노트는 검증용 임시물이 아니라 존치 기능 — 강의 노트는 v2.x 캡처 파이프라인
   (녹음·사진·필기 → LLM 정제 → 삽입, §5.5)의 자연스러운 착지 표면이고, 추후 "노트 → 문제/개념 승격" 플로우의
   출발점이 된다.

## 13. 실사용 피드백 로그 (stage-35 머지 이후)

> 관례: **코드 착수 없음 — 여기 등재·배정·권고까지만**(계획서 먼저). 결정 확정은 각 배정처(§10 · M35 착수 전 결정)에서 한다.

**2026-08-19 (사용자 — stage-35 머지 직후 노트·문서 실사용)**

| # | 성격 | 내용 | 배정 |
|---|---|---|---|
| FB-1 | 결함 | 노트 작성 후 목록 → 재진입 시 작성 내용이 안 보임(새로고침 후 보임). 원인 = `useUpdateNote`가 단건 캐시를 갱신하지 않음 | **별도 수정 진행 중**(frontend-dev — stage-35 후속 결함 처리, 계획 변경 없음·기록만. 처리 결과는 stage-35 문서 완료 기록 계열이 정본) |
| FB-2 | 제안 | 노트에 [저장]·[취소] 버튼, 노트 복제 기능이 추후 필요할 것 | **M35(stage-36) 후보 백로그.** 유의 2건 — [저장] 상시 버튼은 **"상시 [저장] 버튼 없음" 확정(screens §5.16 · Design v1.37)의 개정**이라 채택 시 번복 절차 필요 · 복제는 §4.28 API 확장(신규 엔드포인트 1개) 필요. M35 착수 시 범위 판단 → **판단(2026-08-19, M35 착수)**: [저장]/[취소] = **사용자 확정 대기 유지**(번복 절차 없이는 착수 금지) · **복제 = 보류(M35 범위 제외)** — notes의 거취(documents 이관·퇴역)가 M36(stage-38) 재론 대상이라 그 전의 notes 전용 API 확장은 이관 시 이중 작업이 되고, 요구 시점도 "추후". **M36에서 notes 거취와 함께 재론** **← M36 재론 완료(2026-08-22, stage-38 규약 D)**: 복제 = **v2.x 후보 백로그**(N-1 ⓒ 존치 전제 하 실수요 시점 미도래 — 재확인 후 등재) · [저장]/[취소] = **사용자 확정 대기 존치 불변**(§5.16 번복 절차 없이는 착수 금지) |
| FB-3 | 질문 | 문서 임베드 시 해당 문서 **내용**이 편집 표면 안에서 안 보임 — 추후 추가 예정인가? | **예정 — 이미 M35 범위**(§7.1 7번 "문서 임베드 리치 표면 격상" + §5.4 방언 표의 문서 임베드 커스텀 블록). 현재 편집 표면의 docEmbed가 원자·읽기 전용 카드(제목 추종)까지인 것은 M33/M34의 의도된 범위였다 — 새 결정 불요. **→ 착수(2026-08-19): stage-36 편입**(규약 D — resolve-embeds 봉인 응답만 소비·읽기 전용·중첩 1단) |
| FB-4 | 제안 | 복사 → 붙여넣기 기본은 일반 유지 + 선택형 **"참조 복사"**(붙여넣은 쪽 수정 = 원본 수정 · 원본 수정 = 붙여넣은 쪽 갱신) | **신규 착수 전 결정 D11로 등재**(§10) — ⓐ 참조로 붙여넣기(단방향)와 ⓑ 양방향 편집으로 분해. **ⓑ는 임의 착수 금지**(다층 결정 — D11 참조) |
| FB-5 | 제안 | 실험실 토글 ON이면 문서 상세 1곳만이 아니라 **문서 편집 전반**에서 새 편집기가 쓰이길 기대 | **M35 표면 통합이 커버** — stage-35 규약 F가 신규 작성·DocEditor 모달 경로를 M35로 이월해 둔 바로 그 항목이다(§8 M35 행·마스터 §14 M35 행). 이 피드백을 **그 항목의 우선순위 상향 신호**로 기록 — M35 착수 시 편집 진입점 전수(신규 작성·모달·기타)를 체크리스트로 고정할 것. **→ 착수(2026-08-19): stage-36 본체**(진입점 전수표 6곳 — 코드 실측·개조 지점 = 공용 DocEditor 분기 1곳 · 정본 = `stage-36-surface-unify.plan.md` 규약 A) |

## 14. 프로젝션 손실 목록 (정본 — R34 손실 규칙 명문화, 2026-08-22 stage-38 G-7 승격)

> **정본 선언**: 블록↔Markdown 프로젝션 및 앱 블록↔BlockNote(엔진 모델) 방향의 손실은 **이 절이 단일
> 정본**이다. 출처 = `stage-34-notes-dialect.plan.md` 말미 "프로젝션 손실 목록 초안" 10항(2026-08-16~21
> 누적)의 **이동·상태 대조**(stage-38 G-5 — **원문 무재작성·상태 주석만 가산**. 이후 갱신은 여기에만).
> 상태 집계(2026-08-22): **해소 3(2·6·9) · 부분 해소 1(4) · 유지 6(1·3·5·7·8·10)**.
>
> **손실 규칙(R34 명문화)**
> 1. **손실 3분류** — ⓐ **명문화된 강등 손실**(프로젝션 비대상으로 계약된 것 — 예: 10번 메타 캐시)
>    ⓑ **보고 유지**(흡수 불가·표본 0 — 예: 1번) ⓒ **알려진 한계**(엔진 귀책 또는 의도한 비용·실질
>    투영 무변 — 예: 5번).
> 2. **신규 손실 발견 시 절차** — 발견 즉시 이 목록에 기재(분류·근거·표본 수 병기). **기재 없는 손실 =
>    조용한 손실로 간주**(R34 위반 — 강등 도피 금지·보강 원칙, M32~M35 게이트 규칙 계승).
> 3. 흡수(사이드카 등)로 해소되면 **상태만 갱신·행 삭제 금지**(이력 보존·해소 근거 병기).

1. **인라인 수식에 걸린 서식** — `math` 스펙 propSchema가 비어 담을 자리 없음. `inline:mathStyles`로
   **보고 유지**(코퍼스 표본 0건). 툴바 입력은 원자 가드로 차단. 흡수하려면 **자체 수식 스펙으로 전환**해야 한다.
   → **[상태: 유지 — 분류 ⓑ 보고 유지(표본 0. stage-37 완료 기록에도 잔여 미지원 사유 1종으로 재확인)]**
2. **원자 가드의 원리적 한계** — BlockNote **코어 버튼과 그 단축키(`Ctrl+B` 등)** 는 막지 않는다. 원자가 포함된
   선택에 코어 마크를 얹으면 그 원자의 마크가 저장·재로드에서 사라진다. 사라지는 것은 사용자의 **텍스트가
   아니라 원자에 얹은 표현 속성 하나**이고, 코어 키맵을 통째로 감싸려면 커맨드 레이어 확장이 필요해 **M35 범위**
   (`toolbar/atoms.ts` "알려진 한계"에 기록).
   → **[상태: 해소(stage-36 F-8, 2026-08-20) — 원자 서식 가드를 코어 버튼 4종(가드 덮개)·코어 단축키
   4종(원자 포함 시에만 차단)으로 확장(stage-36 완료 기록 실측). 잔여 후속 후보 = 칩 포함 선택의 링크
   허용(stage-36 "구현 중 판단 기록" 3 — 손실 아님·기능 제한)]**
3. **같은 블록·같은 href·서로 다른 link title** → 마지막 값이 남는다(허용 · 주석 명시).
   → **[상태: 유지 — 분류 ⓒ 알려진 한계(허용 계약·주석 명시)]**
4. **`table:align`은 열 수가 바뀌면 버려진다**(의도 — 열 증감 시 정렬 배열의 대응이 성립하지 않는다).
   → **[상태: 부분 해소(stage-36 F-6) — 편집 UI 경로는 열 증감 시 정렬을 `tr.mapping` probe로 승계해
   어긋남 자체가 소멸. 어댑터의 "열 수 불일치 = 폐기" 방어 규칙은 **편집기 밖 유입 데이터**에 유지
   (분류 ⓒ — 의도된 방어)]**
5. **run 경계의 `\n`은 서식을 잃는다** — BlockNote가 `hardBreak` PM 노드를 앞 run에 `\n`으로 붙이며 그 노드의
   mark를 무시한다. 어댑터에서 "run 끝의 `\n` = 서식 없음, run 안쪽의 `\n` = run 서식 유지"로 양방향을 맞춰
   **실질 영향은 Markdown 투영 무변**.
   → **[상태: 유지 — 분류 ⓒ 알려진 한계(투영 실질 무변 — 양방향 정합 처리 완료)]**
6. **콜아웃 본문의 시각적 소속** — children이 BlockNote 중첩 그룹으로 렌더돼 콜아웃 상자 "**바깥 아래**"에
   붙는다. `notes.css`에서 좌측 보더+들여쓰기로 이어 붙였으나 **한 상자로 감싸는 마감은 M35**.
   → **[상태: 해소(stage-36 F-8, 2026-08-20) — children 시각 소속 CSS 계층 마감(색 리터럴 0). 표시
   문제였고 저장 손실 아님 — 해소로 종결]**
7. **수식이 든 문단을 통째로 선택하면 방언 서식 버튼 6종이 비활성**으로 보인다(원자 가드에 `math`를 넣은
   결과 — **의도한 비용**).
   → **[상태: 유지 — 분류 ⓒ 알려진 한계(의도한 비용 — 원자 보호가 우선)]**
8. **`:t` 혼합 선택에 색·크기를 적용하면 다른 run의 미지 속성이 소멸한다**(검토 경미-2 — 2026-08-16 추가).
   `toolbar/textStyle.ts`의 `setTextStyleKey`는 `getActiveStyles()`(선택 **head 한 곳**)의 `:t` 쌍을 읽어
   **선택 전체에 덮어쓴다**. 그래서 `:t{x=1}`처럼 화이트리스트 밖 속성을 가진 run이 섞인 구간에 색을 적용하면
   그 run의 미지 속성이 조용히 사라진다 — `t`를 통짜 JSON으로 둔 이유("손실 0")와 어긋나는 유일한 지점이다.
   **정확히 고치려면 run 단위 병합 API가 필요**한데 BlockNote의 스타일 적용은 선택 전체 단위라, 어댑터가 아니라
   **툴바 커맨드 레이어의 확장(M35)** 이 있어야 한다. 실사용 노출도는 낮다(미지 속성은 반입 데이터에서만 온다).
   → **[상태: 유지 — 분류 ⓒ 알려진 한계. stage-36 UX 마감에 미포함 실측(F-8은 원자 가드 확장이며
   textStyle run 병합과 별개 — stage-36 완료 기록 대조). 노출도 낮음(미지 `:t` 속성 = 반입 데이터 한정·
   현재 표본 0) — run 단위 병합 커맨드는 v2.x 후보(실수요 실측 후)]**
9. **마이크로 마크 접힘이 사용자에게 고지되지 않는다**(검토 중요-1의 잔여분 — 2026-08-16 추가).
   저장 경로가 `fromBlockNoteBlocks`를 부르며 `fromBlockNoteResult().microMarkCollapses` 집계를 버리므로,
   역방향 방어적 정규화(규약 C 강제 지점 ②)가 마이크로 마크를 접어도 배너가 뜨지 않는다. **툴바 우회로
   (기본 밑줄 버튼)와 붙여넣기 경로는 이번에 닫았으므로**, 남은 유입 경로는 **편집기 밖에서 만들어진
   `content_blocks`에 마이크로 마크 2개 이상이 겹쳐 있는 경우**뿐이다(노트는 신규 저장소라 현재 표본 0).
   `documents`를 탑재하는 **stage-35에서 유입 경로가 열리므로** 그때 고지 결선을 함께 볼 것.
   → **[상태: 해소(stage-35 규약 D·F-3, 2026-08-18) — documents 저장 경로를 `fromBlockNoteResult`
   경유로 고정(직접 호출부 0)하고 `microMarkCollapses > 0` 배너 고지 결선. stage-36 규약 B가 "접힘
   고지 전건 계승"을 명시(통합 진입점 전체에 유효)]**
10. **웹 임베드 메타 캐시는 프로젝션 비대상**(stage-37 규약 B — 2026-08-21 추가). `::web` directive에는
    `url`·`title`만 실리고 §4.30 조회 캐시(`meta` — description·siteName·image·favicon·fetchedAt)는
    Markdown 프로젝션에서 빠진다 — **명문화된 강등 손실**. 전환 문서는 블록 JSON이 소스라 실손실 0이고,
    md→블록 재전환 시 meta 없음 = 텍스트 카드로 시작 → [메타 새로고침]으로 복원된다(stage-37 규약 B 정본).
    → **[상태: 유지 — 분류 ⓐ 명문화된 강등 손실(계약 — 전환 문서 실손실 0·명시 복원 경로 있음)]**
