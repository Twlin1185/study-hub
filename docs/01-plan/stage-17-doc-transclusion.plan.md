# Stage 17 — 문서 상호참조·표현 UX: 트랜스클루전·가리기/접기·이동 (M17: F43)

> 상위: `study-app.plan.md` **v0.28** §14(M17)·**F43**(결정 사항·채택하지 않은 후보·착수 전 결정 목록의 단일 출처) · 설계: **§4.19 신설 완료(2026-08-02, Design v1.21 — 상세 계약·자리표시자 문구·구현 앵커의 정본)**
> 배경(사용자 요청 2026-07-28): "품질경영기사-필기-통계적 품질관리"에 "확률과 확률분포" 문서를 만들면 산술평균·모분산 같은 내용이 그 안에 보여야 하는데, 이미 모듈화된 "산술평균" 문서가 따로 있다 — 이를 **레퍼런스로 접근해 문서 안에서 그 자리에 보이게**(트랜스클루전) 한다. 개념 단위 모듈 문서를 여러 상위 문서가 임베드해 재사용하는 것이 핵심.
> **상태: 구현 완료·검토 통과(2026-08-02).** D1~D4 전건 확정 → 구현 → stage-reviewer(Opus) 검토 2회(조건부 통과 → 중요 2건 수정 후 **통과**). 경미 결함 후속 수정 완료(4건 수정·1건 의도 보류) — 잔여 = 폰 실기기 확인뿐. 하단 "완료 기록" 참조.
> 순서 관계(plan §14): 권장 M16 → M17. M15·M16과 파일 충돌 없음(문서 렌더·열람 계열) — 독립 진행 가능.
> 불변 규칙 재확인: **채점은 서버에서만·정답/해설 응답 미포함(1) — 임베드 해석 API가 새 유출면이 되지 않게 계약 수준 봉인** · 소프트 삭제(3 — 참조가 삭제를 막지 않는다, 자리표시자로 표시) · 색상 하드코딩 금지(5 — 임베드 카드·접기 효과 전부 토큰) · 에러 규약 §3.

## 현행 실측 (2026-07-28 — 계획서 §14 F43이 정본, 여기는 요지)

- `documents.content` = **Markdown**(TEXT, §6.2). 렌더러 = 공용 `MarkdownView`(react-markdown + remark-gfm + rehype-highlight).
- 열람·편집·풀이는 문서 3대 공용 모듈(DocViewer/DocEditor/QuizCard — F37)로 수렴 — **렌더 확장 1곳이 전 화면(문서 상세·학습·커리큘럼·탐색·퀴즈·플래시카드·인쇄)에 파급**된다.
- `document_relations`의 `relation`·`created_by`는 자유 텍스트(CHECK 없음) — `'embeds'`/`'embed'` 값 추가에 DDL 불요(§6.2 주석 갱신만, 완료).

## 착수 시 선행 절차 (게이트 아님 — 결정·문서를 먼저 굳힌다)

- [x] **D1. 레퍼런스 문법 최종 확정** — 잠정: `![[DOC-0012]]`(임베드) / `[[DOC-0012]]`(링크·이동) / `[[#절 제목]]`(같은 문서 앵커), 표시명 별칭(`![[DOC-0012|산술평균]]`) 지원 여부 포함. 참조 키는 `doc_no` 고정(R1 — 재분류에 불변).
  — **확정(2026-08-02, §4.19 ①)**: 잠정안 그대로 + **별칭 `|별칭` 지원**(세 형태 공통). doc_no 정규식 **`DOC-\d{4,}`**(`document_service._generate_doc_no()` = `DOC-{n:04d}` 실측).
- [x] **D2. 가리기·접기 구간 문법 확정** — 후보: remark-directive 컨테이너(`:::fold[제목]`…`:::` 류 — 프론트 플러그인 1개 추가) vs 원시 HTML `<details>`(rehype-raw 필요 — **XSS 표면 확대로 잠정 기피**). 채택 플러그인은 계획서 F43에 등재.
  — **확정(2026-08-02, §4.19 ②)**: **remark-directive 채택** — `:::fold[제목]`(접기)·`:::hide[제목]`(가리기), 제목 생략 시 기본 라벨, 중첩은 directive 표준(외곽 콜론 증가). rehype-raw **기각 확정**.
- [x] **D3. 세부 결정** (계획서 F43 "착수 전 결정 필요" ③~⑦): 임베드 깊이 상한 값(잠정 2~3) · 인쇄 뷰(F22) 임베드 펼침 여부(잠정: 펼침) · 한국어 헤딩 슬러그 규칙 · 해석 API 형태(배치 1회 vs 문서별, 캐시) · DocEditor 참조 삽입 도우미 범위(최소 = 검색 팝업 → doc_no 삽입).
  — **확정(2026-08-02, §4.19 ③~⑤·⑧·⑨)**: 깊이 상한 **2** · 인쇄 = **임베드 펼침 + fold/hide 전부 공개** · 슬러그 = **github-slugger 계열(rehype-slug)** · 해석 = **배치 1회 `POST /api/documents/resolve-embeds`**(상한 50, 열람 세션 메모리 캐시) · 도우미 = **최소안**(검색 팝업 → 커서 삽입 + 문법 힌트).
- [x] **D4(문서). 설계 §4.19 신설** — 임베드 해석 엔드포인트 계약(**응답 스키마에 answer·explanation 필드 자체가 없음** — 필터링이 아니라 부재), 자리표시자 3종(순환/삭제됨/부재) 문구, 문법 명세(§8.2 반입 규격에 영향 없음 확인 — 본문 문자열일 뿐), embeds 인덱스 동기화 시점(documents POST/PATCH·반입 commit). plan §6.2 DDL 변경 없음 전제를 설계에서 재확인(어긋나면 보고).
  — **완료(2026-08-02, Design v1.21 §4.19)**: 해석 계약(answer·explanation 스키마 부재·부분 성공 `found:false`·삭제 문서 content null) + 자리표시자 **4종**(순환/삭제/부재/**깊이 초과** 추가) 문구 + §8.2 영향 없음 확인 + 동기화 3지점(POST/PATCH/commit, 같은 트랜잭션 — **임베드만 인덱스, 링크 칩 제외**) + 전량 재계산 멱등 함수(R20) + 삭제 경고 = **기존 DocumentDetail.relations 재사용(신규 API 0)** + **DDL 0건·Alembic 0건 재확인** + 구현 앵커(파일 경로 실측) 등재.

## 목표

끝났을 때, "확률과 확률분포" 문서 안에서 `![[DOC-xxxx]]` 참조 하나로 기존 "산술평균" 문서가 **그 자리에 임베드 카드로 펼쳐져 보이고**, 같은 모듈 문서를 여러 상위 문서가 재사용한다. 구간 가리기·접기로 암기형 열람이 되고, 문서·구간 이동 버튼이 동작하며 — 이 모든 확장에서 **정답·해설이 새로 유출되는 경로가 0**이다.

## 작업 체크리스트

> 권장 순서: **1(해석 API·인덱스) → 2(임베드 렌더) → 3(가리기·접기·이동) → 4(편집 도우미·사용처 표시) → 5(테스트) → 6(문서·매뉴얼)**.

### 1. 백엔드 — 임베드 해석 API·참조 인덱스

- [x] **임베드 해석 엔드포인트(신규 1개, 설계 §4.19 계약)**: doc_no 목록 배치 조회 → 항목당 `{doc_no, found, id, title, type, content, is_active}`**만** 반환(id = 내비게이션용 — §4.19 보완 2026-08-02). **answer·explanation은 응답 스키마에 존재하지 않는다**(불변 규칙 1 — QuizCard가 같은 렌더러를 쓰므로 계약 수준 봉인). 존재하지 않는 doc_no는 부재 표시로 반환(404 아님 — 배치 부분 성공)
- [x] 본문 파서(서버): content에서 임베드·링크 참조 추출(D1 문법) — 저장(documents POST/PATCH)·반입(commit) 시 `document_relations(relation='embeds', created_by='embed')` **동기화**(본문에서 사라진 참조 행은 삭제, 본문이 단일 출처·행은 파생 인덱스)
- [x] **전량 재계산 경로 확보**(R20): 기존 문서 전체를 재파싱해 embeds 인덱스를 재구축하는 관리 함수 1개(도입 시 1회 백필 겸용 — 새 테이블·마이그레이션 없이 인덱스가 언제든 재생성 가능함을 보장)
- [x] 문서 소프트 삭제 시 "N개 문서에 임베드됨" 경고 정보 제공(embeds 역참조 집계 — §4.19 ⑦ 확정: **기존 DocumentDetail.relations 재사용, 신규 API·필드 0**). **삭제 차단은 하지 않는다**(§6.3 원칙)

### 2. 프론트 — 트랜스클루전 렌더 (MarkdownView 확장)

- [x] MarkdownView에 참조 문법 처리 추가(D1·D2 채택 플러그인) — **공용 모듈 1곳 수정으로 전 화면 일관 적용**, 화면별 분기 금지
- [x] 임베드 카드: 대상 문서 본문 렌더 + 출처 배지(DOC-번호·제목) + [원문 열기] — 스타일은 토큰만
- [x] **순환·깊이 방어**: 중첩 깊이 상한(D3) + 방문 집합 순환 검출 → "순환 참조" 자리표시자(무한 재귀·무한 fetch 0)
- [x] **자리표시자 4종**(§4.19에서 "깊이 초과" 추가 확정): 순환 / 삭제된 문서(is_active=0 — 내용 미노출, 복원 시 재표시) / 존재하지 않는 참조 / 깊이 초과 — 문구는 설계 §4.19 확정안(+해석 요청 실패는 [다시 시도] 실행 상태로 별도)
- [x] 해석 호출은 배치·캐시(D3 확정안) — 한 문서 열람에 임베드 수만큼 개별 요청을 쏘지 않는다
- [x] **학습 의미론 불변 확인**: 임베드 열람이 임베드 대상 문서의 study_progress·SRS·attempts에 어떤 기록도 만들지 않는다(열람한 것은 상위 문서 — 이중 진도 방지)

### 3. 가리기·접기·이동

- [x] **접기(collapse)**: D2 문법 구간 — 제목 줄만 표시, 클릭 펼침(기본 접힘). 전환 애니메이션은 토큰·CSS만
- [x] **가리기(hide)**: D2 문법 구간 — 내용 가림 처리, 탭/클릭으로 공개(암기·자가 테스트용). 인쇄 뷰에서의 처리(가린 채 vs 공개)는 설계 §4.19에서 확정
- [x] **이동 버튼**: `[[DOC-xxxx]]` = 문서 칩 → 문서 상세 이동 / `[[#절 제목]]` = 같은 문서 헤딩 앵커 스크롤(D3 슬러그 규칙). 임베드 카드 [원문 열기]와 내비게이션 재사용
- [x] 폰(모바일) 동작 확인 — 탭 공개·앵커 스크롤·임베드 카드 반응형 — **구현 수준(반응형·print CSS·SSR 렌더)은 검증 완료, 실기기 확인만 사용자 몫으로 잔류(DoD 4)** — **2026-08-02 사용자 확인으로 완료 처리**

### 4. 편집 도우미·사용처 표시

- [x] DocEditor에 **참조 삽입 도우미**(D3 범위 — 최소: 문서 검색 팝업 → 선택 시 커서 위치에 `![[DOC-xxxx]]` 삽입) + 문법 힌트 표시. WYSIWYG 미리보기는 만들지 않는다(기존 편집 폼 유지)
- [x] 문서 상세 사용처 영역에 "이 문서를 임베드한 문서 N개" 역참조 목록(embeds 인덱스 활용 — 기존 "N곳에서 사용 중" 표시와 나란히)
- [x] 인쇄 뷰(F22): D3 확정안대로 임베드 펼침 처리(잠정: 펼침 포함 — A4 정리본 자기완결성). `break-inside` 규칙이 임베드 카드에도 적용되는지 확인

### 5. 테스트·검증

- [x] `services/sm2.py` 외 실행 스모크 원칙(불변 규칙 7) — 단 **본문 파서·인덱스 동기화·해석 응답 스키마는 단위 테스트 작성**(정답 유출·인덱스 정합은 핵심 로직 취급): ① 해석 응답에 answer/explanation 부재 ② 저장 시 embeds 동기화(추가·제거) ④ 재계산 함수 멱등성 — `tests/test_embed_service.py` 16건 통과(전체 회귀 296). ③ 순환·깊이·삭제·부재 자리표시자는 서버 응답(pytest) + 프론트 remark 파이프라인 SSR 실측으로 확인
- [x] 스모크: 퀴즈 화면에서 임베드 포함 문제 지문 열람 시 네트워크 응답 전수에 정답·해설 부재 확인(불변 규칙 1 — DoD 3 직결) — 실기동 스모크: 해석 응답 키 전수 = `{doc_no,found,id,title,type,content,is_active}`(answer·explanation 키 부재), 422 검증 3종(초과 50·빈 배열·형식 위반) 확인. quiz/session 응답 미변경
- [x] stage-reviewer(Opus) 검토: DoD + 해석 API 유출면 점검 + MarkdownView 외 화면별 렌더 분기 0건 — 1차 조건부 통과(중요 2: QuizRun embeds 칩 노출·진척 문서 미갱신) → 수정 후 재검토 **통과**(치명·중요 0, 경미 5 잔여 — 하단 완료 기록)

### 6. 문서

- [x] 계획서 F43에 구현 확정 사항 기록(확정 문법·깊이 상한·플러그인) · 설계 §4.19 확정본 반영(구현 중 보완 2건: id 필드·요청 실패 상태) · R20 갱신(재계산 멱등 함수로 대응 경로 확정 — §15 표 자체 변경 없음)
- [x] 사용자 매뉴얼(F39): 문서 작성 팁 섹션에 참조 문법·가리기/접기·이동 버튼 사용법 추가(§4 h3 "본문 작성 문법" + §12 인쇄 안내 — 검토에서 기능 사실과 불일치 0 확인)
- [x] 이 문서 체크박스 갱신(불변 규칙 10) · CLAUDE.md 문서 지도 범위 표기 확인(4.1~4.19·[S1]~[S17]·stage 1~17 완료로 갱신)

## DoD (완료 정의)

1. 개념 모듈 문서 1개를 **서로 다른 상위 문서 2곳 이상에서 임베드**해 각 문서 안에서 내용이 그 자리에 보인다(라이브 확인 — 사용자 예시 시나리오 재현).
2. 순환 참조·삭제된 참조·존재하지 않는 참조가 각각 자리표시자로 안전하게 표시된다(렌더 크래시·무한 재귀·무한 요청 0).
3. **임베드 해석 경로로 정답·해설이 내려오는 응답 0** — 퀴즈·플래시카드 화면 포함 전 화면에서(응답 스키마 부재로 보증 + 스모크 확인).
4. 가리기·접기·이동(문서 칩·헤딩 앵커)이 PC·폰에서 동작하고, 시각효과는 토큰만 사용(다크 모드 공짜).
5. 임베드 열람이 대상 문서의 진도·SRS에 기록을 만들지 않는다.
6. embeds 인덱스는 본문 재파싱으로 전량 재구축 가능하다(재계산 함수 멱등 — 본문이 단일 출처).
7. DDL 변경 0건·Alembic 0건 (설계 §4.19에서 재확인 — 어긋나면 착수 중단 후 보고). 신규 엔드포인트는 임베드 해석 1개뿐.

## 이 단계에서 하지 않는 것

- **블록/구간 단위 트랜스클루전**(`![[DOC-0012#절]]`) — 문서가 이미 개념 단위 모듈이라는 전제와 중복. 실수요 확인 후 v1.x+ 후보로 계획서에 먼저 확정.
- **참조 대상 삭제 차단·강한 참조 무결성** — 소프트 삭제 원칙과 충돌. 자리표시자 + 삭제 시 경고로 해결(계획서 F43 확정).
- **임베드 내용의 FTS 색인 포함** — 검색은 정본 문서에서 이미 걸린다(이중 히트 방지).
- **임베드 문서의 퀴즈 출제 범위 자동 편입** — 출제 범위는 기존 분류 연결·북마크로만.
- **WYSIWYG 편집기·실시간 미리보기 강화** — DocEditor는 텍스트 폼 + 삽입 도우미까지.
- **문서 버전 관리** — R4 결정 유지(updated_at만).
- **rehype-raw(원시 HTML 렌더) 도입** — XSS 표면 확대. D2에서 directive 계열이 기각될 때만 재검토(그 경우 새니타이즈 계획을 설계에 먼저).

## 리스크

- **R20(신설 — 계획서 §15)**: 본문-인덱스 불일치(재파싱 수렴·재계산 함수로 대응) · 순환/깊이(상한+방문 집합) · 해석 API 유출면(스키마 부재로 봉인).
- 렌더 성능: 임베드 다수 문서의 열람 지연 — 배치 해석·캐시(D3)로 대응, 개인 규모에서 과최적화 금지.
- 문법 충돌: `[[…]]`가 기존 본문에 우연히 존재할 가능성 — 도입 시 전 문서 스캔으로 실측(재계산 함수 겸용), 충돌 실측 시 문법 재검토(D1로 회귀). → **실측 완료(2026-08-02, 검토 중 study.db 214문서 스캔): `[[`·`![[`·`:::` 사용 0건 — 충돌 없음, 백필 불필요.**

## 완료 기록 (2026-08-02)

- **경위**: 선행 절차(D1~D4, plan-architect) → 백엔드(sonnet)·프론트(opus 승격 — 재귀 임베드 렌더) 병렬 구현 → 통합 스모크(실기동) → stage-reviewer(Opus) 1차 조건부 통과 → 중요 2건 수정 → 재검토 **통과**. 커밋: `51dde1a`(선행 문서) → `81ce77b`(백엔드) → `2ef0e1b`(프론트) → `138b6c5`(id 보완) → `9c18ace`(검토 반영).
- **구현 중 계약 보완 2건(§4.19 반영)**: ① 해석 응답에 `id` 필드 추가([원문 열기]·링크 칩의 `/docs/{id}` 직행용 — found:true 시 제공) ② 자리표시자 4종 외 "해석 요청 실패" 프론트 실행 상태(+[다시 시도]).
- **DoD 판정**: 7/7 충족(검토 확정). 단 DoD 1의 브라우저 라이브 펼침·DoD 4의 폰 실기기 동작은 **사용자 직접 확인 항목**(서버 실측·SSR 렌더·print CSS 컴파일 검증까지는 완료).
- **경미 결함 후속 처리(2026-08-02, 사용자 지시 "알잘딱 수정" — 커밋 `9732b7e`·`3bc7048`)**: 검토 1차의 경미-2(스테일 주석)는 `9c18ace`에서 기해소.
  1. ✅ `document_service.remove_relation`의 파생 embeds 행 삭제 → `relation != 'embeds'` 조건 추가(+회귀 테스트, test_embed_service.py 17건).
  2. ✅ `refSyntax.ts collectRefDocNos()` 데드코드 → 삭제(+재도입 방지 주석).
  3. ✅ `QuestionCard` doc_no 미전달 → MarkdownView에 `docNo` 전달(퀴즈류 화면 자기 임베드 즉시 순환 검출).
  4. ✅ 전량 재계산 진입점 → `backend/scripts/recompute_embeds.py` 신설 + docstring에 실행 명령(실 DB 검증: 214개 문서·0행 재구축 — 임베드 문법 미사용 상태라 예상대로).
  5. ⏸ 임베드 다수 문서의 렌더 노드 곱셈 상한 — **의도 보류**(개인 규모 무해 · "과최적화 금지" 원칙, 실사용에서 지연 체감 시 재검토).
- **관찰(결함 아님)**: 링크 칩 로딩 중 스타일이 비활성 칩과 동일(순간적) · 열람 세션 캐시는 라우트 내 문서 편집 후 복귀 시 이전 본문 표시 가능(§4.19 "무효화 체계 없음" 계약 준수 — 동작 특성).
