"""사이트 어댑터(F35-2, 설계 §4.13).

`base`(공통 인터페이스·자료구조) · `registry`(등록·우선순위·크롤링 예의) ·
`qnet`(사이트별 어댑터)로 구성된다. 라우터·convert 잡은 `registry`만 의존한다.

S13에서 사설 사이트 어댑터들은 계획서 §14 F35-2 "제거 이력"에 따라 코드와 함께
삭제됐다 — 등록 어댑터는 qnet 하나다.
"""
