# Stage 31 — 에디터 v2: BlockNote 스파이크·집중 분석 게이트 (M31)

> 상태: **분석·판정 완료(2026-08-15) — D2 확정·D1 방향 지지. 잔여 = 사용자 이행 항목(한글 IME 실기기 실측) 1건.**
> 산출물 정본 = `editor-v2.m31-analysis.md`. 경위는 말미 "완료 기록".
>
> (이전 상태: 착수(2026-08-15) — 사용자 지시 "M31 집중 분석 착수")
> 정본: `editor-v2.plan.md` §8 M31 행(범위·게이트) + §10 D1·D2(위임 판정 규칙). 이 문서는 그것을
> 작업 순서로 전개한 지시서다. **미확정 결정(D1·D2) 관련 "구현"은 금지** — 이 단계의 코드는 전부
> **판정용 스파이크(PoC)** 이며, 판정 결과와 무관하게 폐기 가능해야 한다.

## 범위 요약

- **산출물 = 문서다**: ① 가능성·한계 **집중 분석 보고서**(`docs/01-plan/editor-v2.m31-analysis.md` 신설)
  ② D1·D2 위임 판정 ③ 판정 반영 `editor-v2.plan.md` §4·§10 개정. 코드는 판정의 증거물이다.
- **코드 격리(D9)**: 스파이크 코드는 `frontend/src/editor2/poc/` + 라우트 1개(지연 로드)만.
  기존 편집기 4파일(`MarkdownFieldEditor`·`EditablePreview`·`RichBlockEditor`·`DocEditor`)과
  `MarkdownView`는 **1바이트도 수정하지 않는다**. 프로덕션 UI에 진입점(메뉴·링크) 없음 — URL 직접 진입만.
- DDL 0건 · Alembic 0건 · API 0건 · 백엔드 diff 0 · §6.2 미수정.
- **신규 npm 의존**: @blocknote 코어 계열만(MPL-2.0). **XL(GPL) 설치 금지(D10)**. 설치 전
  의존 트리 라이선스 전수 검사(MPL·MIT·Apache-2.0·BSD·ISC 계열만 — R40)가 선행 조건.
- 번들: PoC 청크는 코드 스플리팅으로 분리 — 기존 첫 로드 영향 ≈ 0을 실측으로 확인(R32-c).

## 체크리스트

### 1. 선행 조사 (병렬 리서치 — 코드보다 먼저)
- [x] 1-1. 기능 실태 조사 — 완료(보고서 §2): 표 내장(옵션 활성), 수식·다이어그램은 v0.54 신규 무료 MPL
      별도 패키지(계획서 §4 정정), 찾기/바꾸기·TOC 부재(M35 자체 구현), 커스텀 스펙 API 3종 로컬 .d.ts로
      시그니처 확인, 테마 = `--bn-colors-*` CSS 변수, React 19 공식 지원
- [x] 1-2. 라이선스 전수 조사 — 완료(보고서 §3): @blocknote MPL 계열 9패키지 확인, XL 4종만 GPL 듀얼(미설치)
- [x] 1-3. 리스크 스캔 — 완료(보고서 §4): IME 수정 물결 5회(v0.51~0.54)·잔존 이슈 1679, 성능 이슈 v0.48
      해소·가상화 없음, 모바일 최약, 브레이킹 5회/10개월, 협업 undo 이슈는 비해당

### 2. 스파이크 구축 (frontend/src/editor2/poc/)
- [x] 2-1. 의존 설치 — @blocknote/{core,react,mantine} + @mantine/{core,hooks} 정확 0.54.0 고정,
      dev로 server-util+yjs 계열(헤드리스 변환용, 배포 무관)
- [x] 2-2. PoC 라우트 `/editor2-poc` — App.tsx lazy 라우트(기존 파일 변경은 App.tsx 12줄이 전부)
- [x] 2-3. 토큰 테마 결선 — poc.css 96줄, 라이트/다크 모두 스크린샷 확인(불변 규칙 5 준수)
- [x] 2-4. 방언 커스텀 스펙 3종 — 전부 성립(보고서 §5.1): 형광펜(값 보유 스타일)·스포일러(클릭 공개 동작
      확인)·참조 칩(원자 인라인). 슬래시 메뉴·툴바 통합은 수동 등록 방식(예상 범위)
- [x] 2-5. 장문 하네스 — 1k/3k/6k 생성 버튼 + 로드·타이핑 지연·JSON 크기 통계 표시 내장

### 3. 실측 (게이트 증거)
- [x] 3-1. 라이선스 기계 검사 — production 트리 278패키지: **GPL/LGPL 0건**(UNLICENSED 1 = private 자기 자신)
- [x] 3-2. 번들(R32-c) — 초기 청크 1,577.3→1,577.7KB(+0.4KB), PoC 지연 청크 gzip 271KB+CSS 36KB,
      온디맨드 gzip 81+27KB. 편집 진입 1회 gzip ≈307~415KB — 수용(보고서 §5.2)
- [x] 3-3. 장문 편집 — 로드: 1k 163ms / 3k 733ms / 6k 2.9s(최대 6.4s 관측·일괄 교체 최악 경로),
      **단타 키다운→페인트: 3k 15ms · 6k 28.6ms(최대 30ms) = 합격**(보고서 §5.4)
- [x] 3-4. 대량 문서 — 실데이터로 확대 실측: study.db 실문서 436텍스트 **전건 변환 성공(실패 0)**,
      크기 중앙값 ×2.54·전체 ×3.34, 5천 건 외삽 +3.7MB. 목록/검색/FTS는 프로젝션 소비로 구조상 무영향
      (보고서 §5.5. 1회성 변환 스크립트는 실행 후 폐기 — 계획대로)
- [x] 3-5. 한글 IME — 자동화로는 IME 조합 재현 불가(CDP 한계) → 문헌 검증 + 데스크톱 스모크(슬래시
      메뉴·undo·비조합 입력) + **PoC 페이지에 실기기 체크리스트 6항 내장. 실기기 실측 = 사용자 이행 항목**

### 4. 보고서·게이트
- [x] 4-1. 집중 분석 보고서 — `editor-v2.m31-analysis.md` 작성 완료
- [x] 4-2. **D2 판정 = 확정(위임 판단)** — 게이트 5조건 중 4건 실측 합격 + A·C 대비 전 축 우위.
      잔여 통과 조건 = 사용자 IME 실기기 실측(치명 결함 시 재론). 근거 = 보고서 §7
- [x] 4-3. **D1 판정 = 방향 지지** — 최종 확정은 설계대로 M34 게이트 유지
- [x] 4-4. 판정 반영 — `editor-v2.plan.md` 머리말·§4 B(정정)·§8 M31·§9 R35/R37·§10 D1/D2 갱신.
      마스터 계획 등재는 게이트 완전 종결(IME 실기기) 후 — 계획대로 보류
- [x] 4-5. 검증 — tsc·`npm run build` 성공, `invariant-scan.ps1` **PASS**(오탐 1건은 문구 수정으로 해소),
      기존 편집기 4파일·MarkdownView **diff 0**(변경 = App.tsx 12줄 + package.json, 나머지는 신규 파일)

## 하지 않는 것 (M32 이후 — 게이트 통과 전 착수 금지)

- mdast↔블록 변환 계층·왕복 코퍼스 검증(M32) — 3-4의 크기 실측용 1회성 변환 스크립트는 예외(폐기물)
- `notes` 테이블·노트(베타) 표면(M33) — DDL 금지
- documents 연결·저장 전환(M34)
- 커스텀 스펙의 "완성"(콜아웃·참조 칩 3종 전부·문서 임베드·TOC·웹 임베드·이미지 크롭) — 판정에 필요한
  대표 3종만 만들고 멈춘다
- XL 패키지 설치·실험 일체(D10)

## 게이트 판정 규칙 (editor-v2.plan.md §10 전개)

| 판정 | 조건 | 후속 |
|---|---|---|
| D2 확정 | 스파이크·실측·조사가 "압도적으로 유리"를 지지 (커스텀 스펙 3종 성립 + 라이선스 무결 + 번들 수용 가능 + 장문 반응성 합격 + IME에 알려진 치명 이슈 없음) | 보고서에 확정 기록, §10 갱신, M32 착수 가능(단 IME 실기기 실측은 사용자 확인 후 완전 통과) |
| 후보 C 전환 | BlockNote 고유 한계(커스텀 스펙 불가·번들 과대·성능 불합격)가 발견되나 Tiptap 기반 자체는 유효 | 전환 근거 보고 + 사용자 재보고(공수 증가가 크므로 자체 판단 확정하지 않는다) |
| 사용자 재보고 | 판정이 갈리거나(일부 합격) 전제(공개 저장소·라이선스 등)가 흔들림 | 보고서 + 선택지 제시 |

## 완료 기록 (2026-08-15)

- 경위: 병렬 리서치 2건(기능·라이선스 / 이슈·리스크) → 워크트리 스파이크(0.54.0 고정 설치 → PoC 라우트 →
  토큰 테마 결선 → 커스텀 스펙 3종 → 장문 하네스) → 실측 4종(라이선스 기계 검사·번들 전후 비교·브라우저
  실기 측정·실문서 436텍스트 헤드리스 변환) → 보고서(`editor-v2.m31-analysis.md`) → 계획서 개정 → 검증
  (tsc/build/invariant-scan PASS·기존 편집기 diff 0).
- **판정: D2 확정(위임) · D1 방향 지지.** 근거 요지 — 커스텀 스펙 3종 성립 / GPL 전이 0 / 초기 번들 +0.4KB
  (편집 진입 gzip ≈307~415KB 지연 로드) / 6k 블록 단타 28.6ms / 실문서 전건 변환 성공·크기 ×2.54(중앙값).
- **잔여(게이트 완전 종결 조건) = 사용자 이행 항목 1건**: 한글 IME 실기기 실측 — 서버 기동 후
  `http://localhost:8000/editor2-poc` (또는 dev 서버 `:5173`) 접속, 페이지 하단 체크리스트 6항(PC + 폰).
  치명 결함 발견 시 D2 재론(후보 C 검토), 통과 시 M32(변환 계층) 착수.
- 스파이크 코드는 프로덕션 UI에 미노출(URL 직접 진입 전용)·지연 청크 — 판정 후 폐기 가능 상태 유지.
