# Study Hub — 종합 학습 관리 시스템 기획/설계 초안

> 상태: **확정 준비 (Draft v0.22)** — v0.21 대비: **M15(F41) 착수 게이트 G3 해소 — 설계 §4.17 신설 + §8.2 v1.1 개정 (2026-07-28, 문서만·코드 0)**. ① 설계 **v1.17 §4.17**에 계약 확정: 엔진 레지스트리(id `claude-cli|claude-api|codex-cli`, legacy `cli|api`는 **읽기 시 별칭 매핑** — 마이그레이션 없음) · `GET /api/llm/status` **엔진 배열화**(기존 `cli`/`api` 톱레벨 필드 제거 확정 — 소비처 전수 확인) · 폴백 = **우선순위 목록**(`llm.priority` 배열화, `error_info.fallback_engine` 신설) · codex 설치 엔드포인트 1개(`POST /api/llm/engines/{id}/install`) · **원문 대조 알고리즘**(정규화 후 부분 일치 → 12자 조각 커버리지 ≥0.6, 원본 정규화 200자 미만 = "대조 불가") · S8 온보딩(▲▼ 우선순위·전역 자격증명 감지 건너뛰기·프라이버시 고지 1줄). ② **§8.2를 v1.1로 개정**: `answer_source: original|solved`(변환 파이프라인 문제 타입 필수 — **직접 업로드 JSON은 누락 = original 간주로 기존 파일 호환 유지**) · `content` 필수 · 객관식 `answer` **보기 번호만**(직접 업로드의 텍스트 answer는 choices 전체 일치 시 서버 정규화 수용) · LLM 출력의 순수 JSON 위반 = `invalid_output` 오류. ③ **DDL 0건·Alembic 0건 재확인**(§4.17 말미 근거 — 저장 지점 전수: settings 값 형식·인메모리·preview JSON·디스크 폴더뿐). 잔여 게이트는 **G2 품질 재실험**뿐. §13 엔진 비교표의 Codex 열은 구현 완료 시점에 갱신(지금 하지 않음).
> (v0.21: v0.20 대비: **F42·F43 신설 + M16·M17 신설 (2026-07-28 사용자 요청 — 계획 등록, 게이트 없음)**. ① **F42 다양한 문서 포맷 반입(M16)**: docx·xml·md·txt·html·xlsx 파일 반입 정식 지원 — 실측상 현행 파일 반입은 확장자 검사가 없고 API 엔진이 바이너리(docx/xlsx)를 utf-8 강제 디코드해 **mojibake를 조용히 LLM에 투입**하는 결함이 있다(§14 F42 실측 현황). 텍스트 계열(md/txt/html/xml)은 직투입 정식화, docx/xlsx는 **서버측 텍스트 추출**(파서 라이브러리 채택 — "라이브러리 금지"는 F35-3·F40-④ 한정 결정이었음을 확인하고 **"순수 파이썬·오프라인 파서, 포맷당 1개, 계획서 등재 후 추가"** 원칙 신설), 판별 불가 바이너리는 `unsupported_format` 구조화 거부. ② **F43 문서 상호참조·표현 UX(M17)**: 트랜스클루전(문서 안에 타 문서 임베드 표시 — 개념 모듈 재사용)·가리기/접기·이동 버튼·임베드 시각효과. 임베드 해석은 **본문 전용 경량 API**(정답·해설 미포함 — 불변 규칙 1), 참조 인덱스는 `document_relations(relation='embeds')` 재사용. **둘 다 DDL 0건·Alembic 0건**(§6.2 주석 갱신만). ③ **설계 절 번호 교정**: M15가 예약한 "§4.16"은 설계 v1.16이 '앱 버전 확인'에 사용 중 — **M15=§4.17 · M16=§4.18 · M17=§4.19로 재지정**(전부 착수 시 신설 — stage-15 전례). 순서: **M16 → M17 권장, M15는 게이트 통과와 무관하게 M16과 convert 경로가 겹치므로 동시 착수 금지**(§14). 리스크 **R19·R20** 신설.)
> (v0.20: v0.19 대비: **M14 완료(✅ 2026-07-27)** — 큐넷 공식 오픈API(F35 3단계) 백엔드·프론트 구현 완료. 반영: §14 로드맵 M14 행 완료 표기 · **F35-3 명세에 "구현 확정 사항"**(전 목록 **304건**·종목 버킷 **241개**·메타 빈값 **42건**·안내문 **15건** 기본 숨김, 안내문 토글 계약, `exam_key` **3형태 확정**(연도만·식별 불가는 폴더 파생 없음 = 회차 창작 금지), 상류 실패 **502** 구분, **미검증 구간 2건**을 정직하게 기록) · R17에 미검증 구간·잔여 한계 추가 · 설계 **v1.16**에 계약 반영(`include_notices`/`is_notice` · 잡 `notes` · `alternatives`에 `file_import` · §3 상류 실패 규약). **DDL 변경 없음 · Alembic 0건 · 신규 엔드포인트는 예고된 `fetch/qnet-key` 2개뿐.** 다음 단계는 **M15(멀티 벤더 LLM 엔진 — 착수 게이트: 품질 재실험 통과)**.)
> (v0.19: v0.18 대비: **M13 완료(✅ 2026-07-27)** — 사설 어댑터(comcbt·cbtbank) 제거 + F40 수동 반입 UX 4건 구현·검토 완료. 반영: §14 로드맵 M13 행 완료 표기 · **F40 명세에 "구현 확정 사항" 6건 추가**(재커밋 차단 / 내려받기 버튼 노출 조건 / 대기열의 `previewId` 영속 / URL 반입의 대기열 통합 / 10개 상한 기준 / **처리 중 1건 취소 불가 = 알려진 한계**) · 사용자 매뉴얼 §5 재작성(사설 사이트 서술 전량 삭제, 사이트 반입은 "준비 중", 필기 기출 = 파일·URL 반입 안내). **DDL 변경 없음 · Alembic 0건 · 신규 엔드포인트는 예고대로 내려받기 1개.** 다음 단계는 **M14(큐넷 오픈API, 후순위)**.)
> (v0.18: **F41 신설 — 멀티 벤더 LLM 엔진(Codex CLI) + M15 신설 (2026-07-27 사용자 지시로 계획 등록, 착수 게이트 있음)**. 배경: 앱을 비전공자에게 **도구만 배포**하고(시험 데이터는 각자 수집 — 저작권 안전), LLM 변환은 **각 사용자의 ChatGPT 구독(Codex CLI)** 으로 수행하는 배포 시나리오. 근거는 격리 PoC 실측(2026-07-27, `Desktop\codex_engine_test\` — Opus 검증 1차 반려 후 재검토 통과, **조건부 GO**): 단일 바이너리 자동 설치·전역 자격증명 공유·free 플랜 호출 성공·`exec --json -o` 어댑터 적합이 입증됐고, **최상위 리스크 = 지문·보기 창작**(수식 추출 불가 PDF에서 10문항 중 6건 창작, 동일 프롬프트에서 정답 생성 여부 비결정)이 적발됐다. 반영: §5 F41 행 · §14 M15 로드맵·F41 상세(착수 게이트 = **정답표 있는 정상 원본 품질 재실험** + M13·M14 종료) · 작업 지시서 `stage-15-multi-engine-codex.plan.md`. **DDL 변경 없음(예정) — 단 반입 규격(§8.2) 확장(answer_source·원문 대조)이 수반되며 착수 시 §8.2 개정이 선행 조건.**)
> (v0.17: **단계 우선순위 변경(2026-07-27 사용자 확정, 큐넷 오픈API 실호출 실측 결과)** — 실측으로 이 API가 **실기 작업형·도면 공개문제 카탈로그**이고 **사용자의 학습 대상인 필기·필답형(품질경영기사 `totalCount 0`)은 구조적으로 범위 밖**임이 확인됐다(§14 F35-3 "실측 확정 사항"). 필기 기출이 어차피 수동 반입으로 온다면 **그 경로를 편하게 만드는 것이 학습 기여도가 가장 크다** → 우선순위를 **① 사설 어댑터 제거 → ② 수동 반입(파일/URL) UX 개선 → ③ 큐넷 오픈API 연동(후순위)** 로 바꾼다. 반영: **M13 재편**(어댑터 제거 + 신규 **F40 수동 반입 UX** 4건) · **M14 신설**(큐넷 오픈API — 기존 M13의 qnet 범위 이관) · 작업 지시서 분할(`stage-13-fetch-cleanup-manual-import.plan.md` / `stage-14-qnet-openapi.plan.md`) · F35-3에 실측 확정 사항·커버리지 한계 명기 · R17 갱신 + **R18 신설**(변환 산출물 디스크 보존 정책). **사설 어댑터 제거 결정은 그대로 유지**(v0.16 결정, 번복 아님 — 공개 배포 리스크 해소). **DDL 변경 없음(F40 포함).**)
> (v0.16: **사이트 어댑터를 큐넷 공식 오픈API 단일 경로로 축소(2026-07-27 사용자 확정)** — 사설 문제은행 어댑터 **comcbt·cbtbank를 M13에서 전면 제거**한다(제거 근거·트레이드오프·기각한 대안은 §14 F35-2 "제거 이력"에 보존 — 되살리기 방지). 계기: 앱이 **GitHub 공개 저장소 + 테스터 배포 체계**로 전환(2026-07-27 포터블 패키지)돼 "개인 PC 단독 사용 = 사적 이용" 전제가 성립하지 않는다. 부수 효과로 **다중 어댑터 병합 복잡도(우선순위·날짜 키·level_hint 오병합 방지·어댑터별 robots/스로틀 분기)가 소멸** — M13은 qnet 신규 구현과 사설 어댑터 제거를 같은 단계에서 수행한다. **유지**: F35-1 URL 반입·파일 반입·convert 파이프라인(중립 통로) · **이미 반입된 문서·sources 원본·수집 이미지 전량 보존**(소프트 삭제·원본 불변 규칙 그대로, 마이그레이션 0건). 리스크 R14 해소 표기·R17에 단일 경로 의존 집중 반영. **DDL 변경 없음.** — *M13의 qnet 범위는 v0.17에서 M14로 이관됐다.*)
> (v0.15: **M13 신설(큐넷 공식 오픈API — 공공데이터포털 "국가자격 공개문제 조회 서비스" 기술문서 실사본 확보 2026-07-27)**: F35 3단계 — S10에서 유예된 qnet 목록 스텁(JS 포털 역설계 불가, R14)을 **역설계 없이 공식 REST API로 실가동**. 서비스키 = `secrets.json`(F34 전례 — DB/settings 금지), 상세 `fileUrl`은 JWT 유효 1시간(에러 941) → **상세 조회·첨부 다운로드를 같은 잡에서 즉시 연속 수행**, HWP 전용 회차 = 원본 sources/ 확보 + `unsupported_format` 구조화 안내(조용한 스킵 금지), **커버리지 한계 명문화**(실기 공개문제·예시문제 위주 — 기사급 필기 CBT 기출은 공단 비공개) — 신규 리스크 R17. 시험일정 API 연동(exam_date 자동 채움)은 v1.x+ 후보 기록만(별건 유예). + M12 완료 표기(✅ 2026-07-26). v0.14: M12 신설 — cbtbank·F39. v0.13: M8~M11 완료 표기 — v1.x 종료.)
> 작성일: 2026-07-22 · 갱신: 2026-07-28
> 레벨: Dynamic (로컬 웹앱, Python 백엔드 + 웹 프론트엔드)

---

## 1. 한 줄 정의

> 기출문제·학습자료를 LLM으로 구조화해 쌓고, 여러 자격증/시험이 **같은 지식 문서를 공유**하며,
> 시각화와 간격반복으로 학습→풀이→오답관리→복습 루프를 완성하는 **개인 학습 허브**.

## 2. 목표 / 비목표

### 목표 (이걸 이루면 성공)
- G1. PDF/MD/엑셀 등 원본 자료를 넣으면 구조화된 "문서(Document)"로 정리되어 쌓인다.
- G2. 자격증 > 시험 > 과목 계층 어디서든 같은 문서를 참조·공유하며 크로스 체크로 다듬는다.
- G3. 문제 풀이 기록이 자동 축적되고, 오답노트가 스스로 자란다.
- G4. 약점·진도·리듬이 시각적으로 보인다 (차트, 히트맵, 진도율).
- G5. 간격반복(SM-2)으로 "오늘 복습할 것"을 시스템이 골라준다.
- G6. 집 안 어느 기기에서든 브라우저로 접속한다 (PC 서버 + 폰/태블릿).

### 비목표 (일부러 안 하는 것 — v1 기준)
- 다중 사용자/계정 시스템 (개인용. 인증 없음 or 최소 PIN)
- 클라우드 배포, 앱스토어 출시
- 실시간 협업, 문서 공유 커뮤니티
- 앱 내 LLM 자동변환 버튼 (1단계에서는 Claude Code로 변환 → 3단계에서 API 연동)

## 3. 사용자 시나리오 (핵심 루프)

```
[수집] 기출 PDF 다운로드 → sources/에 저장
   ↓
[정리] Claude Code에 "이 PDF를 문서 JSON으로 변환해줘" → 검토 → DB 반입(import)
   ↓
[분류] 트리에서 자격증/시험/과목에 문서 연결 (기존 문서 재사용 가능)
   ↓
[학습] 개념 문서 읽기 → 플래시카드 → 문제 풀기 (자동 채점·기록)
   ↓
[복습] 대시보드에서 약점 확인 → "오늘의 복습" 큐 소화 → 오답노트 갱신
   ↺ (반복)
```

### 대표 시나리오 3개
- **S1. 저녁 30분 복습**: 폰으로 접속 → "오늘의 복습 24문제" → 풀고 자동 채점 → 틀린 3문제 오답노트에 메모 추가.
- **S2. 주말 기출 반입**: 새 회차 기출 PDF를 Claude Code로 JSON 변환 → 미리보기로 오탈자 확인 → 반입 → 트리에 연결. 이 중 12문제는 기존 개념문서(DOC-xxxx)와 자동 연결 제안됨.
- **S3. 시험 2주 전**: 시험별 대시보드에서 과목별 정답률 확인 → 정답률 60% 미만 단원만 골라 집중 모드 → 히트맵으로 매일 학습 확인.

## 4. 용어 정의 (Phase 1: Schema)

| 용어 | 정의 |
|------|------|
| **문서 (Document)** | 고유번호(DOC-xxxx)를 가진 독립 콘텐츠 단위. 타입: 개념/문제/기출문제/플래시카드 |
| **분류 (Category)** | 자기참조 트리 노드. 자격증 > 시험 > 과목 > 단원 … 깊이 무제한 |
| **연결 (Link)** | 분류↔문서 다대다 매핑. `local_note`로 맥락별 메모 부착 |
| **풀이 기록 (Attempt)** | 문제를 푼 1회의 기록. 정오답, 소요시간, 어느 분류 맥락에서 풀었는지 |
| **오답노트 (Review Note)** | 문서+사용자 메모+틀린 이유 태그의 묶음. 풀이 기록에서 자동 생성 가능 |
| **복습 카드 (SRS Card)** | 간격반복 스케줄 대상. 문서당 1개, SM-2 파라미터 보유 |
| **원본 (Source)** | 반입 전 원본 파일(PDF/MD/XLSX). sources/에 보관, 문서가 출처로 참조 |

## 5. 기능 목록 (우선순위)

### P0 — MVP (이것 없으면 앱이 아님)
| ID | 기능 | 설명 |
|----|------|------|
| F01 | 분류 트리 관리 | 자격증/시험/과목 트리 생성·수정·이동 |
| F02 | 문서 CRUD | 문서 생성·조회·수정. 분류에서 빼기=연결해제, 문서 자체 삭제=소프트 삭제(`is_active=0`) |
| F03 | 문서-분류 연결 | 문서를 여러 분류에 연결, 사용처 표시 ("3개 시험에서 사용 중") |
| F04 | JSON 반입(Import) | Claude Code가 만든 문서 JSON을 검증 후 DB 반입, 중복 감지 |
| F05 | 문제 풀기 모드 | 분류 선택 → 순차/랜덤 출제 → 채점 → 기록 저장 |
| F06 | 오답노트 | 틀린 문제 자동 수집 + 메모/틀린이유 태그 |
| F07 | 홈 네트워크 접속 | 0.0.0.0 바인딩, 폰/태블릿 반응형 UI |
| F19 | 커리큘럼 학습 모드 | 챕터 순서대로 문서 연속 학습(이전/다음), 개념→확인문제 흐름 |
| F20 | 진도 추적·이어하기 | 문서별 학습 상태, 트리 노드별 진도율, 홈 "이어하기" 카드 |

### P1 — 시각화·복습 (앱의 차별점)
| ID | 기능 | 설명 |
|----|------|------|
| F08 | 대시보드 | 시험별 진도율, 과목별 정답률 차트, 학습 히트맵(잔디) |
| F09 | 플래시카드 모드 | 카드 뒤집기 애니메이션, 스와이프(안다/모른다) |
| F10 | 간격반복 복습 큐 | SM-2 기반 "오늘의 복습 N개" 자동 선정 |
| F11 | 약점 분석 | 문서/단원별 누적 정답률, "자꾸 틀리는 개념 Top 10" |
| F12 | 검색 | 전문(full-text) 검색, 태그 검색 |
| F21 | 태그 자동 분류 | 분류별 태그 규칙 → 반입/태그변경 시 자동 연결 제안(승인 후 반영) |
| F22 | A4 인쇄/PDF 내보내기 | 문서·챕터 단위 A4 인쇄 뷰, 해설 포함/제외 토글, 브라우저 인쇄→PDF |
| F24 | 문제↔개념 연결 | 문제 화면에 관련 개념 바로가기, 반입 시 연결 제안 (`document_relations` 기반) — S2/F14의 전제 |
| F28 | 다크 모드 | 라이트/다크/시스템 테마. 디자인 토큰(CSS 변수) 기반 — 첫 단계부터 토큰으로 구축해 후반 재작업 방지 |
| F29 | 북마크 | 문서 별표 → 홈/탐색에서 모아보기, 퀴즈 출제 범위로 "북마크만" 선택 가능 |

### P2 — 자동화·확장
| ID | 기능 | 설명 |
|----|------|------|
| F13 | 앱 내 LLM 변환 | 파일 업로드 → Claude API 호출 → 문서 초안 생성 → 검토 후 반입 |
| F14 | 유사 문서 감지 | 반입 시 기존 문서와 유사도 비교 → "이미 있는 개념인데 연결할까요?" |
| F15 | 문서 분기(Fork) | 공유 문서가 시험별로 갈라져야 할 때 복제 후 독립 |
| F16 | 시험 D-Day 모드 | 남은 기간 기반 **복습 강도 조절** (D-Day 등록·홈 위젯·관리 UI 자체는 S4에서 완성 — 여기 남는 것은 강도 조절뿐) — M11 확정, 상세는 §14 |
| F17 | 통계 내보내기 | 학습 기록 CSV/이미지 내보내기 |
| F18 | PWA 설치 | 폰 홈 화면에 앱처럼 설치 |
| F23 | Claude Code 연동 변환 | 앱이 claude CLI(headless)를 호출해 파일→문서 JSON 변환 자동화 |
| F25 | 실전 모의고사 모드 | 회차/범위 선택 → 시간제한 타이머 → 일괄 제출 채점 → 과목별 점수·합격선(과락) 비교. 퀴즈 모드 화면 재사용 — M11 확정, 상세는 §14 |
| F26 | 학습 목표·스트릭 | 일일 목표(문제 수/시간) 설정, 연속 학습일(스트릭) 표시 — 히트맵·홈 카드와 연동 |
| F27 | 백업/복원 UI | study.db+sources/ 스냅샷 생성·복원 버튼, 주기 자동 백업 옵션 (R5의 기능화) |
| F30 | 문제 오류 신고·재생성 | 문제 화면에서 오류 신고(사유 입력) → claude CLI로 해당 문서만 재생성 → 기존/신규 나란히 미리보기 → 승인 시 교체. 엔진은 R9(CLI), 잡 인프라는 F23 재사용, R7 원칙(미리보기 필수 승인 — 자동 덮어쓰기 금지) 준수 |

### v1 마무리 — 홈 커스터마이즈·레이아웃 UX (S7, 순수 프론트 — 사용자 확정)
| ID | 기능 | 설명 |
|----|------|------|
| F31 | 홈 위젯 레이아웃 커스터마이즈 | 편집 모드에서 위젯 드래그 재배치·숨김 토글·열 수 선택(자동/1/2/3, 넓은 화면 다열 배치). 저장 = `settings:home.layout` JSON — **DDL 변경 없음**, 드래그는 포인터 이벤트 직접 구현(외부 라이브러리 없음) |
| F32 | D-Day 홈 즉석 편집 | 홈 D-Day 위젯에서 팝업으로 추가·수정·삭제 — 설정 화면까지 가지 않음. 설정의 D-Day 관리(DDayManager) 로직·모달 재사용, 새 API 없음 |
| F33 | 사이드바 접힘 토글 | 태블릿·PC에서 좌측 메뉴를 아이콘 레일로 접어 콘텐츠 영역 확보. 상태는 localStorage(기기별 UI 선호 — theme 관례, 서버 settings 아님) |

### v1.x — 실사용 개선 (M8~M11 + 후속 M12~M17, 상세 명세는 §14 로드맵 아래)
| ID | 기능 | 단계 |
|----|------|------|
| F34 | LLM 엔진 관리 — 이중 엔진(CLI+API)·우선순위·폴백·429 구조화·한도 기억·잡 진행 가시화 | M8 |
| F35 | 기출 소스 커넥터 — 1단계 URL 반입, 2단계 사이트 어댑터(**M13에서 사설 어댑터 제거 → 큐넷 단일 경로**), 3단계 큐넷 공식 오픈API(**M14 완료 ✅ 2026-07-27 — 실기 공개문제 전용, 필기·필답형은 범위 밖**) | M8(1단계)·M10(2단계 — comcbt/qnet 스텁)·M12(어댑터 3호 cbtbank)·**M13(comcbt·cbtbank 제거)**·**M14(3단계 qnet 실가동 ✅)** |
| F36 | 학습 UX 다듬기 11건 — 데일리 세션·복습 탭·퀴즈 단축키·틀린이유 원탭·공용 Stepper 등 | M9 |
| F37 | 챕터 학습 파이프라인 — 개념→문제→기출 연결, 임의 깊이 트리, 커리큘럼 내 문서 작성·수정, 문서 3대 공용 모듈 | M9 |
| F38 | 설정 재구성(6그룹) · 태그 관리자(오타 감지·일괄 병합) | M9(골격은 M8 선반영) |
| F39 | 사용자 매뉴얼 앱 통합 — `docs/manual/user-manual.html` 읽기 전용 서빙(`GET /manual`)·새 탭 열람, 원본이 단일 출처(빌드 복사 없음) | M12 |
| F40 | **수동 반입(파일/URL) UX** — ① 변환 결과 보존·복구(LLM 산출 JSON 유실 방지) ② 반입 대기열(여러 파일 연속) ③ 분류 경로 미리 지정 ④ 변환 출력 잘림 오안내 수정. F04·F23·F35-1에 걸친 **반입 경로 UX 묶음**(F36·F38 전례로 독립 번호) — **DDL 없음, 신규 API 1개(내려받기)** | M13 |
| F41 | **멀티 벤더 LLM 엔진(Codex CLI)** — ① 엔진 레지스트리(F34의 cli/api 이항 가정 해체 → 벤더×전송 목록·우선순위·폴백 일반화) ② codex-cli 어댑터(`exec --json -o`) ③ 온보딩(바이너리 자동 설치 + 로그인 진단) ④ **변환 신뢰 게이트**(원문 대조·`answer_source` — **§8.2 반입 규격 확장**, R7 강화) — 근거: 2026-07-27 격리 PoC 조건부 GO(§14 F41) | M15 |
| F42 | **다양한 문서 포맷 반입** — 파일·URL 반입에 docx·xml·md·txt·html·xlsx 정식 지원: ① 텍스트 계열(md/txt/html/xml) 직투입 정식화(인코딩 판별) ② docx/xlsx **서버측 텍스트 추출 후 투입**(파서 라이브러리 — 계획 결정, §14 F42) ③ 판별 불가 바이너리 `unsupported_format` 구조화 거부(현행 mojibake 조용 투입 결함 수정) ④ URL content-type 화이트리스트 확장 — **DDL 0건** | M16 |
| F43 | **문서 상호참조·표현 UX** — ① 트랜스클루전(문서 내 `![[DOC-xxxx]]` 임베드 — 개념 모듈 문서를 여러 상위 문서가 재사용) ② 가리기·접기(구간 단위) ③ 이동 버튼(문서 링크·헤딩 앵커) ④ 임베드 카드·접기 시각효과. 임베드 해석은 본문 전용 API(정답·해설 미포함), 참조 인덱스 = `document_relations('embeds')` — **DDL 0건** | M17 |

## 6. 데이터 모델

### 6.1 ERD 개요

```
categories (트리) ──< category_documents >── documents ──< attempts
     │                    (다대다+메모)          │  │
     └────────────────────────────────────────────┘  ├──< review_notes
                    attempts.category_id (맥락)       ├──1 srs_cards
                                                     ├──> sources (출처)
                                                     └──< document_relations >── documents
                                                          (문제↔개념 등 문서 간 관계)
```

### 6.2 테이블 정의 (SQLite)

```sql
-- 분류 트리 (자기참조)
CREATE TABLE categories (
  id          INTEGER PRIMARY KEY,
  parent_id   INTEGER REFERENCES categories(id),
  name        TEXT NOT NULL,
  level_hint  TEXT,             -- '자격증'|'시험'|'과목'|'단원' (표시용, 강제 아님)
  exam_date   DATE,             -- D-Day 기능용 (선택)
  sort_order  INTEGER DEFAULT 0,
  created_at  DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 문서 (콘텐츠의 단일 출처)
CREATE TABLE documents (
  id          INTEGER PRIMARY KEY,
  doc_no      TEXT UNIQUE NOT NULL,        -- 'DOC-0001' 표시용 고유번호
  type        TEXT NOT NULL,               -- 'concept'|'question'|'past_question'|'flashcard'
  title       TEXT NOT NULL,
  content     TEXT,                        -- 본문/문제 지문 (Markdown)
  choices     TEXT,                        -- 객관식 보기 (JSON 배열, 선택)
  answer      TEXT,                        -- 정답
  explanation TEXT,                        -- 해설 (Markdown)
  difficulty  INTEGER,                     -- 1~5 (선택)
  -- 태그는 document_tags 테이블로 정규화 (자동분류·검색에 필요)
  source_id   INTEGER REFERENCES sources(id),
  source_detail TEXT,                      -- '2023년 2회 17번' 등
  is_active   INTEGER DEFAULT 1,          -- 소프트 삭제
  forked_from INTEGER REFERENCES documents(id),  -- 분기 원본 추적
  created_at  DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at  DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 분류-문서 다대다 연결
CREATE TABLE category_documents (
  category_id INTEGER NOT NULL REFERENCES categories(id),
  document_id INTEGER NOT NULL REFERENCES documents(id),
  sort_order  INTEGER DEFAULT 0,
  local_note  TEXT,                        -- 이 시험 맥락에서만 붙는 메모
  linked_at   DATETIME DEFAULT CURRENT_TIMESTAMP,
  linked_by   TEXT DEFAULT 'manual',       -- 'manual'|'import'|'rule' — 연결 출처 (M6, §11 원칙)
  linked_rule_id INTEGER REFERENCES tag_rules(id),  -- linked_by='rule'일 때 만든 규칙 → "이 규칙이 연결한 것" 일괄 해제
  PRIMARY KEY (category_id, document_id)
);

-- 풀이 기록
CREATE TABLE attempts (
  id          INTEGER PRIMARY KEY,
  document_id INTEGER NOT NULL REFERENCES documents(id),
  category_id INTEGER REFERENCES categories(id),   -- 어느 맥락에서 풀었나
  my_answer   TEXT,
  is_correct  INTEGER NOT NULL,            -- 1/0
  time_spent  INTEGER,                     -- 초
  mode        TEXT,                        -- 'quiz'|'review'|'flashcard'|'study'|'exam' (자유 텍스트 — CHECK 제약 없음. 'exam'은 M11 모의고사, 배치 공통 answered_at이 응시 런 키 — §14 F25)
  answered_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 오답노트 (문서당 1개 — 메모는 갱신·누적, 이력은 attempts가 담당)
CREATE TABLE review_notes (
  id          INTEGER PRIMARY KEY,
  document_id INTEGER NOT NULL UNIQUE REFERENCES documents(id),
  note        TEXT,                        -- 내 메모
  wrong_reason TEXT,                       -- '개념부족'|'실수'|'함정'|'시간부족'
  is_resolved INTEGER DEFAULT 0,           -- 극복 여부
  created_at  DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at  DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 간격반복 카드 (SM-2)
CREATE TABLE srs_cards (
  document_id INTEGER PRIMARY KEY REFERENCES documents(id),
  ease_factor REAL DEFAULT 2.5,
  interval_days INTEGER DEFAULT 0,
  repetitions INTEGER DEFAULT 0,
  due_date    DATE,
  last_reviewed DATETIME
);

-- 원본 파일
CREATE TABLE sources (
  id          INTEGER PRIMARY KEY,
  filename    TEXT NOT NULL,               -- sources/ 내 상대경로
  file_type   TEXT,                        -- 'pdf'|'md'|'xlsx'|'image'|'txt'|'html'|'xml'|'docx' 등 (자유 텍스트 — M16/F42에서 값 종류 확장, CHECK 제약 없음 · 마이그레이션 0건)
  file_hash   TEXT,                        -- SHA-256 — 같은 원본 파일 재반입 감지 (8.3의 문서 단위 중복 감지와 별개)
  imported_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  note        TEXT
);

-- 태그 (정규화 — 검색·자동분류의 기반)
CREATE TABLE tags (
  id          INTEGER PRIMARY KEY,
  name        TEXT UNIQUE NOT NULL         -- '#정규화' → 'normalization' 표기 통일은 UI에서
);
CREATE TABLE document_tags (
  document_id INTEGER NOT NULL REFERENCES documents(id),
  tag_id      INTEGER NOT NULL REFERENCES tags(id),
  PRIMARY KEY (document_id, tag_id)
);

-- 태그 자동 분류 규칙: "이 태그 조합이면 이 분류에 연결"
CREATE TABLE tag_rules (
  id          INTEGER PRIMARY KEY,
  category_id INTEGER NOT NULL REFERENCES categories(id),  -- 연결 대상 분류
  tag_query   TEXT NOT NULL,               -- 예: '정규화' 또는 '데이터베이스 AND 기출'
  mode        TEXT DEFAULT 'suggest',      -- 'suggest'(제안 후 승인) | 'auto'(즉시 연결)
  created_at  DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 태그 규칙 연결 제안함 (F21, M6): 규칙이 만든 "문서→분류 연결" 제안의 저장처
CREATE TABLE suggestions (
  id          INTEGER PRIMARY KEY,
  document_id INTEGER NOT NULL REFERENCES documents(id),
  category_id INTEGER NOT NULL REFERENCES categories(id),
  tag_rule_id INTEGER REFERENCES tag_rules(id),   -- 발생 규칙 (규칙 삭제 시 SET NULL)
  status      TEXT DEFAULT 'pending',             -- 'pending'|'approved'|'rejected'
  created_at  DATETIME DEFAULT CURRENT_TIMESTAMP,
  decided_at  DATETIME,
  UNIQUE (document_id, category_id)               -- 같은 문서-분류 쌍 제안은 1건 — rejected가 남아 있으면 재제안 안 함(거절 기억)
);

-- 학습 진도: 분류 맥락별 문서 학습 상태
CREATE TABLE study_progress (
  category_id INTEGER NOT NULL REFERENCES categories(id),
  document_id INTEGER NOT NULL REFERENCES documents(id),
  status      TEXT DEFAULT 'not_started',  -- 'not_started'|'in_progress'|'done'
  completed_at DATETIME,
  PRIMARY KEY (category_id, document_id)
);

-- 이어하기 위치: 학습 단위(챕터)별 마지막 위치
CREATE TABLE resume_points (
  category_id INTEGER PRIMARY KEY REFERENCES categories(id),
  document_id INTEGER REFERENCES documents(id),
  updated_at  DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 문서 간 관계: 문제↔개념 연결 (S2 "기존 개념문서와 자동 연결 제안", F14 유사 문서 감지의 저장처)
CREATE TABLE document_relations (
  from_document_id INTEGER NOT NULL REFERENCES documents(id),
  to_document_id   INTEGER NOT NULL REFERENCES documents(id),
  relation    TEXT DEFAULT 'explains',     -- 'explains'(개념이 문제를 설명) | 'related' | 'prerequisite' | 'embeds'(M17/F43 — from이 to를 본문에 임베드. 자유 텍스트라 CHECK 없음 · 마이그레이션 0건)
  created_by  TEXT DEFAULT 'manual',       -- 'manual' | 'import'(반입 제안 승인) | 'llm' | 'embed'(M17/F43 — 본문 파싱 동기화. 본문이 단일 출처, 이 행은 파생 인덱스)
  created_at  DATETIME DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (from_document_id, to_document_id, relation)
);

-- 앱 설정 (키-값): 복습 큐 상한, 일일 목표, 백업 주기 등 "설정 가능" 항목의 저장처
-- (테마는 기기별 선호이므로 DB가 아닌 브라우저 localStorage에 저장)
CREATE TABLE settings (
  key   TEXT PRIMARY KEY,
  value TEXT
);

-- 북마크 (F29)
CREATE TABLE bookmarks (
  document_id INTEGER PRIMARY KEY REFERENCES documents(id),
  created_at  DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

> 추가 구현 노트
> - **FTS5**: 검색(F12)은 `documents(title, content, explanation)` 대상 FTS5 가상 테이블 + 동기화 트리거로 구현 (M6). 별도 검색엔진 불필요. **M9**: 한국어 부분어 recall(M6 검토 이월 — "제3정규형"에서 "정규형" 미매칭)을 위해 `tokenize='trigram'`으로 **재구축**(Alembic: DROP→CREATE+트리거+백필). API 계약 불변, 2자 이하 질의는 LIKE 폴백 — 설계 §4.12. 가상 테이블은 §6.2 DDL 본문이 아닌 파생 인덱스이므로 스키마 변경 아님.
> - **updated_at 갱신**: SQLite는 자동 갱신이 없으므로 앱 레이어(SQLAlchemy `onupdate`)에서 처리.
> - **인덱스**: `attempts(document_id)`, `attempts(answered_at)`, `category_documents(document_id)`, `srs_cards(due_date)` — 대시보드·복습 큐 조회 경로.

### 6.3 핵심 설계 원칙
1. **문서는 분류에 소유되지 않는다** — 삭제는 "연결 해제". 고아 문서는 보관함에서 관리.
2. **풀이 기록은 문서+맥락 이중 기록** — 통합 약점 분석과 시험별 통계 둘 다 가능.
3. **소프트 삭제** — `is_active=0`. 학습 기록의 무결성 보존.
4. **원본 불변** — sources/의 원본 파일은 수정하지 않는다. 정리본(문서)만 다듬는다.

## 7. 시스템 아키텍처

```
┌─ 브라우저 (PC/폰/태블릿) ──────────────────────┐
│  SPA 프론트엔드 (React + Vite)                 │
│  - 트리 탐색 / 문서 뷰 / 퀴즈 / 대시보드        │
│  - Chart.js, CSS 애니메이션                    │
└───────────────┬───────────────────────────────┘
                │ REST API (JSON)
┌───────────────▼───────────────────────────────┐
│  FastAPI 백엔드 (Python)                       │
│  - 문서/분류/풀이/복습 API                      │
│  - SM-2 스케줄러, 통계 집계                    │
│  - JSON 반입 검증 (Pydantic)                   │
├───────────────────────────────────────────────┤
│  SQLite (study.db 파일 하나)                   │
│  sources/ (원본 파일 보관)                     │
└───────────────────────────────────────────────┘
     실행: uvicorn main:app --host 0.0.0.0 --port 8000
     접속: PC http://localhost:8000 / 폰 http://192.168.x.x:8000
```

### 기술 스택 선정
| 구분 | 선택 | 이유 |
|------|------|------|
| 백엔드 | Python 3.12 + FastAPI | 데이터 처리·LLM 연동에 강함, 자동 API 문서 |
| DB | SQLite (+FTS5) | 파일 하나, 설치 불필요, 전문검색 내장 |
| ORM | SQLAlchemy 2.x | 표준적, 마이그레이션(Alembic) 지원 |
| 프론트 | React + Vite + TypeScript | 시각효과·상태관리 자유도 최대 |
| 차트 | Chart.js | 가볍고 충분 |
| 스타일 | Tailwind CSS | 반응형(폰 대응) 빠른 구현 |
| 배포 | 로컬 uvicorn (정적 파일 동시 서빙) | 프로세스 1개로 단순화 |

> 대안 검토: 프론트를 React 대신 **서버 렌더링(Jinja2)+htmx**로 하면 더 단순해지나,
> 플래시카드 애니메이션·대시보드 상호작용의 자유도가 떨어짐. 시각효과가 핵심 요구라 React 선택.

## 8. LLM 정리 파이프라인 (반입 규격)

### 8.1 흐름 (1단계: Claude Code 활용)
```
원본 파일 → Claude Code에 변환 요청 → import/*.json 생성
→ 앱 "반입" 화면에서 미리보기·검증 → 승인 → DB 저장 → 분류 연결
```

### 8.2 반입 JSON 규격 (v1.1 — S15 개정 확정 2026-07-28. 파이프라인 계약·대조 알고리즘은 설계 §4.17이 담당)

> **v1.1 개정 요지(F41 변환 신뢰 게이트 — Codex PoC 실측 근거, 전 엔진 공통)**: `answer_source` 필드 추가 + 검증 강화 3건. `format_version`은 **1 유지** — 구조 변경이 아니라 필드 추가·검증 강화이고, 기존 파일 거부가 목적이 아니다. **강제 범위는 경로로 구분한다**: 강화 규칙은 **LLM 변환 파이프라인(convert·fetch 잡) 산출물에만** 강제하고, **사용자가 직접 올린 반입 JSON은 하위 호환**(아래 각 항목의 호환 규칙)을 유지한다.

```json
{
  "format_version": 1,
  "source": { "filename": "2023_2회_기출.pdf", "note": "정보처리기사 필기" },
  "documents": [
    {
      "type": "past_question",
      "title": "2023-2회 17번: 정규화",
      "content": "다음 중 제3정규형의 조건은...",
      "choices": ["이행적 함수 종속 제거", "...", "...", "..."],
      "answer": "1",
      "answer_source": "original",
      "explanation": "3NF는 2NF에서 이행적 함수 종속을 제거...",
      "difficulty": 3,
      "tags": ["데이터베이스", "정규화"],
      "source_detail": "2023년 2회 17번",
      "suggest_categories": ["정보처리기사/필기/데이터베이스"],
      "suggest_relations": ["DOC-0012"]
    }
  ]
}
```

- **`answer_source`(v1.1 신설)**: `"original"`(정답이 원본에 명시됨) `| "solved"`(LLM이 스스로 풀어 채움). 문제 타입(question·past_question)에서 **변환 파이프라인 산출물은 필수**(누락 = 항목 오류 — 프롬프트 `prompts/convert.md`에 지시 추가). **직접 업로드 JSON은 누락 허용 → `original` 간주**(기존 파일 무변경 통과). `solved`는 미리보기에서 경고 배지 + **기본 반입 제외**(항목별 명시 승인 시에만 포함 — R7 강화). DB에는 저장하지 않는다(반입 게이트 신호 — DDL 0건).
- **객관식 `answer`(v1.1 규격 좁힘)**: `choices`가 있으면 **1-base 보기 번호 문자열(`"1"`~`"n"`)만 허용**. `"1"`~`"n"` 범위 숫자 문자열은 항상 번호로 해석(수치형 보기의 번호/텍스트 이중 해석 제거). 직접 업로드의 텍스트 answer는 choices 전체 일치(트림 후) 시 서버가 번호로 정규화해 수용, 불일치 = 항목 오류.

### 8.3 반입 시 검증 규칙
- 필수 필드 검사 (type/title, 문제면 answer 필수. **v1.1**: 개념·문제 타입 `content` 필수 — 변환 파이프라인 산출물에서 누락 = 항목 오류)
- **순수 JSON(v1.1)**: LLM 변환 출력에 코드펜스·전후 잡문이 섞이면 관대한 벗겨내기 없이 `error_info.kind:'invalid_output'` 오류로 처리(설계 §4.11·§4.17)
- **원문 대조(v1.1 — 서버측, LLM 아님)**: 변환 파이프라인 preview 생성 시 문제 문항의 지문·보기가 원본 추출 텍스트에 실재하는지 대조 — 불일치 = `fabrication_suspect` 경고 + 기본 반입 제외, 추출 불가 원본 = "대조 불가" 표시(조용한 통과 금지, 기본 반입은 유지). 정규화·임계·알고리즘은 **설계 §4.17 ⑥이 단일 출처**
- 중복 감지: 제목+내용 해시 비교 → 중복 의심 시 나란히 보여주고 선택 (건너뛰기/새로 추가/기존에 병합)
- `suggest_categories`: 없는 분류면 생성 제안, 있으면 연결 제안 — **자동 실행이 아니라 승인 후 반영**
- `suggest_relations`: 기존 문서 번호(DOC-xxxx)로 문제↔개념 연결 제안 — 승인 시 `document_relations`에 `created_by='import'`로 기록. 존재하지 않는 번호는 경고 후 무시

## 9. 화면 구성 (주요 12개)

| # | 화면 | 핵심 요소 |
|---|------|----------|
| 1 | **홈 대시보드** | **"이어하기" 카드(최우선)**, 오늘의 복습 N개, 학습 히트맵, D-Day 배지(시험 분류 + 임의 D-Day 병합). S7: 위젯 편집 모드(드래그 재배치·숨김·열 수), D-Day 배지 팝업 즉석 편집 |
| 2 | **탐색 (트리+문서목록)** | 좌: 분류 트리(노드별 진도바) / 우: 문서 카드 목록. "N곳에서 사용 중" 배지 |
| 3 | **문서 상세** | 본문(Markdown 렌더), 태그 칩, 사용처 목록, 풀이 이력 미니차트, 편집 |
| 4 | **커리큘럼 뷰** | 시험 선택 시: 과목→챕터 아코디언, 각각 진도바, "여기서 시작/이어하기". 편집 모드로 분류 추가·수정·이동·삭제(M4 — 탐색과 동일 기능, 공용 모달 재사용) |
| 5 | **학습 모드** | 챕터 문서 연속 넘김(이전/다음), 챕터 진행바, 개념→확인문제 인터리브 |
| 6 | **퀴즈 모드** | 문제 카드, 보기 선택, 즉시 채점+해설, 진행바, 종료 후 결과 요약 |
| 7 | **플래시카드** | 카드 뒤집기, 스와이프 판정(안다/모른다), 남은 장수 |
| 8 | **오답노트** | 분류 트리 범위 필터(하위 포함), 분류 계층(대/중/소/하위 단위) 그룹 보기, 틀린이유 태그 필터, 메모 편집, "극복" 처리, 재도전 버튼 |
| 9 | **반입(Import)** | JSON 업로드 → 문서 미리보기 표 → 중복/태그규칙/분류 확인 → 반입 실행 |
| 10 | **인쇄 뷰** | A4 미리보기, 범위 선택(문서/챕터/오답노트), 해설 포함/제외, 인쇄→PDF |
| 11 | **설정** | 테마(라이트/다크/시스템), 복습 큐 상한, D-Day 관리(시험 분류 날짜 + 임의 D-Day), 백업/복원(M6) |
| 12 | **모의고사** (M11) | 과목 구성(시험 노드 직계 자식)·제한 시간·합격선 설정 → 응시(문항 네비게이터·카운트다운 타이머) → 일괄 제출 리포트(과목별 점수·과락·합격) — 퀴즈 문항 렌더 재사용 |

모바일: 하단 탭바(홈/커리큘럼/퀴즈/복습/오답노트 — 복습 탭은 M9, F36-②), 트리는 드로어로 전환.
공통 레이아웃(태블릿·PC): 좌측 사이드바 접힘 토글(F33, S7) — 접으면 아이콘 레일, 상태는 localStorage(기기별).
북마크(F29): 탐색 카드·문서 상세에 별 아이콘, 홈에 "북마크 모아보기" 진입점.

### 9.2 연속 학습 흐름 (핵심 UX)

사용자가 말한 계층 그대로: **자격증 → 필기/실기 → 과목(1~6) → 챕터 → 학습내용(문서들)**.
이 흐름이 "다음에 뭘 할지 고민할 필요 없이" 이어지는 것이 UX의 중심이다.

```
홈 ──▶ [이어하기: 정보처리기사 · 필기 · 2과목 · Ch.3  ▓▓▓▓░░ 7/12]  ← 원클릭 복귀
              │
              ▼
     학습 모드 (챕터 단위 러닝 트랙)
     ┌────────────────────────────────────────────┐
     │  Ch.3 진행바  ▓▓▓▓▓▓▓░░░░░  7/12            │
     │                                            │
     │  [개념] SQL JOIN의 종류        ← 지금 위치   │
     │   ...본문...                                │
     │                                            │
     │  ◀ 이전        다 읽었어요 → 다음 ▶          │
     └────────────────────────────────────────────┘
       개념 → 개념 → 확인문제 2~3개 → 개념 → ... → 챕터 끝
                                                │
                                                ▼
     챕터 완료 화면: 정답률 요약 + 틀린 문제 즉시 복습 제안
              │
              ▼
     [다음 챕터 시작 ▶]  (과목 마지막 챕터면 → 과목 완료 + 다음 과목 제안)
```

**동작 규칙**
- **진도 판정**: 개념 문서는 "다음" 버튼 = 완료 처리(`study_progress.done`). 문제는 풀어야 다음으로.
- **이어하기**: 학습 모드 진입/이탈 시 `resume_points` 갱신 → 홈 카드와 커리큘럼 뷰에 반영.
- **인터리브 배치**: 챕터 내 문서 순서(`sort_order`)는 "개념 몇 개 + 확인문제"가 섞이도록 반입 시 배치. LLM 변환 시 `suggest_categories`와 함께 순서도 제안.
- **진도율 전파**: 챕터 진도 = done 문서 비율 → 과목/시험/자격증으로 집계되어 트리에 진도바 표시.
- **중간 이탈 허용**: 폰으로 3분 공부하다 꺼도, 다음 접속 때 정확히 그 문서에서 재개.

## 10. 간격반복(SM-2) 동작 규칙

- 문제를 풀거나 플래시카드 판정 시 품질점수 q 산출:
  - **회복 정답**: 그 문서의 **직전 시도가 오답**이었으면, 어느 화면(퀴즈/복습/재도전)에서 맞혔든 q=3 — "간신히 회복" 신호. 화면 종류(mode)가 아니라 직전 attempt 결과로 판정한다(경로 무관 일관성).
  - 그 외 정답: 빠름(문항 평균시간 이하) q=5, 느림 q=4. 첫 풀이 정답(직전 시도 없음)도 여기 해당.
  - 오답 q=1
  - 플래시카드 "안다" q=4, "모른다" q=1
  - 근거: q=3은 EF를 −0.14 깎는 신호라 "복습 세션 정답=무조건 q3" 방식은 잘 아는 카드의 EF가 복습할수록 감소하는 역설을 만든다(2026-07 결정).
- SM-2 갱신 (표준 규칙 준수):
  - 오답(q<3): repetitions=0, interval=1일 (EF는 유지)
  - 정답: 1회차 interval=1일 → 2회차 6일 → 3회차부터 interval = 이전 interval × EF
  - EF 갱신: `EF' = EF + (0.1 − (5−q) × (0.08 + (5−q) × 0.02))`, 하한 1.3
- **카드 생성 시점**: 문서를 처음 풀거나 플래시카드로 처음 판정한 순간 자동 생성. 반입 시 일괄 생성하지 않음 — 기출 500문제 반입 즉시 복습 큐가 폭주하는 것을 방지.
- "오늘의 복습" = `due_date <= 오늘`인 카드, 우선순위: 오답노트 미해결 > 기한 초과 오래된 순.
- 상한: 하루 복습 큐 기본 30개 (`settings`에 저장, 변경 가능) — 과부하 방지.

## 11. 태그 자동 분류 설계

### 동작 방식
1. 문서에 태그 부여 (직접 입력 or LLM 변환 시 자동 제안 — `#정규화 #데이터베이스`)
2. 분류 노드에 **태그 규칙** 등록 — 예: `정보처리기사/필기/DB` 노드에 `tag_query: "정규화 OR 트랜잭션"`
3. 트리거 시점: **문서 반입 시** / **태그 변경 시** / **규칙 생성·수정 시(기존 문서 일괄 스캔)**
4. 매칭되면 → `mode='suggest'`: "이 분류에 연결할까요?" 목록에 쌓임 (기본값) / `mode='auto'`: 즉시 연결

### 원칙
- **기본은 제안-승인.** 완전 자동은 오분류가 조용히 쌓이는 위험이 있어, 규칙별로 사용자가 auto를 명시적으로 켠 경우만.
- 자동 연결된 링크는 `category_documents`에 출처 표시(수동/규칙) → 나중에 "이 규칙이 연결한 문서들" 일괄 해제 가능.
- 태그는 정규화 테이블(`tags`)로 관리 → 오타 태그 병합 기능 제공.

## 12. A4 인쇄 / PDF 내보내기 설계

### 방식: 브라우저 인쇄 엔진 활용 (v1)
- 모든 문서 본문은 **A4 폭(210mm) 기준으로 렌더링 최적화** — 이미지 `max-width:100%`, 표 자동 축소, 코드블록 줄바꿈.
- `@media print` CSS + `@page { size: A4; margin: 15mm }` — 화면용 UI(버튼·네비)는 인쇄에서 제거.
- 페이지 나눔 제어: 문제와 보기가 페이지 경계에서 잘리지 않도록 `break-inside: avoid`.
- 사용자는 **인쇄 뷰 → 브라우저 인쇄 → "PDF로 저장"** — 추가 라이브러리 0개로 A4 PDF 확보.

### 인쇄 뷰 3종
| 종류 | 구성 | 옵션 |
|------|------|------|
| 개념 정리본 | 챕터/과목 단위 개념 문서 모음 | 목차 자동 생성, 문서번호 표시 |
| 문제집 | 문제만 앞에, 정답·해설은 뒤에 모아서 | 해설 제외(시험 연습용), 풀이 칸 여백 |
| 오답노트 | 틀린 문제 + 내 메모 + 틀린이유 | 기간/분류 필터 |

### v2 확장 (필요 시)
- 서버 사이드 PDF 생성(WeasyPrint) — 버튼 한 번에 파일 생성, 헤더/푸터(페이지번호·시험명) 정교화.

## 13. Claude Code 연동 방안 (LLM 엔진 선택)

앱이 LLM을 쓰는 방법 비교 (M15에서 D. Codex CLI 추가 — 멀티 벤더 레지스트리, 설계 §4.17):

| | A. 수동 (Claude Code 대화) | B. **claude CLI headless 연동** | C. Claude API 직접 호출 | D. **Codex CLI (ChatGPT 구독)** — M15 |
|---|---|---|---|---|
| 동작 | 사용자가 Claude Code에 "이 PDF 변환해줘" → JSON 반입 | 앱 서버가 `claude -p "변환 프롬프트" --output-format json` 실행 | 앱이 API 키로 Anthropic API 호출 | 앱 서버가 `codex exec --json -o` 실행 (원본은 pypdf 추출 텍스트 삽입 — G2 실증) |
| 비용 | 기존 구독에 포함 | **기존 구독에 포함** | API 별도 과금 | **사용자의 ChatGPT 구독에 포함** (free 플랜도 호출 가능 실측) |
| 구현 난이도 | 0 (규격만 있으면 됨) | 중 (서브프로세스 관리, 타임아웃) | 중 (키 관리, 스트리밍) | 중 (서브프로세스 + 자동 설치·로그인 진단 온보딩) |
| 자동화 수준 | 낮음 — 손이 감 | **높음 — 앱 안 버튼으로 완결** | 높음 | 높음 — 설치~진단까지 앱 마법사로 완결 |
| 제약 | — | PC에 Claude Code 설치 필수 (서버가 로컬이라 가능) | 인터넷 + 키 필요 | ChatGPT 로그인(브라우저 OAuth) 필요 · 변환 원문이 전역 `~/.codex` 로그에 남음(온보딩 고지) · 429 형식 미실측(보수 안내) |

**잠정 결론: A → B 단계적 진행.**
- M2(반입)에서는 A로 시작: 변환 프롬프트 템플릿을 `prompts/convert.md`로 만들어두고 Claude Code에 파일과 함께 전달 → 규격 JSON 생성.
- M6에서 B 구현: 앱 서버가 로컬에서 실행되므로 `claude -p` 서브프로세스 호출이 가능. "파일 업로드 → 변환 → 미리보기 → 반입"이 앱 안에서 완결. API 키 관리 불필요.
- C는 B가 제약에 걸릴 때(예: 다른 PC에서 서버 운영)의 대안으로 보류.

## 14. 개발 로드맵

| 단계 | 내용 | 완료 기준 |
|------|------|----------|
| **M1. 뼈대** | DB 스키마, FastAPI 기본 API, 트리+문서 CRUD 화면, 디자인 토큰+다크 모드(F28) | 문서를 만들고 두 시험에 연결 가능 |
| **M2. 반입** | JSON 반입 규격+검증+미리보기, 실제 기출 1세트 반입 | 실데이터 100문제 이상 적재 |
| **M3. 학습 루프** | 커리큘럼 뷰+학습 모드+진도/이어하기, 퀴즈, 오답노트 | 폰으로 "이어하기"→학습→오답노트까지 끊김 없이 |
| **M4. 시각화·인쇄** | 대시보드(차트·히트맵), 문서 사용처·문제↔개념 연결 표시(F24), 북마크(F29), A4 인쇄 뷰 3종, D-Day 관리(시험 분류+임의), 오답노트 분류 계층 그룹 | 약점이 한눈에 보이고, 문제집 PDF 출력 가능 |
| **M5. 복습** | SM-2, 오늘의 복습 큐, 플래시카드 | 매일 복습 루틴 동작 |
| **M6. 자동화·다듬기** | 태그 자동분류, claude CLI 연동 변환, 문제 오류 신고·재생성(F30), 검색(FTS5), PWA, 백업/복원 UI(F27) | 반입~분류가 앱 안에서 완결 |
| **M7. 홈 커스터마이즈·레이아웃 UX** | 홈 위젯 편집 모드(드래그 재배치·숨김·열 수), D-Day 홈 팝업 편집, 사이드바 접힘(F31~F33) — **순수 프론트, 새 API·DDL 없음**(settings 재사용) | 내 위젯 배치가 저장돼 어느 기기에서든 재현 — **v1 완성** ✅ 2026-07-24 |
| **M8. LLM 인프라** (v1.x 착수) | F34 LLM 엔진 관리(이중 엔진·우선순위·폴백·429 구조화·한도 기억) + F35 1단계(URL 반입) | 한도·오류가 사람 말로 보이고, API 폴백·URL 반입으로 변환이 끊기지 않음 ✅ 2026-07-25 |
| **M9. 일상 다듬기** | F36 학습 UX 11건 + **F37 챕터 파이프라인(개념→문제→기출)** + **F38 설정 재구성·태그 관리자** + 검색 한국어 recall(trigram) + 복원 후 재시작 UX — 작업 지시서 `stage-9-daily-polish.plan.md` · 설계 §4.12 | 앱 열기→공부 시작이 원탭, 챕터가 한 흐름으로 완주, 태그가 통제됨 ✅ 2026-07-25 |
| **M10. 콘텐츠·동기** | F35 2단계(사이트 어댑터 — comcbt·큐넷 2종, 큐넷 우선) + F26 학습 목표·스트릭(F36-③ 연계) — 작업 지시서 `stage-10-content-motivation.plan.md` · 설계 §4.13 | 자격증 선택→회차 자동 반입, 스트릭이 히트맵·홈과 연동 ✅ 2026-07-26 — **comcbt 어댑터는 M13에서 제거됨**(어댑터 골격·수집 파이프라인·F26은 그대로 유효, §14 F35-2 제거 이력) |
| **M11. 시험 직전 도구** | F25 실전 모의고사(일괄 제출 채점·과목별 리포트) + F16 D-Day 복습 강도 조절(SRS 큐 부스트) — 작업 지시서 `stage-11-exam-tools.plan.md` · 설계 §4.14 | 실제 시험 형식 모의 응시·리포트 — **v1.x 완성** ✅ 2026-07-26 |
| **M12. 콘텐츠 확장·도움말** (v1.x 후속 — 실사용 요청 2026-07-26) | F35 어댑터 3호(**cbtbank.kr** — FetchedExam 구조화 추출 경로 첫 실사용, 중복 우선순위 큐넷>cbtbank>comcbt·날짜 자연 키 병합) + F39 사용자 매뉴얼 앱 통합(`GET /manual` 읽기 전용 서빙) — 작업 지시서 `stage-12-content-help.plan.md` · 설계 §4.13(갱신)·§4.15 | cbtbank 회차가 문항·정답·해설·과목·이미지까지 구조화돼 미리보기에 도달, 매뉴얼이 앱 안 진입점에서 새 탭으로 열림 ✅ 2026-07-26 — **cbtbank 어댑터는 M13에서 제거됨**(F39 매뉴얼 통합·FetchedExam 인터페이스는 유지, §14 F35-2 제거 이력) |
| **M13. 사설 어댑터 제거 · 수동 반입 UX** (후속 — 공개 배포 전환 + 큐넷 실측 2026-07-27) | ① **사설 사이트 어댑터 전면 제거**(comcbt·cbtbank 모듈 삭제, registry·병합 로직 단일 어댑터로 단순화, 프론트 어댑터 id·매뉴얼 갱신 — 근거 §14 F35-2 제거 이력. 제거 후 등록 어댑터 = **qnet 스텁 1개**, 실가동은 M14) + ② **F40 수동 반입 UX 4건**(변환 결과 보존·복구 / 반입 대기열 / 분류 경로 미리 지정 / 잘림 오안내 수정) — 작업 지시서 `stage-13-fetch-cleanup-manual-import.plan.md` · 설계 §4.3·§4.11·§4.13(S13)·§5.9 | **코드베이스에 cbtbank/comcbt 참조 0건 · 기존 반입 문서·sources 원본 전량 보존**(마이그레이션 0건)이고, 여러 회차 파일을 한 번에 걸어 순서대로 검토·반입할 수 있으며 **서버를 재시작해도 변환 결과가 살아남는다** ✅ 2026-07-27 — 사이트 반입 탭은 "준비 중" 안내 + [URL로 반입]·[파일로 반입] 대안(큐넷 실가동은 M14), URL 반입은 파일 대기열로 통합, 매뉴얼에서 사설 사이트 서술 전량 삭제 |
| **M14. 큐넷 공식 오픈API** (후순위 — 실측상 필기·필답형 미수록 2026-07-27) | F35 3단계(qnet 어댑터 실가동 — 공공데이터포털 **"국가자격 공개문제 조회 서비스"**: 서비스키 등록(`secrets.json`)·종목 검색/공개문제 목록·첨부 반입(1시간 토큰 제약 대응)·오픈API 에러코드 사람 말 매핑·비지원 포맷(ZIP·HWP) 정책) — **실측 확정 사항**(numOfRows 50 상한·메타 빈값 43건 방어·안내문 게시물 필터·제목 파싱 불규칙성)은 §14 F35-3 · 작업 지시서 `stage-14-qnet-openapi.plan.md` · 설계 §4.13(S14) | 서비스키 등록 후 자격증 검색→큐넷 **실기** 공개문제 목록·PDF 반입이 미리보기까지 완결(ZIP·HWP 전용 게시물은 원본 확보 + 파일 반입 폴백 안내)하고, **필기·필답형은 범위 밖임이 화면·매뉴얼에 명시**된다 ✅ **2026-07-27** — 실측: 전 목록 **304건**(50×7페이지, 930 에러 0건)·종목 버킷 **241개**·메타 빈값 **42건**(가짜 종목 0건, 제목 검색으로 반입 가능)·안내문 **15건** 기본 숨김. "품질경영기사" 검색은 **정상 200 + 0건**으로 끝나고 커버리지 한계는 어댑터 notice·매뉴얼에 고정. **미검증 1건**: 941 토큰 만료 재시도(재현 실패 — convert→미리보기 완주는 S14 검토(2026-07-27) 라이브 반입으로 확인·해소) — 아래 F35-3 참조 |
| **M15. 멀티 벤더 LLM 엔진** (계획 등록 2026-07-27 — **착수 게이트 있음**: M13·M14 종료 + **정답표 있는 정상 원본 품질 재실험 통과**) | F41 — ① 엔진 레지스트리(cli/api 이항 해체) ② codex-cli 어댑터 ③ 온보딩(자동 설치·로그인 진단) ④ 변환 신뢰 게이트(원문 대조·`answer_source` — **§8.2 개정 선행**) — 작업 지시서 `stage-15-multi-engine-codex.plan.md` · 실측 근거 `Desktop\codex_engine_test\results\SUMMARY.md`(v2) · 설계 문서 **§4.17** 신설 필요(착수 시 — 당초 §4.16으로 예약했으나 설계 v1.16이 §4.16을 '앱 버전 확인'에 사용해 **2026-07-28 재지정**) | ChatGPT 구독만 있는 사용자가 **키·터미널 없이** 온보딩(설치→로그인→진단)을 마치고 파일 반입·변환을 완주하며, **원본에 없는 지문·보기·정답이 경고 없이 반입되는 경로가 0**이다 ✅ **2026-07-28** — 게이트 3건 통과(G2 재실험 = `Desktop\codex_engine_g2\results\G2_report.md`: 깨끗한 원본 정답 10/10·창작 0 실증) → 구현(레지스트리·codex stdin 어댑터·신뢰 게이트(§8.2 v1.1)·S8 온보딩, 테스트 144→218) → Opus 검토 3회전 조건부 통과(치명·중요 0, 경미 8건 잔여 — stage 문서 5절). 로그인만은 브라우저 OAuth 특성상 `codex login` 안내 경유(설계 §4.17⑦ 확정 — "터미널 없이"의 유일한 예외). codex 단독 PC 완주는 실사용 확인 항목 |
| **M16. 다양한 문서 포맷 반입** (계획 등록 2026-07-28 — 게이트 없음) | F42 — ① 텍스트 계열(md/txt/html/xml) 파일 반입 정식화(확장자·매직 바이트 검사 + 인코딩 판별) ② docx/xlsx 서버측 텍스트 추출(파서 라이브러리 채택 — 원칙·후보는 아래 F42) ③ 판별 불가 바이너리 `unsupported_format` 구조화 거부(mojibake 조용 투입 결함 수정) ④ URL content-type 화이트리스트 확장 — 작업 지시서 `stage-16-doc-formats.plan.md` · 설계 **§4.18 신설 필요(착수 시)** | docx·xlsx·xml·md·txt·html 파일이 파일/URL 반입에서 미리보기까지 완주하거나, 지원 불가 시 **구조화 오류 + 폴백 안내**로 끝난다(조용한 쓰레기 변환 0건). LLM 사용량 사전 안내·R7 승인·sources/ 불변 유지 |
| **M17. 문서 상호참조·표현 UX** (계획 등록 2026-07-28 — 게이트 없음) | F43 — ① 트랜스클루전(`![[DOC-xxxx]]` 임베드 — 문법은 착수 전 확정) ② 가리기·접기(구간 단위) ③ 이동 버튼(`[[DOC-xxxx]]`·헤딩 앵커) ④ 임베드 카드·시각효과 — 임베드 해석은 **본문 전용 경량 API(정답·해설 미포함 — 불변 규칙 1)**, 순환·깊이 상한·삭제 대상 자리표시자, 참조 인덱스 = `document_relations('embeds')` 동기화 — 작업 지시서 `stage-17-doc-transclusion.plan.md` · 설계 **§4.19 신설 필요(착수 시)** | "확률과 확률분포" 문서 안에서 기존 "산술평균" 문서가 **그 자리에 펼쳐져 보이고**, 순환·삭제 참조가 자리표시자로 안전하게 표시되며, 정답·해설이 임베드 경로로 유출되는 경우가 0이다 |

> v1.x는 위 로드맵 M8~M11로 확정 (2026-07-24, 시험 일정 무관 순차 진행 — 사용자 확정). **M8~M11 전 단계 구현·검토·머지 완료 — v1.x 종료 (2026-07-26).** M12는 v1.x 종료 후 첫 실사용 요청(cbtbank 반입·매뉴얼 통합 — 2026-07-26)으로 신설·완료(✅ 2026-07-26). **M13은 큐넷 오픈API 스펙 실사본 확보(2026-07-27)로 신설**됐고 같은 날 사설 어댑터 제거가 편입됐다(공개 배포 전환).
> **M13/M14 재편 (2026-07-27 사용자 확정 — 우선순위 변경)**: 같은 날 오후 **큐넷 오픈API 실호출 실측**(§14 F35-3 "실측 확정 사항")으로 이 API가 **실기 작업형·도면 공개문제** 카탈로그이며 **사용자의 학습 대상인 필기·필답형은 구조적으로 범위 밖**(품질경영기사 `totalCount 0`)임이 확인됐다. 필기 기출이 어차피 수동 반입으로 들어온다면 그 경로를 실사용 가능하게 만드는 편이 학습 기여도가 크므로 — **M13 = 사설 어댑터 제거 + F40 수동 반입 UX**, **M14 = 큐넷 오픈API(후순위)** 로 나눴다(작업 지시서도 분할). **사설 어댑터 제거 결정은 유지**(공개 배포 리스크 — 번복 아님). qnet 실가동은 여전히 "역설계가 아닌 공식 계약"이므로 S10 유예 결정의 번복이 아니다(순서만 뒤로). 아래는 각 기능의 상세 명세. F15(문서 분기)는 실수요 확인 전 보류.
> **M15~M17 순서 관계 (2026-07-28 확정)**: M16·M17은 사용자 요청 기능으로 **착수 게이트가 없다** — M15의 게이트(품질 재실험)는 F41 고유 조건이므로 M16·M17이 그와 무관하게 선행할 수 있다. 권장 순서 **M16 → M17**(요청 순서·M16이 M15 원문 대조의 추출 가능 포맷을 넓혀 주는 부수 효과). 단 **M15와 M16은 동시 착수 금지** — 둘 다 `convert_service`·반입 경로를 수정하므로(M15 G1 "작업공간 동시 수정 충돌 방지"의 연장) 먼저 시작한 쪽이 끝난 뒤 착수한다. M17은 문서 렌더·열람 계열이라 M15와 파일 충돌이 없다(독립 진행 가능). M15 게이트가 M16 착수 전에 통과되더라도 이 배타 규칙이 우선한다.
> M6 검토에서 기록된 v1.x 개선 후보 2건: ① 한국어 부분어 검색 recall 개선(FTS5 trigram/토크나이저 검토 — "제3정규형"에서 "정규형" 미매칭), ② 백업 복원 후 서버 재시작 강제 UX(구동 중 커넥션 stale 방지).
>
> **F34 LLM 엔진 관리** (v1.x 확정 후보 — 2026-07-24 실사용 429 경험으로 범위 확정. **이중 엔진 + 우선순위 + 폴백** 구조, 사용자 확정):
> 1. **엔진 2종 등록**(설정 화면, 카드 2개):
>    - **CLI 카드**: 설치·로그인·호출 가능 진단 + 마지막 성공/실패 시각 + 한도 상태. 로그인은 앱이 대행 불가(대화형 브라우저 절차) — "터미널에서 `claude` 실행해 로그인" 단계 안내 + [다시 확인] 재검사 버튼의 안내형 마법사.
>    - **API 카드**: 키 등록 UI(입력 시 즉석 연결 테스트, 이후 마지막 4자리만 마스킹 표시, write-only). **키 저장은 settings/DB 금지** — 루트 별도 로컬 파일(예: `secrets.json`, .gitignore + 백업(F27) 제외 대상)에 저장해 백업·git으로 키가 복제되는 것을 원천 차단. 환경변수/`ant auth login` 프로필이 있으면 그것을 우선 인식(키 파일 불필요).
> 2. **우선 엔진 선택**(CLI↔API) + **폴백 정책**: 우선 엔진이 한도 초과·타임아웃이면 보조 엔진 시도 — 방식은 **자동**(API 과금 발생 안내에 명시 동의한 경우만) / **물어보기**(기본값 — "API로 계속할까요?" 확인창) / **끔**(A방식 수동 반입 안내). API는 건별 과금이므로 조용한 자동 폴백 금지.
> 3. **오류 구조화** — convert/regenerate 잡 실패 시 CLI/API 응답(`api_error_status`·`result` 등)을 백엔드가 파싱해 **사람이 읽는 메시지로 분류 표출** (원문 JSON 노출 금지): ① **사용량 한도(429)** — 한도 종류(세션/일일/주간/특정 모델/전체)와 **리셋 시각** (예: "Claude 구독 세션 한도 초과 — 오늘 21:40 리셋 후 재시도"), ② 인증 만료/재로그인, ③ CLI 미설치/키 미등록, ④ 타임아웃, ⑤ 기타. 유형별 다음 행동 안내.
> 4. **한도 상태 기억**: 최근 429의 한도 종류·리셋 시각을 서버가 기억해 카드에 표시하고, 리셋 전 변환 시도 시 실행 전에 미리 경고 + 폴백 정책 적용(불필요한 실패 호출 방지).
>
> **F35 기출 소스 커넥터** (v1.x 후보 — 2026-07-24 사용자 제안: 문제은행 사이트에서 긁어와 LLM 후처리로 자격증 대분류·회차까지 자동 반입):
> - 기존 파이프라인 재사용이 원칙 — 신규는 **수집기(fetcher)** 뿐: 수집 → convert 잡 큐(LLM 정리) → 미리보기·중복 감지 → 분류 제안(회차 트리 자동 생성) → 승인 반입. F30(오류 신고)·F34(엔진 한도)와 연동.
> - **1단계 (URL 반입)**: 공개 기출 PDF/페이지 URL 입력 → 서버가 다운로드 → convert 투입. 어댑터 불요, 공단 공개 자료(큐넷 등) 우선 경로.
> - **2단계 (사이트 어댑터, M10 — 설계 §4.13. M13에서 큐넷 단일 경로로 축소)**: 자격증·회차 선택 UI → 수집 → 문제/보기/정답/해설 구조 추출 → "정보처리기사/실기/2023년 2회" 형태 분류 자동 생성. 그림 문제는 이미지를 sources/에 저장해 본문 삽입. 어댑터는 소스별 모듈로 분리(외부 구조 변경 시 해당 모듈만 수정 — R14).
>   - **대상 = 큐넷(Q-Net) 공식 오픈API 단일 경로 (2026-07-27 사용자 확정)**: 사이트 어댑터는 **공단이 공식 제공하는 오픈API(3단계)만** 사용한다. 사설 문제은행 어댑터(**전자문제집 CBT(comcbt.com)** — M10, **CBT문제은행(cbtbank.kr)** — M12)는 **M13에서 전면 제거**한다. 어댑터 2호 이상은 **공식 오픈API를 제공하는 기관에 한해** 실수요 확인 후 검토(사설 사이트 대상 어댑터는 재도입 대상이 아니다).
>   - **[제거 이력] 사설 사이트 어댑터 제거 — comcbt·cbtbank (2026-07-27 사용자 확정, M13 범위. 되살리려 하지 말 것)**
>     - *무엇이 있었나*: M10에서 comcbt(회차 PDF 첨부 → FetchedFile), M12에서 cbtbank(회차 페이지 정적 HTML → FetchedExam 구조 추출)를 붙였고, 중복 회차 우선순위(큐넷 > cbtbank > comcbt)·시험 날짜 자연 키 병합·`refs`/`also_on` 대안 어댑터 재시도·어댑터별 robots/스로틀 분기가 그 위에 얹혀 있었다.
>     - *왜 없앴나*: ① **배포 형태가 바뀌었다** — 앱이 **GitHub 공개 저장소 + 테스터 배포 체계**로 전환(2026-07-27 포터블 패키지 도입)되면서 "개인 PC 단독 사용 = 사적 이용"이라는 M10 당시 전제가 성립하지 않는다. 사설 사이트의 DOM을 겨냥한 수집 코드를 공개 배포하는 것은 리스크의 성격 자체가 다르다. ② **큐넷 오픈API는 공단이 공식 제공하는 수단**이라 이 문제가 없다(허용된 계약 위에서 동작). ③ 어댑터 다중화가 만들던 복잡도(우선순위 병합·중복 회차 날짜 키 병합·level_hint 오병합 방지·어댑터별 예의 규칙 분기)가 대부분 소멸한다 — 신규 구현(qnet)과 제거를 같은 단계(M13)에서 하면 병합 로직을 **단순한 형태로 새로 짤 수 있다**.
>     - *수용한 트레이드오프(기대치 명문화)*: **기사급 필기 CBT 기출(2022년 3회~)의 자동 수집 경로가 사라진다.** 큐넷 공개문제에는 필기 기출이 없다(공단 비공개 방침). 앞으로 **필기 기출은 F35-1 URL 반입 / 파일 반입(수동)** 경로로만 처리한다 — 사용자가 명시적으로 검토·수용한 결과다(2026-07-27). **학습 루프(퀴즈·오답노트·SM-2·복습)와 이미 반입된 문서는 영향 없다.**
>     - *유지되는 것(제거 대상 아님)*: **F35-1 URL 반입 · 파일 업로드 반입 · convert 파이프라인** — 특정 사이트를 겨냥한 코드가 아니라 사용자가 가진 자료를 넣는 **중립적 통로**다. 어댑터 인터페이스(`base.py`)·registry·수집 파이프라인(FetchedFile/FetchedExam 두 경로)·SSRF/스로틀/매직 바이트 등 안전 장치도 그대로 둔다(qnet이 사용).
>     - *데이터 처리 방침*: **이미 반입된 문서·`sources/` 원본·수집 이미지는 전량 보존.** 어댑터를 지운다고 과거 수집물을 지우지 않는다 — 소프트 삭제 원칙(불변 규칙 3)·`sources/` 불변(규칙 4) 그대로. `sources.note`에 남은 `comcbt · …`/`cbtbank · …` 출처 문자열도 **역사적 기록으로 보존**(정리·마이그레이션 없음 — DDL 0건).
>     - *기각한 대안(재제안 금지 — 기록만)*: "대상 사이트를 코드에서 감춰 저격 여부를 알 수 없게 하기". **은닉은 침해 판단에 영향이 없고 오히려 고의성 정황으로 작용**하므로 채택하지 않는다.
>   - **회차 동일성·대표 키(M13 단순화)**: 어댑터가 하나뿐이므로 **어댑터 간 병합·우선순위 채택은 존재하지 않는다**(`also_on`은 항상 빈 배열, `refs`는 단일 항목). 남는 것은 **exam_key 형식과 폴더명 파생**뿐 — `YYYY-N` → "YYYY년 N회", 날짜형 `YYYY-MM-DD` → "YYYY년 M월 D일"(회차 번호를 알 수 없는 회차용 폴백). 문서 단위 중복은 기존 반입 중복 감지(§8.3) 재사용 — 별도 장치 없음. (M10~M12의 우선순위 병합·날짜 자연 키 병합 규칙은 제거된 다중 어댑터 전제에서만 의미가 있었다.)
>   - **스키마 변경 불필요 근거(2026-07-25 확정 · M12 재검증 2026-07-26 · M13 재확인 2026-07-27)**: 회차 메타는 기존 구조로 충분 — 회차 = `categories` 노드(분류 자동 생성 재사용), 문항 위치 = `documents.source_detail`("YYYY년 N회 M번"), 수집 출처 = `sources.note`(URL·어댑터 id). "이미 반입된 회차" 표시는 분류 경로 존재 여부에서 파생. 새 테이블·컬럼 없음. 어댑터 자료구조의 추가 정보(회차 시험 날짜·문항 과목 구분)는 **파이썬 dataclass·프롬프트 계층에서만 흐른다** — DB에는 기존 경로(분류 경로·source_detail·태그 제안)로만 반영, DDL 0건. **어댑터 제거도 DDL·마이그레이션 0건**(어댑터 id는 DB 스키마가 아니라 `sources.note` 자유 텍스트에만 등장).
> - **3단계 (큐넷 공식 오픈API, **M14 — 후순위**. 2026-07-27 스펙 확보 후 같은 날 실호출 실측으로 범위·한계 확정. **M13 이후 유일한 사이트 어댑터**)**: S10에서 유예된 qnet 목록 스텁(`available:false` — JS 포털 역설계 불가, R14 실측 사례)을 공공데이터포털 공식 오픈API **"국가자격 공개문제 조회 서비스"**(`apis.data.go.kr/B490007/openQst` — REST GET, `serviceKey` 쿼리 인증, XML/JSON)로 해소한다. 역설계·스크랩이 아닌 **공식 계약** — robots·DOM 파손 리스크 자체가 소멸하고, 어댑터 격리(R14) 덕에 **qnet.py만 수정, fetch API 계약 불변**(사설 어댑터 제거 후에도 `adapters`/`certs`/`exams`/`import` 계약 형태는 유지 — 설계 §4.13).
>   - **[실측 확정 사항 — 2026-07-27, 실제 호출로 확인. 구현은 이 값을 전제로 한다]** 서비스키로 `getOpenQstList`(`qualgbCd=T`) 전수 수집 **304건** + 상세 12건 무작위 샘플(`resultCode 00 NORMAL SERVICE`):
>     1. **`numOfRows` 최대 50**(스펙 문서에 없던 제약) — 51 이상은 `resultCode 930`. 전체 순회 **7페이지**. 페이지네이션 필수, 페이지 크기 50 고정.
>     2. **계열 분포**: 기능사 151 · 기사/산업기사 87 · **메타 빈값 43** · 기능장 18 · 변리사 2 · 공인노무사 2 · 주택관리사보 1.
>     3. **메타데이터 누락 43건(14%)** — `qualgbCd/qualgbNm/seriesCd/seriesNm/jmCd/jmNm`이 **전부 빈 문자열**인 게시물 존재(artlSeq·title·일시는 정상). **종목 기준 그룹핑·필터가 깨진다 → 방어 처리 필수**(빈 값으로 가짜 종목을 만들지 않고, 제목 검색으로는 노출).
>     4. **첨부 포맷(샘플 첨부 18개)**: **PDF 14 · ZIP 4 · HWP 0** — 예상과 달리 실제 비대응 포맷은 HWP가 아니라 **ZIP(도면·과제 묶음)** 이다. pdf+zip 동시 보유 사례 있음(양식조리산업기사). 압축 해제는 앱이 하지 않는다(원본 저장 + 파일 반입 폴백).
>     5. **커버리지 실측**: `jmNm=품질경영기사` → **`totalCount 0`**(기사 계열 87건에도 없음). 수록 대상은 **실기 작업형·도면 공개문제** — **필기·필답형 종목은 구조적으로 이 API 범위 밖**이다. → 이 사실이 **M14 후순위 결정의 근거**다.
>     6. **제목 형식 불규칙**: `[공개문제]봉제기능사(20260714)` · `[공개문제]양식조리산업기사(2026년도)` · `[문제공개]와전류비파괴검사기사` · `[공개문제] 2025년 미용사(일반) 실기시험관련 안내` · `미용사(일반)과제 조별 집행 안내` — 접두어·공백·회차 표기가 제각각이고 **공개문제가 아닌 안내문 게시물이 섞여 있다**(기본 숨김 + 토글 노출, 판정은 제목 텍스트만 — 상세 조회는 쿼터 소모).
>   - **[구현 확정 사항 — 2026-07-27, M14 완료 시점. 위 실측값의 재확인 + 구현에서 굳은 계약]**
>     1. **전 목록 재실측**: `numOfRows=50` × **7페이지 = 304건**, **930 에러 0건**. **종목 버킷 241개**. 메타 전 필드 빈값은 **42건**(위 실측 시점 43건 — 데이터 갱신으로 증감하는 값이다). 빈값 항목은 **가짜 종목을 0건 만들고**, **제목 검색으로 노출되며 반입까지 가능**하다(`cert_ref`가 `title:…` 형태 — 종목명을 모르므로 **분류 경로 자동 생성은 하지 않는다**).
>     2. **안내문 게시물 15건 기본 숨김 + 토글**: `POST /api/fetch/exams` 요청에 `include_notices`(기본 false), 응답 항목에 `is_notice` — **같은 24h 캐시에서 파생하므로 토글에 드는 추가 API 호출은 0건**(설계 §4.13). 판정은 제목 텍스트만 사용한다.
>     3. **`exam_key` 3형태 확정**(실측 분포 date 1 / **year 32** / unknown 271): `YYYY-MM-DD`(폴더 파생 O) · **`YYYY`(연도만 — 폴더 파생 X)** · `qnet-{artlSeq}`(식별 불가). **뒤 두 형태는 `imported=false` 고정이고 분류 경로를 만들지 않는다** — "2026년도"만 읽히는 게시물에 회차 번호를 지어내지 않는다는 원칙(추측 금지)의 귀결이며, 그 대가로 **자동 분류가 되는 회차는 소수**다(사용자가 반입 후 분류).
>     4. **"검색 0건"과 "상류 실패"의 구분**: "품질경영기사" 검색은 **HTTP 200 + 빈 배열**(정상 안내)로 끝난다. 반면 **쿼터 초과·키 오류는 502**로 내려 0건으로 위장되지 않게 했다(설계 §3 상류 실패 규약 — **에러 코드 집합은 늘리지 않고 상태 코드만 구분**, code는 `INTERNAL` 재사용).
>     5. **성공 소표기**: pdf+zip 동시 보유 게시물은 PDF 대표 + ZIP도 `sources/` 저장 후 잡 응답 `notes[]`에 완성 문장으로 안내(설계 §4.10 — 문구는 서버가 만든다).
>     6. **검증 상태**: ① **convert→미리보기 구간은 S14 검토(2026-07-27)에서 라이브로 완결 확인 — 해소.** 봉제기능사(artlSeq 5253924) 반입 잡이 `fetching→preparing→llm_running→parsing→preview_building`을 거쳐 **142초 만에 done**, 미리보기 3항목·`suggest_categories=["봉제기능사/실기/2026년 7월 14일"]` 생성. 상세 조회와 첨부 다운로드가 LLM 이전 같은 잡 구간에서 연속 수행됨도 phase 추이로 확인. ② **941(fileUrl 토큰 만료) 재시도 경로는 여전히 실제 발생을 재현하지 못했다**(코드 경로·단위 테스트만 — 다음 실사용에서 확인).
>     7. **알려진 한계**: 안내문 판정이 **접두어 없는 게시물 한정**이라 `[공개문제] …관련 안내`처럼 접두어가 붙은 안내성 게시물은 목록에 남는다. 실측상 이런 게시물은 전부 ZIP 전용이라 반입 시 `unsupported_format` 안내(원본은 저장됨)로 귀결된다 — 판정을 더 넓히면 진짜 공개문제를 숨길 위험이 커서 **현 범위를 유지**한다.
>   - **목록·상세**: `getOpenQstList`(종목명 `jmNm` 검색·`totalCount` 페이지네이션 — 자격증 검색·공개문제 목록의 공통 소스, 기존 24h 목록 캐시 재사용)·`getOpenQst`(상세 — 첨부 `fileList[]{fileNm, fileSn, fileUrl}`). 범위는 **qualgbCd=T(국가기술자격)만** — 과정평가형(C)·일학습병행(W)은 실수요 확인 후.
>   - **서비스키 = 루트 `secrets.json`**(키명 `qnet_service_key` — F34 API 키 전례와 동일 원칙: **DB/settings 저장 금지**, 백업(F27)·git 제외, write-only·마지막 4자리 마스킹, 등록 시 즉석 검증 호출 후 저장). 키 미등록 상태는 기존 스텁과 동일하게 `available:false` + 등록 안내 — 기존 동작의 상위 호환.
>   - **1시간 토큰 제약(핵심 설계 구속)**: 상세 응답의 `fileUrl`은 **JWT 토큰 URL로 유효기간 1시간(만료 시 에러 941)** — 따라서 상세 조회와 첨부 다운로드는 **같은 잡에서 즉시 연속 수행**하고, 목록·캐시 단계에서 fileUrl을 저장하지 않는다. 파일 호스트 `openapi.hrdkorea.or.kr`·API 호스트 `apis.data.go.kr`를 SSRF 검증 허용 목록에 추가(사설/루프백 차단·50MB 상한·도메인 2초 스로틀은 기존 장치 그대로).
>   - **비지원 포맷 정책(확정 — 명칭은 "HWP 정책"이었으나 실측 4로 **ZIP이 실제 대상**임이 확인됨)**: 첨부 중 **PDF 우선 대표 1건을 FetchedFile로 convert 투입**(FetchedFile→convert 경로 재사용 — PDF는 base64 document 블록으로 LLM 직투입, 서버측 PDF/HWP 라이브러리 추가 없음). **PDF가 없고 ZIP·HWP 등 비지원 포맷만 있으면 조용히 스킵하지 않는다** — 첨부 원본을 sources/에 저장(원본 확보 자체가 가치 — 실기 도면 등, 원본 불변 규칙 그대로)한 뒤 잡을 구조화 오류 `unsupported_format`으로 종료하고 포맷별 폴백을 안내한다: **ZIP** = "도면·과제 묶음 — 압축을 풀어 PDF/이미지를 파일 반입", **HWP** = "한글에서 PDF로 변환 후 파일 반입". PDF와 ZIP을 함께 가진 게시물은 PDF를 대표로 삼되 **ZIP도 저장**하고 결과에 소표기한다(실측 4). 목록 시점에는 포맷을 알 수 없으므로(fileList는 상세 조회 전용 — 상세 호출은 쿼터 소모) **반입 실행 시점 판정이 유일하게 가능한 지점**이다.
>   - **커버리지 한계(기대치 명문화 — 2026-07-27 사용자 수용, 같은 날 실측으로 확정)**: 이 API의 공개문제는 **실기 작업형·도면 위주** — **필기·필답형 종목은 아예 수록되지 않는다**(실측 5: 품질경영기사 `totalCount 0`, 필답형 실기는 공개 대상이 아님). 기사급 필기 CBT 기출(2022년 3회 이후)도 공단 비공개라 나오지 않는다. 사설 어댑터 제거(위 제거 이력) 이후 **필기 기출의 자동 수집 경로는 존재하지 않는다** — 필기 기출은 **F35-1 URL 반입 / 파일 반입(수동)** 으로만 처리한다. 이는 공개 배포 전환에 따라 사용자가 명시적으로 수용한 트레이드오프이며, 어댑터 notice·매뉴얼에도 같은 취지로 안내한다(기대치 어긋남 방지). 실기 회차는 어댑터 자료구조의 `level_hint='실기'`로 분류 경로 "자격증명/실기/…"를 생성(기존 필드 — DDL 아님).
>   - **운영 제약**: 개발계정 일 1,000건·30 TPS·데이터 갱신주기 월 1회 — 기존 24h 목록 캐시·한 번에 1회차 반입으로 충분(쿼터 관리 UI는 과설계). 오픈API 에러코드(22 쿼터 초과·30/31 키 오류·941 토큰 만료 등)는 **사람 말 메시지로 매핑**(원문 XML/JSON 노출 금지 — F34 오류 구조화 원칙 동일). → 리스크 R17.
>   - **스키마 변경 불필요 근거(2026-07-27 확정)**: 서비스키 = secrets.json 파일(DB 밖), 목록 = 메모리 캐시(24h), 출처 = `sources.note`(artlSeq·API URL — 기존 계약), 회차·경로·imported = 기존 categories·source_detail 파생 — **새 테이블·컬럼 없음, DDL 0건·Alembic 0건**.
>   - **v1.x+ 후보(기록만 — 별건 유예)**: 같은 기관의 **시험일정 조회 API로 `categories.exam_date` 자동 채움**(D-Day·F16 연계). 수동 등록(S4 D-Day 관리)으로 이미 충분할 수 있어 M14 범위에 넣지 않는다 — 실수요 확인 후 계획서에 먼저 확정.
> - **원칙(설계 §4.13에 강제 조항으로 명문화)**: **수집 대상은 공식 오픈API·공개 배포가 허용된 경로만**(M13 확정 — 사설 사이트 DOM을 겨냥한 어댑터는 만들지 않는다). 개인 학습 전용·재배포 금지·요청 간격 제한(소스별 **최소 2초**, robots 존중, 목록 캐시 24h). 로그인/CAPTCHA 필요 사이트는 범위 외(우회 코드 금지). 실행 전 예상 LLM 사용량(문항 수 기준) 안내 필수. 파싱 실패는 구조화 오류(`parse_failed`)로 표출하고 **URL 반입(1단계)·파일 반입** 폴백을 안내(대안 어댑터 재시도는 단일 어댑터 구성에서 해당 없음), 반입 후 오류는 F30 신고·재생성.
>
> **F26 학습 목표·스트릭** (M10 — 2026-07-25 상세 확정, F36-③ 연계 · 설계 §4.13):
> - **일일 목표**: 문제 수·시간(분) 2종 — `settings` 키(`goal.daily_questions`·`goal.daily_minutes`, 미설정/0 = 목표 없음). 설정 "학습" 그룹에 배치. 시간은 attempts의 `time_spent` 합산 기준(문제 풀이 시간 — 개념 열람 시간은 미측정, 한계 명문화).
> - **스트릭(연속 학습일)**: 활동일 = 히트맵과 동일 모수(attempts + 개념 완료) — **목표 달성 여부와 무관**(목표를 바꿔도 과거 스트릭이 재해석되지 않게). 오늘 활동이 아직 없어도 어제까지의 연속은 유지 표시.
> - **연동 3곳**: 홈 스트릭 위젯(레지스트리 11종으로 확장) · 히트맵 목표 달성일 링 강조 · 복습 완료 화면 "N일 연속" 배지(F36-③ 내일 예고와 나란히).
> - **스키마 변경 불필요 근거(2026-07-25 확정)**: 목표 = settings 키-값(기존 관례 — `srs.daily_limit`와 동일 패턴), 스트릭·오늘 진행·목표 달성 = 전부 **attempts·study_progress에서 파생**(개인 규모 전수 계산 충분). 별도 저장은 이중 기록·불일치 위험만 만든다 — 새 테이블·컬럼 없음. 목표 변경 이력 저장도 하지 않음(YAGNI — 히트맵 과거 달성 표시는 현재 목표로 재평가).
>
> **F36 학습 UX 다듬기** (v1.x 후보 — 2026-07-24 전체 채택. "기능 완비 후, 매일 공부하게 만드는 마찰 제거" 관점 11건):
> - **A. 시작 마찰 제거**: ① 원탭 데일리 세션 — 홈 [오늘 공부 시작] CTA: 복습 큐 소화 → 이어하기 지점 자동 연결, 분량 예고("복습 12 + 3-2장, 예상 25분") ② 폰 하단 탭바에 "복습" 탭 추가(현재 홈 경유 필수) ③ 복습 완료 화면에 내일 예정 개수 예고(F26 스트릭과 연계)
> - **B. 풀이·학습 속도**: ④ 퀴즈 키보드 단축키(1~4 보기·Enter 다음·B 북마크) ⑤ 오답 직후 해설 화면에서 틀린이유 원탭 기록(개념부족/실수/함정/시간부족 — 기억이 생생할 때) ⑥ 정답 시 1.5초 자동 다음 진행(설정 토글, 오답은 항상 정지) ⑦ 학습 모드 집중 모드(사이드바·헤더 숨김 전체화면 토글)
> - **C. 폰 완성도**: ⑧ 플래시카드 판정 undo 1회(오조작으로 인한 SM-2 오염 방지) ⑨ 본문 글자 크기 설정 3단계 ⑩ 문서 상세 SRS 표기를 사람 말로("3일 후 복습 예정" — EF/interval 용어 숨김)
> - **D. 어포던스**: ⑪ **공용 Stepper 컴포넌트** (2026-07-24 사용자 지적: 반입 ①②③이 눌릴 것처럼 생겼는데 안 눌림) — 지나온 단계(✓)는 **실제 클릭으로 되돌아가기**(파괴적이면 확인 후), 현재(●)는 강조 상태 표시, 미래(○)는 흐림+비클릭이 명확한 스타일·점선 연결. 반입 위저드에 우선 적용, F37 챕터 파이프라인(개념→문제→기출) 3단 표시에도 재사용.
> - 우선순위: ①②⑤가 체질 개선, ④⑥⑧⑩⑪은 quick win 묶음.
>
> **F37 챕터 학습 파이프라인 — 개념→문제→기출 연결** (M9 — 2026-07-24 사용자 확정: "커리큘럼을 통해 개념학습–문제풀이–기출풀이가 자연스럽게 연결"):
> - 데이터는 기존 문서 타입(concept/question/past_question/flashcard) 그대로 — **DDL 없음, 동선·표시만**.
> - **챕터 3단계 여정**: ① 개념 학습(기존 순차 흐름 — 개념+확인문제 인터리브 유지) → 개념 트랙 완료 화면에 **[문제 풀이로 이어가기]** → ② 챕터 연습문제 퀴즈 → 결과 요약에 **[기출로 마무리]** → ③ 챕터 기출 퀴즈 → **챕터 최종 완료 화면**(단계별 정답률 요약 + 틀린 문제 재도전 + [다음 챕터 ▶]). 각 단계 건너뛰기 가능, 이어하기(resume)는 단계 위치까지 기억.
> - **임의 깊이 트리 대응(사용자 확정)**: "챕터"는 고정 계층이 아니다 — 품질경영기사/필기/실험계획법/요인배치법/… 처럼 하위가 계속 나뉠 수 있으므로, **어느 깊이의 노드든 파이프라인 단위가 될 수 있다**. 모든 집계(개수 칩·3단 진도)와 [학습]·[문제]·[기출] 범위는 **해당 노드 + 하위 트리 전체**(§4.6 deep 원칙) — 상위 노드에서 누르면 하위 전부 포함, 하위 노드에서 누르면 그 범위만. 아코디언도 임의 깊이 중첩 렌더.
> - 커리큘럼 상세(설계 §5.4): 노드 행에 **3단 진도 표시**(개념 ✓ · 문제 ✓ · 기출 —)와 타입별 개수 칩(개념 N·문제 N·기출 N), 단계별 직접 진입 버튼([학습]·[문제]·[기출] — 순서 강제 아님, 여정은 권장 동선). 노드 펼침 시 타입별 탭 목록(학습내용/문제/기출).
> - **커리큘럼 내 문서 작성·수정(사용자 확정)**: 타입별 탭에 **[+ 문서 추가]**(현재 탭 타입이 기본값, 해당 분류 자동 연결 — 탐색 §5.2의 문서 생성 모달 재사용) + 각 문서 행에서 **편집 진입**(문서 상세 §5.3 편집 모드 재사용). Stage 4의 "분류 편집도 커리큘럼에서" 원칙을 문서까지 확장. **새 API 없음**(documents POST/PATCH + 링크 재사용).
> - 백엔드: `quiz/session`에 `types` 필터 파라미터(채점 규칙·트랜잭션 불변), `categories/tree?pipeline=1`(노드별 하위 포함 3단 집계) + `study-track`에 `deep`·`types` 확장 — 설계 §4.12.
> - **스키마 변경 불필요 근거(2026-07-25 확정, M9 계획 시 재검증)**: 3단 진도·단계 완료는 전부 **파생값** — 개념 done = 기존 `study_progress`, 문제·기출 done = 해당 문서 활성 attempt 존재(정오 무관), 단계 재개(next_stage) = 집계에서 첫 미완료 단계 산출. 커리큘럼 내 문서 작성·수정도 documents/links 재사용. 따라서 새 테이블·컬럼 없음(`resume_points`에 stage 컬럼 추가 불요 — 개념 트랙 내 위치는 기존 그대로, 퀴즈 단계는 세션 단위).
> - **구현 원칙 — 문서 3대 공용 모듈(사용자 확정)**: **DocViewer**(Markdown+문제 정답 가림)·**DocEditor**(작성/수정 폼)·**QuizCard**(풀이+채점+해설)를 화면 독립 공용 모듈로 유지·완성한다. 퀴즈카드·뷰어는 이미 공용(퀴즈/학습/복습/재도전에서 재사용 중), **DocEditor가 남은 조각** — 현재 문서 상세 페이지 전용인 편집을 모달/패널 모듈로 분리해 커리큘럼·탐색·검색 결과·오답노트·홈(북마크) 어디서든 "그 자리에서 보고·풀고·수정"이 되게 한다.
>
> **F38 설정 재구성 · 태그 관리자** (M9 — 2026-07-24 사용자 제안: "설정에 기능이 혼재, 태그 관리가 너무 어려움"):
> - **설정 카테고리화**: 6개 그룹 — 학습(복습 상한·기본 문항 수·F36 글자 크기·자동 다음) / 일정(D-Day) / 태그·분류(태그 관리자·태그 규칙) / LLM 엔진(S8) / 데이터(백업·복원·CSV) / 화면(테마). 좌측 목차(카테고리 점프), 모바일은 아코디언. **골격(카테고리 내비)은 M8에서 LLM 섹션 신설과 함께 선반영**(두 번 작업 방지), 그룹 재배치 완성은 M9.
> - **태그 관리자** (병합 "도구"→관리 "화면"으로 승격): ① 태그 목록 테이블(이름·사용 문서 수·규칙 사용 배지, 검색·정렬) ② **오타 후보 자동 감지** — 공백/대소문자/편집거리 1 이내 유사 태그 쌍을 계산해 "『정규화』↔『정규 화』 병합?" 제안 목록 ③ 행 액션: 이름 변경(전 문서 일괄 반영)·병합(기존 merge 재사용)·삭제(미사용만)·클릭 시 사용 문서 보기(탐색 필터 링크) ④ 백엔드: `PATCH /api/tags/{id}`(rename) + 유사 후보 산출 — **DDL 없음**.
>
> **F25 실전 모의고사** (M11 — 2026-07-26 상세 확정 · 설계 §4.14):
> - **흐름**: 시험 노드의 직계 자식 체크리스트로 과목 구성(회차 노드 1개만 선택 = "회차 그대로" 모의 — 전체 문항·순차) → 제한 시간 카운트다운 → **일괄 제출 채점** → 과목별 점수·합격선 비교 리포트. 퀴즈 모드의 문항 렌더(QuizCard)를 재사용하되 응시 중 채점 상호작용은 없다.
> - **일괄 채점은 전용 경로**(`POST /api/exam/submit`): 기존 문항별 즉시 채점(`POST /api/attempts`)을 문항마다 부르면 응시 중 정답·해설이 네트워크로 내려와 불변 규칙 1이 깨진다 — 세션(`exam/session`) 응답에는 정답·해설이 없고, 제출 시 서버가 전 문항을 한 번에 채점한다. 채점 규칙은 단건과 **같은 코어 공유**(이원화 금지).
> - **기록**: 문항마다 attempts(mode=`'exam'`, category_id=과목 노드) + 오답 시 오답노트 + SM-2 갱신 — **배치 전체가 한 트랜잭션**(불변 규칙 2). 미응답 = 오답(q=1, 오답노트 생성).
> - **합격선**: 기본 **과목 40점 미만 과락 · 과목 평균 60점 합격**(국가기술자격 표준) — 실행 시 조정 가능한 **요청 파라미터**(settings 저장 없음 — 표준 기본값으로 충분, YAGNI). 타이머는 **프론트 주도**(개인용 로컬 — 서버 검증은 과설계), 시간 초과 시 미응답 포함 자동 제출.
> - **스키마 변경 불필요 근거(2026-07-26 확정)**: 리포트·응시 이력 = 전부 **attempts 파생** — 응시(런) 식별은 **배치 공통 answered_at**(mode='exam') 그룹, 과목별 점수는 category_id(과목 노드) 그룹 재계산, 합격 재평가는 기본 컷 고정(F26 goal_met의 "현재 기준 재평가" 전례). 새 테이블·컬럼 없음 — attempts.mode 주석에 'exam' 추가만(값 제약 아님, 마이그레이션 0건). 당시 컷·제한시간 미보존 한계는 R15.
>
> **F16 D-Day 복습 강도 조절** (M11 — 2026-07-26 상세 확정 · 설계 §4.14. D-Day 등록·홈 위젯·관리 UI는 S4 완성 — 남은 것은 강도 조절뿐):
> - **발동**: 시험 분류 `exam_date`의 D-Day가 **14일 이내**(지난 날짜 제외)일 때. 임의 D-Day(`ddays.custom`)는 제외 — 분류 서브트리가 없어 범위 우선이 정의되지 않는다. 끄기 토글 = settings `srs.dday_boost`(기본 on).
> - **조절 3종 — 전부 큐 구성 계층**(sm2.py 알고리즘·srs_cards 저장값 불변): ① **유효 상한 확대**(D≤7 ×2, D8~14 ×1.5, 올림 — 큐·개수·summary가 같은 함수 공유) ② **큐 우선순위에 임박 시험 서브트리 삽입**(미해결 오답노트 최우선은 기존 결정 유지) ③ **선행 복습**(D≤7만 — due 미도래 카드를 남는 상한만큼 due순으로 당김: 시험 이후로 잡힌 카드를 시험 전에 한 번은 보게).
> - **스키마 변경 불필요**: settings 키 1개(`srs.dday_boost`) + 나머지 전부 파생(exam_date·srs_cards·category_documents 조합). 시험이 지나면 날짜 파생이라 저절로 원복.
>
> **F39 사용자 매뉴얼 앱 통합** (M12 — 2026-07-26 사용자 요청 "매뉴얼 html을 프로그램에 통합" · 설계 §4.15):
> - **서빙**: FastAPI가 `docs/manual/user-manual.html`(자기완결 HTML — 인라인 CSS, 토큰 팔레트 내장)을 `GET /manual`로 **읽기 전용 서빙**(FileResponse). **원본 파일이 단일 출처** — 빌드 복사·번들 포함 없음이라 문서 갱신 = 즉시 반영. `docs/manual/`을 쓰는 코드는 금지(sources/ 원본 불변 원칙과 동일 정신).
> - **진입점**: 사이드바 하단 "도움말"(새 탭 열람 — 학습 중 화면 상태 보존) + 설정 화면 링크(모바일: 사이드바가 없어 설정 경유). in-app 렌더러·검색은 만들지 않는다(자기완결 HTML이 자체 목차·스타일 보유 — YAGNI).
> - **스키마 변경 불필요**: 정적 파일 서빙뿐 — 저장·상태 없음. DDL 0건.
>
> **F40 수동 반입(파일/URL) UX** (M13 — 2026-07-27 사용자 확정. 큐넷 실측으로 **필기 기출의 자동 수집 경로가 존재하지 않음**이 확정된 결과, **수동 반입이 정본 경로**가 됐다 — 그 경로를 실사용에 견디게 만드는 것이 이 기능의 목적 · 설계 §4.3·§4.11·§5.9):
> - 성격: F04(JSON 반입)·F23(CLI 변환)·F35-1(URL 반입)에 **걸쳐 있는 반입 경로 UX 묶음**이라 기존 F의 확장이 아니라 독립 번호를 부여한다(F36 학습 UX 11건·F38 설정 재구성의 전례). **범위는 4건으로 한정** — 아래 "채택하지 않은 후보" 참조.
> - **① 변환 결과 보존·복구** *(가장 손실이 큰 문제)*: 변환 산출 JSON이 **프로세스 메모리 preview 캐시(TTL 1시간)에만** 존재해, 1시간 초과·서버 재시작 시 **LLM 비용을 치른 결과가 복구 수단 없이 소멸**한다(원본은 sources/에 남지만 재변환 = 재과금). 60문항을 항목별로 검토하면 1시간 초과가 현실적. → 잡 성공 시 반입 JSON을 `import/auto/{preview_id}__{원본해시12|nosrc}__{원본파일명}.json`으로 저장(**파일명 규칙이 복구 계약**), preview 캐시 미스 시 그 파일로 **재생성해 복구**(원본 바이트는 해시로 `sources/`에서 되읽어 원본 연결까지 복원), commit도 만료 시 같은 복구를 1회 시도. **[변환 JSON 내려받기]** 버튼으로 최후의 수동 폴백(기존 "JSON 파일 선택" 경로) 보장. 보존은 **최근 50건**, git·백업(F27) 제외 → 리스크 **R18**.
> - **② 반입 대기열(여러 파일 연속)**: 현재 파일 입력이 단일 선택이고 프론트가 잡 1개만 추적해, 파일마다 "선택 → 수 분 대기 → 검토 → 반입 → 새로 반입하기"를 반복해야 한다. 서버 워커는 이미 **동시 1개 큐**라 순차 처리는 그대로 가능하다. → 다중 선택(최대 10) → 기존 `POST /api/convert`를 파일 수만큼 호출해 큐에 적재(**새 API 0건, 병렬 실행 없음**) → 대기열 목록(대기/변환 중/검토 대기/반입 완료/실패)에서 완료된 것부터 검토·반입. localStorage 잡 기록을 배열로 확장해 새로고침 후에도 복원. **①이 선행돼야 의미가 있다**(뒤 항목 검토 중 앞 항목 preview 만료 방지).
> - **③ 분류 경로 미리 지정**: 사이트 반입 경로에는 분류 경로를 LLM에 **강제 고정**하는 장치가 이미 있는데(`_fetch_directives`) 파일·URL 반입에는 없어, 회차 경로를 매번 LLM 추론에 맡기고 preview에서 손보거나 반입 후 재분류하게 된다(다중 반입이면 N배). → `POST /api/convert`에 선택 파라미터 `category_path`(예 `"품질경영기사/필기/2022년 2회"`, 최대 5단계) 추가 + **사이트 반입과 같은 지시 생성기 공유**. 대기열에서는 "모든 파일에 같은 상위 경로" + 회차만 개별 입력. **자동 반입이 아니다** — 제안을 고정할 뿐 확정은 preview 승인(R7).
> - **④ 변환 출력 잘림 오안내 수정**: JSON 파싱 실패가 `kind:'other'` + **"잠시 후 다시 시도하세요"** 로 안내돼(같은 파일로 재시도하면 같은 실패) 원인·해결책 없이 LLM 비용만 반복 지출된다 — 문항이 많으면 출력 토큰 상한에서 잘리는 사례가 이미 코드 주석에 기록돼 있다. → `error_info.kind:'invalid_output'` 신설: "응답이 중간에 잘렸을 수 있습니다 — 원본을 과목·회차 단위로 나눠 올려 다시 변환". **엔진 교체 문구는 넣지 않는다(S13 구현 확정)** — 출력 상한이 CLI·API 공통이라 바꿔도 잘림이 그대로이고 [API로 재시도] 버튼도 뜨지 않는다. 재생성(F30) 경로는 같은 kind에 "재생성 사유를 더 짧고 구체적으로" 안내. **서버측 PDF 분할은 하지 않는다**(라이브러리 의존 금지 — F35-3 비지원 포맷 정책과 같은 원칙). 원문(raw) 노출 금지 유지.
> - **구현 확정 사항(2026-07-27, M13 완료 — 설계 §4.3·§5.9가 상세 단일 출처)**:
>   - **재커밋 차단(보존이 만든 새 위험)**: 종전에는 "커밋 성공 = preview 캐시 소거"가 곧 재커밋 방지였는데 디스크 보존이 그 방어를 없앤다 → **커밋 완료된 preview_id는 복구 대상에서 제외**하고 409("이미 반입이 완료된 미리보기입니다"). 보존 파일은 지우지 않아 내려받기 탈출구는 유지.
>   - **[변환 JSON 내려받기]는 보존본이 있는 경로(convert·fetch 잡 preview)에만 노출** — 사용자가 직접 올린 JSON의 preview는 보존 대상이 아니고(파일이 이미 손에 있음) 눌러도 404이므로 버튼 자체를 두지 않는다.
>   - **대기열이 `previewId`를 함께 영속(localStorage)** — 잡 레코드는 서버 메모리라 만료·재시작 후 404가 되지만 preview_id만 있으면 ①의 디스크 복구로 검토를 이어갈 수 있다(**잡 소실 ≠ 검토 불가**). 큐의 1차 키는 잡, **검토의 1차 키는 preview_id**.
>   - **URL 반입을 파일 대기열로 통합** — 단건 추적을 따로 두지 않는다(폴링·재시도·새로고침 재개 코드의 이원화 방지). 반입 화면 탭 4종·Stepper 3단계는 불변.
>   - **10개 상한 = "대기열 잔여 + 신규 선택" 합계 기준**(신규분만 세지 않는다). 완료·건너뛴 항목을 정리하면 다시 걸 수 있다.
>   - **알려진 한계: 처리 중인 1건은 취소·건너뛰기 불가** — 서버에 잡 취소 API가 없고 이 단계에서 신설하지 않았다(YAGNI — 동시 1개라 곧 끝난다). 취소·건너뛰기는 미시작 대기 항목에만 노출하고, 한계로 화면·매뉴얼에 명시. 잡 취소 API는 실수요 확인 후 후속 검토.
>   - 위 6건 모두 **계약 형태 변경 없음** — DDL 0건·Alembic 0건, 신규 엔드포인트는 예고대로 내려받기 1개뿐.
> - **채택하지 않은 후보(검토 후 제외 — 재제안 시 근거 필요)**: **진행 표시 개선**(단계 스텝·경과·토큰·ETA·잡 소실/서버 연결 실패 구분이 이미 구현돼 있음 — 추가 이득 없음) · **실패 항목만 부분 재반입**(항목 단위 LLM 재호출은 새 파이프라인 — 반입 후 오류는 F30 재생성이 담당) · **반입 이력 화면·테이블**(sources·문서에서 역추적 가능, DDL 유발 — YAGNI) · **preview 상태의 DB 이전**(디스크 JSON 보존으로 충분) · **분류 경로 프리셋 저장**(직전 입력 기억은 프론트 로컬로 충분 — settings 키 신설 금지).
> - **스키마 변경 불필요 근거(2026-07-27 확정)**: ①은 **파일 저장 + 기존 preview 재생성**(`import/auto/` — DB 밖, 백업 비대상), ②는 **프론트 상태 + localStorage**(서버 큐는 기존 그대로), ③은 **요청 파라미터 1개 + 프롬프트 지시**(분류 생성은 기존 commit 경로 재사용), ④는 **error_info kind 1종**(응답 문자열) — **새 테이블·컬럼 없음, DDL 0건·Alembic 0건.** 신규 엔드포인트는 `GET /api/import/preview/{id}/json`(내려받기) 1개뿐이고 나머지는 기존 계약의 상위 호환 확장이다.

> **F41 멀티 벤더 LLM 엔진 — Codex CLI** (M15 — 2026-07-27 사용자 지시로 계획 등록. 배포 시나리오: **도구만 배포 + 데이터는 각자 수집(저작권 안전) + LLM은 각자의 ChatGPT 구독** — 운영자 아이디·키 대여 없음):
> - **실측 근거(단일 출처)**: 격리 PoC `C:\Users\rlawh\Desktop\codex_engine_test\`(2026-07-27, 계획 Fable/구현 sonnet/검증 Opus 2회 — 1차 반려 후 시정, 재검토 통과). 종합은 `results\SUMMARY.md`(v2)가 정본. **조건부 GO** — 기술 연동 GO / 변환 품질 보류.
> - **입증된 것**: 단일 바이너리 자동 설치(GitHub 릴리스, npm/Node 불필요, 4.4초) · 자격증명 전역 공유(`~/.codex` — 재설치·격리 설치본도 재로그인 불필요) · **free 플랜에서도 exec 호출 성공**(무료 맛보기→구독 유도 여지) · `exec --json --skip-git-repo-check -C -o` 어댑터가 기존 `claude -p stream-json` 자리에 대응 · 스모크 반복 3/3 안정.
> - **적발된 최상위 리스크(초판 검토가 놓쳐 Opus 검증이 적발)**: **지문·보기 창작** — 수식 폰트가 추출 불가한 PDF에서 10문항 중 6건의 지문/보기를 모델이 지식으로 재구성(창작 보기 4개에 정답 공식이 없는 문항까지 발생 = 문제 위조). 정답 생성 여부도 **동일 프롬프트에서 비결정**(같은 규칙으로 한 번은 공란, 한 번은 자체 풀이 10/10). → **프롬프트 규칙만으로는 통제 불가가 실측 결론**이다.
> - **범위 4묶음**: ① **엔진 레지스트리** — F34의 `cli|api` 이항 가정(`other = "api" if engine=="cli" else "cli"`, `llm.priority` 2값, 한도 기억·헬스·S8 카드 2개)을 벤더×전송 목록으로 일반화, 폴백은 우선순위 목록화. ② **codex-cli 어댑터** — 호출·JSONL 파싱·**한도/오류 메시지 분류기 codex판**(429 형식 실측은 착수 후 과제). ③ **온보딩 마법사** — 바이너리 자동 다운로드→로그인 감지(있으면 건너뛰기)→진단([다시 확인] 패턴 재사용). ④ **변환 신뢰 게이트** — **§8.2 규격 확장**: `answer_source: original|solved` + **원문 대조**(산출 지문/보기가 원본 추출 텍스트에 실재하는지 검증, 불일치 → preview 경고·기본 반입 제외). R7(미리보기 승인)의 기계적 보강이며 **모든 엔진(Claude 포함)에 공통 적용**한다.
> - **착수 게이트(순서 조건)**: ⓐ M13·M14 종료(작업공간·반입 경로 안정 — **✅ 2026-07-27 충족**) + ⓑ **품질 재실험 통과**(**미충족 — 이것만 남았다**) — 정답표 있는 텍스트 정상 원본으로 지문·보기·정답 추출 정확도 측정(이번 PoC 원본은 정답표 부재+수식 추출 불가라 정량 측정이 오염됨 — SUMMARY §6). 재실험 시 PoC 하니스 재사용 전 `pure_json`을 pass 조건에 반영할 것(검증 수용 조건).
> - **설계 계약 확정(✅ 2026-07-28 — stage-15 게이트 G3 해소, 문서만·코드 0)**: 설계 **v1.17 §4.17** 신설 + **§8.2 v1.1** 개정. 핵심 결정 — status의 기존 `cli`/`api` 필드는 **호환 유지 없이 엔진 배열로 교체**(소비처 전수 = 같은 단계에 재작성되는 설정 카드 + `limit`만 쓰는 배너 — 이중 유지가 오히려 이항 잔재), legacy 설정·요청 값은 읽기 시 별칭 매핑, 직접 업로드 JSON은 `answer_source` 누락=original 간주·텍스트 answer 정규화 수용으로 **기존 파일 호환 유지**, 원문 대조는 정규화 후 부분 일치→12자 조각 커버리지 ≥0.6(문제 타입만, 개념 제외). 신규 엔드포인트 예정 1개(`POST /api/llm/engines/{id}/install`). **DDL 0건·Alembic 0건 재확인(§4.17 말미)** — 구현 중 어긋나면 착수 중단 후 보고.
> - **검증기 이월 결함 3건(PoC 검증 수용 조건 — 본 구현 시 반영)**: `overall_pass`에 `pure_json` 포함 / `content` 필수 검사 / 수치형 보기의 answer "번호 vs 보기 텍스트" 이중 해석 금지(규격에서 번호만 허용 검토).
> - **비범위(이 기능에서 하지 않는 것)**: 멀티유저·호스팅(별도 로드맵 결정) · OpenAI API 키 방식(BYO-구독이 원칙, 키는 F34 API 카드 전례 있을 때만 후속 검토) · 앱 패키징(온보딩의 나머지 절반 — 별도 F로 분리).

> **F42 다양한 문서 포맷 반입** (M16 — 2026-07-28 사용자 요청. docx·xml·md·txt·html·xlsx 등 파일을 반입 경로에서 정식 지원):
> - **실측 현황 (2026-07-28, 코드 실측 — 이 결함들이 기능의 출발점)**:
>   - **파일 반입에 확장자 화이트리스트가 없다**(`routers/convert.py` — 어떤 파일이든 잡 투입). 실효 지원은 엔진별로 다르다: CLI 엔진 = 파일 경로 전달(Claude Code Read 도구 — 텍스트·PDF·이미지), API 엔진 = PDF(base64 document 블록)·이미지(image 블록)·**그 외 전부 utf-8 강제 디코드(`errors="replace"`) 본문 삽입**(`convert_service._api_content_blocks_for_file`).
>   - 따라서 **docx/xlsx(zip 바이너리)를 올리면 mojibake가 조용히 LLM에 투입된다** — 구조화 오류 없이 LLM 비용을 치르고 쓰레기 산출이 나오거나, 최악의 경우 파일명만 보고 내용을 **창작**한다(F41 PoC가 적발한 병리와 같은 계열).
>   - URL 반입은 content-type 화이트리스트(pdf·html/xhtml·markdown·**text/plain**·이미지 4종)가 있어 txt/html/md는 이미 통과하지만 **xml·docx·xlsx 계열 MIME은 거부**된다.
>   - 서버 파싱 라이브러리는 **0개**(requirements 실측 — pypdf도 아직 없음. F35-3 "PDF는 LLM 직투입, 서버측 라이브러리 없음" 방침의 결과).
> - **포맷 3군 전략(확정)**:
>   - **A군 — 텍스트 직투입(라이브러리 불요)**: `md`·`txt`·`html`·`xml`. 확장자 + 내용 판별(텍스트인지 검사) 후 기존 "본문 삽입" 경로로 투입 — 사실상 이미 동작하는 경로의 **정식화**(화이트리스트 등재·화면 안내·인코딩 판별 추가). 인코딩은 utf-8 우선, 한국어 레거시(cp949) 폴백 — 순서·방식은 착수 전 결정.
>   - **B군 — 서버측 텍스트 추출(파서 라이브러리 필요)**: `docx`·`xlsx`. 서버가 텍스트를 추출해 A군과 같은 형태로 LLM 투입. 원본 바이트는 그대로 `sources/` 보존(불변 규칙 4 — 추출본은 파생물).
>   - **C군 — 구조화 거부(현행 유지 + 결함 수정)**: `hwp`·`zip`·판별 불가 바이너리 → `error_info.kind:'unsupported_format'` + 포맷별 폴백 안내(F35-3 관례 — "한글에서 PDF로 변환 후 반입" 등). **조용한 mojibake 투입 경로를 이 판별로 차단하는 것이 이 기능의 핵심 수정**이다. HWP 파서 도입은 이번에도 하지 않는다(순수 파이썬 성숙 파서 부재 — F35-3 폴백 안내 유지).
> - **라이브러리 의존 방침(계획 결정 — 2026-07-28)**: 기존 "서버측 라이브러리 추가 금지"는 **전역 원칙이 아니라 국소 결정**이었다 — F35-3(qnet 첨부의 PDF/HWP: LLM 직투입으로 충분)·F40-④(PDF 분할: 실익 대비 과설계)에 한정된 판단이고, **M15는 이미 pypdf+cryptography 의존 추가를 계획**하고 있다(원문 대조). M16에서 신설하는 원칙: **① 순수 파이썬·오프라인 동작 파서만(네이티브 빌드·외부 서비스 금지 — 단 wheel 배포로 설치가 무해한 C 확장 의존은 착수 시 개별 판단) ② 포맷당 1개 ③ 계획서(이 절)에 등재 후 추가**. 후보: docx = python-docx 또는 docx2txt, xlsx = openpyxl — **최종 채택은 착수 전 결정**.
> - **기존 불변 규칙 위에서 동작**: 원본은 `sources/`에 불변 보존(추출 텍스트는 저장하지 않음 — 재추출 가능한 파생물, R18의 preview 보존과 동일 정신) · 미리보기 승인 후 반입(R7) · 실행 전 LLM 사용량 사전 안내(F35 원칙 — 추출 텍스트 길이 기준으로 정확도 향상) · 50MB 상한·매직 바이트 검사 관례 유지.
> - **부수 효과**: B군 추출기는 **M15 변환 신뢰 게이트(원문 대조)의 "원본 추출 텍스트" 소스로 재사용**된다 — M16이 선행하면 M15의 대조 가능 포맷이 넓어진다(§14 순서 관계).
> - **착수 전 결정 필요(추측 확정 금지 — stage-16 착수 게이트 문서 절차)**: ① docx/xlsx 파서 최종 채택(python-docx의 lxml 의존 수용 여부 포함) ② 인코딩 판별 순서(utf-8 BOM→utf-8→cp949 잠정 — chardet류 추가 도입은 잠정 반대) ③ xlsx 직렬화 형식(시트별 Markdown 표 vs TSV)과 시트·행 상한 ④ csv 포함 여부(요청 목록 밖 — 인접 포맷) ⑤ URL content-type 확장 목록(xml·officedocument MIME) ⑥ 추출 텍스트 길이 상한과 초과 시 안내(F40-④ `invalid_output` 잘림 예방 연계 — 서버측 자동 분할은 하지 않음).
> - **스키마 변경 불필요 근거(2026-07-28 확정)**: 포맷 판별·추출은 전부 변환 잡 파이프라인(메모리·임시 파일) 안의 일이고, 저장은 기존 계약 그대로 — 원본 = `sources/`(`file_type`은 자유 텍스트라 'docx'|'txt' 등 값 확장에 제약 없음 — §6.2 주석 갱신만), 산출 = 기존 반입 JSON(§8.2 불변). **새 테이블·컬럼 없음, DDL 0건·Alembic 0건.** 신규 엔드포인트 0건(기존 `POST /api/convert`의 수용 포맷 확장).

> **F43 문서 상호참조·표현 UX — 트랜스클루전·가리기/접기·이동** (M17 — 2026-07-28 사용자 요청. 예: "품질경영기사-필기-통계적 품질관리"의 "확률과 확률분포" 문서 안에서, 이미 모듈화돼 있는 "산술평균" 문서를 레퍼런스로 접근해 **그 자리에 내용이 보이게** — 개념 단위 모듈 문서를 여러 상위 문서가 임베드해 재사용):
> - **본문 포맷 실측(2026-07-28)**: `documents.content` = Markdown(TEXT, §6.2) · 렌더러 = 공용 `MarkdownView`(react-markdown + remark-gfm + rehype-highlight) · 열람은 문서 3대 공용 모듈(DocViewer/DocEditor/QuizCard — F37)로 수렴돼 있어 **렌더 확장 1곳이 전 화면에 파급**된다(재작업 없음).
> - **① 트랜스클루전(핵심)**: 본문 내 레퍼런스 문법으로 타 문서를 참조하면 열람 화면에서 **그 자리에 대상 문서 본문이 임베드 카드로 펼쳐진다**(출처 배지 DOC-번호 + [원문 열기]). 참조 키는 `doc_no`(R1 단순 연번 — 재분류에 불변인 안정 키). 문법은 위키링크 계열 잠정(`![[DOC-0012]]` 임베드) — **착수 전 확정**.
>   - **해석은 서버 경량 API(신설 1개)**: 참조된 doc_no 목록 → `{doc_no, title, type, content, is_active}`**만** 배치 반환. **answer·explanation은 어떤 경우에도 미포함** — QuizCard도 같은 MarkdownView를 쓰므로, 임베드 해석이 퀴즈 화면의 정답 유출 경로(불변 규칙 1 위반)가 되지 않게 계약 수준에서 봉인한다.
>   - **순환·깊이**: 중첩 임베드 깊이 상한(잠정 2~3 — 착수 전 확정) + 해석 경로의 방문 집합으로 순환 검출 → "순환 참조" 자리표시자(무한 재귀 금지).
>   - **삭제·부재 대상**: 소프트 삭제(is_active=0) 문서 참조 = "삭제된 문서(DOC-xxxx)" 자리표시자(내용 미노출 — 복원하면 다시 보임), 존재하지 않는 doc_no = "존재하지 않는 참조" 표시. **참조가 삭제를 막지 않는다**(문서는 분류에 소유되지 않는다는 §6.3 원칙의 연장 — 참조 무결성은 강제가 아니라 표시로 해결). 삭제 시 "N개 문서에 임베드됨" 경고는 아래 인덱스로 제공.
>   - **참조 인덱스 = `document_relations` 재사용(새 테이블 불요)**: 문서 저장·반입 시 본문을 파싱해 `relation='embeds'`, `created_by='embed'` 행으로 동기화(본문에서 사라진 참조는 해당 행 삭제). **단일 출처는 본문 텍스트**이고 이 행은 재계산 가능한 파생 인덱스 — 사용처 표시("N개 문서에 임베드됨")·삭제 경고·역참조 탐색에 쓴다. relation·created_by는 자유 텍스트(CHECK 없음)라 값 추가에 DDL이 들지 않는다(§6.2 주석 갱신만).
> - **② 가리기·접기(구간 단위)**: 본문 구간을 마킹하면 — **접기(collapse)** = 제목 줄만 보이고 클릭 시 펼침(기본 접힘), **가리기(hide)** = 내용이 가려진 채 탭/클릭으로 공개(암기·자가 테스트용). 구간 문법은 착수 전 결정 — 후보: remark-directive 컨테이너(`:::fold[제목]`…`:::` 류, 프론트 플러그인 1개 추가) vs 원시 HTML `<details>`(rehype-raw 필요 — **XSS 표면이 넓어져 잠정 기피**).
> - **③ 이동 버튼**: `[[DOC-xxxx]]`(비임베드 참조) = 문서 칩/버튼 → 해당 문서 상세로 이동. 같은 문서 내 이동 = 헤딩 앵커(`[[#절 제목]]` 류 — 슬러그 규칙 착수 전 확정). 임베드 카드의 [원문 열기]도 같은 내비게이션 재사용.
> - **④ 시각효과**: 임베드 카드 스타일(경계·출처 배지)·접기/가리기 전환 애니메이션 — 전부 디자인 토큰만(불변 규칙 5, 다크 모드 공짜 원칙).
> - **학습 모드와의 상호작용(이번 단계 범위 확정)**: 렌더는 MarkdownView 공통이므로 문서 상세·학습 모드·커리큘럼·탐색·퀴즈·플래시카드에 일관 적용된다(임베드 해석이 본문 전용이라 전 화면 안전). 단 **진도·SRS 의미론은 불변** — 임베드로 "산술평균"을 읽어도 그 문서의 study_progress·SRS 카드는 갱신되지 않는다(열람한 것은 상위 문서다 — 이중 진도 방지). 임베드 문서를 퀴즈 출제 범위·SRS에 넣는 것은 기존 분류 연결로만(임베드는 출제 범위가 아니다).
> - **채택하지 않은 후보(검토 후 제외 — 재제안 시 근거 필요)**: **임베드 전용 새 테이블**(document_relations로 충분 — 본문이 단일 출처인데 테이블을 정본화하면 이중 기록) · **참조 대상 삭제 차단**(소프트 삭제 원칙과 충돌 — 자리표시자로 해결) · **블록/구간 단위 트랜스클루전**(`![[DOC-0012#절]]` — 문서가 이미 개념 단위 모듈이라는 전제와 중복, 필요가 실측되면 v1.x+ 후보) · **임베드 내용의 FTS 색인 포함**(검색은 정본 문서에서 이미 걸린다 — 이중 히트만 생김) · **WYSIWYG 편집기**(DocEditor는 텍스트 폼 유지, 참조 삽입 도우미만).
> - **착수 전 결정 필요(추측 확정 금지 — stage-17 착수 게이트 문서 절차)**: ① 레퍼런스 문법 최종 확정(임베드 `![[…]]` / 링크 `[[…]]` 구분, 표시명 별칭 `![[DOC-0012|산술평균]]` 지원 여부) ② 가리기·접기 구간 문법(remark-directive 채택 여부 = 프론트 의존 1개 추가 — rehype-raw는 잠정 기피) ③ 임베드 깊이 상한 값 ④ 인쇄 뷰(F22)에서 임베드 펼침 포함 여부(잠정: 펼침 포함 — A4 정리본의 자기완결성) ⑤ 헤딩 앵커 슬러그 규칙(한국어 헤딩) ⑥ 해석 API 형태(배치 1회 vs 문서별 — 캐시 전략 포함) ⑦ DocEditor 참조 삽입 도우미 범위(최소 = 검색 팝업 → doc_no 삽입).
> - **스키마 변경 불필요 근거(2026-07-28 확정)**: 참조는 본문 Markdown 텍스트에 살고(content 컬럼 그대로), 인덱스는 기존 `document_relations`의 값 확장(relation·created_by 자유 텍스트 — §6.2 주석 갱신만), 렌더·접기 상태는 프론트(펼침 여부는 화면 상태 — 저장 안 함). **새 테이블·컬럼 없음, DDL 0건·Alembic 0건.** 신규 엔드포인트는 임베드 해석 1개(본문 전용 배치 조회)뿐.

## 15. 리스크 & 미결 논점 (같이 고민할 것)

| # | 논점 | 초안의 잠정 입장 |
|---|------|----------------|
| R1 | 문서 고유번호 체계 — 단순 연번(DOC-0001) vs 의미 포함(DB-NORM-001) | **단순 연번** + 태그로 의미 부여 (번호에 의미 넣으면 재분류 때 꼬임) |
| R2 | 이미지 포함 문제(그림 문제) 처리 | content에 Markdown 이미지 링크, 파일은 sources/images/에 저장 |
| R3 | 플래시카드를 별도 타입으로 둘까, 개념 문서에서 자동 생성할까 | v1은 별도 타입, 추후 "개념→카드 자동 생성" 검토 |
| R4 | 문서 버전 관리(수정 이력) 필요? | v1은 updated_at만. 이력 테이블은 필요성 확인 후 |
| R5 | 백업 전략 | study.db 파일 복사 = 백업 끝. 주기적 자동 복사 옵션 |
| R6 | 여러 기기 동시 접속 충돌 | SQLite WAL 모드로 개인용 수준 충분 |
| R7 | LLM 변환 품질 관리 — **PoC 실측(2026-07-27): 추출 불가 원본에서 지문·보기 창작, 정답 자체 풀이가 프롬프트 규칙만으로 통제 불가** | 반입 전 미리보기 필수 승인 + source_detail로 원본 대조 가능하게. **M15 기계적 보강(전 엔진 공통, §8.2 v1.1·설계 §4.17)**: ① `answer_source: original\|solved` — LLM이 스스로 푼 정답은 경고 배지 + 기본 반입 제외 ② 서버측 원문 대조(문제 타입 지문·보기가 원본 추출 텍스트에 실재하는지 — 불일치 = `fabrication_suspect` 기본 제외, 추출 불가 원본 = "대조 불가" 명시 표시) ③ 검증 강화(순수 JSON·content 필수·객관식 answer 번호만). G2 재실험(2026-07-28): 깨끗한 원본에서는 창작 0·정답 10/10 실증 — 게이트는 더러운 원본 대비 안전망 |
| R8 | 태그 자동분류의 완전 자동 vs 제안-승인 | **기본 제안-승인**, 규칙별로 auto 옵트인 (오분류 조용히 쌓이는 것 방지) |
| R9 | LLM 엔진: claude CLI vs API | **CLI(headless) 우선** — 구독 활용, 키 관리 불필요. 원격 운영 시 API 재검토 |
| R10 | 개념 문서 "완료" 판정 기준 | v1은 "다음" 버튼=완료. 체류시간 기반 자동판정은 과설계로 보류 |
| R11 | 필기/실기의 성격 차이 (실기는 서술·작업형) | v1 문서 타입에 '서술형' 추가 검토 — 자가채점(내 답안 vs 모범답안 비교) 방식 |
| R12 | 홈 네트워크 보안 (0.0.0.0 바인딩) | 내부망 전용 원칙 — **공유기 포트포워딩으로 외부 노출 금지** 명시. 최소 PIN 잠금은 M6에서 검토 |
| R13 | tag_query 문법 범위 | v1은 **단일 태그 + OR만** 지원. AND/괄호 조합은 필요 확인 후 확장 (파서 과설계 방지) |
| R14 | 사이트 어댑터(F35-2)의 지속성 — 대상 사이트 DOM 변경·차단으로 언제든 깨질 수 있음 | 어댑터를 사이트별 모듈로 격리(해당 모듈만 수정), 파싱 실패는 `parse_failed` 구조화 오류로 표출 + URL 반입(F35-1)·수동 반입(A방식) 폴백이 항상 살아있게. 예의 규칙(간격 2초·robots·캐시)으로 차단 리스크 최소화. 스크랩 결과에 의존하는 저장 구조를 만들지 않음(반입 후에는 일반 문서와 동일). **실측 사례(2026-07-25, S10 구현)**: comcbt는 문항 본문이 JS CBT 앱이라 HTML 추출 불가 → PDF 첨부 경로로 우회, qnet은 JS 포털이라 목록 스텁(available=false) — 어댑터 격리 덕에 설계 계약 변경 없이 흡수(설계 §4.13 실측 노트). **실측 사례 2(2026-07-26, 사후 수정)**: comcbt 첨부 다운로드 3중 결함 — 한글 포함 URL 미인코딩 크래시 + 302 Location의 latin-1 mojibake(원 바이트 복원 후 quote로 해결) + `.pdf` 파일명이면 HTML 오류 페이지도 통과하던 매직 바이트 검사 — registry 정규화·comcbt 검사 강화·미분류 예외 traceback 로깅으로 수정(회귀 테스트 4건). **실측 사례 3(2026-07-26, M12 사전 조사)**: cbtbank.kr은 robots 명시 Allow + 서버 렌더 정적 HTML(전 문항·정답·해설·과목·이미지) — 어댑터 계약 변경 없이 FetchedExam 경로(그간 실사용 대기)로 수용, 단 라벨에 회차 번호가 없어 병합 키를 시험 날짜로 확장(§14 F35) — **[해소 2026-07-27, M13] 사설 사이트 어댑터(comcbt·cbtbank)를 전면 제거해 이 리스크의 원인(사설 사이트 DOM 의존·차단·공개 배포 시 저격 코드)이 소멸**했다. 남은 어댑터는 큐넷 공식 오픈API 하나이며, 그 외부 의존 리스크는 **R17**이 담당한다. 위 실측 사례들은 "역설계 의존 수집의 취약성" 교훈으로 보존(§14 F35-2 제거 이력 참조 — 재도입 금지) |
| R15 | 모의고사(F25) 응시 이력을 저장 없이 파생(런 키 = mode='exam' 배치 공통 answered_at)하는 방식의 한계 — 응시 당시의 합격선(cut)·제한시간·상세 리포트가 남지 않음 | **리포트는 제출 응답이 정본**(결과 화면에서 소비), 이력(exam/history)은 점수·합격을 기본 컷(40/60)으로 재평가 표시 — F26 goal_met의 "현재 기준 재평가" 전례와 일관. 컷을 시험별로 바꿔 쓰는 실수요가 확인되면 exam_runs 테이블 신설 재검토(§6.2 갱신 + Alembic 세트) |
| R16 | 정적 파일 서빙의 경로 탈출 — SPA 폴백(`main.py`)이 퍼센트 인코딩된 `..`을 정규화 없이 이어 붙여 `study.db`·백엔드 소스·OS 파일까지 서빙됨 (**2026-07-26 S12 검토 실측 발견**, S1부터 존재하던 결함) | `resolve()` + `is_relative_to(FRONTEND_DIST)` 검사로 dist 밖 경로는 index.html로 회귀시켜 차단(S12에서 수정 완료·회귀 테스트 5건, 수정 전 코드에서 실패함을 역검증). 교훈: R12(내부망 전용)는 노출면을 줄일 뿐 경로 검증을 대신하지 않는다 — **사용자 입력이 파일 경로에 닿는 지점은 전부 정규화 후 루트 종속 검사**(`/images`·`/manual`도 동일 규약) |
| R17 | 큐넷 오픈API(F35 3단계)의 외부 의존 제약 — 쿼터(개발계정 일 1,000건)·`fileUrl` JWT 1시간 만료(에러 941)·스펙 변경/서비스 중단·커버리지 기대치. **[실측 갱신 2026-07-27]** 실호출로 확인: `numOfRows` **최대 50**(초과 시 930 — 스펙 문서 미기재), 목록 304건 중 **메타데이터 전 필드 빈값 43건(14%)** 으로 종목 그룹핑이 깨질 수 있음, 첨부 실제 비대응 포맷은 HWP가 아니라 **ZIP**, 제목 형식 불규칙 + **안내문 게시물 혼입**, 그리고 **필기·필답형 종목 미수록**(품질경영기사 `totalCount 0`) — 즉 **이 API는 사용자의 주 학습 대상을 커버하지 못한다**(→ M14 후순위 결정 근거). **M13 갱신(2026-07-27): 사설 어댑터 제거로 자동 수집 경로가 이 API 하나로 집중** — 오픈API가 중단·개편·키 발급 정책 변경되면 **자동 수집 기능 전체가 멈춘다**(부분 저하가 아니라 전면 정지) | 24h 목록 캐시·1회차 단위 반입으로 쿼터 여유 확보(월 1회 갱신 데이터에 충분), 941은 **상세 조회→다운로드 같은 잡 연속 수행**으로 원천 회피(만료 시 상세 재조회 1회 재시도), 에러코드는 사람 말 매핑(원문 미노출 — F34 원칙). **의존 집중 대응**: ① 자동 수집은 어디까지나 편의 경로이고 **정식 반입 경로는 파일 반입·URL 반입(F35-1)** — 두 경로는 외부 서비스에 의존하지 않으므로 오픈API가 죽어도 앱의 핵심(학습 루프·반입)은 무손상, ② qnet 장애 시 `available:false` + "URL/파일 반입으로 진행" 안내(스텁 동작 복귀 경로가 이미 존재 — 키 미등록 상태와 동일), ③ 사설 어댑터로의 폴백은 **정책상 재도입하지 않는다**(§14 F35-2 제거 이력 — 가용성 목적의 번복 금지). 커버리지 한계(필기 기출 미제공)는 §14 F35-3·어댑터 notice·매뉴얼에 명문화. 스키마는 API 결과에 의존하지 않음(반입 후엔 일반 문서). **④ 실측 반영(M14 착수 조건)**: 페이지 크기 50 고정·메타 빈값 방어·안내문 필터·제목 파싱 표본 테스트를 stage-14 체크리스트·DoD로 고정해 "추측 구현"을 막는다. **⑤ M14 완료 후 잔여(2026-07-27)**: **미검증 1건**(941 재시도 실발생 — convert→미리보기 완주는 S14 검토 라이브 반입으로 확인·해소, F35-3 "검증 상태" 참조)은 다음 실사용에서 확인한다 — 실패해도 폴백(파일 반입)이 살아 있어 학습 루프에는 영향이 없다. **접두어 있는 안내성 게시물이 목록에 남는 한계**는 수용(전부 ZIP 전용 → `unsupported_format` 안내로 귀결). **자동 분류 커버리지가 낮은 것도 수용**(연도만/식별 불가 회차는 폴더를 만들지 않는다 — 회차 창작 금지가 우선) |
| R18 | **변환 산출 JSON의 디스크 보존(F40-①, `import/auto/`)** — 백업(F27) 대상이 아니라 복원 후에는 복구되지 않고, 방치하면 디스크에 쌓인다. 반대로 보존하지 않으면 preview TTL 1시간·서버 재시작만으로 **LLM 비용을 치른 결과가 사라진다**(현행 동작) | **보존을 택하되 관리 비용을 상수로 묶는다** — 최근 **50건**만 유지(초과 시 오래된 것부터 삭제), git·백업 제외를 명시. 손실 시 대가는 "재변환 비용"뿐이다(원본은 sources/에 있고 백업된다 — 자료 자체는 안전). DB로 옮기지 않는 이유: preview는 본질적으로 **임시 상태**이고 테이블화하면 정리·마이그레이션 부담이 영구화된다(§6.2 DDL 0건 원칙 유지). 실사용에서 50건이 부족하거나 복원 후 복구 요구가 생기면 그때 계획서에 먼저 확정 |
| R19 | **문서 파서 의존·추출 품질(F42 B군)** — 서버 파싱 라이브러리 0개 원칙이 처음 깨진다(M16에서 docx/xlsx 파서, M15에서 pypdf 예정). 파서가 못 읽는 변형 파일(암호 걸린 xlsx, 구버전 doc/xls, 손상 파일)과 **추출 품질 저하**(표·수식·이미지 소실 — docx 수식은 OMML이라 텍스트 추출에서 유실됨)가 새 실패 유형이 된다 | **의존은 원칙으로 묶는다**(F42 — 순수 파이썬·오프라인·포맷당 1개·계획서 등재 후 추가). 파서 실패는 mojibake 투입이 아니라 **구조화 오류(`parse_failed`/`unsupported_format`) + 폴백 안내**("PDF로 변환 후 반입" 등 — 조용한 실패 금지, F35-3 관례)로 표출. 구버전 `doc`/`xls`는 지원 목록에서 명시적으로 제외(변환 안내). 추출 품질 한계(수식·복잡 표)는 화면·매뉴얼에 기대치로 명문화하고, 최종 방어선은 기존 그대로 **미리보기 승인(R7)** — 추출이 부실하면 사용자가 반입 전에 본다. M15 원문 대조가 들어오면 추출 텍스트가 대조 기준으로도 쓰이므로 품질 문제가 이중으로 드러난다(조용히 썩지 않음) |
| R20 | **트랜스클루전 참조 무결성·유출면(F43)** — 참조가 본문 자유 텍스트에 살므로: 본문 수정과 `document_relations('embeds')` 인덱스가 어긋날 수 있고, 순환 참조·삭제 대상·깊이 폭주가 렌더를 깨뜨릴 수 있으며, 임베드 해석 API가 새 **정답 유출면**이 될 수 있다(QuizCard가 같은 렌더러를 공유) | **본문이 단일 출처, 인덱스는 파생** — 어긋나면 저장 시 재파싱으로 수렴(전량 재계산 명령 1개 확보 — 인덱스는 언제든 버리고 재생성 가능). 순환 = 방문 집합 검출 + 자리표시자, 깊이 = 상한 고정, 삭제 = 자리표시자(참조가 삭제를 막지 않음 — §6.3 연장). 유출은 **계약 수준 봉인**: 해석 API 응답 스키마에 answer·explanation 필드 자체가 없다(필터링이 아니라 부재 — 불변 규칙 1). 학습 의미론 불변(임베드 열람은 진도·SRS 미갱신 — 이중 기록 방지) |

---
> 다음 단계: 상세 설계는 `docs/02-design/study-app.design.md` (API 명세·화면 상세),
> 구현 순서는 `docs/01-plan/stage-1-skeleton.plan.md` ~ `stage-14-qnet-openapi.plan.md` 참조.
> MVP = Stage 1~3, v1 완성 = Stage 7 (2026-07-24), **v1.x = Stage 8~11 완료 (2026-07-26)**, v1.x 후속 = Stage 12(M12, ✅ 2026-07-26) · **Stage 13**(M13 — 사설 어댑터 제거 + 수동 반입 UX F40, `stage-13-fetch-cleanup-manual-import.plan.md`, ✅ 2026-07-27) · **Stage 14**(M14 — 큐넷 오픈API, `stage-14-qnet-openapi.plan.md`, ✅ 2026-07-27). 구 `stage-13-qnet-openapi.plan.md`는 두 문서로 분할된 표지만 남아 있다(2026-07-27).
> 계획 등록·착수 전: **Stage 15**(M15 — 멀티 벤더 LLM 엔진, 착수 게이트 있음) · **Stage 16**(M16 — 다양한 문서 포맷 반입 F42, `stage-16-doc-formats.plan.md`) · **Stage 17**(M17 — 문서 상호참조·표현 UX F43, `stage-17-doc-transclusion.plan.md`) — 순서 관계는 §14 "M15~M17 순서 관계" 참조(M16·M17은 게이트 없음, M15↔M16 동시 착수 금지).
> 이후 항목은 실사용 피드백을 모아 신규 로드맵을 이 계획서에 먼저 확정한 뒤 착수한다.
