# Study Hub — 상세 설계 (API 명세 · 화면 상세)

> (2026-07-31 문서 구조 변경 — 판번 유지: §4 API 명세 → `study-app.design.api.md`, §5~7 화면·테마·상태 → `study-app.design.screens.md`로 **내용 이동만** 분할. § 번호·[S#] 태그는 그대로 — "설계 §4.x" 참조는 api 파일, "§5~7"은 screens 파일에서 `### <번호>` Grep)
> 상태: **Design v1.29** — v1.28 대비: **screens §5.15 신설(S23 — F49 분할 반입 위저드 화면 상세, 2026-08-04 — 문서만·코드 0·API 계약 증감 0)**: §4.25 프론트 앵커·stage-23 문서 §4 체크리스트를 화면 계약으로 고정 — 진입(convert 잡 `too_large`의 `alternatives:'split_import'` → [분할 반입], fetch 잡 발생분 미추가)·위저드 4스텝(① 휴리스틱 즉시 분석·uncertain 시 정밀 분석 제안+EngineSelect ② 분할안 확인 게이트: 체크박스·인접 [합치기]만·`category_path` 초안 편집 ③ 선택분 비용 합계 확인 ④ enqueue → ImportQueue 합류)·재사용(LlmJobProgress·공용 복원 훅·jobRoutes 매핑·작업 센터 취소 합류)·같은 원본(hash12) 재사용 선택 UI·서버 완성 문장 렌더·토큰 전용. 두 출처에 없는 세부는 전부 "구현 재량" 표기(새 결정 0) + §5 서두 카운트·파일 분할 지도 갱신.
> (v1.28: v1.27 대비: **F49·F50·F51 확정 계약 반영 (2026-08-04 사용자 전건 확정 — 계획서 v0.35 연동, 문서만·코드 0)**: ① **§4.25 신설(S23 — F49 대용량 원본 LLM 분할 반입, 착수 전 결정 전건 해소분)** — 다단계 사용자 개입형 플로우 5단계(휴리스틱 우선·LLM 정밀 분석은 확인 스텝 뒤·분할안 확인(체크박스+[합치기])·기존 convert 잡 N개 투입·조각별 미리보기 승인), 상한 3종(원본 200만 자·조각 40개·조각당 20만 자), **신규 엔드포인트 4개**(split 시작(동기·LLM 0)·analyze 잡(kind `'split_analyze'`)·상태·enqueue), 조각 = 원문 연속 부분 문자열 결정론 검증, `import/split/` JSON 20건 보존, **기존 결정·서술 개정 지점 전수 표**(F40-④ 두 곳 문면·`too_large` 안내·`alternatives` `'split_import'`·§4.23 ⓒ/§4.24 ⓒ 8→9곳·§4.24 kind 8→9종·§4.22 F46 수집 지점 — §4.11 "PDF 분할 금지"는 불변). **DDL 0건·Alembic 0건·settings 키 0(§4.25 말미)** ② **§4.21 내 S24 개정 블록 병기(S24 — F50 누적·1회성 모드)** — 본문 보존 + 병기(§4.23 ⓑ 관례·신설 절 없음): generate `mode?: 'accumulate'(기본)|'oneshot'`, accumulate = 출력 스키마 `items[].tags` 확장(같은 호출·추가 LLM 0·위반 = 태그만 무시) + DocumentTag 저장 + `scan_document` **auto 규칙도 suggested 강제**(승인 없는 자동 연결 경로 0 유지), oneshot = 종전 그대로 — "태그 미부여" 서술 2곳에 개정 병기, 격리 저장·마커·기본 제외 불변. **DDL 0건·신규 엔드포인트 0개** ③ **screens §5.3에 S25 확정 서술(F51 해설·정답 표시 품질 — 프론트 전용·백엔드 계약 변경 0)** — 정답 표기 공용 포맷터 1곳("③"만·적용 6곳 전수) + 공용 MarkdownView에 remark-breaks·remark-math·rehype-katex 추가(전 화면·인쇄 파급·소급 0, 신규 프론트 의존 4건). §4 단계 표기 [S1]~[S25]로 확장. 작업 지시서 3종: `stage-23`(구체화)·`stage-24`·`stage-25`(신규 — 착수 순서 S25→S24→S23).)
> (v1.27: v1.26 대비: **F49(대용량 원본 LLM 분할 반입, 가칭 — 계획서 v0.34 §14 F49 등재분) S23 개정 예고 2줄 병기 (2026-08-04, 문서만·코드 0·계약 변경 0)**: §4.18 원칙 재확인의 "서버측 자동 분할 금지(F40-④ 결정 유지)"와 §4.18 ⑤ `too_large` 분할 권고 문구에 각각 **S23 개정 예고 1줄**(본문은 S16 시점 기준 보존 — §4.23 ⓑ 예고 병기 관례). 개정의 실질 경계 명시: 금지의 실질 3건(부분 잘림 투입·서버측 PDF 파일 분할(§4.11 불변)·무승인 자동 반입)은 전부 계승, 20만 자 상한·LLM 호출 전 비용 0 차단 불변 — 개정 후보는 문면·안내 문구·`alternatives` 값(`'split_import'`)뿐. **§4.25는 잔여 착수 전 결정(계획서 §14 F49 ㉮~㉳ — 사용자 확정 대기) 확정 후 신설**(stage-23 문서 D7). 엔드포인트·DDL 증감 0. ※ 같은 날 계획서에 등재만 된 F50(응용 모의고사 누적·1회성 — §4.21 격리 규약 부분 개정 예고 대상)·F51(해설·정답 표시 품질 — 렌더 계층)은 결정 확정 전이라 설계 반영 없음(확정 시 각각 §4.21 예고 병기·해당 절 개정).)
> (v1.26: v1.25 대비: **§4.24 신설(S22 — F48 LLM 작업 센터, 계약 확정 2026-08-03 — stage-22 착수 전 결정 ①~⑥ 해소분. 문서만·코드 0)**: ① **전역 잡 목록** — 신규 `GET /api/llm/jobs`(전 잡 kind 8종 공유 큐에서 인메모리 파생 — LLM 0·DB 0, `label` 서버 완성 문장·`ref` kind별 참조 id, 종료분은 잡 TTL 1시간 내 레코드만·서버 재시작 시 소실 수용) ② **취소 = queued 제거(비용 0)/running 중단(CLI 프로세스 트리 종료·API 스트림 중단 — 부분 과금 정직 표기) 이원 + 상태 값 `'cancelled'` 전 kind 순수 확장 + 취소-완료 레이스 = 완료 승리·409**(신규 `POST /api/llm/jobs/{id}/cancel` — v1.15 ④ "잡 취소 API 신설 안 함"·§5.9 S13 "처리 중 1건 취소 불가" 한계를 필요 실측으로 개정) ③ **일시정지 = 대기열 수준만**(`queue/pause`·`resume` — 다음 잡 시작 보류·running은 완료까지, paused 인메모리·재시작 시 해제. **LLM 호출 중간 일시정지·재개 기각 확정** — 스트리밍 재개 = 재호출 = 과금 중복) ④ **요청 단위 `model?` 파라미터 — §4.23 ⓑ "요청 단위 오버라이드 없음" 결정 개정**(필요가 2026-08-03 사용자 요청으로 실측 — 단서 절차 이행): 잡 시작 8곳, 명시 엔진 전제(auto+model = 422)·소목록 밖 = 422·1회성(settings 무변경)·폴백 시 소멸·재산출(§4.23 [중요①] 관례). §4.23 본문 보존 + 예고 1줄 병기 ⑤ **재진입 진행 복원 = 잡 목록 단일 출처 공용 훅**(적용 화면 6곳 전수 — 화면별 localStorage 신설 기각, ImportQueue 기존 복원은 예외 불변) ⑥ **UI 배치** — PC/태블릿 사이드바 하단 고정 버튼(진행 배지)·모바일 좌측 드로어 항목(하단 탭바 5개 불변)·라우트 아닌 전역 슬라이드 오버. **신규 엔드포인트 4개 · DDL 0건·Alembic 0건·DB 쓰기 0·settings 키 0(§4.24 말미)**. screens **§5.14 작업 센터 절 신설** + §5 공통 레이아웃 진입점·§5.9 한계 개정 예고·§5.12 복원 반영.)
> (v1.25: v1.24 대비: **§4.23 신설(S21 — F47 엔진 운용 제어, 계약 확정 2026-08-03 — stage-21 착수 전 결정 ①~⑥ 해소분. 문서만·코드 0)**: ① **활성 토글** — settings `llm.disabled`(비활성 엔진 id **부정 목록** — 신규 엔진 기본 활성 전방 호환, 긍정 목록·엔진별 boolean 키 기각) + 후보 자격 일관 개정 **"`available()` && enabled"**(§4.17 ①③ 개정 지점 전수 — auto 해석·폴백 다음 후보·`fallback_available`, 후보 함수 1곳·신규 오류 kind 0) ② **모델 선택 = 레지스트리 수준 확장** — 항목에 `models` 소목록(하드코딩)·`default_model` + settings `llm.models`(`{엔진id: 모델id}`) + **`invoke()` 공통 적용**(claude-cli `--model`·claude-api model·codex-cli `-m` — 전 잡 kind 일관, **자유 텍스트 입력 기각 확정**·legacy `llm.api_model` 읽기 별칭·**무설정 시 동작 불변**, 소목록 구체 값은 구현 실측 이월 — 계약 문장 고정) ③ **status `engines[]` 필드 4종 순수 추가**(`enabled`·`models`·`default_model`·`selected_model` — 톱레벨·기존 필드 불변) ④ **선택 UI 게이팅** — 공용 EngineSelect(비가용·비활성 = 선택 불가 + 사유 4종 라벨 + [다시 확인] 재진단 연계 — F34 패턴 재사용) + **서버 방어 = 명시 지정 비가용·비활성 엔진 422 동기 거부**(잡 시작 8곳 전수·공통 헬퍼 — 잡 전 검증이라 error_info 아닌 §3 관례). **DDL 0건·Alembic 0건·신규 엔드포인트 0개 재확인(§4.23 말미)** — 저장은 settings 키 2개·레지스트리 상수뿐. §4.17에는 개정 예고 1줄만 병기(본문 보존 — §4.11 관례). screens §5.9·§5.11 ④·§5.12에 게이팅·토글·모델 UI 반영.)
> (v1.24: v1.23 대비: **§4.22 신설(S20 — F46 반입 자기 개선 루프, 계약 확정 2026-08-02 — stage-20 착수 전 결정 ①~⑦ 해소분. 문서만·코드 0)**: ① **결정 확정 7건** — `suggestions` 확장 = **테이블 재사용 기각·파일 기반 별도 저장 + 제안함 "화면" 일반화**(현행 suggestions는 document_id·category_id NOT NULL 전용 구조 — 타입 확장 = DDL이라 기각, 기존 §4.9 계약 불변) · 실패 사례 보존 = **`improve/` JSON 레코드, cases·proposals 각 최근 50건(R18 상수 관례)·git·백업 제외**(중복 = count·last_seen_at 갱신) · 사례집 경계 = **`prompts/convert.cases.md` 신설**(few-shot 예시만 — 정책 문장은 본문 전용·본문 우선 조항 1줄) · 트리거 = **수동 버튼만**(자동 임계치 기각 — F35 충돌·R23 과적합) · 회귀 = **수동 + 선택 사례 1~10건 + estimate 확인 스텝**(전체 일괄 기각) · F30 연결 = **신고 시점 자동 사례 수집(비용 0·계약 불변)** · 프롬프트 반영 안전장치 = **정책 잠금 구간 확정**(`<!-- policy-lock -->` 마커 + 서버 결정론 검증 — 위반 422) ② **신규 엔드포인트 13개**(cases 3 + gen 3(prepare(LLM 0)→generate(잡 kind 'improve_proposal')→상태) + proposals 4(목록·상세 미리보기·**apply = 유일한 파일 쓰기**·reject) + regression 3(잡 kind 'improve_regression' — preview·DB 무기록 검증 전용)) ③ prompt_edit 계약 = **치환 헝크(before 정확·유일 일치 — unified diff 파서 불도입)** · 사례집 20,000자 상한 · code_issue = 재현 패키지 인계(파일 쓰기 0 — 코드 자기 수정 금지 상한). **DDL 0건·Alembic 0건 재확인(§4.22 말미)** — **DB 쓰기 0인 첫 기능**(저장 = improve/ 파일·prompts/ git 파일·인메모리뿐). **구현 정합 반영(2026-08-02, 같은 날 구현 완료분)**: 응답 형태 4건 구현 확정을 §4.22에 병기(proposals 목록 = 평면 배열 · 제안 상세 = 평면 필드(preview_after·policy_check 최상위) · cases 목록 항목 = 상세 동일 전체 필드 · regression `results[].detail` = 한국어 문자열 — 실질·불변 조건 동일) + **§5.13 제안함 절 신설**(screens — 수신함 2탭·ImproveInbox 컴포넌트 실명).)
> (v1.23: v1.22 대비: **§4.21 신설(S19 — F45 응용 모의고사, 계약 확정 2026-08-02 — stage-19 착수 전 결정 ①~⑦ 해소분. 문서만·코드 0)**: ① **결정 확정 7건** — 저장 = **격리 저장 확정**(documents type='question' + 예약 루트 "AI 응용 모의고사"(settings `applied_exam.root_category_id` 포인터) 아래 **런 단위 분류**에만 연결, `linked_by='applied_exam'` — 불변 규칙 2 정합: 오답노트·SM-2가 참조할 실문서) · `attempts.mode` = **'applied_exam' 신설**(exam/history 런 키 혼입 방지) · 표기 = **F44 마커 관례 상속**(content 서두 고정 마커, 접두 `[AI 생성 문항]` — 무-DDL) · 근거 연결 = **`document_relations('derived_from','applied_exam')`**(F43 'embeds' 전례 — 값 확장만) · 부분 실패 = **부분 성공 저장·부분 응시 허용**(통과 문항만 잡 말미 한 트랜잭션, 0건 = 잡 실패·저장 0, 응시는 저장 완료 후에만) · 비용 = **prepare(LLM 0) estimate + 확인 스텝**(F35·F44 관례, 컨텍스트 200,000자 상한 재사용) · 품질 게이트 = **§4.17 검증 재사용 + basis doc_no 서버 결정론 검증 + 복제 검출(SourceMatcher 역적용 — "원본에 있으면 실패") + 자기 검증(2차 LLM) 불채택**(방어선은 사후 검토 이동 — R22) ② **신규 엔드포인트 5개**(`POST /api/applied-exam/prepare`(LLM 0)→`…/generate`(잡 kind 'applied_exam')→`GET …/{gen_id}`→`…/submit`(채점 코어 공유·mode·basis 리포트)→`…/history`) — **응시 구성은 기존 `exam/session` 재사용**(응시 응답에 정답·해설·basis 스키마 수준 부재 — 계약 봉인 그대로) ③ 격리 규약(일반 퀴즈·F25 = 분류 격리로 자연 제외 · SRS 유입은 응용 오답 경유뿐(= 격리 저장의 목적) · 탐색·검색은 표기 상시로 노출 유지 · 태그 미부여로 규칙 자동 연결 차단) ④ 생성 문항 = 전 문항 객관식 고정(생성 단답 채점 취약 — R22). **DDL 0건·Alembic 0건 재확인(§4.21 말미)** — §6.2는 mode·linked_by·relation·created_by 주석 갱신만.)
> (v1.22: v1.21 대비: **§4.20 신설(S18 — F44 해설·정답 보완 플로우, 계약 확정 2026-08-02 — stage-18 착수 전 결정 ①~④ 해소분. 문서만·코드 0)**: ① **결정 확정 4건** — 선택 시점·단위 = **반입 후 경로만**(답지 반입 = 분류 범위 단위 일괄 · 풀이 생성 = 문항 단위, 미리보기 단계 확장 기각 — YAGNI) · 매칭 키 = **분류 범위 + `source_detail` 말미 문항 번호(`(\d+)\s*번` 최말단, 서버 결정론 매칭 — LLM 매칭 판단 불사용, 모호·부재 = "매칭 안 됨" 표시·제외)** · 생성분 반입 후 표기 = **explanation 서두 고정 마커 라인(`[AI 생성 해설` 접두 — 무-DDL, F45 결정 ③이 상속)** · 비용 = 기존 사용량 안내 관례 재사용(fetch `estimate` 필드 + 확인 스텝 / 풀이는 확인 다이얼로그) ② **답지 반입 신규 엔드포인트 4개**(`POST /api/import/answer-key`(판별·추출만 — LLM 0) → `…/process`(잡, kind 'answer_key') → `GET …/{key_id}`(미리보기 — matched/unmatched·`fabrication_suspect` 답지 텍스트 기준 원문 대조 §4.17 ⑥ 재사용) → `…/apply`(**빈 필드만 병합·유일한 쓰기·멱등**)) ③ **풀이 생성 신규 엔드포인트 3개**(`POST /api/documents/{id}/explain`(explanation 부재 문항만 — 있으면 409) → `GET` → `apply`(마커 서버 부착) — F30 재생성 잡 인프라 재사용, `explanation_source:"generated"` 응답 전용) ④ 퀴즈 진행·시험 응시 화면에 explain 진입점 금지(정답 우회 방지 — 불변 규칙 1의 정신). **DDL 0건·Alembic 0건 재확인(§4.20 말미)** — 저장 지점은 기존 컬럼 UPDATE·인메모리·sources/뿐, 신규 엔드포인트 7개.)
> (v1.21: v1.20 대비: **§4.19 신설(S17 — F43 문서 상호참조·표현 UX, 계약 확정 2026-08-02 — stage-17 선행 절차 D1~D3 해소분. 문서만·코드 0)**: ① **레퍼런스 문법 확정** — `![[DOC-xxxx]]` 임베드 / `[[DOC-xxxx]]` 링크 칩 / `[[#절 제목]]` 헤딩 앵커, **표시명 별칭(`|별칭`) 지원**, doc_no 정규식 `DOC-\d{4,}`(생성 로직 `DOC-{n:04d}` 실측) ② **가리기·접기 = remark-directive 채택**(`:::fold[제목]`·`:::hide[제목]` — rehype-raw 기각 확정) ③ **임베드 해석 신규 엔드포인트 1개** `POST /api/documents/resolve-embeds`(배치 상한 50, 항목 `{doc_no, found, title, type, content, is_active}` — **answer·explanation은 스키마에 부재**, 부재 doc_no = `found:false` 부분 성공, 삭제 문서 = content null) ④ **깊이 상한 2 확정** + 자리표시자 4종(순환/삭제/부재/깊이 초과) 확정 문구 ⑤ 헤딩 슬러그 = **github-slugger 계열(rehype-slug)** ⑥ embeds 인덱스 동기화(documents POST/PATCH·반입 commit, 같은 트랜잭션 — 임베드만 인덱스·링크 칩 제외) + 전량 재계산 멱등 함수 1개(R20) ⑦ 삭제 경고·사용처 = **기존 DocumentDetail.relations 재사용(신규 API 0)** ⑧ 인쇄 뷰 = 임베드 펼침·fold/hide 전부 공개 ⑨ DocEditor 삽입 도우미 최소안. **DDL 0건·Alembic 0건 재확인(§4.19 말미)** — 프론트 의존 2건(remark-directive·rehype-slug)뿐.
> (v1.20: v1.19 대비: **§4.18 ②-1·⑥ 보강(S16 재검토 통과(DoD 6/6) 시 지적된 문서-코드 불일치 1건 해소 — 2026-07-29, 문서만)**: ① **암호화 OOXML 판별 세칙 명문화** — OLE CFB 매직 + 확장자 `.docx`/`.xlsx` = **ECMA-376 암호화 OOXML로 판정 → C군 `unsupported_format`이 아니라 `parse_failed`**(④의 암호 파일 확정을 실현하는 세칙 — 확정 문구·원본 sources/ 저장 포함, ⑥ 표 비적용 주석 추가) ② **hwpx 휴리스틱 알려진 한계 기록** — zip 내부 `mimetype == application/hwp+zip` 또는 최상위 `Contents/` 존재로 판정(구현 확정), `Contents/`만으로는 일반 zip이 hwp 문구로 오안내될 수 있음(C군 거부 자체는 동일 — 강화 여부는 후속 결정). 계약 형태·DDL 증감 0.)
> (v1.19: v1.18 대비: **§4.18 ④·⑥ 보강 + §4.11 주석 (S16 구현의 Opus 검토 적발 2건 해소 — 2026-07-29, 문서 확정·코드 지시는 별도)**: ① **`parse_failed` 종료 시에도 원본을 `sources/`에 저장 후 종료**(`unsupported_format`과 대칭 — 손상·암호 파일도 사용자의 원본 자료이고 잡 tmp는 정리되므로 저장하지 않으면 서버에 남지 않는다. stage-16 DoD 2 문면과 정합 — 종전 §4.18 ⑥이 저장을 unsupported 한정으로 읽히게 한 어긋남 해소) ② **convert 잡(파일·URL 반입)에서 발생한 `unsupported_format`·`parse_failed`의 `alternatives` = 빈 배열 확정**(실패한 반입 경로 자신([파일로 반입] → 같은 화면 회귀)이나 같은 판별·파서에 다시 걸릴 경로는 대안이 아니다 — **fetch 잡 발생분은 기존 기본값 유지**, §4.11 계약 불변). DDL·Alembic·엔드포인트 증감 0.)
> (v1.18: v1.17 대비: **§4.18 신설(S16 — F42 다양한 문서 포맷 반입, 계약 확정 2026-07-29 — stage-16 선행 절차 D1·D2·D3 해소분. 문서만·코드 0)**: ① 지원 포맷 매트릭스(A군 md·txt·html·xml·**csv** 직투입 / B군 docx·xlsx 서버 추출 / C군 hwp·zip·doc·xls·판별 불가 = 구조화 거부) ② 판별 규칙(**매직 바이트 우선** — zip 내부 판별(docx/xlsx/hwpx도 zip)·OLE 시그니처·텍스트성 검사, content-type·확장자 사칭 방어) ③ 인코딩 판별 확정(utf-8 BOM→utf-8→cp949, chardet류 불도입 — **cp949는 CLI 전달 시 utf-8 재인코딩**) ④ 파서 채택(docx=**python-docx**·xlsx=**openpyxl**, `==` 정확 핀) + xlsx **시트별 Markdown 표**(시트 10·행 500·열 50 상한) + 추출기 `services/doc_extract.py` 분리(원문 대조 §4.17 ⑥ 재사용) + 파서 예외 = **`parse_failed` 재사용 확정** ⑤ **`error_info.kind:'too_large'` 신설**(추출·디코드 텍스트 **200,000자** 상한 — LLM 호출 전 비용 0 차단, 분할 권고 — 서버측 자동 분할 금지) ⑥ `unsupported_format` 포맷별 폴백 **확정 문안**(hwp→PDF 저장·zip→압축 해제·doc/xls→docx/xlsx 저장) ⑦ URL content-type 화이트리스트 확장(xml 2종·text/csv·officedocument 2종 — `application/octet-stream`은 미허용 유지). **DDL 변경 0건·Alembic 0건·신규 엔드포인트 0건 재확인(§4.18 말미 근거).**)
> (v1.17: v1.16 대비: **§4.17 신설(S15 — F41 멀티 벤더 LLM 엔진, 계약 확정 2026-07-28 — 착수 게이트 G3 해소. 구현은 게이트 G2 통과 후)**: ① **엔진 레지스트리** — 엔진 id 3종(`claude-cli`·`claude-api`·`codex-cli`)·항목 인터페이스(진단/호출/분류기/한도·헬스 키)·legacy `cli|api` 별칭의 **읽기 시 매핑**(마이그레이션 없음) ② `GET /api/llm/status`를 **엔진 배열로 교체**(기존 `cli`/`api` 톱레벨 필드 **제거 확정** — 소비처 전수 확인 근거는 §4.17 ②) ③ **폴백 = 우선순위 목록**(`llm.priority` 배열화, `error_info.fallback_engine` 추가) ④ codex-cli 어댑터·설치 계약(신규 엔드포인트 **`POST /api/llm/engines/{id}/install` 1개**) ⑤ **변환 신뢰 게이트(전 엔진 공통, R7 보강)** — 계획서 §8.2 v1.1 개정 연동(`answer_source` 필수·순수 JSON 위반=오류·`content` 필수·객관식 `answer` 번호만) + preview `warnings` 필드 ⑥ **원문 대조 검사 알고리즘 확정**(정규화·12자 조각 커버리지 ≥0.6·대조 불가 판정) ⑦ S8 화면(§5.11 그룹 ④) 확장 — 카드 목록 렌더·▲▼ 우선순위·Codex 온보딩·프라이버시 고지. **DDL 변경 0건·Alembic 0건 재확인(§4.17 말미 근거).**)
> (v1.16: v1.15 대비: **S14(큐넷 공식 오픈API) 구현 완료(2026-07-27)에 따른 계약 확정 반영 — 엔드포인트 증감 0건, 기존 응답에 필드 추가만**: ① `POST /api/fetch/exams` **요청 `include_notices?`(기본 false)** + **응답 항목 `is_notice`** — 안내문 게시물 기본 숨김 + "안내문 N건 보기" 토글(같은 24h 캐시에서 파생 — **추가 API 호출 0건**) ② `GET /api/convert/{job_id}` 응답에 **`notes: string[]`(기본 `[]`)** — 성공 소표기(예: ZIP 동시 저장). **문구는 서버가 완성해 내려준다**(프론트 포맷 분기 금지) ③ `error_info.kind`에 **`unsupported_format` 확정** + `alternatives`에 **`'file_import'`** 값 추가 ④ `GET /api/fetch/adapters` qnet 항목에 `key_registered`·`key_suffix?`(예고대로 구현) ⑤ **`exam_key` 3형태 확정** — `YYYY-MM-DD`(폴더 파생 O) · **`YYYY`(연도만 — 폴더 파생 X, 회차를 창작하지 않음)** · `qnet-{artlSeq}`(식별 불가). 뒤 두 형태는 **`imported=false` 고정**·분류 경로 미생성 ⑥ **§3에 상류 실패 규약 명문화**(외부 공식 API 거절 = **HTTP 502 + code `INTERNAL`**, 코드 집합은 4종 유지 — 판단 근거는 §3). **DDL 변경 없음 · Alembic 0건.**)
> (v1.15: v1.14 대비: **S13 구현 완료(2026-07-27)에 따른 확정 사항 반영 — 계약 변경 없음, 명세 보강만**: ① §5.9 대기열이 **`previewId`를 localStorage에 함께 영속**(잡 레코드가 만료·404가 된 뒤에도 F40-① 디스크 복구로 검토를 이어가기 위함 — 검토 단계의 1차 키는 preview_id) ② **URL 반입을 파일 대기열로 통합**(폴링·재시도·재개 코드 이원화 방지 — 탭 4종·Stepper 3단계는 불변) ③ **10개 상한 = "대기열 잔여 + 신규 선택" 합계 기준** ④ **처리 중인 1건은 취소·건너뛰기 불가**를 알려진 한계로 명시(서버에 잡 취소 API 없음 — 신설하지 않음) ⑤ **[변환 JSON 내려받기]는 보존본이 있는 경로(convert·fetch 잡 preview)에만 노출**(직접 업로드 preview는 404 — §4.3). **DDL 변경 없음 · 엔드포인트 증감 없음.**)
> (v1.14: **단계 재편(계획서 v0.17 — 큐넷 실측에 따른 우선순위 변경)**: ① 큐넷 오픈API 계약(서비스키 `fetch/qnet-key`·목록/상세·`unsupported_format`·설정 카드)의 단계 태그를 **S13 → S14**로 이관(**내용 삭제 없음** — 계약은 그대로, 착수 시점만 뒤로). 사설 어댑터 제거·단일 어댑터화는 **S13 유지**. ② **F40 수동 반입 UX 계약 신설(S13)**: §4.3에 **변환 결과 디스크 보존·복구**(`GET /api/import/preview/{id}` 캐시 미스 시 복구) + **`GET /api/import/preview/{id}/json`(내려받기, 신규 1개)**, §4.11 `POST /api/convert`에 **`category_path?`**(분류 경로 제안 고정 — 사이트 반입 지시 생성기 공유) + `error_info.kind`에 **`'invalid_output'`**(출력 잘림 오안내 수정), §5.9에 **반입 대기열**(여러 파일 연속 — 새 API 0건, 서버 동시 1개 유지)·분류 경로 입력·복구 표시. **DDL 변경 없음.**)
> (v1.13: **사이트 어댑터 단일화(S13 — 사설 어댑터 comcbt·cbtbank 제거, 계획서 v0.16 §14 F35-2 제거 이력)**: §4.13에서 어댑터 3종 목록·우선순위 병합(qnet>cbtbank>comcbt)·날짜 자연 키 병합·`also_on`/`refs` 대안 어댑터 재시도·level_hint 오병합 방지 조건을 **큐넷 단일 어댑터 기준으로 정리**. **계약 형태는 유지**(`GET /api/fetch/adapters`는 계속 **배열** 반환, `POST /api/fetch/exams` 항목의 `also_on`(항상 `[]`)·`refs`(단일 항목)·`exam_key?`도 필드 유지) — 프론트 변경은 어댑터 id 유니온 축소(`'qnet'`)와 이름 폴백 맵 정리뿐. §5.9 대안 어댑터 재시도 버튼은 사문화(빈 `also_on`이라 미렌더). DDL 변경 없음.)
> (v1.12: **S13(M13 큐넷 공식 오픈API) 계약**(§4.13 S13 갱신 — qnet 어댑터를 공공데이터포털 **"국가자격 공개문제 조회 서비스"**(getOpenQstList/getOpenQst)로 실가동: 서비스키 등록 `POST/DELETE /api/fetch/qnet-key`(secrets.json — F34 전례), `fileUrl` JWT 1시간 → **상세 조회·다운로드 같은 잡 연속 수행**, HWP 전용 회차 = 원본 저장 + `error_info.kind:'unsupported_format'` 신설, 병합 그룹 level_hint 동일 조건 명시. **기존 fetch 계약(adapters/certs/exams/import)·프론트 스텝 흐름 불변**) + §5.11 데이터 그룹 서비스키 카드·§5.9 실패 렌더 1종 추가)
> 이전 이력: v1.11 — S12 계약(§4.13 cbtbank FetchedExam 첫 실사용·날짜 자연 키 병합·`fetch/import` `exam_key?`, §4.15 `GET /manual`) · v1.10 — S11 계약 신설(§4.14 F25·F16) · v1.9 — S10 구현 실측 반영(comcbt PDF 경로, qnet available:false 스텁)
> 작성일: 2026-07-22 · 갱신: 2026-08-04
> 상위 문서: `docs/01-plan/study-app.plan.md` (Draft v0.35)
> 구현 계획: `docs/01-plan/stage-1-skeleton.plan.md` ~ `stage-13-fetch-cleanup-manual-import.plan.md`(S13) · `stage-14-qnet-openapi.plan.md`(S14) · `stage-15-multi-engine-codex.plan.md`(S15 — §4.17) · `stage-16-doc-formats.plan.md`(S16 — 착수 2026-07-29, §4.18) · `stage-17-doc-transclusion.plan.md`(S17 — 계약 확정 2026-08-02, §4.19) · `stage-18-answer-explanation.plan.md`(S18 — 계약 확정 2026-08-02, §4.20) · `stage-19-applied-exam.plan.md`(S19 — 계약 확정 2026-08-02, §4.21) · `stage-20-import-self-improve.plan.md`(S20 — 계약 확정 2026-08-02, §4.22) · `stage-21-engine-controls.plan.md`(S21 — 계약 확정 2026-08-03, §4.23) · `stage-22-llm-job-center.plan.md`(S22 — 계약 확정 2026-08-03, §4.24) · `stage-23-llm-split-import.plan.md`(S23 — 계약 확정 2026-08-04, §4.25) · `stage-24-applied-exam-mode.plan.md`(S24 — 계약 확정 2026-08-04, §4.21 S24 개정 블록) · `stage-25-explanation-display.plan.md`(S25 — 계약 확정 2026-08-04, screens §5.3 · **착수 순서 최우선**)

---

## 1. 범위

계획서의 데이터 모델(§6)을 전제로, **REST API 명세**와 **화면 상세**를 확정한다.
스키마 DDL은 계획서 §6.2가 단일 출처(source of truth)이며 여기서 반복하지 않는다.

## 2. 프로젝트 구조

```
study-hub/
├─ backend/
│  ├─ main.py               # FastAPI 앱 생성, 라우터 등록, 정적 파일(frontend/dist) 서빙, GET /manual(S12 — §4.15)
│  ├─ database.py           # engine(SQLite WAL), SessionLocal, get_db
│  ├─ models.py             # SQLAlchemy 모델 (계획서 §6.2 그대로)
│  ├─ schemas/              # Pydantic 요청/응답 모델 (리소스별 파일)
│  ├─ routers/              # categories, documents, imports, study, quiz, srs,
│  │                        # review_notes, stats, search, tags, suggestions, settings, exam(S11)
│  ├─ services/             # sm2.py, import_service.py, stats_service.py,
│  │                        # tag_rule_service.py, convert_service.py(M6), backup_service.py(M6),
│  │                        # fetchers/(S10 — base·registry·qnet, §4.13. S13: 사설 어댑터 comcbt·cbtbank 제거), exam_service.py(S11 — §4.14)
│  └─ alembic/              # 마이그레이션
├─ frontend/
│  └─ src/
│     ├─ api/               # client.ts(fetch 래퍼), 리소스별 React Query 훅
│     ├─ components/        # 공용: Tree, DocCard, MarkdownView, ProgressBar, TagChip, …
│     ├─ pages/             # 화면 12개 (§5)
│     ├─ stores/            # zustand: quizSession, flashcardSession, examSession(S11), theme, sidebar(S7)
│     ├─ styles/tokens.css  # 디자인 토큰 (§6)
│     └─ App.tsx            # React Router 라우트
├─ sources/                 # 원본 파일 (불변)
├─ import/                  # 반입 JSON — 최상위: 사람이 넣은 파일(Claude Code 산출물 등)
│  └─ auto/                 # S13(F40-①): 앱이 변환한 반입 JSON 자동 보존(최근 50건, git·백업 제외 — §4.3)
├─ prompts/convert.md       # LLM 변환 프롬프트 템플릿
└─ study.db
```

## 3. API 공통 규약

- Base URL: `/api` — JSON, UTF-8. 프론트는 같은 origin에서 서빙되므로 CORS 불필요.
- **에러 포맷** (모든 4xx/5xx):
  ```json
  { "error": { "code": "NOT_FOUND", "message": "문서를 찾을 수 없습니다", "detail": null } }
  ```
  코드: `VALIDATION_ERROR`(422) · `NOT_FOUND`(404) · `CONFLICT`(409) · `INTERNAL`(500)
- **상류(외부 공식 API) 실패 규약 — S14 확정(2026-07-27)**: 큐넷 오픈API 같은 **외부 서비스가 요청을 거절**한 경우(쿼터 초과·서비스키 오류·토큰 만료 등)는 **HTTP `502` + `code:"INTERNAL"`** 로 응답한다.
  - **코드 집합은 위 4종에서 늘리지 않는다.** 괄호 안 상태 코드는 각 코드의 *기본값*이며, `INTERNAL`만 예외적으로 **500(우리 쪽 결함) / 502(상대편 사정)** 두 상태를 쓴다.
  - **이렇게 정한 이유**(구현 확정 사항의 사후 검토 결과 — 그대로 채택): ① 이 실패를 **"검색 결과 0건"으로 위장시키지 않는 것**이 목적인데(§4.13 S14 — 0건은 "이 종목의 공개문제가 없습니다"라는 **정상 안내**라 뜻이 정반대다), 그 구분은 상태 코드 하나로 충분히 달성된다. ② 프론트는 **`code`로 분기하지 않는다** — `client.ts`가 `code`를 열린 문자열로 두고 화면은 서버가 준 **사람 말 `message`를 그대로 렌더**하므로 새 코드를 만들어도 소비자가 없다(YAGNI). ③ 원인 구분자가 필요하면 이미 `detail.reason`(`quota`|`key`|`token`|`no_key`|`other` — 서버 내부 분류)이 실려 온다.
  - `message`는 항상 **한국어 사람 말 + 다음 행동**이며, **원문 XML/JSON·서비스키 원문은 어떤 필드에도 담지 않는다**(§4.11 원칙 동일).
  - **후속 조건(기록만 — 지금 만들지 않는다)**: `code` 값으로 분기해야 하는 소비자(예: 자동 재시도 클라이언트)가 실제로 생기면, 그때 `UPSTREAM_ERROR`(502)를 계획서에 먼저 확정한 뒤 신설한다. 현재는 코드 신설이 **프론트 유니온 타입 수정만 유발하고 얻는 것이 없다.**
- **페이지네이션**: `?page=1&size=50` → `{ "items": [...], "total": 231, "page": 1, "size": 50 }`
- **날짜**: ISO 8601, 서버 로컬(Asia/Seoul) 기준. DATE는 `YYYY-MM-DD`.
- **소프트 삭제**: 삭제된 문서(`is_active=0`)는 모든 목록에서 기본 제외, `?include_inactive=1`로 노출.
- ID는 정수 PK. `doc_no`(DOC-0001)는 표시·반입 참조용.

## 파일 분할 지도 (2026-07-31)

| 파일 | 내용 |
|---|---|
| (이 파일) | §1 범위 · §2 프로젝트 구조 · §3 API 공통 규약 · §8 비고 — 모든 구현 에이전트 공통 선행 지식 |
| `study-app.design.api.md` | §4 API 명세(4.1~4.25, [S1]~[S25] 단계 태그) — 백엔드 구현 시 해당 절만 부분 읽기 |
| `study-app.design.screens.md` | §5 화면 상세(13개 + 전역 패널 §5.14 + 분할 반입 위저드 §5.15 — 5.13 제안함은 S20, 5.14 작업 센터는 S22, 5.15 분할 반입 위저드는 S23 신설) · §6 테마 토큰 · §7 상태 관리 — 프론트 구현 시 해당 절만 부분 읽기 |

## 8. 비고

- 서버 채점 원칙: 정답·해설은 `quiz/session`·`exam/session` 응답에 포함하지 않는다 (풀기 전 노출 방지, 기록 무결성).
- attempts 저장과 SM-2 갱신·오답노트 생성은 하나의 트랜잭션 (모의고사 일괄 제출은 **배치 전체가 한 트랜잭션** — §4.14).
- 이 문서와 실제 구현의 갭은 각 stage 완료 시 `/pdca analyze`로 점검.
