# Stage 16 — 다양한 문서 포맷 반입 (M16: F42)

> 상위: `study-app.plan.md` **v0.24** §14(M16)·**F42**(실측 현황·포맷 3군 전략·라이브러리 방침·착수 전 결정·구현 확정 사항의 단일 출처) · 설계: **§4.18(계약 확정 2026-07-29 — 설계 v1.18 신설, v1.19에서 ④·⑥ 보강: parse_failed 원본 저장·convert 잡 alternatives 빈 배열, v1.20에서 ②-1·⑥ 세칙: 암호화 OOXML = parse_failed·hwpx 휴리스틱 한계. 매트릭스·판별 규칙·폴백 문안·상한의 상세 단일 출처)**
> 배경: 필기 기출의 자동 수집 경로가 없음이 확정(M13·M14)된 뒤 **수동 반입이 정본 경로**가 됐다(F40). 사용자가 가진 자료는 PDF만이 아니다 — docx·xml·md·txt·html·xlsx 파일이 반입 경로에서 **정식으로**(조용한 실패 없이) 처리돼야 한다.
> **상태: 구현 완료(2026-07-29)** — 선행 절차 D1·D2·D3(설계 v1.18 §4.18 신설 · 계획서 v0.23 F42 확정) → 구현 → stage-reviewer(Opus) 1차 조건부(중요 4건) → 수정 → **재검토 통과(DoD 6/6)**. 전체 테스트 281 passed. 잔여 = 경미 4건(hwpx 휴리스틱 오탐 여지·sources.file_type 확장자 파생·docx 병합 셀 표 열 불일치 — 아래 참조) + DoD 1의 실 LLM 라이브 완주는 사용자 실사용 확인 항목.
> 순서 관계(plan §14): **M15(stage-15)와 동시 착수 금지**(둘 다 `convert_service`·반입 경로 수정 — 먼저 시작한 쪽 종료 후). M17과는 파일 충돌 없음.
> 불변 규칙 재확인: `sources/` 원본 불변(4 — 추출 텍스트는 저장하지 않는 파생물) · **미리보기 승인 없는 자동 반입 금지(R7)** · 실행 전 LLM 사용량 사전 안내(F35 원칙) · 오류 원문 노출 금지(`error_info` 구조화) · 에러 포맷·페이지네이션 §3 규약.

## 현행 실측 (2026-07-28 — 계획서 §14 F42 "실측 현황"이 정본, 여기는 요지)

- 파일 반입(`POST /api/convert`)에 **확장자 화이트리스트가 없다** — 어떤 파일이든 잡에 들어간다.
- 실효 지원은 엔진별: CLI = Claude Code Read 도구(텍스트·PDF·이미지) / API = PDF(base64 document 블록)·이미지 블록·**그 외 전부 utf-8 강제 디코드(`errors="replace"`) 본문 삽입**.
- 따라서 **docx/xlsx(zip 바이너리)는 mojibake로 조용히 LLM에 투입**된다 — 구조화 오류 없이 비용 지출 + 쓰레기/창작 산출 위험. **이 결함의 수정이 이 단계의 중심이다.**
- URL 반입 content-type 화이트리스트: pdf·html/xhtml·markdown·text/plain·이미지 4종 — xml·office 계열 MIME은 거부.
- 서버 파싱 라이브러리 0개(requirements 실측).

## 착수 시 선행 절차 (게이트 아님 — 결정·문서를 먼저 굳힌다)

- [x] **D1. 파서 라이브러리 최종 채택** — **✅ 2026-07-29 확정: docx = python-docx**(lxml C 확장 의존 수용 — 주요 플랫폼 wheel 배포로 설치 무해, 표 추출 API 보유. docx2txt 기각: 표 추출 불완전·유지보수 정체) · **xlsx = openpyxl**. requirements `==` 정확 핀(pypdf 전례 — 구체 버전은 구현 시 설치 검증 후 기록). 계획서 F42 등재 완료.
- [x] **D2. 세부 결정 5건** — **✅ 2026-07-29 전건 확정(계획서 F42 ②~⑥·설계 §4.18)**: ② 인코딩 utf-8 BOM→utf-8→cp949(chardet류 불도입 확정 — cp949는 CLI 전달 시 utf-8 재인코딩) ③ xlsx **시트별 Markdown 표**·시트 10/행 500/열 50(초과 = 부분 잘림 없이 실행 전 중단) ④ **csv 포함**(A군 직투입) ⑤ URL content-type **5종 추가**(xml 2종·text/csv·officedocument 2종 — octet-stream 미허용 유지) ⑥ 추출 텍스트 상한 **200,000자** + **`error_info.kind:'too_large'` 신설**(LLM 호출 전 차단·분할 권고 — 서버측 자동 분할 금지).
- [x] **D3(문서). 설계 §4.18 신설** — **✅ 2026-07-29(설계 v1.18)**: 지원 포맷 매트릭스(포맷×경로×엔진 3종), 판별 규칙(**매직 바이트 우선** — zip 내부 판별·OLE 시그니처·텍스트성 검사·사칭 방어), `unsupported_format` 포맷별 **확정 문안**(서버 완성 문장), 파서 예외 = **`parse_failed` 재사용 확정**(판별 확정 후 추출 실패 한정 — 경계 규칙 포함), B군 추출기 `services/doc_extract.py` 분리(원문 대조 §4.17 ⑥ 재사용). **DDL 0건·Alembic 0건·신규 엔드포인트 0건 재확인 완료**(§4.18 말미 — file_type 값 확장은 §6.2 주석 갱신만).

## 목표

끝났을 때, docx·xlsx·xml·md·txt·html·csv(D2-④ 포함 확정) 파일이 파일/URL 반입에서 **미리보기까지 완주**하거나, 지원 불가 파일이 **구조화 오류 + 폴백 안내**로 끝난다. 어떤 경로로도 "바이너리 mojibake가 조용히 LLM에 투입돼 비용만 쓰는" 일이 없다.

## 작업 체크리스트

> 권장 순서: **1(판별·거부 — 결함 수정) → 2(A군 정식화) → 3(B군 추출) → 4(URL·프론트) → 5(테스트) → 6(문서·매뉴얼)**.
> 1이 끝난 시점에 기존 지원 포맷(PDF·이미지·md/txt)이 회귀 없이 동작해야 한다(판별 계층 단독 배포 가능 상태).

### 1. 포맷 판별·거부 계층 (mojibake 조용 투입 차단)

- [x] `convert_service`에 반입 파일 판별 단계 신설: **매직 바이트 우선**(zip `PK\x03\x04` + 내부 판별(docx/xlsx/hwpx)·OLE `D0CF11E0…`) + 텍스트성 검사(널 바이트) + 인코딩 판별 — 규칙은 §4.18 ② 확정안. 판별 결과를 잡에 기록(`phase='preparing'` 구간)
- [x] 판별 불가 바이너리·미지원 포맷(hwp·zip·doc·xls 등) → `error_info.kind:'unsupported_format'` + **포맷별 폴백 안내**(서버가 완성 문장 — "한글에서 PDF로 변환 후 반입", "압축을 풀어 PDF/이미지를 파일 반입" 등 F35-3 관례). 원본은 sources/에 저장된 뒤 종료(조용한 스킵 금지 — qnet과 동일 정책)
- [x] API 엔진의 무조건 utf-8 디코드 폴백(`_api_content_blocks_for_file`)을 판별 계층 통과분에만 적용 — **판별을 거치지 않은 바이너리가 텍스트로 흘러가는 경로 0**
- [x] 회귀 확인: PDF·이미지·기존 텍스트 파일 반입이 판별 계층 추가 후에도 동일 동작 (스모크)

### 2. A군 — 텍스트 계열 정식화 (md·txt·html·xml)

- [x] 인코딩 판별(**D2-② 확정: utf-8 BOM→utf-8→cp949, 전부 실패 = 판별 불가·chardet류 불도입**) 후 텍스트 투입 — cp949 파일이 mojibake 없이 변환되는지 확인
- [x] html은 원문 그대로 투입(서버측 태그 제거 파서 추가하지 않음 — LLM이 처리, 기존 URL 반입 경로와 동일). xml도 원문 투입
- [x] CLI 엔진 경로: utf-8 계열 텍스트 파일은 기존대로 파일 경로 전달(Claude Code Read — 변경 최소화), **cp949 판별 파일은 tmp를 utf-8로 재인코딩해 전달**(§4.18 ③ — Read가 utf-8 전제라 경로 전달만으로는 DoD 3 불성립. sources/ 원본은 원 바이트 불변). API 엔진 경로는 판별된 인코딩으로 디코드해 본문 삽입
- [x] 추출·디코드된 텍스트 길이가 상한(**D2-⑥ 확정: 200,000자**) 초과 시 **`error_info.kind:'too_large'`로 실행 전 종료**(분할 권고 문구 — LLM 비용 지출 전에 멈춘다, §4.18 ⑤)

### 3. B군 — 서버측 텍스트 추출 (docx·xlsx)

- [x] D1 채택 파서(**python-docx·openpyxl**)를 requirements에 추가(`==` 정확 핀) — **포맷당 1개 원칙**. 추출 함수는 신규 **`services/doc_extract.py`**로 분리하고 `source_match.extract_source_text`가 재사용(§4.18 ④ — M15 원문 대조 포맷 확대)
- [x] docx: 문단·표(Markdown 표) 텍스트 추출 → A군과 같은 텍스트 투입 형태로 정규화. 수식(OMML)·이미지 소실은 한계로 수용 — **소실 감지 시 잡 `notes` 소표기 1건으로 확정**(§4.18 ④ 문안)
- [x] xlsx: 시트별 **Markdown 표** 직렬화(D2-③ 확정) — 시트 10·행 500·열 50 상한 초과 시 **부분 잘림 없이 `too_large`로 실행 전 중단**(초과 지점을 message에 명시 — §4.18 ④·⑤)
- [x] 파서 예외(암호 파일·손상 파일·구버전 포맷 위장) → `error_info` 구조화(**`parse_failed` 재사용 확정** — 판별이 docx/xlsx로 확정된 뒤의 추출 실패 한정, 판별 자체 미지원은 `unsupported_format` — §4.18 ④ 경계 규칙) + 폴백 안내. **`parse_failed`도 원본을 sources/에 저장한 뒤 종료**(unsupported와 대칭 — 2026-07-29 확정, §4.18 ④·DoD 2). **파서 예외가 잡 크래시·조용한 실패가 되는 경로 0**
- [x] 추출 텍스트는 저장하지 않는다(임시 파일만 — sources/에는 원본 바이트, R18 정신). LLM 사용량 사전 안내는 추출 텍스트 길이 기준으로 정확화

### 4. URL 반입·프론트

- [x] URL content-type 화이트리스트 확장(**D2-⑤ 확정 5종**: `application/xml`·`text/xml`·`text/csv`·officedocument 2종 — octet-stream 미허용 유지, 거부 문구 갱신 — §4.18 ⑦) + 다운로드 후에도 1절 판별 계층 통과(content-type 사칭 방어 — 매직 바이트 우선)
- [x] 반입 화면: 파일 선택 input에 지원 확장자 `accept` 힌트 + 안내 문구 갱신("PDF·이미지 등" → 지원 포맷 명시). 대기열(F40-②)·분류 경로 지정(F40-③)과의 조합 동작 확인
- [x] `unsupported_format`·`parse_failed` 렌더가 포맷별 폴백 문구를 그대로 나열하는지 확인(프론트 포맷 분기 금지 — 서버 완성 문장 관례). **convert 잡 발생분은 `alternatives` 빈 배열 — 대안 버튼이 뜨지 않아야 정상**(같은 화면 회귀 버튼 금지 — 2026-07-29 확정, §4.18 ⑥. fetch 잡은 기존 기본 유지)
- [x] 색상 하드코딩 금지(토큰만)

### 5. 테스트·검증

- [x] `services/sm2.py` 외 실행 스모크 원칙(불변 규칙 7) — 단 **판별·추출 계층은 단위 테스트 작성**(mojibake 차단·파서 예외 구조화는 반입 정확성 직결 — qnet `_magic_ok`·`_unsupported_message` 테스트 전례)
- [x] 포맷별 스모크: md/txt(cp949 포함)/html/xml/csv/docx/xlsx 각 1건 미리보기 도달 + hwp/zip/손상 docx 각 1건 구조화 거부 확인 — LLM 실호출은 최소화(텍스트 판별·추출까지는 LLM 없이 검증 가능)
- [x] stage-reviewer(Opus) 검토: DoD + "판별 미통과 바이너리의 텍스트 투입 경로 0건" 코드 검색

### 6. 문서

- [x] 계획서 F42에 구현 확정 사항 기록(채택 파서·상한값·확정 문구) · 설계 §4.18 확정본 반영 · R19 갱신
- [x] 사용자 매뉴얼(F39) 반입 섹션에 지원 포맷 표·폴백 안내 추가
- [x] 이 문서 체크박스 갱신(불변 규칙 10) · CLAUDE.md 문서 지도 범위 표기 확인

## DoD (완료 정의)

1. docx·xlsx·xml·md·txt·html·csv 파일이 파일 반입에서 **미리보기까지 완주**한다(각 포맷 실파일 1건 이상 라이브 확인).
2. **바이너리 mojibake가 LLM에 투입되는 경로 0** — hwp·zip·손상 파일은 원본 저장 + `unsupported_format`/`parse_failed` 구조화 오류 + 폴백 안내로 끝난다.
3. cp949 텍스트 파일이 깨지지 않고 변환된다.
4. 기존 지원 경로(PDF·이미지·URL 반입·대기열 F40) 회귀 0.
5. 신규 의존은 D1 채택 파서만(계획서 F42 등재와 일치) — 그 외 라이브러리 추가 0건.
6. DDL 변경 0건·Alembic 0건 (설계 §4.18에서 재확인 — 어긋나면 착수 중단 후 보고).

## 이 단계에서 하지 않는 것

- **HWP 파서 도입** — 순수 파이썬 성숙 파서 부재. 폴백 안내(PDF 변환 후 반입) 유지 — F35-3 결정 그대로.
- **구버전 doc·xls 지원** — 지원 목록에서 명시 제외(docx·xlsx로 변환 안내). 실수요가 확인되면 계획서에 먼저 확정.
- **서버측 PDF 파싱·분할** — F40-④ 결정 유지(PDF는 LLM 직투입). pypdf 도입은 M15(원문 대조)의 몫.
- **OCR·이미지 내 텍스트 추출** — 이미지 반입은 기존 경로(LLM 비전) 그대로.
- **추출 텍스트의 저장·색인** — 재추출 가능한 파생물은 저장하지 않는다(R18 정신).
- **변환 프롬프트의 포맷별 분기 튜닝** — convert.md 단일본 유지(stage-15 원칙과 동일).

## 리스크

- **R19(신설 — 계획서 §15)**: 파서 의존·추출 품질(수식·표 소실, 변형 파일). 대응 = 구조화 오류 + 폴백 안내 + 미리보기 승인(R7)이 최종 방어선.
- 출력 잘림(F40-④): xlsx 대용량이 텍스트로 풀리면 출력 상한 잘림이 잦아질 수 있다 — 실행 전 길이 상한 안내(2·3절)로 비용 지출 전에 차단.
- M15와의 접점: 이 단계의 추출기는 M15 원문 대조의 "원본 추출 텍스트" 소스로 재사용된다 — 인터페이스를 convert 전용으로 숨기지 말고 서비스 함수로 분리해 둔다(과설계 금지 — 분리만, 대조 로직은 M15).
