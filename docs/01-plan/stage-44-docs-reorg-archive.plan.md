# Stage 44 — 문서 체계 재편 1: v1 이력 대이동 — 아카이브·패치노트 분리 (v2.00.1 준비 · 문서 전용)

> 상태: **완료 (2026-08-31)** — 구현(Fable 직접·스크립트 이관) → 검토(Opus) 조건부 통과(치명·중요 0·경미 4 = 당일 전건 수정) → **Fable 최종 게이트 통과**(무손실 소실 0·참조 끊김 0·크기 2/3+편차 수용). **DoD 6 충족(2026-08-31 사용자: "가시성은 많이 좋아졌어")** — 잔여 = D-1 물리 삭제(사용자 1회 — git 무영향).
> (편성 시 상태 — 이력: 편성 완료 (2026-08-31 생성 — Fable 점검·계획).)
> 진척은 이 줄 + 말미 "완료 기록"이 정본.
> (생성 경위: 2026-08-31 사용자 지시 — "2.00.1 출시 전 1.x 데이터를 모듈화·정리·인덱스해서 토큰 낭비를
> 막고, 패치노트를 v1/v2로 구분하고, Fable이 직접 점검→계획→구현→검토→최종 점검할 것. 한 스테이지로는
> 어려울 수 있음." → **stage-44(이력 이동) + stage-45(인덱스·버전 체계) 2단계 분할**. 같은 날 추가 지시
> 2건 — ① 핵심/사소 스테이지 구분 버전 체계(→ stage-45 ②) ② "억제기 제거 검토 + 문서정리 집중"(→
> stage-45 ③).)
> 범위: **문서 전용 — 코드 0 · DDL 0 · API 0 · 의존 0**. 원문의 **무손실 이관**(요약·재작성 금지)과
> 참조 무결성이 이 단계의 전부다. 코드가 손대는 파일 0이므로 앱 동작 무영향.
> 실행 순서: **stage-44 → stage-45 → (Fable 최종 점검 → v2.00.1 발행) → stage-43(구 편집기 퇴역 — 핵심)**.
> stage-41~42 완료 후·stage-43 착수 전에 끼운다 (사용자 지시 — "퇴역 전에 선행").
> 진행 방식(사용자 지시 — Fable 주관): 구현 = **Fable 메인 대화 직접**(대량 이관을 에이전트에 위임할 경우에도
> 이관 블록 지정·검수는 Fable이 한다) · 검토 = `stage-reviewer`(Opus) · 최종 게이트 판정 = **Fable**.

## 점검 실측 (2026-08-31 Fable — 이 표가 이관 대상의 단일 근거)

| 파일 | 크기 | 비대 지점(실측) |
|---|---|---|
| `docs/01-plan/study-app.plan.md` | **350KB / 959줄** | ① 머리말 문서 이력 `> (v0.x: …)` 블록 **~74KB**(v0.1~v0.59, 항목당 2~5.6KB) ② §14 로드맵 표 M17~M36 행 — "압축 표"라는 선언과 달리 행당 1~3KB 서사 원문 **~48KB** ③ §14 아래 F43~F56 기능 상세(줄 640~899 — **전부 완료된 기능**) **~100KB** ④ §15 리스크 R1~R32 + 말미 버전 정의 **~43KB**(종결 리스크 원문 다수) |
| `docs/01-plan/editor-v2.plan.md` | 123KB / 646줄 | 머리말 문서 이력 **~18KB**(줄 1~126). 본문(D1~D11·§13·§14)은 살아 있는 정본 — **이번 단계는 머리말 이력만** 이관(본문 분해는 stage-43 퇴역 후 재론) |
| `docs/02-design/study-app.design.md` | 66KB / 116줄 | 색인 문서인데 설계 이력 `> (v1.x: …)`가 대부분(줄당 평균 580B — 헤딩+이력 45건) |
| `docs/04-archive/` | 62KB | **M1~M16 시점(2026-07-31 이동)에서 멈춤** — M17~M36·F43~F56 미이관이 위 비대의 원인 |
| `docs/.pdca-status.json`·`.pdca-snapshots/`·`.bkit-memory.json` | **345KB** | 과거 도구(bkit/PDCA) 잔재 · **git 미추적** · 2026-07-29 이후 사망 상태 |
| `docs/[한국산업인력공단]…​.docx` · `docs/국가 자격 종목 목록 정보.pdf` | 888KB | 사용자 원자료(바이너리·git 추적)가 docs 루트에 노출 |

토큰 낭비의 기전: 계획·설계 문서는 "부분 읽기" 규약이 있으나, **머리말·로드맵 표·리스크 표는 현황 파악
때마다 걸려 드는 구간**이고 그 안에 완료 이력이 눌어붙어 있다. 이력을 아카이브로 옮기면 현행 문서에는
"지금 유효한 것"만 남는다.

## 착수 전 결정 (권고 확정 — 사용자 이의 시 재론)

| # | 결정 | 판정 |
|---|---|---|
| ① | 이관 원칙 | **원문 그대로 이동**(2026-07-31 M1~M16 이관 전례 — "원문 그대로 보존"). 요약·윤문·재작성 금지. 잔류 측에는 압축 1줄 + 아카이브 포인터. 이관 무손실은 DoD 2의 기계 대조로 증명 |
| ② | 패치노트 위치 | 신설 `docs/03-release/` — `CHANGELOG.md`(v2 현행 정본 · 앞으로 stage 완료마다 1항목) + `CHANGELOG-v1.md`(v1 대 — M1~M30·v1.90.1 · 동결). 문서 자체 이력(plan v0.x·design v1.x)은 릴리스가 아니므로 `docs/04-archive/*-changelog.md`로 분리 |
| ③ | stage 문서 42개 | **이동하지 않는다** — 문서 전반이 `stage-{n}-*.plan.md` 경로를 교차 참조하고 있어 이동 시 파괴 반경이 이득보다 크다. 구분·탐색은 stage-45의 인덱스(`stage-index.md`)가 담당 |
| ④ | `.pdca-*`·`.bkit-memory.json` | **삭제 + `.gitignore` 등재**(git 미추적 도구 잔재 — 이력 가치 0 · 복구 불요) |
| ⑤ | docx·pdf 원자료 2건 | `docs/03-refs/`로 **git mv**(이력 보존 이동). 본문 참조 0건 확인 후 실행(참조 발견 시 경로만 갱신) |

## 체크리스트 (구현 = Fable 메인 대화)

### A. 패치노트 분리 (`docs/03-release/` 신설)

- [x] **A-1** `docs/03-release/CHANGELOG-v1.md` — v1 대 앱 릴리스 이력(동결): M1~M7(v1 완성 2026-07-24) · M8~M11(v1.x) · M12~M30 → v1.90.1(2026-08-15) 정의 원문. §14 압축표 M1~M16 행 + 말미 "구 버전 정의" 블록을 근거로 작성(이것만은 신규 집필 — 근거 행 인용)
- [x] **A-2** `docs/03-release/CHANGELOG.md` — v2 현행 정본: v2.00.0(2026-08-22 — M31~M36 = stage 31~38) 항목 + stage-39·40·41·42 완료분을 소급 1항목씩 등재(각 stage 문서 머리말 "상태" 줄 근거). 머리에 "버전 체계 규약은 stage-45에서 확정" 예고 1줄
- [x] **A-3** `study-app.plan.md` 말미 버전 정의 블록 → 현행 정의 1문단만 남기고 상세는 CHANGELOG 포인터로 대체

### B. 문서 자체 이력(changelog) 이관 (원문 무손실)

- [x] **B-1** `study-app.plan.md` 머리말 `> (v0.x: …)` 전 블록(~74KB) → `docs/04-archive/plan-changelog.md` 원문 이동. 머리말 잔류 = 현행 버전 줄 + 최근 이력 3건 + 아카이브 포인터 1줄
- [x] **B-2** `study-app.design.md` 이력 블록 → `docs/04-archive/design-changelog.md` 동일 방식 (분할 색인·§1~3 본문은 불변)
- [x] **B-3** `editor-v2.plan.md` 머리말 이력(~18KB) → `docs/04-archive/editor-v2-changelog.md` 동일 방식. **본문(§1~§14)은 무접촉**
- [x] **B-4** design.api.md·design.screens.md 머리말에 같은 유형의 이력 블록이 있으면 동일 처리(착수 시 실측 — 없으면 해당 없음 기록)

### C. 마스터 계획 §14·§15 이관 (최대 절감 구간)

- [x] **C-1** §14 로드맵 표 **M17~M36 행의 서사 원문** → `docs/04-archive/roadmap-history.md`에 추가 이관(M1~M16 전례와 같은 형식). 잔류 행은 M1~M16 스타일 압축 1줄(단계명·요약·완료일·"상세 = 아카이브 + stage-n 문서")
- [x] **C-2** §14 아래 **F43~F56 상세 명세 블록**(줄 640~899 상당 — 전부 완료 기능) → `docs/04-archive/feature-specs-done.md`에 추가 이관. §14 잔류 = "완료 기능 상세는 아카이브" 포인터(기존 M1~M16 안내문에 병합)
- [x] **C-3** §15 리스크 표 전수 판정(R1~R41+) — **종결·완료·해당 stage 종료 리스크는 원문을 `docs/04-archive/risk-history.md`(신설)로 이관**하고 색인 1줄(번호·제목·종결일)만 잔류. 현재 유효한 리스크만 본문 잔류(판정표를 완료 기록에 남길 것)
- [x] **C-4** §5 기능 목록·§8·§12 등 본문 중 "v1 기준" 서술의 시제 정리는 **하지 않는다**(재작성 금지 — 필요 시 stage-45 억제기 재심사에서 항목별 판정)

### D. 잔재·원자료 정리

- [ ] **D-1**(부분 — .gitignore 등재 완료 · 물리 삭제 = 사용자 잔여) `docs/.pdca-status.json` · `docs/.pdca-snapshots/` · `docs/.bkit-memory.json` 삭제 + `.gitignore`에 `docs/.pdca-*`·`docs/.bkit-*` 등재 (결정 ④)
- [x] **D-2** docx·pdf 2건 → `docs/03-refs/` git mv (결정 ⑤ — 사전에 저장소 전체 참조 grep 0건 확인)

### E. 참조 무결성 결선

- [x] **E-1** 이동·이관으로 위치가 바뀐 모든 대상에 대해 저장소 전수 grep(`docs/`·`CLAUDE.md`·`.claude/`·`scripts/`) — 끊긴 참조 0건. 특히 CLAUDE.md 문서 지도의 "§14 로드맵"·"버전 정의(말미)" 서술 갱신(최소 수정 — 전면 개편은 stage-45)

## DoD (완료 정의)

1. **무손실 증명**: 이관 블록별 이관 전 원문 ↔ 아카이브 본문 기계 대조(정규화 후 바이트 동일 — 대조 스크립트나 diff 로그를 완료 기록에 첨부). 요약으로 대체된 원문 0건
2. **크기 실측**: `study-app.plan.md` 350KB → **120KB 이하** · `study-app.design.md` 66KB → 20KB 이하 · `editor-v2.plan.md` 123KB → 105KB 이하 (이관분은 전부 아카이브·CHANGELOG에 존재)
3. 참조 끊김 0건(E-1 grep 로그)
4. `docs/03-release/` 2파일이 서로 겹치지 않게 v1/v2를 분담(v1 동결 · v2 현행)
5. 검토 = `stage-reviewer`(Opus) 통과 → **최종 게이트 = Fable 점검**(무손실·참조·크기 3축 확인)
6. 사용자 확인 1회(패치노트 2파일 열람 — "이 구분이 원하던 가시성인가")

## 이 단계에서 하지 않는 것

- stage 문서 이동·개명(결정 ③) · stage 인덱스 생성(→ stage-45)
- 버전 체계·버전 가시화 코드(→ stage-45)
- 억제기(낡은 게이트·병존 규약) 제거(→ stage-45 재심사 → 사용자 승인)
- CLAUDE.md 전면 슬림화(→ stage-45 — 여기서는 E-1 최소 수정만)
- `editor-v2.plan.md` 본문 분해 · 설계 문서(api·screens) 본문 재편 · 매뉴얼 개편
- 문서 내용의 요약·윤문·시제 교정 일체(무손실 원칙)

## 완료 기록

### 구현 (2026-08-31 — Fable 메인 대화 직접, 스크립트 이관)

이관은 전부 **라인 범위 스크립트 추출 → 아카이브 cat 조립 → awk 재조립**으로 수행 — 사람 손 재타이핑 0(무손실은 구성상 보장 + 아래 기계 대조로 증명).

**이관 블록 대조표 (DoD 1 — 전 블록 `tail -n | diff` 바이트 동일 = 7/7 PASS)**

| 블록 (이관 전 위치 — Draft v0.59 기준 라인) | 행선지 | 크기 | SHA-256(16) |
|---|---|---|---|
| B-1 plan 머리말 이력 v0.15~v0.55 (7~48행) | `plan-changelog.md` (신설) | 76,818B | `1ffa234b2e4e525f` |
| B-2 design 이력 v1.12~v1.47 (8~44행) | `design-changelog.md` (신설) | 55,272B | `e3b26d61394acf13` |
| B-3 editor-v2 머리말 서사 (3~107행) | `editor-v2-changelog.md` (신설) | 14,996B | `1ed8d2e881232133` |
| C-1 §14 로드맵 M17~M36 원문 행 (619~638행) | `roadmap-history.md` (추가) | 37,470B | `fe15785d833ba629` |
| C-2 §14 F43~F56 상세 명세 (641~898행) | `feature-specs-done.md` (추가) | 123,592B | `e88d023701a9d5de` |
| C-3 §15 종결 리스크 24행 | `risk-history.md` (신설) | 21,303B | `488d0da0364c33e8` |
| A-3 말미 v1 경과 서사 (943~946행) | `roadmap-history.md` (추가) | 4,200B | `2788115557f7bcc4` |
| A-3 구 버전 정의 (948행) | `CHANGELOG-v1.md` 말미 인용 | 250B | `e73fd2f0d2486696` |
| A-3 말미 에디터 v2 경과 서사 (950~959행) | `roadmap-history.md` (추가) | 11,628B | `0b6726976edce639` |

- **B-4 = 해당 없음** — `design.api.md`·`design.screens.md` 머리말에 이력 블록 없음(실측 — 분할 안내 2줄뿐).
- **C-4 이행** — 본문 시제 정리·재작성 0건(무손실 원칙 — stage-45 재심사로 이월).

**C-3 리스크 전수 판정표 (35행 → 잔류 11 · 이관 24)**

- 판정 기준: **잔류 = 지금도 코드 작업 시 지켜야 할 제약·외부 의존·명시된 재론 조건이 있는 행** / **이관 = 결정·대응이 완료돼 기록 가치만 남은 행**(색인 1줄 잔류).
- 잔류 11: R7(창작 금지 게이트 — 살아있는 안전 원칙) · R12(외부 노출 금지) · R16(경로 검증 규약) · R17(큐넷 외부 의존 현존) · R18("R18 관례"로 인용되는 보존 규약) · R19(파서 의존 원칙) · R20(정답 유출면 봉인 불변 규칙 연동) · R26(반입 마크업 주입 경계) · R33~R40(색인 행 — 별지 §9 정본 유효) · R41(notes 격리 현존·stage-43 관련) · R42(이중 저장 드리프트 현존)
- 이관 24: R1~R6·R8~R11·R13~R15(v1 초기 결정 — 확정·구현 종결) · R21~R23(F44~F46 대응 구현 완료) · R24·R25(F48·F49 계약 확정으로 종결) · R27(F53)·R28(F55)·R29~R31(S30 — 에디터 v2에서는 R34·R35가 계승)·R32(원문에 "종결 2026-08-15" 명시)

**DoD 2 크기 실측 (이관 후)**

| 파일 | 이관 전 | 이관 후 | 목표 | 판정 |
|---|---|---|---|---|
| `study-app.plan.md` | 350KB/959줄 | **90,653B(88.5KB)/648줄** | ≤120KB | ✅ |
| `study-app.design.md` | 66KB/116줄 | **12,429B(12.1KB)/80줄** | ≤20KB | ✅ |
| `editor-v2.plan.md` | 123KB/646줄 | **110,570B(108.0KB)/542줄** | ≤105KB | ⚠ **약 3KB 초과** — 사유: 잔류 규칙(최근 3건) 우선. 잔류 3건 중 "stage-42 예정(2026-08-24)" 항목은 **살아있는 사용자 확인 대기 항목**("기존 노트기능" ⓐ/ⓑ 해석)을 담고 있어 아카이브 이관 부적절 판단. 목표 수치는 머리말 전량(~18KB) 이관 가정의 추정치였음 |

- **A-1·A-2**: `docs/03-release/CHANGELOG-v1.md`(v1 동결 — M1~M30·v1.90.1, 구 버전 정의 원문 인용) + `CHANGELOG.md`(v2 현행 — v2.00.0 + stage-39·40·41·42 소급 등재 + stage-45 버전 체계 규약 예고 1줄) 신설.
- **D-1**: `.gitignore`를 `docs/.pdca-*`·`docs/.bkit-*` 패턴으로 일반화(기존에 개별 3건은 이미 등재돼 있었음 — 실측). **물리 삭제는 잔여**: 본 체크아웃의 미추적 파일 삭제가 세션 권한(워크트리 격리)에 막힘 — 3종 전체를 잡 백업 후 사용자 1회 삭제로 이월(git 무영향 — 이미 ignore됨).
- **D-2**: docx·pdf 2건 `docs/03-refs/`로 git mv(참조 전수 grep 0건 — 이 문서 점검표의 역사 기록 1건뿐, 기능 참조 아님).
- **E-1**: 참조 전수 grep — 설계 api의 "계획서 §14 F43~F45" 근거 참조는 `feature-specs-done.md` 헤더에 인수 문구 추가로 결선(2026-07-31 전례 방식). CLAUDE.md 문서 지도 최소 수정(마스터 행 갱신 + `docs/03-release/` 행 추가 + 아카이브 행 갱신). design.md §3의 `/pdca analyze` 사문 지시는 재작성 금지 원칙에 따라 stage-45 억제기 재심사로 이월.

### 검토 (2026-08-31 — stage-reviewer·Opus) = **조건부 통과 (치명 0 · 중요 0 · 경미 4)**

- 검토자가 무손실을 **전량 기계 검증**으로 독립 재확인: 이관 블록 9종 전건 byte-identical + **삭제 라인 집합 완전성**(plan 삭제 360줄 = 이관 359줄 + 포인터로 대체된 구 안내문 1줄 · design/editor-v2 삭제분 = 블록과 diff 일치 · 잔류 리스크 11행도 byte-identical) — **소실 0 · 창작 0**.
- DoD 2 editor-v2 3KB 초과의 재량 판단(살아있는 사용자 확인 대기 항목 잔류) = **"타당(범위 내 도달 불가 수치)"** 평가.
- 경미 4건 = 전부 신규 안내문 계층의 정확성 문제 → **당일 전건 수정**: ① `feature-specs-done.md` 헤더 F34~F42→F34~F56·낙후 문구 갱신 ② `roadmap-history.md` 헤더 M1~M16→M1~M36·낙후 문구 갱신 ③ 포인터 "v0.1~"→"v0.15~"(실측 — v0.1~v0.14 항목은 원래 부존재) ④ D-1 체크박스를 미이행 실제에 맞게 정정(불변 규칙 10).
- 관찰 4건(결함 아님)은 stage-45 이월: 말미 보존 원문의 "stage-42 = 퇴역" 낙후 서술 vs CHANGELOG의 stage-42/43 번호 이월 병치 · design.md `/pdca analyze` 사문 지시 · `docs/03-refs/` 문서 지도 미등재 · `.gitignore` bkit 패턴 중복(무해).

### Fable 최종 게이트 (2026-08-31) = **통과**

- 3축 판정: **무손실** = 검토자 전량 검증 소실 0·창작 0 ✅ · **참조** = 끊김 0(포인터 전건 실존·2단 체인 성립) ✅ · **크기** = plan 88.5KB(≤120)·design 12.1KB(≤20) ✅ + editor-v2 108.0KB(목표 105 — 편차 사유 타당 판정으로 **수용**, stage-43 퇴역 후 본문 분해 시 재절감).
- 검토자 제기 R25 재점검: stage-42가 고친 것은 분할 반입의 **구현 결함**(정리·병합)이고 R25가 다룬 **계약**(결정론 절단·커버리지·디스크 보존)은 확정 유지 — **이관 판정 유지**.
- D-1 물리 삭제 잔여의 백업 실체: 잡 임시 디렉터리 `~/.claude/jobs/1060c1dc/tmp/pdca-backup/`(357KB — 잡 삭제 시 소멸하는 임시 안전판). 원본 3종이 메인 체크아웃에 온전히 남아 있으므로 실질 위험 0 — 사용자 삭제 1회로 종결.

**잔여 처리**: DoD 6 = **충족(2026-08-31 사용자 확인: "가시성은 많이 좋아졌어")** · D-1 물리 삭제만 잔여(사용자 — `docs/.pdca-status.json`·`docs/.pdca-snapshots/`·`docs/.bkit-memory.json` 3종, git 무영향).
