# Study Hub 테스터 가이드

Python·Node 등 아무것도 설치되지 않은 Windows PC에서 Study Hub를 실행하는 방법입니다.

## 필요한 것

- Windows 10/11 (64bit)
- **최초 1회만** 인터넷 연결 (포터블 Python과 패키지를 자동으로 내려받음, 약 100MB)
- 그 외 설치할 프로그램: **없음**

## 실행 방법

1. 프로젝트를 받습니다 — zip이면 압축을 풀고, GitHub이면 `Code → Download ZIP`으로 받아도 됩니다.
   - **주의**: `C:\Users\...\Desktop\StudyHub` 처럼 일반 폴더에 두세요. (OneDrive 동기화 폴더나 `Program Files`는 피할 것)
2. **`2_StartServer.bat` 더블클릭** — 이것 하나면 됩니다.
   - 처음 실행이면 초기 설정(`1_Setup.bat`)이 자동으로 먼저 돌아갑니다. 몇 분 걸립니다.
   - 설정이 끝나면 서버가 켜지고 브라우저가 자동으로 열립니다 (`http://localhost:8000`).
3. 종료: 검은 창을 닫거나 `Ctrl+C`. 안 닫히면 `수동 서버종료.bat` 실행.

초기 설정을 미리 따로 해두고 싶으면 `1_Setup.bat`을 먼저 실행해도 됩니다 (같은 작업, 순서만 분리).

## 자주 묻는 문제

| 증상 | 해결 |
|---|---|
| "Windows에서 PC를 보호했습니다" 파란 창 | `추가 정보 → 실행` 클릭 (서명 없는 배치 파일이라 뜨는 경고) |
| 방화벽 허용 창이 뜸 | 폰(같은 Wi-Fi)에서도 접속하려면 `허용`, PC에서만 쓰면 아무거나 |
| 다운로드 실패 | 인터넷 연결 확인 후 `1_Setup.bat` 다시 실행 (이어서 진행됨) |
| 설치가 꼬인 것 같음 | `python-embed` 폴더를 통째로 삭제 후 `1_Setup.bat` 다시 실행 |
| 브라우저가 안 열림 | 직접 주소창에 `http://localhost:8000` 입력 |
| 화면이 안 뜨고 에러 JSON만 보임 | `frontend\dist` 폴더가 빠진 패키지 — 배포자에게 문의 |

## 알아둘 것

- 처음 실행하면 **빈 데이터베이스**로 시작합니다. 학습자료는 앱의 가져오기(반입) 기능으로 넣습니다.
  (배포자가 zip에 `study.db` 파일을 포함해 주면 그 데이터가 그대로 보입니다.)
- LLM 자동 구조화 기능은 설정 화면에서 Anthropic API 키를 등록해야 동작합니다. 키가 없어도 나머지 기능(학습·퀴즈·오답노트·복습)은 모두 사용 가능합니다.
- 사용 설명서: 서버 실행 후 `http://localhost:8000/manual`
- 이 앱은 홈 네트워크 전용입니다. 외부 인터넷에 포트를 열지 마세요.

## 폴더 구조 (참고)

```
Project_Study/
├─ 1_Setup.bat            ← 초기 설정 (포터블 Python + 패키지 + DB 생성)
├─ 2_StartServer.bat      ← 서버 시작 (테스터는 이것만 실행)
├─ 수동 서버종료.bat       ← 서버 강제 종료
├─ 개발용 서버시작.bat     ← 개발 PC 전용 (실수로 눌러도 자동으로 2번으로 넘어감)
├─ python-embed/      ← 초기 설정이 만드는 포터블 Python (지우면 재설치됨)
├─ backend/           ← 서버 코드
├─ frontend/dist/     ← 화면 (빌드 결과물)
└─ study.db           ← 데이터베이스 (백업 = 이 파일 복사)
```
