# Stage 34 — 에디터 v2: 노트(베타) 방언 이식·참조 칩·이미지·붙여넣기 (M33-2, **M33 게이트**)

> 상태: **착수 가능(2026-08-16)** — 선행 **stage-33 완료·검토 통과**(DoD 1~9 충족 · 10은 사용자 이행 병행).
> 인계 사항은 `stage-33-notes-surface.plan.md` "완료 기록 → stage-34 인계 사항"이 정본이며,
> 그중 **코어 범위 제약 7종**은 아래 **규약 I**로 편입했다(2026-08-16 검토 지적 반영).
> 범위: **M33의 뒤 절반** — 코어 블록 위에 **F52 방언 + F43 참조 3종 + 수식 + 이미지 + 붙여넣기**를 얹어
> **현행 기능 패리티**를 완성하고, **M33 게이트**(패리티 체크리스트 + 노트 실사용 = 한글 IME 2차 관문)를 판정한다.
> 분할 근거·M↔stage 대응표는 `stage-33-notes-surface.plan.md` "범위 분할" 절이 정본이다.
>
> 정본: `editor-v2.plan.md` §5.4(블록 팔레트 · **참조 칩 UX ①~④ 확정** · **M32 전달 사항 ⓐ~ⓕ 확정**)·§8 M33 행 ·
> 화면 계약 = screens **§5.16** · 블록 스키마 = `frontend/src/editor2/schema/blocks.ts`(**수정 금지, import만**) ·
> 이미지 서버 계약 = api **§4.27**(재사용 — 신규 API 0).

## 범위 요약

- **프론트 전용** — 신규 API 0건 · DDL 0건 · Alembic 0건 · **백엔드 diff 0**.
  참조 칩 피커의 문서 검색·제목 조회는 **기존 API 재사용**(**`GET /api/search?q=`** — item에 `doc_no`·`title`·
  `snippet`이 이미 있다 · 목록 보조 `GET /api/documents`(§4.2) · 제목 조회
  `POST /api/documents/resolve-embeds`(§4.19)) — **읽기 전용 사용**이라 `documents` 무접촉 계약과 정합.
  이미지 업로드는 **기존 `POST /api/uploads`(§4.27) 그대로**(요청당 1장·순차 루프·매직 바이트 판정 서버 몫).
- **커스텀 스펙 추가분**: 스타일 3종(highlight·spoiler·`t`(ink/bg/size)) · 인라인 2종(refChip·inlineImage) ·
  블록 3종(callout·docEmbed·sourceFallback) + 수식(mathBlock·inlineMath — 도입 판정은 아래 규약 F).
- **코어 범위 제약 7종 처리(규약 I — 2026-08-16 추가)**: 방언 말고도 **순수 CommonMark/GFM인데 BlockNote
  0.54 코어가 담지 못하는 표현 7종**이 stage-33 어댑터에서 미지원으로 보고된다. 그중 **4종은 이 단계가
  흡수**하고 **3종은 사용자 판정 대기**다(임의 손실 수용 금지 — R34). 배정 정본 = `editor-v2.plan.md`
  §5.4 말미 "M33 어댑터가 확인한 코어 범위 제약".
- **신규 npm 의존은 최대 2건, 전부 사전 라이선스 확인 필수** — 아래 "의존 도입 규칙".
- D9 격리 계약은 **stage-33과 동일**(그 문서 "격리 계약" 절이 정본). 이 단계의 허용 접촉점은
  `frontend/src/editor2/**` 신규·수정 + `frontend/scripts/**` 신규 + (필요 시) `package.json`·lockfile 뿐이다.
  **`App.tsx`·설정 화면·백엔드는 이 단계에서 diff 0이 정상.**

## 의존 도입 규칙 (D10 · R40 — 저장소 public)

| 후보 | 용도 | 판정 절차 |
|---|---|---|
| `@blocknote/math-block@0.54.0` | `$…$`·`$$…$$` 시각 편집 | **① 라이선스 실측(패키지 `license` 필드 + LICENSE 파일)이 MPL-2.0인지 ② 전이 의존에 GPL/LGPL 0건인지**(stage-31의 라이선스 전수 검사 스크립트 재사용) ③ 성숙도(m31 §2.2 유보분) 실측 → 3건 모두 통과해야 도입 |
| `@blocknote/code-block@0.54.0` | 코드 블록 문법 하이라이트 | 같은 절차. **미도입도 정상** — 앱에 `highlight.js`가 이미 있고 코어 코드 블록은 lang 속성만으로 왕복이 성립한다(하이라이트는 리더에서 이미 동작) |
| `@blocknote/xl-*` | (PDF/DOCX 내보내기·AI 등) | **영구 금지 — GPL-3.0.** 어떤 기능도 이 패키지로 해결하지 않는다 |

- 통과 실패 시 **자체 커스텀 블록으로 대체**(수식 = 기존 `katex` 의존 재사용해 `createReactBlockSpec` 1개).
  대체도 어려우면 착수 중단·보고(임의 도입 금지).
- **버전은 0.54.0 정확 고정**(D2 — `^`·`~` 금지). 도입분은 완료 기록에 **패키지명 + 버전 + 라이선스**를 표로 남긴다.

## 확정 규약 (구현 전 결정 — 이 문서 + `editor-v2.plan.md` §5.4가 정본)

### A. 참조 칩 UX 재설계 확정 (§5.4 요구 ①~④)

**① 대상 선택 = 피커 필수(하드코딩·무피커 삽입 금지)**

- 진입: 툴바 [참조] 버튼 + 슬래시 메뉴 `/참조`(문서 링크 칩) · `/임베드`(문서 임베드 블록) · `/앵커`(문서 안 앵커).
- 피커 = 인라인 팝오버 1개(3종 공용): 검색 입력 → **`GET /api/search?q=`**(디바운스 250ms) 결과 목록 +
  **최근 열람 문서**(로컬 최근 목록 — 신규 API 0). 키보드 ↑↓·Enter·Esc 지원.
- **앵커 칩(`[[#절 제목]]`)은 자유 입력을 받지 않는다** — **현재 노트의 heading 블록 목록**에서 고른다
  (ⓕ target 도메인 문제의 원천 차단). 도메인 밖 제목은 목록에 **비활성 + 사유**로 노출한다(숨기지 않는다 —
  왜 못 쓰는지 보여야 사용자가 제목을 고칠 수 있다).
- 문서 임베드는 **블록**(docEmbed)으로 삽입한다(문단 안 임베드는 기존 데이터 수용 전용).

**② 선택 텍스트 보존 = (a) 승계 채택, 실패 시 (b) 폴백 — 조용한 삭제 0**

- 선택 없음 → 커서 위치에 삽입, **추종형 칩**(`label` 미저장).
- 선택 있음 → 선택 텍스트를 **라벨 후보**로 삼아 규약 B(`isSafeRefText`) 검사:
  - **통과** → 지정 라벨형 칩(`[[DOC-0012|선택텍스트]]`)으로 치환 — 글자가 사라지지 않고 라벨로 남는다
    (2026-08-15 사용자 피드백 "글자가 사라지면서 참조 문서 7이 되는" 현상의 해소 지점).
  - **미통과** → **삽입을 강행하지 않는다**. 선택을 유지한 채 피커에 사유 + 라벨 입력란을 띄우고,
    [라벨 없이 삽입](= 선택 해제 후 커서 삽입 = (b))·[취소]를 제공한다.
- **어떤 경로에서도 사용자의 텍스트를 조용히 지우거나 바꾸지 않는다.**

**③ 라벨 규칙·칩 클릭 동작**

- 기본 = **추종형**(라벨 문자열을 저장하지 않고 대상 문서 제목을 표시 — 제목이 바뀌면 자동 추종. M32 규약 C).
  제목 조회는 `POST /api/documents/resolve-embeds`(§4.19) **배치 재사용**(편집 세션 메모리 캐시 — 신규 API 0).
- **사용자 지정 라벨 허용** — 피커의 라벨 입력란, 또는 칩 팝오버 [라벨 편집]. [제목 추종으로 되돌리기]로 언제든
  추종형 복귀(= `label` 삭제). 지정 라벨형은 대상 제목이 바뀌어도 라벨을 유지한다(경고 표시 없음 — YAGNI).
- 미해결 대상(`found:false`)·삭제된 문서 = **자리표시자 칩**(중립 토큰 색 + 사유) — 기존 F43 자리표시자 문구 계승.
- **칩 클릭 동작 = 표면에 따라 다르다(일관 규칙)**:
  - **편집 표면(노트 편집)** = 팝오버 열기(대상 제목 · [문서 열기] · [라벨 편집] · [대상 변경] · [삭제]).
    편집 중 클릭이 곧바로 라우팅되면 편집 흐름이 끊긴다 — 원자 인라인 콘텐츠의 표준 동작.
  - **읽기 표면(프로젝션을 `MarkdownView`로 보는 미리보기·기존 문서 화면)** = **기존 F43 동작 그대로**(이동).
    리더는 1바이트도 수정하지 않으므로 자연히 충족된다(D9).

**④ 문법 왕복 = M32 결론 그대로** — 추종형 `[[DOC-0012]]` · 지정 라벨형 `[[DOC-0012|라벨]]` ·
앵커 `[[#절 제목]]` · 임베드 `![[DOC-0007]]`. **UX 결정이 스키마·문법을 바꾸지 않음**을 재확인했다(M32 규약 C).

### B. 참조 칩 라벨·target 문자 도메인 (M32 전달 사항 ⓒ·ⓕ) — **거절 정책 + 단일 판정 함수**

- **판정 함수 1개**: `frontend/src/editor2/schema/refDomain.ts`의 **`isSafeRefText(s): boolean`** 을 신설하고
  **라벨과 target 양쪽에 같이 적용**한다(둘의 금지 집합이 실질적으로 같다 — 함수를 미리 쪼개지 않는다: YAGNI).
  - 금지: `]` · `|` · 개행(구조적 불가) · **인라인 문법을 여는 문자** `*` `` ` `` `~` `$` `[` `:` `_`(강조 위치) ·
    선행·후행 공백 · 빈 문자열. 내부 공백·탭은 **허용**(파서가 보존한다 — M32 실측).
  - **M32 검증 스크립트(`s32-roundtrip-blocks.mjs` 계열 ⑥)가 이 함수를 import하도록 전환**한다 —
    도메인의 단일 출처를 코드에 두고 스크립트와 UI가 갈라지는 것을 막는다(스크립트 수정은 허용 접촉점).
- **정책 = 거절(치환 금지)** — 도메인 밖 문자를 앱이 몰래 제거·치환하는 것은 M32의 "조용한 본문 변형 0" 계약과
  충돌한다. 위반 시 입력 시점에 사유를 보여주고 사용자가 고치게 한다.
  - **유일한 자동 정리 = 가장자리 공백 트림**(파서가 어차피 버리는 자리 = 표기 정규화이지 값 변경이 아니다.
    M32 규약 A의 예외 ⓓ와 같은 논리).
- **target 경로별 적용**: doc/embed target = 피커가 고른 문서의 `doc_no`(정규식 `DOC-\d{4,}`) — 자유 입력 경로
  자체가 없다. anchor target = 현재 노트 heading에서 선택 + `isSafeRefText` 필터.
- **역방향(반입 데이터)**: 도메인 밖 target·label을 가진 기존 데이터는 M32 변환기가 이미 **평문 강등**으로
  처리한다(텍스트 보존 = 손실 0). 어댑터는 **새 규칙을 만들지 않고** 그 결과를 그대로 표시한다.

### C. 마이크로 마크 중첩 차단 (M32 전달 사항 ⓐ) — **상호 배타 + 대체**

- `++`(밑줄) · `==`(형광) · `||`(스포일러) 3종을 **"마이크로 그룹"** 으로 묶어 **한 구간에 최대 1개**만 허용한다.
  이미 다른 마이크로 마크가 걸린 구간에 새 마이크로 마크를 적용하면 **대체(replace)** 한다(라디오 동작).
  - 근거: 적용을 무시(차단)하면 사용자는 버튼이 고장 난 것으로 읽는다. 대체는 결과가 즉시 눈에 보이고 되돌리기 쉽다.
  - 안내 토스트는 만들지 않는다 — 툴바 버튼의 active 상태 표시로 충분(과설계 금지).
- CommonMark 마크(bold·italic·strike)와의 조합은 **자유** — 변환기가 `MARK_ORDER`로 CommonMark를 바깥에 둔다.
- **강제 지점 3곳**: ① 툴바·단축키 커맨드를 감싸는 어댑터 1곳(`editor2/adapter/marks.ts`)
  ② 역방향 변환(BlockNote→앱 블록)의 **방어적 정규화**(2개 이상이면 `MARK_ORDER` 우선 1개만 남기고 **집계 보고**)
  ③ 붙여넣기 경로(HTML `u`+`mark` 중첩 유입) — 같은 규칙으로 1개로 접는다.

### D. inlineImage 어댑터 (M32 전달 사항 ⓑ) — **원자 인라인 커스텀 스펙**

- 채택: `createReactInlineContentSpec`(content:`"none"`, props = url·alt·title·width) — refChip과 같은 메커니즘
  (M31 스파이크에서 성립 확인).
- **블록 승격을 기각한 이유**: 문단 안 이미지를 블록으로 올리면 문단이 쪼개져 **왕복 무손실이 깨진다**.
  M32가 `inlineImage`를 스키마에 넣은 이유가 정확히 그 손실 방지였다(`blocks.ts` 주석 참조).
- **에디터에서 새로 삽입하는 이미지는 언제나 블록 이미지**(내장 `image`)다. 인라인 이미지는 **기존 데이터 수용
  전용**(보존·표시·삭제만 가능, 새로 만드는 UI 없음). 크기 조절 UI도 블록 이미지에만(내장 `previewWidth` ↔ `{w=}`).

### E. 블록 메타데이터 입력 도메인 (M32 전달 사항 ⓔ) — **UI가 도메인 밖을 받지 않는다**

- **콜아웃 variant = 고정 목록만**: `note` · `warn` · `tip` · `fold` · `hide`(자유 입력 금지 → directive 이름이
  `.`·`:`로 붕괴하는 경로 소멸). 목록 밖 variant를 가진 기존 데이터는 **보존만**(표시는 기본 스타일).
- **콜아웃 title(`[제목]`) = `isSafeRefText` 재사용 검증**(규약 B와 같은 함수 — 라벨류 통일). 위반 = 실시간 거절.
- **`{key=value}` 속성 자유 편집 UI를 만들지 않는다** — 기존 데이터의 `attrs`는 어댑터가 보존만 한다.
  따라서 M32가 경고한 **키·값 도메인 문제는 "입력 경로 없음"으로 해소**된다(수렴 = 값 변경이므로 회피가 정답).
- 코드 블록 info 문자열 = stage-33 규약 E 그대로(드롭다운만·자유 입력 없음).
- `:t` 색·크기 = **기존 화이트리스트 그대로**(팔레트 7색 ∪ `#rrggbb` ∪ size 3종 — `palette.ts` 단일 출처.
  편집기 v2가 새 색 규칙을 만들지 않는다 — 불변 규칙 5).

### F. 수식·코드 블록

- 수식 도입 순서: ① `@blocknote/math-block` 라이선스·전이 의존·성숙도 3건 통과 시 채택 →
  ② 하나라도 실패하면 **자체 커스텀 블록/인라인**(기존 `katex` 재사용, 편집은 원시 텍스트 입력 + 미리보기).
  어느 쪽이든 **앱 블록 스키마(`mathBlock`·`inlineMath`)는 무변경**이고 어댑터만 분기한다.
- 코드 블록 하이라이트는 **선택 사항**(미도입 정상 — "의존 도입 규칙" 표).

### G. 붙여넣기 파이프라인

- **HTML 붙여넣기(`text/html` 있음)** = 기존 `frontend/src/utils/htmlPasteMarkdown.ts` 화이트리스트 계약(R30·S30 ④) **재사용**
  (**import만·무수정**) → 방언 Markdown → `markdownToBlocks`(M32) → 어댑터 → BlockNote 삽입.
  경로를 통일하면 **붙여넣기 정화 규칙이 앱에 1벌**만 남는다(F52 "raw HTML 기각" 방어선 유지).
- **`text/plain`** = **평문 그대로 삽입**(BlockNote 기본 동작). Markdown으로 해석하지 않는다 —
  평문 속 `*`·`#`이 서식으로 둔갑하는 사고를 막는다. ("Markdown으로 붙여넣기" 명령은 하지 않는 것 목록.)
- **이미지 파일 붙여넣기·드롭** = §4.27 업로드 경로(아래 H).
- 붙여넣기 결과에도 규약 C(마이크로 상호 배타)·규약 B(도메인) 필터가 적용된다.

### H. 이미지 3진입점 (§4.27 재사용 — 신규 API 0)

- 진입 3종 = **붙여넣기 · 드래그앤드롭 · 툴바(슬래시) 이미지 삽입** — 셋이 **같은 순차 업로드 루프**를 공유한다
  (요청당 파일 1개 · 실패는 파일 단위로 표시 · 서버 `message` 그대로 렌더). §4.27 ⑦의 프론트 계약을 그대로 계승하되
  **삽입 지점만 BlockNote 이미지 블록**(props.url = 응답 `url`)으로 바뀐다.
- 업로드는 **BlockNote의 `uploadFile` 훅 1곳**에 결선한다(사용처별 분기 금지).
- **크기 조절** = 블록 이미지의 `previewWidth` ↔ 앱 `width` ↔ 프로젝션 `{w=<px>}`(M32 규약 E) 왕복.

### I. 코어 범위 제약 7종 (stage-33 어댑터 실측 — 2026-08-16 신설. 배정 정본 = `editor-v2.plan.md` §5.4 말미 표)

어댑터가 미지원으로 보고하는 사유 중 **방언이 아니라 순수 CommonMark/GFM**인 것이 7종 있다:
`listItem:spread`(느슨한 목록) · `listItem:groupBreak`(인접 목록 경계) · `table:align`(GFM 열 정렬) ·
`inline:hardBreak`(경성 줄바꿈) · `link:title` · `image:title`/`image:height` · `block:meta`.
**이 단계의 방언 팔레트에는 없던 항목이며, 소유자가 없어 stage-35(M34)에서 기존 문서가 통째로 폴백으로
떨어질 수 있다**는 것이 등재 사유다(검토 지적).

| 구분 | 항목 | 이 단계의 의무 |
|---|---|---|
| **흡수(이 단계 소유)** | `inline:hardBreak` · `link:title` · `image:title`/`image:height` · `block:meta` | **왕복 보존**한다. 경로 후보 = ⓐ **잔여 속성 사이드카**(BN이 못 담는 속성을 **블록 id 키**로 보관 → 역변환에서 복원) · ⓑ **원자 인라인 커스텀 스펙**(hardBreak — 규약 D inlineImage와 같은 메커니즘). **내장 블록 스펙 재정의는 1차 후보가 아니다**(내장 UI 재구현 비용). **전부 기존 데이터 수용 전용** — 새로 만드는 UI를 만들지 않는다 |
| **사용자 판정 대기(R34)** | `listItem:spread` · `listItem:groupBreak` · `table:align`(열 증감 시) | **흡수하지 않는다. 손실 수용으로 임의 확정하지도 않는다.** 이 단계는 **명시적 미지원 보고를 유지**(조용한 손실 0)하고, 필요하면 표본 수·영향 범위를 정리해 보고만 한다. `table:align`은 **열 수 불변 조건의 왕복 보존까지는 시도**(정렬 **편집 UI**는 M35 = stage-36 범위). 판정 시한 = **stage-35 착수 전** |

- **유입 경로 주의**: 노트는 신규 저장소라 BlockNote로 만든 문서에는 이 값이 생기지 않지만,
  **규약 G의 붙여넣기 경로**(HTML→방언 Markdown→`markdownToBlocks`)로는 `hardBreak`·`link:title`이
  **실제로 유입된다**. 흡수 4종의 우선순위가 여기서 나온다.
- 흡수를 시도했다가 불가로 판명되면 **임의 강등 금지** — 사유와 표본 수를 정리해 사용자 보고(게이트 표
  "왕복에서 조용한 손실 발견" 행 = R34 경로).

## 체크리스트

전 항목 **프론트 묶음**(백엔드 묶음 없음 — 백엔드 diff 0이 DoD). 담당 `frontend-dev`(Sonnet ·
**G-2/G-4/G-5는 opus 승격 권장** — 도메인 필터·피커 UX·왕복 계약이 걸린다).

### 1. 방언 스펙 이식

- [x] G-1. **커스텀 스타일 3종** — highlight(값 보유: 색) · spoiler(boolean) · `t`(ink/bg/size 3개 스타일)
      — PoC(`editor2/poc/specs.tsx`)의 성립 패턴을 **복제**(PoC 파일은 수정 금지). 값 도메인은
      `components/markdown/palette.ts` **재사용**(신규 색 0 — 불변 규칙 5) · 렌더는 `tokens.css` 변수 경유.
      → Phase 1 완료(`editor2/blocknote/specs/styles.tsx`). **계획서 표현과 의도적 차이 2건**:
      highlight는 **boolean**(앱 스키마 `InlineStyles.highlight?: true`와 1:1 — 색은 `:t{bg=}`가 담는다) ·
      `t`는 **string 스타일 1개**(값 = `JSON.stringify(AttrPair[])`). ink/bg/size 3개 화이트리스트 prop으로
      쪼개면 `:t 화이트리스트 밖`·`:t 속성 중복 키`·`:t 비-ASCII 속성 키` 같은 실재 표본이 조용히
      사라진다(손실 0 계약 위반). 렌더·툴바용 ink/bg/size 뷰는 `interpretTextStyle(pairs)`로 파생.
- [ ] G-2. **마이크로 마크 상호 배타(규약 C)** — `editor2/adapter/marks.ts`(툴바 커맨드 래퍼) + 역방향
      방어적 정규화 + 붙여넣기 접기. 3지점 전부 같은 상수(`MICRO_MARKS`)를 참조할 것.
      → Phase 1에서 **상수·순수 함수·역방향 정규화까지** 완료(`adapter/marks.ts` — `MICRO_MARKS` ·
      `activeMicroMarks` · `collapseMicroMarks` / `fromBlockNoteResult().microMarkCollapses`로 집계 보고).
      **툴바 커맨드 래퍼(강제 지점 ①)·붙여넣기 접기(③)는 Phase 2** — 그 둘이 붙어야 이 항목이 닫힌다.
      → Phase 2-B에서 **강제 지점 ① 완료**(`blocknote/toolbar/microMarks.ts` — `toggleMicroMark`/`applyMicroMark`가
      `MICRO_MARKS`만 보고 라디오 대체 · 툴바 버튼 3종과 `useMicroMarkShortcuts.ts`(Mod+U 가로채기)가 그 한 곳만
      호출한다. 기본 `underline` 단축키를 가로채지 않으면 툴바만 규약을 지키고 단축키는 우회한다). **남은 것 = ③ 붙여넣기 접기.**
- [x] G-3. **커스텀 블록 3종** — callout(variant 고정 목록 + title 검증 — 규약 E) · docEmbed(원자·읽기 전용 카드) ·
      sourceFallback(원문 Markdown을 **그대로 보존**하는 읽기 전용 블록 — 편집 불가·삭제만 가능).
      → Phase 1 완료(`editor2/blocknote/specs/blocks.tsx`). variant 고정 목록은 `CALLOUT_VARIANTS`
      (note·warn·tip·fold·hide)로 두되 **propSchema에 values 화이트리스트를 걸지 않았다** — 목록 밖
      variant를 가진 기존 데이터(`:::노트[…]`)를 그대로 보존해야 하기 때문이다(값을 바꾸지 않는 것이 계약).
      입력 UI만 고정 목록으로 좁힌다(Phase 2·M35). `attrs`는 `t`와 같은 이유로 통짜 JSON 보존.
- [x] G-4. **참조 칩 인라인 스펙 + 피커 UX(규약 A ①~④ 전건)** — 3종 진입·검색·최근 문서·앵커는 heading 선택·
      추종형/지정 라벨·자리표시자·칩 팝오버(문서 열기/라벨 편집/대상 변경/삭제). 제목 조회 = `resolve-embeds` 배치.
      → Phase 1에서 **인라인 스펙·렌더(자리표시자)까지** 완료(`specs/inline.tsx` `refChipInlineSpec`).
      → Phase 2-B에서 **피커·팝오버 UX 완료**(`blocknote/refPicker/**`): 3종 공용 팝오버 1개(`RefPickerPanel`) ·
      툴바 [참조] + 슬래시 `/참조`·`/임베드`·`/앵커`(기본 항목에 **덧붙이기만**) · `GET /api/search` 250ms 디바운스 ·
      최근 문서 = `editor2/lib/recentDocs.ts`(localStorage, 신규 API 0 — **"노트에서 최근 참조한 문서"**라는 한계 명시) ·
      앵커는 `editor.document`의 heading에서 선택하고 도메인 밖 제목은 **비활성 + 사유**로 노출 ·
      선택 텍스트는 통과 시 라벨 승계, 미통과 시 **선택을 그대로 둔 채** 사유 + 라벨 입력란 + [라벨 없이 삽입]/[취소]
      (조용한 삭제 0 — DoD 4) · 제목 추종은 `RefUiProvider`가 30ms 배치로 `resolveEmbeds` **1회**(N+1 0, 세션 메모리 캐시).
- [x] G-5. **`editor2/schema/refDomain.ts`(규약 B)** — `isSafeRefText` 신설 + **`s32-roundtrip-blocks.mjs` 계열 ⑥이
      이 함수를 import하도록 전환**(도메인 단일 출처) + s32 **723건 전건 재통과 확인**.
      → 계열 ⑥-b 신설(기존 표는 그대로 두고 함수 판정 일치 + "통과시킨 라벨은 반드시 왕복" 계약을
      79종 스윕으로 기계 고정) · s32 **723 → 751건 · 실패 0**(기존 723건 무변).
- [x] G-6. **inlineImage 원자 인라인 스펙(규약 D)** — 보존·표시·삭제만. 새 삽입 UI 없음.
      → 완료(`specs/inline.tsx` `inlineImageSpec`, content `'none'`). 블록 승격 없음(문단이 쪼개지면 왕복이 깨진다).
- [x] G-7. **수식(규약 F)** — 도입 판정 기록(라이선스·전이 의존·성숙도 3건) 후 채택 또는 자체 구현.
      `mathBlock`·`inlineMath` 왕복 성립이 판정 기준.
      → **`@blocknote/math-block@0.54.0` 채택**(MPL-2.0 · 전이 의존 GPL 0건 · 코어와 동일 릴리스 라인).
      `--save-exact`로 0.54.0 고정. `mathBlock`(content `'plain'`) · 인라인 `math`(content `'plain'`)
      둘 다 실제 스키마 적재 왕복 성립. `@blocknote/code-block`는 계획대로 **미도입**.

### 2. 어댑터·왕복 확장

- [x] G-8. 어댑터(`toBlockNote`/`fromBlockNote`)를 **전 팔레트**로 확장 — stage-33의 코어 범위 + 위 스펙 전부.
      stage-33에서 둔 "미지원 서식 = 읽기 전용 폴백" 경로는 **방언 팔레트에 대해 제거**한다
      (방언에는 더 이상 미지원이 없어야 한다). **단 규약 I의 코어 범위 제약 7종은 예외** — 흡수 4종은
      보존으로, 판정 대기 3종은 **명시 보고 유지**로 남으므로 `report()` 창구 자체를 지우지 않는다.
      → 완료. 코퍼스 131종 중 **코어 60 → 123종**, 미지원 보고는 판정 대기 3종(`listItem:spread` 4 ·
      `listItem:groupBreak` 6 · `table:align` 1)만 남았다. `report()` 창구·구조적 사유
      (`link:nested`·`listItem:orderedChecked`·`heading:level`·`inline:unknown`·`block:unknown`)는 그대로.
      신규 사유 1건 추가: `inline:mathStyles`(채택 스펙 propSchema가 비어 인라인 수식에 걸린 서식을 담지 못한다 —
      코퍼스 표본 0건, 구조적 사유).
- [x] G-8b. **규약 I 이행** — 흡수 4종(`hardBreak`·`link:title`·`image:title`/`height`·`block:meta`)의 왕복
      보존 경로를 어댑터에 1곳으로 넣고(사이드카 또는 원자 인라인 — 방식 선택과 근거를 완료 기록에),
      판정 대기 3종은 보고를 유지한다. **흡수 불가 판명 시 사유·표본 수를 정리해 사용자 보고**(임의 강등 금지).
      → 완료. 방식: `hardBreak` = **원자 인라인**(편집기 타입명 `lineBreak` — BlockNote 코어에 이미
      `hardBreak` PM 노드가 있고 되읽기에서 무조건 `\n`으로 접히는 것을 실측해 이름을 피했다) ·
      `link:title`·`block:meta` = **사이드카**(`ToBlockNoteResult.sidecar`, 블록 id 키. quote 끌어올리기
      경로는 `${quoteId}-p`로 이관) · `image:title`/`height` = 내장 image 스펙 **prop 확장**
      (`codeBlockSpecWithInfo` 전례). 판정 대기 3종은 보고 유지, `table:align`만 사이드카로 값도 보존
      (열 수가 바뀌면 복원하지 않는다).
- [x] G-9. `frontend/scripts/s33-adapter-roundtrip.mjs` **표본 확장** — M32 공용 코퍼스 **전 표본**(방언 포함)으로
      md→블록→BN→블록→md 왕복. 계약: **id 제외 블록 동등 + M32 정규형 동등 + 조용한 손실 0**.
      총 검사 수·실패 수 보고(실패 0이 DoD).
      → **677 → 961건 · 실패 0**. 코어 123/123 블록 동등·정규형 동등·고정점, 실제 `noteSchema` 적재
      왕복 123/123 + BN 출발 28/28.

### 3. 입력 파이프라인

- [x] G-10. **붙여넣기(규약 G)** — HTML 화이트리스트 경로 결선(기존 변환기 import만) · plain은 평문 · 이미지 분기.
- [x] G-11. **이미지 3진입점(규약 H)** — `uploadFile` 훅 1곳 + 순차 루프 + 진행/실패 표시(`role="status"`) +
      크기 조절 왕복(`previewWidth` ↔ `{w=}`).

### 4. 패리티·게이트

- [ ] G-12. **현행 기능 패리티 체크리스트 작성·실행**(M33 게이트 항목) — 최소 항목:
      F52 방언 전항(밑줄·형광 7색·스포일러·글자색·바탕색·크기 3종·콜아웃 3종 + fold/hide) ·
      F43 참조 3종(링크 칩·앵커·임베드, 별칭 유무 각각) · 수식(인라인·블록) · 표(헤더·정렬) ·
      목록 3종·중첩·체크박스 · 코드 블록(lang) · 인용 · 구분선 · **이미지 3진입점** · 붙여넣기(Word/웹 표본 2종) ·
      undo/redo · 각 항목의 **저장→새로고침→프로젝션 미리보기 일치**. 결과 표를 완료 기록에 남긴다.
- [ ] G-13. **검증 일괄** — `tsc -b`·`npm run build` 성공 · **초기 청크 증가 ≤ 5KB(min) 유지**(R37) ·
      `invariant-scan.ps1` PASS · s32(723)·s30(365) 회귀 무변 · `run-tests.ps1` 통과.
- [ ] G-14. **문서 반영** — 본 문서 체크박스 + 완료 기록(도입 의존 표·패리티 결과·프로젝션 손실 목록 초안 ·
      **규약 I 처리 결과 표**(흡수 4종의 방식·판정 대기 3종의 잔존 보고) + **stage-35 인계**) ·
      `editor-v2.plan.md` §8 M33 행 종결 표기 · 계획서 §14 M33 행 상태 갱신 · 필요 시 screens §5.16 실측 정정.

## 이 단계에서 하지 않는 것 (M35 이후 — 착수 금지)

- **슬래시 메뉴 전면 커스터마이즈·블록 드래그 재배열·찾기/바꾸기·안정 undo 강화** — M35(stage-36).
  (기본 제공되는 슬래시·드래그는 **끄지 않는다** — 있는 그대로 쓰되 정비는 M35.)
- **TOC 블록 · 웹사이트 임베드 · 이미지 크롭 · 다단 · 문제 블록** — M35 이후. 스키마 등재도 하지 않는다(YAGNI).
- **표 열 정렬 편집 UI**(규약 I의 `table:align`) — M35(stage-36). 이 단계는 **왕복 보존까지만**.
- **`documents` 연결·지연 마이그레이션·`content_blocks` 컬럼·구 편집기 개선** — M34(stage-35) / D9 격리.
- **리더(`MarkdownView`·`remarkStudy`)의 이미지 `{w=}` 반영** — M34(stage-35)로 이월 유지.
- **모바일 터치 마감** — M36(stage-37). 이 단계는 폰에서 "쓸 수 있다"까지만 확인한다.
- "Markdown으로 붙여넣기" 명령 · 노트 인쇄 · 노트 FTS 검색 · 노트↔문서 변환 · 복구(휴지통) UI — 실수요 확인 전 금지.
- `@blocknote/xl-*`(GPL) 및 라이선스 미확인 패키지 도입 — **영구/절대 금지**(D10·R40).

## DoD

1. **패리티 체크리스트 전건 통과**(G-12) — 미충족 항목이 있으면 항목별 사유와 대안을 표로 보고(임의 축소 금지).
2. **s33 어댑터 왕복(확장 표본) 전건 통과** — 실패 0 · **조용한 손실 0** ·
   **방언 팔레트의 미지원 폴백 경로 잔존 0**(2026-08-16 정정 — 종전 문구 "미지원 0"은 규약 I의 코어 범위
   제약 7종과 모순된다. 7종 중 **흡수 4종은 왕복 보존으로 미지원 목록에서 빠지고**, **판정 대기 3종은
   명시적 미지원 보고가 남아 있는 것이 정상**이다. 보고가 남은 항목은 완료 기록에 표로 남긴다).
3. **s32(723)·s30(365) 회귀 무변** — 특히 G-5의 도메인 함수 전환 후 계열 ⑥이 동일 결과.
4. **참조 칩 UX ①~④ 실동작** — 피커로 대상 선택 · 선택 텍스트 승계(도메인 통과 시) 및 거절 경로 ·
   추종형 제목 변경 추종 · 지정 라벨 · 칩 팝오버 동작 · **텍스트가 조용히 사라지는 경로 0**.
5. **이미지 3진입점 동작** + 크기 조절 왕복(`{w=}`) — 서버 계약 §4.27 **무변경**(요청 형태 diff 0).
6. **붙여넣기** — Word·웹 표본에서 서식이 방언으로 보존되고, 매핑 밖 태그는 텍스트로만 남으며, 원시 HTML이
   블록·프로젝션 어디에도 들어가지 않는다.
7. **의존 도입 기록** — 추가한 패키지의 이름·버전·라이선스·전이 GPL 0건 확인 결과를 완료 기록에 표로.
   추가 0건이면 "0건"으로 기록.
8. `tsc -b`·`npm run build` 성공 · **초기 청크 증가 ≤ 5KB(min)** · `invariant-scan.ps1` PASS · `run-tests.ps1` 통과.
9. **격리 확인** — `git diff -- . ':!frontend/dist'`가 `editor2/**` + `frontend/scripts/**` +
   (도입 시)`package.json`·lockfile 밖으로 나가지 않음. **백엔드 diff 0 · DDL 0 · API 0.**
10. **프로젝션 손실 목록 초안 작성** — 이 단계 팔레트에서 손실 0이 계약이나, 발견분이 있으면 §5.2 규칙 형태로 기록
    (M36 DoD "프로젝션 손실 목록 문서화"의 입력).
11. 문서 반영 완료(G-14).
12. **(사용자 이행 — M33 게이트 본체) 노트 실사용 통과** — PC + 폰에서 실제 학습 노트를 1편 이상 작성하며
    ⓐ 한글 IME(조합 중 서식·확정·blur·Enter) 이상 없음(**R35 2차 관문**) ⓑ 저장→재열람 왕복 무손실
    ⓒ 참조 칩·이미지·붙여넣기가 실사용에서 불편 없음. **미통과 항목은 원인과 함께 보고 — 게이트를 임의로 통과시키지 않는다.**

## 게이트/판정 규칙 (M33 게이트)

| 상황 | 판정 | 후속 |
|---|---|---|
| DoD 1~11 충족 + 12 사용자 확인 | **M33 종결** | M34(= stage-35, documents 탑재·저장 전환) 착수 가능. D1 최종 확정은 그 단계 게이트 |
| 패리티 일부 미충족(엔진 한계) | 항목별로 ⓐ 어댑터 우회 ⓑ 자체 커스텀 스펙 ⓒ 범위 이월 중 택일해 **사유와 함께 보고** | ⓒ 선택 시 M35 범위에 등재하고 M33 게이트는 "부분 통과"로 기록 — **임의 종결 금지** |
| 한글 IME에서 치명 결함 재현 | **M33 게이트 불합격** — R35 실현으로 보고 | 엔진 재론(후보 C)은 사용자 결정 사항. M34 착수 금지 |
| 왕복에서 조용한 손실 발견 | **스키마·어댑터 보강이 원칙**(강등 도피 금지 — M32 게이트 규칙 계승) | 보강 불가 시 손실 규칙 후보 + 영향 표본 수를 정리해 사용자 보고(R34) |

## 구현 순서·분배 힌트

- G-1·G-3(스펙) → G-2·G-5(필터·도메인) → G-4(피커) 순. **G-5는 G-4보다 먼저** — 피커가 도메인 함수를 쓴다.
- G-8·G-9(어댑터·왕복)는 각 스펙이 하나씩 붙을 때마다 **증분 실행**한다(M32의 TDD형 루프 계승 — 스펙을 다 만든
  뒤 한 번에 돌리면 실패 원인 분리가 어렵다).
- G-10·G-11(입력 파이프라인)은 스펙 완료 후 착수(붙여넣기 결과가 스펙에 의존).
- 완료 후 리뷰는 `stage-reviewer`(Opus). 말미에 "완료 기록"을 추가한다.
