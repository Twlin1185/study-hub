# Study Hub — 상세 설계 §5~7 화면 · 테마 · 상태 관리

> `study-app.design.md`(색인 · §1~3 공통 규약)에서 2026-07-31 분할된 **§5 화면 상세(12개) · §6 테마·디자인 토큰(F28) · §7 프론트 상태 관리**다(내용 이동만 — § 번호는 분할 전과 동일, 판번은 색인 문서를 따른다).
> 부분 읽기: 화면은 `### 5.` 패턴 Grep(예: §5.9 → `### 5.9`).

## 5. 화면 상세 (13개 + 전역 패널 1개 + 반입 서브플로 위저드 1개 + **베타 표면 1개** — S20에서 §5.13 제안함 절 신설: 화면 자체는 S6부터 존재·문서화는 F46 수신함 2탭 일반화 시점에 추가 · **S22에서 §5.14 LLM 작업 센터 신설: 라우트가 아닌 전역 패널** · **S23에서 §5.15 분할 반입 위저드 신설: 상시 라우트가 아닌 §5.9 `too_large` 대안 서브플로** · **S33에서 §5.16 노트(베타) 신설: 상시 메뉴에 없는 격리 표면 — 에디터 v2 드래프트 격리(D9)**)

라우팅: React Router. 모바일(<768px)은 하단 탭바(홈/커리큘럼/퀴즈/**복습**/오답노트 — 복습 탭은 S9, F36-②: 홈 경유 없이 오늘의 복습 직행) + 트리 드로어.

**문서 3대 공용 모듈(S9, F37 구현 원칙)**: **DocViewer**(Markdown 렌더 + 문제 정답 가림) · **QuizCard**(풀이+채점+해설) · **DocEditor**(작성/수정 폼). 앞 둘은 이미 공용(퀴즈/학습/복습/재도전 재사용 중) — S9에서 **DocEditor를 문서 상세 전용 폼에서 모달/패널 모듈로 분리**해 커리큘럼·탐색·검색 결과·오답노트 어디서든 "그 자리에서 보고·풀고·수정"이 되게 한다.

**공통 레이아웃 — 사이드바 접힘(S7, F33)**: 태블릿·PC(≥768px)의 좌측 사이드바에 접힘 토글(« / »).
- 접으면 **아이콘 전용 레일**(라벨 숨김, `title` 툴팁 유지) — 완전 숨김이 아니라 레일 유지: 다시 펼칠 진입점이 항상 보이고, 내비게이션은 한 번의 클릭 거리를 유지.
- 상태: zustand `sidebar` 스토어 + **localStorage `sidebar`**(`'expanded' | 'collapsed'`) — 기기별 UI 선호이므로 서버 settings가 아닌 localStorage(theme 관례, §6).
- 저장값 없을 때 기본: **768~1023px(태블릿) = collapsed, ≥1024px(PC) = expanded.** 모바일(<768px)은 사이드바 자체가 없으므로 토글 미노출(하단 탭바 유지).

**도움말 진입점(S12, F39 — §4.15)**: 사이드바 **하단**에 "도움말" 항목(물음표 아이콘) — `/manual`을 **새 탭**(`target="_blank" rel="noopener"`)으로 연다(학습 중 화면·세션 상태를 떠나지 않게 — 앱 내 라우트 아님). 접힘 레일에서는 아이콘+`title` 툴팁. 모바일(<768px — 사이드바 없음)은 설정 화면의 매뉴얼 링크(§5.11)가 진입 경로(하단 탭바에 도움말 탭은 추가하지 않는다 — 탭 5개 유지).

**작업 현황 진입점(S22, F48 — §4.24·§5.14)**: 사이드바 **하단 고정 버튼** "LLM 작업"(도움말 항목 **위** — F39 하단 배치 관례) — 진행 중(running+queued) 잡이 있으면 건수 배지, 접힘 레일에서는 아이콘+`title`+배지 점. 모바일(<768px — 사이드바 없음)은 **좌측 드로어에 같은 항목**(하단 탭바에 작업 탭은 추가하지 않는다 — 탭 5개 유지·F39 관례. 드로어 내 구체 위치는 기존 드로어 구성에 맞춰 구현 실측). 누르면 §5.14 전역 패널이 열린다 — 라우트 이동이 아니므로 현재 화면·퀴즈/시험 세션 상태를 떠나지 않는다.

### 5.1 홈 대시보드 — `/`
- **구성(우선순위 순)**: ① 이어하기 카드(최대 3, 탭하면 `/study/:categoryId`로 즉시 복귀) ② "오늘의 복습 N개" 버튼(S5) ③ 학습 히트맵(S4 — 당초 12주 고정, S7부터 반응형 주 수) ④ D-Day 배지들(시험 분류 + 임의 D-Day 병합 — §4.8, S4 완성) ⑤ 북마크 모아보기 진입(S4)
- **API**: `GET /api/dashboard`
- **엣지**: 데이터 0건이면 온보딩 카드("기출 JSON을 반입해 시작하세요" → `/import`).
- **위젯 레지스트리(S7, F31)** — 홈 섹션을 9개 위젯 컴포넌트로 분리, id 고정:
  `continue`(이어하기) · `today_review`(오늘의 복습) · `dday`(D-Day) · `heatmap`(학습 히트맵 — 반응형 주 수) · `exam_progress`(시험별 진도 도넛) · `recent7d`(최근 7일 풀이/정답률) · `accuracy_trend`(최근 정답률 추이) · `weakness`(자꾸 틀리는 개념 Top 10) · `bookmarks`(북마크 모아보기 진입) · **`daily_start`(S9 — 아래, 레지스트리 10종으로 확장)** · **`streak`(S10 — 아래, 레지스트리 11종으로 확장)**.
  기존 "데이터 0건이면 위젯 미표시" 관례는 유지하되, 사용자 숨김(`visible:false`)이 우선하며 숨긴 위젯은 **데이터 쿼리도 실행하지 않는다**(비마운트 또는 `enabled:false` — 쿼리 미실행이면 구현 방식 무관, S7 구현은 비마운트). 온보딩 카드는 위젯이 아님(편집 대상 제외).
- **편집 모드(S7, F31)**: 홈 헤더 [편집] → 위젯마다 드래그 핸들(≡)·숨김(눈) 토글, 상단 열 수 세그먼트(자동/1/2/3), 하단 "숨긴 위젯" 목록(탭하면 복귀), [기본값 복원], [완료]=`PUT /api/settings`(`home.layout`, §4.10 — 저장은 완료 시 1회, 취소 시 드래프트 폐기).
  - 드래그는 **Pointer Events 직접 구현**(pointerdown/move/up + setPointerCapture, 핸들 `touch-action:none`) — **외부 D&D 라이브러리 금지**. 같은 열 내 순서 변경 + 열 간 이동(col 배정), 드래그 중 자리표시자 표시.
  - 폴백: 핸들 옆 ▲▼(순서)·◀▶(열 이동) 버튼 — 터치 스크롤 충돌·접근성 대비. 모바일은 이 버튼만으로도 전체 편집 가능. **단 auto 모드에서는 배치 조작 전체가 비활성** — 아래 "편집 모드의 열 조작 규칙" 참조.
- **다열 렌더(S7, F31)**: `columns:'auto'` = **콘텐츠 영역 실측 폭 기준**(사이드바 제외 가용 폭을 ResizeObserver로 관측 — 창 크기 변화와 사이드바 접힘/펼침에 실시간 반응): `<704px` 1열 · `704~1343px` 2열 · `≥1344px` 3열. 고정 1/2/3. **콘텐츠 폭 <640px는 항상 1열 강등**(col 값은 보존, order·col 순으로 평탄화). 열 수 축소 시 초과 col은 마지막 열로 클램프. 컨테이너 폭 1열 `max-w-2xl`(672) · 2열 `max-w-4xl`(896) · 3열 `max-w-6xl`(1152) + `mx-auto`.
  - **컨테이너 최대 폭은 그 열 수가 흔히 쓰이는 콘텐츠 폭보다 확실히 작게** 잡는다 — 일반 PC 폭(콘텐츠 ≥896px)에서 항상 수십~수백 px의 좌우 여백이 남아 **가운데 정렬이 눈에 보인다**. 검산: 콘텐츠 1088px(1280 창)→2열 여백 96px씩 · 1344px(1536 창)→3열 여백 96px씩 · 1728px(1920 창)→3열 여백 288px씩. (v1.6 초안 5xl/7xl은 현실적인 창 폭 대부분에서 컨테이너가 영역을 꽉 채워 여백 0~32px — 정렬 체감 불가. 2026-07-24 사용자 피드백 3차 반영.)
  - **자동 분배(3차 피드백 반영)**: 표시 위젯들의 col 값이 사실상 미지정(전원 동일 col — 기본 레이아웃이 이 상태)인데 렌더 열이 2~3열이면, 배열 순서대로 **라운드로빈 자동 분배**한다(0,1,2,0,1,…) — 우선순위 높은 위젯이 각 열 상단에 오고, 창이 넓어질 때 위젯이 자연스럽게 열로 퍼진다. 사용자가 편집 모드에서 열을 지정한(서로 다른 col 존재) 레이아웃은 자동 분배하지 않고 지정값 그대로. 편집 모드는 현재 화면과 동일한 분배로 렌더(WYSIWYG).
  - **편집 모드의 열 조작 규칙(4·5차 피드백 반영)**: `columns:'auto'`에서 배치는 **미리보기 전용(비활성)** — 드래그 핸들·▲▼·◀▶ 전부 미동작/미노출로 현재 자동 배치를 보여주기만 한다. 배치를 수정하려면 고정 1/2/3을 선택해야 하며(전환 시 현재 보이던 분배가 시작점), 고정 모드에서만 드래그·▲▼·◀▶가 활성화되고 열 간 이동이 col을 실체화한다. 숨김 토글(눈)·숨긴 위젯 탭 복귀는 열 배치와 무관하므로 auto에서도 동작한다(단 드래그 기반 숨김/복귀는 고정 모드 전용).
  - **숨긴 위젯 드래그(4차 피드백 반영)**: 편집 모드 하단 "숨긴 위젯" 목록의 항목을 그리드로 드래그하면 해당 위치에 visible로 복귀하고, 그리드의 위젯을 숨김 영역으로 드래그하면 숨김 처리된다(탭 복귀·눈 토글도 유지).
  - 태블릿 DoD 정합: 뷰포트 768px + 접힘 레일(64px) → 콘텐츠 704px → 2열 유지.
  - (v1.5까지는 뷰포트 innerWidth 기준 — 사이드바 폭 때문에 PC에서 여백이 안 보이고 리사이즈 반응이 체감되지 않아 콘텐츠 폭 기준으로 변경.)
- **히트맵 반응형 주 수(S7)**: 히트맵은 12주 고정이 아니라 **위젯 컨테이너 폭에 맞춰 표시 주 수를 유동 조정**한다. 열 수·사이드바 접힘에 따라 위젯 폭이 달라지므로 기준은 뷰포트가 아닌 **위젯 컨테이너 폭(ResizeObserver)**.
  - 셀 크기·간격은 고정(토큰 기반) — `표시 주 수 = floor((컨테이너 폭 − 요일 라벨 폭) / (셀 + 간격))`을 **최소 8주 ~ 최대 26주**로 클램프. 항상 **최근 N주**를 오른쪽 끝(오늘)에 정렬해 렌더.
  - 데이터는 기존 `GET /api/stats/heatmap?from&to`로 **최대 범위(26주)를 1회 조회**하고 클라이언트에서 최근 N주만 잘라 렌더 — 리사이즈·열 변경 시 재요청 없음. **새 API·파라미터 변경 없음.**
  - 주 수 **수동 지정**(위젯별 설정)은 v1.x 후보로 계속 제외 — 이 항목은 자동 조정만 다룬다.
- **스트릭 위젯(S10, F26) — `streak`**: "N일 연속 학습" 대표 수치 + 오늘 진행(목표 설정 시 "문제 12/20 · 15분/30분" 게이지, `goal_met`이면 달성 배지) + 최고 기록 소표기. 데이터 = `GET /api/stats/streak`(§4.13). 스트릭 0 **그리고** 목표 미설정이면 위젯 미표시(기존 0건 관례). 위젯 순서 기본값은 `heatmap` 바로 위 — 기존 저장 레이아웃에는 전방 호환 규칙(누락 id 보충)으로 자동 등장.
- **히트맵 목표 연동(S10, F26)**: heatmap 응답의 `goal_met=true` 날짜 셀에 **링(테두리) 강조** + 툴팁에 "목표 달성" 표기 — 토큰 기반 스타일, 목표 미설정이면 기존 렌더 그대로. 새 요청 없음(기존 heatmap 1회 조회의 필드 확장).
- **원탭 데일리 세션(S9, F36-①) — `daily_start` 위젯**: [오늘 공부 시작] CTA + 분량 예고("복습 12 + 2과목 Ch.3 이어하기 · 예상 25분" — `dashboard.today_review`·`continue[0]`·`today_review_minutes` §4.12). 탭 → 복습 큐 잔여가 있으면 `/review`로, 복습 완료 화면의 [이어하기로 계속]이 `/study/:categoryId`로 연결(F36-③, §5.7). 복습 0건이면 곧장 이어하기로. 복습 0건·이어하기도 없으면 위젯 미표시(기존 관례). 위젯 순서 기본값은 `continue`보다 위(첫 번째) — 기존 저장 레이아웃에는 전방 호환 규칙(누락 id = 표시·마지막 순서 보충)으로 자동 등장.
- **D-Day 즉석 편집(S7, F32)**: D-Day 위젯 헤더의 연필 아이콘 → 모달로 **설정 §5.11의 D-Day 관리(DDayManager) 컴포넌트 재사용**(시험 분류 `exam_date` + 임의 D-Day 추가·수정·삭제 — 기존 categories PATCH·`settings:ddays.custom` 경로 그대로, 새 로직 없음). D-Day 위젯만은 데이터 0건이어도(visible이면) "등록된 D-Day 없음 + [추가]"로 표시 — 홈에서 첫 등록 가능. 저장 시 dashboard invalidate → 배지 즉시 갱신.

### 5.2 탐색 — `/explore`
- **구성**: 좌측 분류 트리(노드별 진도바·문서수, 우클릭/⋯ 메뉴로 추가·이름변경·이동·삭제), 우측 문서 카드 그리드(제목·타입 배지·태그 칩·"N곳에서 사용 중" 배지·북마크 별).
- **필터바**: 타입, 태그, 북마크만, **단일 문서만**(어느 분류에도 연결되지 않은 문서 — API 파라미터는 `orphan=1` 유지, UI 표기는 "단일 문서"). "하위 포함" 토글(`deep=1`).
- **API**: `categories/tree`, `documents`, 링크 연결/해제, 문서 생성 모달.
- **상호작용**: 문서 카드를 다른 분류 노드로 드래그 = 연결 추가(이동 아님 — 다대다 원칙 시각화). 모바일은 카드 ⋯ 메뉴 → "분류에 연결".

### 5.3 문서 상세 — `/docs/:id`
- **구성**: 제목+doc_no+타입, 본문 Markdown 렌더(코드 하이라이트, 이미지), 태그 칩(클릭=탐색 필터), 사용처 목록(분류 경로 + local_note 인라인 편집), 관련 문서(relations — 문제면 "이 문제의 개념", 개념이면 "확인 문제")(S4), 풀이 이력 미니차트(최근 10회 ○×, S3), 북마크 별(S4).
- **문제 타입 뷰 모드**: 지문(content) → 보기 목록(①~④ 각각 별도 행) → **정답·해설은 기본 가림** — "정답·해설 보기" 토글(스포일러)로만 펼침. 문서를 열람만 해도 정답이 눈에 들어오지 않게 한다.
- **편집 모드**: 본문·보기·정답·해설 필드 폼. 문제 타입이면 미리보기 탭(실제 퀴즈 카드 모양).
- **SRS 사람 말 표기(S9, F36-⑩)**: 복습 상태를 "3일 후 복습 예정"·"오늘 복습 대상" 형태로 표시 — EF·interval·repetitions 수치는 기본 숨김(접힘 "자세히"로만 노출). 데이터는 기존 `documents/{id}`의 `stats.srs` 그대로 — 표기만 변경.
- **본문 글자 크기(S9, F36-⑨)**: `settings:study.font_scale` 3단계를 본문 Markdown 렌더(문서 상세·학습 모드 공통)에 적용 — 토큰 기반 크기 클래스만 사용.
- **오류 신고·재생성(S6, F30)**: [오류 신고] 버튼(진입점: 문서 상세·학습 모드 §5.5·퀴즈 해설 §5.6) → 사유 입력 모달 → 재생성 잡 생성(진행 중 배지 표시) → 완료 시 **문서 상세에서 기존 vs 신규 나란히 비교** → [교체](apply — 같은 문서 유지) / [폐기]. 자동 덮어쓰기 없음.
- **API**: `documents/{id}`, PATCH, tags PUT, relations, bookmark, regenerate(S6, §4.10).
- **정답 표기 통일(S25 — F51 ①, 계획서 §14 F51 확정 2026-08-04)**: answer 표기는 **공용 포맷터 1곳**(utils — 화면별 복붙 금지)으로 수렴 — 순수 번호("1"~"9") = **"③" 형태만**(종전 "③ (3)" 이중 병기 제거), 그 외 텍스트 answer = 원문 그대로, 빈 값 = 기존 화면별 문구("-"·"미응답") 유지. **적용 지점 전수 7곳** = 문서 상세(`formatAnswer`)·시험 결과(`ExamRun.formatChoiceAnswer`)·플래시카드(`Flashcards`)·오늘의 복습(`Review`) — 이상 이중 표기 4곳 — + 학습 모드 오답 목록(`Study` — 종전 raw 숫자)·퀴즈 결과(`QuizRun` — 종전 raw 숫자) + **AI 풀이 초안 검토(`ExplainJobPanel`, S22 신설 화면 — 최초 열거(6곳) 누락 보완, stage-25 표적 재검토 반영)**. `AnswerKeyImportWizard`의 답지 대사(diff) 뷰는 원본·추출본 원문을 나란히 대조하는 목적이라 raw 표기 유지(공용 포맷터 미적용이 의도). `RegenerateJobPanel`의 `CompareField`("정답" 원본 vs 신규 대조)도 같은 diff 뷰 성격이라 raw 유지가 타당(2026-08-04 검토 관찰 반영). 보기 목록의 ①~④ 마커(`choiceMarker`)는 무변경(이중 표기와 무관).
- **해설·본문 렌더 개선(S25 — F51 ②, B안 확정)**: 공용 `MarkdownView` 1곳에 **remark-breaks**(단일 개행 = 줄바꿈 — 종전엔 Markdown 규칙상 공백으로 접혀 "줄바꿈 안 됨" 증상) + **remark-math + rehype-katex**(수식 `$…$` 인라인·`$$…$$` 블록 — 종전엔 LaTeX가 원문 문자로 노출) 추가 — 렌더러 1곳 확장이 열람·학습·퀴즈·시험·플래시카드·오답노트·인쇄 전 화면에 자동 파급(F43 구현 앵커 원칙), **기존 저장 해설·본문도 재변환·소급 없이 즉시 개선 렌더**. KaTeX CSS는 katex 배포 CSS(색상은 currentColor 기반 — 토큰 정합 확인, 하드코딩 색 발견 시 토큰으로 재정의: 불변 규칙 5). 신규 프론트 의존 4건(remark-breaks·remark-math·rehype-katex·katex — 계획서 등재 후 추가, F43 전례). **백엔드·API·저장 데이터 무변경**(순수 렌더 계층). remark-breaks의 본문 표시 변화는 표본 확인(stage-25 DoD — 필요 시 해설 한정 스코프 분리 실측). C안(해설 전용 LLM 후처리)은 보류 확정(계획서 §14 F51 ③ — 재검토는 계획서 먼저).
- **인라인 표현 문법(S26 — F52, 계획서 §14 F52 확정 2026-08-09 · Design v1.30)**: 구현처 = 자체 플러그인 `components/markdown/remarkStudy.ts` 확장 + 공용 `MarkdownView` 1곳(전 화면·인쇄 자동 파급 — F43·F51 앵커 원칙. rehype-raw는 F43 D2 기각 유지 — 화이트리스트 문법만). **문법 5종 계약**:

  | 문법 | 표기 | 렌더 |
  |---|---|---|
  | 밑줄 | `++텍스트++` | `<u>` 계열 |
  | 형광펜 | `==텍스트==` | 배경 `--mark-yellow`(기본 노랑 — 색 지정은 `:t`) |
  | 인라인 스포일러 | `\|\|텍스트\|\|` | 가림 기본·클릭(키보드 포함) 토글 공개. **채점 경계 아님**(렌더 계층 — quiz/session 정답 부재 계약은 서버가 담당·불변 규칙 1, 매뉴얼 오해 방지 명시) |
  | 통합 인라인 | `:t[텍스트]{c=red bg=yellow s=large}` | `c`(글자색)·`bg`(형광펜) ∈ 팔레트 7색 이름 **∪ `#rrggbb`(S30 개정 2026-08-15 — 자유 hex 허용·다크 자동 보정, §5.3 S30 ⓓ. 3자리 축약·`rgb()`·색 이름 문자열은 불허)**, `s` ∈ `small·large·xl`(기존 font_scale `small\|default\|large` 명명 + xl — F53 size 통일 결정 연동, 기본 크기 = 속성 생략. 크기는 상대 배율(em) — font_scale·F53과 곱 합성). **화이트리스트 밖 값·미지 속성 = 스타일만 무시·텍스트 그대로**(오류 아님) |
  | 콜아웃 | `:::note[제목]`·`:::warn[제목]`·`:::tip[제목]` … `:::` | 좌측 보더+제목·아이콘 색 = 기존 토큰(note=`--accent`·warn=`--warning`·tip=`--correct` 계열)·배경 `--surface-raised` — 콜아웃용 신규 토큰 0. F43 `:::fold`/`:::hide` 계열 이름 화이트리스트 확장 |

  **엄격 플랭킹 규칙**(마이크로 3종 공통): 여는 기호 바로 뒤·닫는 기호 바로 앞 공백 금지 + 빈 내용 불가(remark-math `$` 관례와 동일 — `C++ and D++`·`a == b`·`a \|\| b`는 구조적으로 탈락). 코드 스팬·펜스·수식 내부는 선행 파싱이 소비해 원리상 미매칭(표 파싱이 소비하는 것은 `\|`뿐 — **표 셀 안의 인라인 문법은 적용된다**·바람직한 동작, 2026-08-10 구현 실측 정정. 표 행 포함 재실측도 우연 매칭 0건). 백슬래시 이스케이프로 리터럴 표기. **기존 저장 문서 표본 실측이 착수 계약**(R26 ② — stage-26 0절: 충돌 실측 시 기호 변경/스코프 축소, 조용한 전면 적용 금지). **퇴로**: `MarkdownView`에 `inlineFormat?: boolean`(기본 true) prop — 신규 문법 전체를 1줄로 off(F51 `breaks?` 전례). 임베드 카드 안에서도 그대로 적용(내용의 일부 — 재귀 렌더에 prop 전달). 검색(FTS5 trigram)은 recall 무영향·스니펫 문법 문자 노출은 경미·수용. 인쇄는 §6 참조(`print-color-adjust: exact` — 형광펜·글자색 한정)·인라인 스포일러는 인쇄에서 전부 공개(F43 fold/hide 인쇄 공개 관례 연장). **표현 문법은 사용자 손편집 전용**(F52 결정 ⑧ — 변환 프롬프트는 순수 내용만·무변경). 프론트 전용 — DDL 0건·API 0건·LLM 0·신규 의존 0 목표(자체 플러그인 충족 여부는 구현 실측 — 불가피 시 계획서 등재 후 추가).
- **DocEditor 편집 UX(S26 — F52 ④)**: 공용 DocEditor 1곳 확장(전 사용처 자동 파급 — 사용처별 분기 금지). **툴바** = 선택 영역 래핑 버튼: B(`**`)·기울임(`*`)·취소선(`~~`)·밑줄(`++`)·형광펜(7색 드롭다운·기본 노랑 원클릭)·글자색(7색)·크기·인라인 스포일러·콜아웃 3종 + 기존 참조 삽입(F43) 유지 — 기존 `insertAtCursor`를 선택 영역 래핑(wrapSelection)으로 확장(선택 없으면 자리표시 텍스트 삽입 후 선택). **단축키** = Ctrl+B(`**`)·Ctrl+I(`*`)·Ctrl+U(`++`)·Ctrl+Shift+H(`==`) — 그 이상은 툴바 전용(추가 단축키는 실수요 후). **분할 라이브 미리보기** = textarea 옆 공용 MarkdownView 실시간 렌더(같은 렌더러 재사용 — 미리보기 전용 렌더 경로 금지, <768px는 탭 전환 허용·구현 재량). **저장 소스는 Markdown 텍스트 유지** — F43 "WYSIWYG 기각" 불번복(체감 개선만). **편집 UX 보완(S26 후속 — 2026-08-10 사용자 피드백 확정)**: ⓐ 문법 안내는 나열식 힌트가 아니라 **접이식 도움말 패널**(기본 접힘 — 카테고리 그룹: 참조·임베드/구간 블록/인라인 꾸미기/콜아웃, 항목 = 문법 코드 칩 + 이름) ⓑ **뷰 모드 3종** [편집|분할|미리보기] — 원본 소스 표시·숨김 토글 ⓒ 툴바는 그룹 구분(서식/색·크기/삽입) + 툴팁 ⓓ **본문·해설 양 필드 동일 편집기**(공용 서브컴포넌트 — 필드 분기 금지) ⓔ **팝업 모달과 전용 라우트(창) 양쪽 지원** — 모달 헤더 "창으로 열기", 수정·신규 작성 모두 전 기능 동작(저장·검증 로직 공용) ⓕ **닫힘 보호(2차 보완)**: 오버레이 닫힘은 mousedown·mouseup 모두 오버레이일 때만 + Esc 외 키 닫힘 금지 + 미저장 변경 시 닫기 전 확인 ⓖ 편집 모달은 광폭(다른 모달 불변)·분할 균등, 툴바에 목록 3종(글머리·숫자·들여쓰기 — 줄 prefix 토글, 파싱 변경 0), 넓은 화면 기본 뷰 모드 = 분할·마지막 모드 기억(localStorage).
- **편집 가능 미리보기 — 블록 단위 소스 편집(S27 — F55, 계획서 §14 F55 확정 2026-08-11 · F43 "WYSIWYG 기각" 부분 개정)**: 적용 지점 = **`MarkdownFieldEditor`의 '미리보기' 뷰 모드 한정**(분할 모드 미리보기 패널·읽기 전용 화면(MarkdownView 전 사용처)은 불변). **계약**: ⓐ 미리보기를 최상위 블록 단위로 렌더(각 블록 = 공용 MarkdownView 재사용 — 미리보기 전용 렌더 경로 금지) ⓑ 블록 분해 = `{start,end}` 오프셋 목록(빈 줄 구분 + 코드 펜스(```·~~~)·`:::` 컨테이너는 내부 빈 줄 포함 **원자 블록** — 닫히지 않은 펜스·컨테이너는 문서 끝까지 한 블록(보수적 원자화, R28)) ⓒ 블록 클릭 → 그 블록만 자동 높이 textarea(로컬 초안)로 전환, **확정**(blur·Ctrl+Enter) 시 원본 value의 오프셋 구간만 문자열 치환·**Esc = 취소** — onChange는 확정 시점에만(타이핑마다 전체 재파싱·임베드 API 호출 없음), 재조립은 구간 치환뿐(**무편집 = 원본 1바이트 불변**이 계약). **창 포커스 상실 예외(정밀화 2026-08-14, S29 실사용 계기 — 번복 아님)**: 여기서 "확정(blur)"의 blur는 ***페이지 안에서*** 편집을 떠난 경우만을 뜻한다 — **브라우저 창 전체가 포커스를 잃은 경우(`document.hasFocus() === false`: 파일 탐색기·다른 앱 클릭·알트탭 — `focusout`의 `relatedTarget`이 null로 오는 경로)는 확정 사유가 아니며** 블록 편집 상태·캐럿·초안을 그대로 유지해 창으로 돌아오면 이어서 편집한다. 나머지 확정 경로는 전건 유지(편집기 루트 밖 클릭(document pointerdown capture)·다른 블록 클릭·Ctrl+Enter·뷰 모드 전환·언마운트 안전망·저장 버튼) — **초안이 조용히 사라지는 경로는 여전히 0**이고, 달라지는 것은 "다른 앱에 갔다 돌아오면 편집이 유지된다" 하나뿐이다 ⓓ 툴바·단축키 4종·참조 삽입 = **활성 블록 textarea 대상**(포커스가 툴바로 이동해도 블록 유지 — mousedown 가드, 활성 블록 없으면 role=status 안내) ⓔ 말미·빈 문서에 "클릭해서 입력…" 자리표시자 블록(확정 시 빈 줄 구분 삽입) + 내용 전부 삭제 후 확정 = 블록 제거(잉여 빈 줄 정리) ⓕ 인터랙티브 요소(링크·임베드 [원문 열기]·스포일러·fold/hide 토글 등) 클릭은 편집 진입 아님(기존 동작 유지 — 편집 진입은 비인터랙티브 영역 클릭만, 모바일 탭 동일). **알려진 한계(수용·매뉴얼 명시)**: 확정 단위로 브라우저 undo 스택 단절(블록 편집 중 네이티브 undo 정상) · 느슨한 목록의 블록 분리 개별 렌더 · 블록 경계 걸친 참조 링크·각주(앱 관례상 사용 0). 프론트 전용 — DDL 0·API 0·LLM 0·신규 의존 0 목표. **S30 부분 개정(2026-08-15 — 계획서 §14 F55 개정 예고의 이행)**: ⓒ의 "블록 클릭 → **소스 textarea** 전환"은 **리치 표면 대상 블록(문단·헤딩·목록·인용 — 판정 술어는 S30 ⓐ)에서만** 리치 편집 표면으로 대체된다. 그 블록들의 계약 정본은 아래 **S30 절**이고, 나머지 블록(표·코드펜스·수식·임베드·콜아웃·이미지 포함 블록)은 **이 절이 계속 정본**이다. ⓐ·ⓑ·ⓓ·ⓔ·ⓕ와 "무편집 = 원본 1바이트 불변"·확정 수명주기는 **전건 유지**(S30이 그 위에 얹힌다).
- **문서별 스타일 폼(S28 — F53 ②, §4.26 계약 정본 · 계획서 §14 F53 확정 2026-08-09)**: DocEditor(편집 모드)·문서 상세에서 **폼 필드**로 그 문서의 폰트·글자크기·배경을 전역과 **독립** 지정 — `font ∈ sans·serif·mono`(시스템 폰트 스택 프리셋 3종) · `size ∈ small·default·large·xl`(기존 FontScale 명명 + xl) · `bg ∈ 팔레트 7색 이름`(색상 견본 = `--doc-bg-*` 토큰 — §6). 각 항목 "전역 따름(기본)" + **부분 지정·개별 해제**(저장 = documents PATCH `style` — **본문 directive 방식 기각 확정**: 스타일은 본문 내용이 아니라 문서 메타, 폼이 본문 텍스트를 조작하지 않는다). **적용 = 그 문서 본문 렌더 영역만**(앱 크롬 불변 — R27 ③), 우선순위 = **문서 지정값 > 전역 설정 > 기본 토큰**. `style.size` 지정 문서에서는 그 값이 `study.font_scale`(F36-⑨)을 **대체**(미지정 문서는 기존 동작 — §4.26 ④). **임베드 카드 안에서는 문서 스타일 무시**(resolve-embeds 응답에 style 부재 — 계약 봉인, §4.26 ②) · **인쇄는 배경 무시·폰트/크기 유지**(§6). 색상 전부 토큰(불변 규칙 5).

- **이미지 첨부 — 붙여넣기·드래그앤드롭(S29 — F54, §4.27 계약 정본 · 계획서 §14 F54 확정 2026-08-09)**: 진입점 = 공용 **`MarkdownFieldEditor` 1곳**(DocEditor 본문·해설 전 사용처 자동 파급 — 사용처별 분기 금지. 읽기 전용 화면·반입 §5.9는 무변경). **ⓐ 붙여넣기**: 클립보드에 이미지 파일이 있을 때만 가로채(`preventDefault`) 업로드 — **텍스트·HTML 붙여넣기는 기존 동작 그대로**(캡처 후 Ctrl+V가 실사용 핵심 경로). **ⓑ 드래그앤드롭**: 편집기 영역 `dragover` 기본 동작 차단(브라우저가 파일을 열어 앱을 이탈하는 것 방지) + 드롭 처리, 드래그 중 시각 피드백은 **기존 토큰만**(`border-accent`·`bg-accent-soft` 계열 — 신규 색 하드코딩 0, 불변 규칙 5). **ⓒ 삽입 = `insertAtCursor` 경유 고정**: 활성 편집 표면 추상화(`getSurface()` — 편집·분할 모드 = 본문 textarea / **미리보기 모드 = 활성 블록 textarea**, S27 자산)를 그대로 재사용해 표면 3종에 대한 분기 코드를 새로 만들지 않는다. 삽입 문자열 = **`![](/images/…)`**(alt 비움 — 크기·정렬 문법 없음). **ⓓ 업로드 전 표면 확인**: 미리보기 모드에서 활성 블록이 없으면 기존 안내(`role="status"` — "편집할 블록을 먼저 클릭하세요")만 띄우고 **업로드 요청을 보내지 않는다**(삽입하지 못할 파일을 디스크에 쌓지 않는다). **ⓔ 진행·실패 표시**: 업로드 중 `role="status"` 1줄(기존 안내 영역 재사용 — 신규 컴포넌트·진행률 바 없음), 실패는 **서버 `message`를 그대로** 렌더(§3 — 프론트 문구 창작 금지: 10MB 초과·비이미지 사유가 서버 완성 문장으로 온다). **ⓕ 다중 파일 = 순차 업로드**(동시 다발 금지 — 삽입 순서 보장, 요청당 파일 1개가 서버 계약 §4.27 ①), 한 장이 실패해도 나머지는 계속하고 실패분만 파일명 + 사유 안내. **ⓖ 비이미지 드롭·이미지 없는 붙여넣기 = 요청 0**(짧은 안내만 — 조용한 무시 금지). **ⓗ 툴바 이미지 삽입 버튼(2026-08-14 사용자 제안 채택 — S29 후속. 진입점은 붙여넣기·드롭·툴바 3종이 된다)**: 위치 = 기존 `MarkdownFieldEditor` 툴바(F52 툴바와 같은 줄 체계·삽입 그룹), 레이블은 기존 툴바 관례에 맞춘 짧은 한국어. **신규 색 0**(토큰만)·**신규 컴포넌트 0**(숨긴 `<input type="file">` 1개). `accept`는 서버 화이트리스트와 같은 4종(png·jpeg·gif·webp), `multiple` 허용 — 선택된 파일은 **ⓒ~ⓖ의 기존 업로드 경로 그대로**(순차 업로드·부분 실패 계속·실패는 서버 `message`·삽입은 `insertAtCursor` 경유)를 타므로 **신규 업로드 경로 0, 진입점만 하나 늘어난다**. 표면 규약도 동일 — **업로드 전 `requireSurface()` 선확인**(미리보기 모드에서 활성 블록이 없으면 안내만·요청 0). 툴바 버튼 클릭은 ⓓ(S27 — 편집기 크롬으로의 포커스 이동은 편집 이탈이 아님)와 S27 창 포커스 상실 예외에 따라 **활성 블록을 해제하지 않는다** — 파일 대화상자를 여는 조작으로 초안을 잃지 않는 것이 계약이며, 이것이 탐색기 드래그앤드롭이 어려운 상황(창 전환)에서도 성립하는 삽입 경로다. 같은 파일을 다시 고를 수 있도록 하는 input 값 초기화는 구현 재량. **API 변경 0 · DDL 0 · 신규 의존 0**. 서버 계약(매직 바이트 판별·10MB·중복 시 같은 URL·svg 제외)은 §4.27.

- **WYSIWYG 인라인 편집 — 리치 편집 표면(S30 — F56, 계획서 §14 F56 착수 전 결정 ①~⑧ 확정 2026-08-15 · **이 절이 계약 정본**: API 0건이라 §4.x 절을 만들지 않는다)**: 적용 지점 = **S27과 같은 자리**(`MarkdownFieldEditor`의 '미리보기' 뷰 모드 한정 — 분할 미리보기 패널·읽기 전용 화면(MarkdownView 전 사용처)은 **불변**). S27을 대체하지 않고 **블록 종류에 따라 자동으로 갈리는 병존**이며, S27 절의 계약(블록 분해·확정 수명주기·툴바 규약)은 **전건 계승**된다 — 아래는 그 위에 얹히는 차이분만 서술한다. **프론트 전용 — DDL 0 · API 0 · LLM 0 · 신규 npm 의존 0**(붙여넣기 변환기는 브라우저 내장 `DOMParser` 사용).

  - **ⓐ 리치 대상 판정(술어 1개 — 신규 블록 분해 로직 0)**: S27 `scanBlocks`가 낸 블록 각각에 대해 "리치 표면 대상인가"만 판정한다. **판정은 기존 remark 파이프라인으로 그 블록 슬라이스를 파싱한 결과로 한다**(별도 정규식 판별 금지 — 렌더와 같은 해석이어야 한다). **참 조건 = 최상위 자식이 전부 `paragraph`·`heading`·`list`·`blockquote`** 이고 **서브트리에 배제 노드가 하나도 없을 것**. **배제 노드** = `code`(펜스)·`table`·`math`/`inlineMath`가 아닌 블록 수식·`containerDirective`(콜아웃·`:::fold`·`:::hide`)·`html`·**`image`**(이미지 전용 블록이든 문단 중간 혼재든 **포함하면 제외** — 보수적 원자화 R28 정신, 코퍼스 실측상 이미지 전용 블록 6.5%·혼재는 드묾) · `footnoteDefinition`·`definition`. 판정이 거짓이면 **S27 소스 textarea 그대로**(회귀 0). 코퍼스 실측(2026-08-15 — 활성 문서 224건·최상위 블록 292개): 문단 76.0%·인용 6.5%·목록 4.1%·헤딩 0 → **대상 86.6%**.
  - **ⓑ 인라인 모델과 직렬화 3층 계약(이 기능의 심장 — R29 대응)**: 블록 진입 시 그 블록 소스를 **기존 remark 파이프라인**으로 파싱해 인라인 노드 목록을 만든다(**신규 파서 금지**). 각 노드 = `{원본 슬라이스, position(start/end offset), 서식 속성, 텍스트, dirty 플래그}`.
    - **선행 작업 1건(실측 근거 — 계획서 §14 F56 ⑨ 1-1)**: 표준 mdast 인라인 노드는 중첩 포함 `position.offset`이 정확하지만 **F52 마이크로 3종(`++`·`==`·`\|\|`)이 만드는 `studyInline` 합성 노드는 position이 없다**(`remarkStudy.ts`가 텍스트 노드를 정규식 분할로 생성). → **remarkStudy 분할부에서 부모 text 노드 오프셋 + 매치 인덱스로 position을 전파**하는 것이 2층 계약의 전제다(파서는 여전히 1개 — 렌더 결과 diff 0이어야 한다).
    - **1층**: 편집하지 않은 블록은 **1바이트도 변하지 않는다**(S27·R28 ① 계승 — 재조립은 원본 문자열 구간 치환뿐).
    - **2층**: 편집한 블록 안에서도 **dirty가 아닌 인라인 노드는 원본 슬라이스를 그대로 재출력**한다(`__굵게__`가 `**`로 정규화되지 않고, 이스케이프 `\*`가 보존된다 — `node.value`에서는 이스케이프가 이미 풀려 있으므로 **모델 값 재직렬화는 금지**하고 슬라이스를 쓰는 것이 규약).
    - **3층(안전 밸브)**: 직렬화 결과를 **다시 파싱해 인라인 트리 동형성을 검사**하고(노드 종류·순서·평문 텍스트 일치), 어긋나면 **그 편집을 적용하지 않고** 해당 블록을 **소스 편집으로 폴백** + `role="status"` 1줄 안내(기존 안내 영역 재사용 — 신규 컴포넌트·신규 색 0). 사용자가 `*`·`[`·`\|`·`==`·`:`를 그냥 입력해 우연히 문법이 되는 경로가 **조용한 본문 변형**으로 번지지 않게 하는 구조적 방어다.
    - **정규 표기 표(dirty 노드에만 적용)**:

      | 서식 | 정규 표기 | 비고 |
      |---|---|---|
      | 굵게 | `**텍스트**` | `__`는 읽기만(미변경 시 슬라이스 보존) |
      | 기울임 | `*텍스트*` | `_`는 읽기만 |
      | 취소선 | `~~텍스트~~` | GFM |
      | 밑줄 | `++텍스트++` | F52 마이크로 |
      | 형광펜(기본 노랑) | `==텍스트==` | 색 지정 시 `:t{bg=…}` |
      | 인라인 스포일러 | `\|\|텍스트\|\|` | F52 마이크로 |
      | 글자색·바탕색·크기 | `:t[텍스트]{c=… bg=… s=…}` | **속성 병합** — 같은 구간에 서식을 겹칠 때 `:t[:t[…]{…}]{…}` 중첩을 만들지 않는다(2026-08-14 툴바 구현분 계승). `c`·`bg` ∈ **팔레트 7색 이름 또는 `#rrggbb`**(ⓓ), `s` ∈ `small·large·xl` |
      | 링크 | `[텍스트](URL)` | 리치 표면에서는 **텍스트만 편집**, URL 변경은 툴바 또는 [소스로 편집] |

    - **원자 인라인 노드(내용 편집 불가·통째 선택/삭제만)**: `inlineCode`(코드 스팬)·`inlineMath`(`$…$`)·F43 참조 3종(`![[DOC-]]`·`[[DOC-]]`·`[[#앵커]]`). 렌더 시 `contenteditable="false"`로 감싸고 모델에서는 **항상 원본 슬라이스 보존**(캐럿이 안으로 들어가 문법이 드러나거나 파괴되는 경로를 구조로 차단). 내용을 고치려면 [소스로 편집].
  - **ⓒ HTML 붙여넣기 화이트리스트 변환(결정 ④ — plain text 강제 폐기, R30 대응 격상)**: `paste`에서 `text/html`을 읽어 **`DOMParser`로 파싱한 뒤 방언 Markdown 문자열로 단방향 변환**하고, 그 문자열을 **평소의 입력과 똑같이** 모델에 태운다(변환 결과는 다시 remark 파이프라인을 타므로 신규 해석 경로 0). **`text/html`이 없으면 `text/plain` 폴백** · **이미지 파일 붙여넣기는 F54 기존 경로 그대로**(S29 ⓐ — 신규 업로드 경로 0).

    | 원본 태그 | 변환 결과 |
    |---|---|
    | `b`·`strong` | `**…**` |
    | `i`·`em` | `*…*` |
    | `u` | `++…++` |
    | `s`·`del`·`strike` | `~~…~~` |
    | `mark` | `==…==` |
    | `code` | `` `…` `` |
    | `a[href]` | `[텍스트](URL)` — `href`는 **`http`·`https`·상대경로만**(`javascript:`·`data:` 스킴은 링크를 버리고 텍스트만) |
    | `h1`~`h6` | `#`~`######` + 앞뒤 빈 줄 |
    | `ul`·`ol`·`li` | Markdown 목록(`- ` / `1. `, 중첩은 2칸 들여쓰기) |
    | `blockquote` | 줄마다 `> ` |
    | `p`·`div` | 빈 줄 구분 문단 |
    | `br` | 줄바꿈 1회 |
    | `table`·`tr`·`th`·`td` | **GFM 표**(헤더 행 없으면 첫 행을 헤더로 승격 — 셀 안 줄바꿈은 공백으로) |
    | `span`·`font` 등 **매핑 밖 태그** | **텍스트만 남김**(태그 소멸) |
    | `script`·`style`·`iframe`·`object`·`embed`·`svg` | **노드째 제거**(내부 텍스트도 버린다) |

    **속성은 전부 폐기**한다 — 취하는 것은 `a[href]`와 **색(`color`·`background-color`) 뿐**이며, 색은 계산값을 `#rrggbb`로 정규화해 `:t{c=…}`·`:t{bg=…}`로 환원한다(ⓓ — 정규식 검증을 통과한 6자리 hex만. `style`·`class`·`on*`은 어떤 경우에도 본문에 남지 않는다). **파싱한 DOM은 문서에 삽입하지 않는다**(스크립트 미실행). **최종 방어선 불번복**: 저장 소스는 Markdown이고 `rehype-raw`는 여전히 기각이므로, 변환기를 빠져나간 문자열이 있어도 **렌더되지 않고 텍스트로 보인다**(F43 D2·R30 ①). **turndown류 신규 의존 0** — 출력이 이 앱의 방언이라 범용 라이브러리가 애초에 맞지 않는다. **"DOM 스크래핑 역직렬화 기각"과 무관함을 명시**: 그 기각은 *편집 표면의 직렬화 경로*(편집 결과 DOM을 긁어 본문을 복원 — 자체 문법이 뭉개지는 경로)에 대한 결정이고, 여기는 **외부 클립보드 HTML → 방언 단방향 변환**이라 잃을 자체 문법이 없다. 편집 표면의 직렬화는 ⓑ 인라인 모델에서만 한다.
  - **ⓓ 자유 hex 색 렌더·다크 모드 자동 보정(결정 ④-2 — 계획서 F52 결정 ③ "팔레트 7색 고정"의 공식 개정)**: 문법 = `:t[…]{c=#rrggbb}`·`:t[…]{bg=#rrggbb}` — `remarkStudy`의 값 화이트리스트를 **팔레트 7색 이름 ∪ `^#[0-9a-fA-F]{6}$`** 로 확장한다(3자리 축약·`rgb()`·색 이름 문자열은 **불허** — 종전대로 스타일만 무시하고 텍스트는 그대로. 임의 CSS 문자열이 속성값으로 사는 경로 0).
    - **렌더 규약**: hex 값은 요소 인라인 스타일의 **커스텀 프로퍼티 1개로만** 넘긴다(`style="--u-ink:#3366ff"`). 실제 `color`/`background-color`는 **`tokens.css`의 규칙**이 계산한다 — `color: color-mix(in oklab, var(--u-ink), var(--text) var(--hex-ink-adjust))` · `background-color: color-mix(in oklab, var(--u-bg), var(--surface) var(--hex-mark-adjust))`. **보정량 토큰 2개 신설**: `--hex-ink-adjust`·`--hex-mark-adjust` — **라이트 = `0%`(원색 그대로)**, **다크 = 0보다 큰 값**(글자색은 다크의 `--text`(밝은 색) 쪽으로 섞여 자동으로 밝아지고, 형광펜 배경은 `--surface` 쪽으로 섞여 채도가 눌린다. 구체 수치는 실측 조정 — 계약은 "라이트 0 · 다크 양수"와 "섞는 상대는 항상 토큰"뿐).
    - **`light-dark()`를 쓰지 않는 이유(결정)**: 이 앱의 다크 모드는 `darkMode:'class'`(`<html class="dark">` 토글)이고 `light-dark()`는 `color-scheme` 연동이라 **수동 토글과 어긋난다**. 기존 토큰 구조(라이트·다크 각 1세트)에 보정량 변수를 얹는 편이 §6 규약과 정합한다. `color-mix()` 미지원 브라우저용으로 `@supports not (color: color-mix(in oklab, red, blue))` 에서는 **원색 그대로**(라이트 동작) 폴백.
    - **불변 규칙 5와의 경계(명시)**: 코드·CSS에 등장하는 색 리터럴은 **0개**다 — hex는 전적으로 **사용자 본문 데이터(런타임 값)**이고, 보정 대상·보정량은 전부 토큰 변수다. 따라서 `invariant-scan` 기준선의 "색 하드코딩 0" 계약은 그대로 유지된다.
    - **툴바**: 색 드롭다운은 **팔레트 7색(토큰) + [사용자 지정] 피커** 병행 — 팔레트가 여전히 1차 경로(의미 표시)이고 hex는 확장 경로. **인쇄**: hex 색도 형광펜·글자색 요소이므로 §6의 `print-color-adjust: exact` 적용 대상이며, 인쇄는 항상 라이트라 **보정 0% = 원색 그대로** 찍힌다(§6 규약 무변경 — 신규 인쇄 토큰 0). **F53 문서 배경(`style.bg`)은 이번 개정 대상이 아니다**(§4.26 서버 화이트리스트 = 팔레트 7색 이름 — 무변경).
  - **ⓔ IME·조합 입력 규약(결정 ⑥ — R31 대응. 코어의 필수 계약)**: `compositionstart`~`compositionend` 사이에는 **서식 명령·모델 동기화·재렌더를 전부 보류**하고 `compositionend`에서 일괄 처리한다(조합 중 툴바·단축키 입력은 **무시**하되 조합을 끊지 않는다). **리치 블록 서브트리는 React 재렌더 대상에서 제외**한다 — 초기 1회 렌더 후 DOM은 수동 관리이며, 이는 S27 `EditablePreview`의 블록 memo 렌더 구조에 대한 **예외 1건**으로 코드 주석과 stage 완료 기록에 남긴다.
  - **ⓕ 목록·인용 키 규약(결정 ⑧ 확장분 — "Notion스럽게". 전부 **한 블록 안**의 조작이라 S27 블록 경계·구간 치환 계약을 건드리지 않는다)**:
    - **Enter(내용이 있는 목록 항목)** = **같은 종류·같은 들여쓰기 수준의 새 항목**을 만든다. 마커 스타일은 **직전 항목을 계승**(`-`/`*`/`+` 그대로, 순서 목록은 **직전 번호 + 1**). 캐럿이 항목 중간이면 **캐럿 뒤 텍스트가 새 항목으로 이동**(항목 분할).
    - **빈 항목에서 Enter** = **중첩 항목이면 한 수준 내어쓰기(outdent)**, **최상위 항목이면 목록에서 빼내 문단으로 강등**(= 목록 해제).
    - **빈 항목에서 Backspace** = **목록 해제(문단화)** — 위 강등과 같은 결과.
    - **인용(blockquote)에서 Enter** = 인용 안 줄바꿈(`> ` 계승). 빈 인용 줄에서 Enter = 인용 해제·문단화(목록과 같은 관례).
    - **1단계에서 하지 않는 것(무시 = `preventDefault` 후 아무 일도 일어나지 않음. 그 조작의 정식 경로는 항상 [소스로 편집])**: 내용이 있는 항목의 맨 앞 Backspace(= **항목 병합**) · 첫 항목 맨 앞 Backspace나 마지막 위치 Delete로 **다른 블록과 병합** · 문단에서 Enter로 **블록 분할** · Tab/Shift+Tab **들여쓰기 변경** · 블록 드래그 재배열. 전부 후속 단계이며 매뉴얼에 알려진 한계로 명시한다(조용한 실패 금지).
  - **ⓖ 표면 추상화 = 4번째 표면(신규 분기 금지)**: `getSurface()`/`requireSurface()`가 반환하는 활성 표면 집합에 **리치 블록**을 추가한다(편집·분할 본문 textarea / 미리보기 활성 블록 textarea / **리치 블록**). 툴바·단축키(Ctrl+B/I/U·Ctrl+Shift+H)·F54 이미지 3진입점은 **같은 인터페이스로 흡수**하고 모드별 분기를 새로 만들지 않는다.
    - `insertAtCursor(text)` = 리치 표면에서는 캐럿 위치에 **텍스트 노드 삽입**(dirty 표시), `applyRangeEdit`(선택 영역 서식) = 선택 구간 인라인 노드에 서식 속성 적용 → **그 자리에서 즉시 굵게·빨간 글자·형광펜으로 보이고 선택은 유지**된다(사용자 요구 원문의 수용 조건).
    - **이미지 삽입의 세칙**: 삽입 문자열 `![](/images/…)`는 ⓐ의 배제 노드를 만든다 → **현재 리치 블록을 먼저 확정(직렬화 적용)한 뒤 소스 기준 오프셋에 삽입**하고, 그 블록은 판정 술어에 따라 **자동으로 소스 편집 표면으로 재진입**한다(예외 규칙을 새로 만들지 않는다). 진행·실패 표시는 S29 ⓔ 그대로.
  - **ⓗ 확정·이탈 수명주기 = S27 규약 전건 계승**: 확정(페이지 내 blur·Ctrl+Enter)·취소(Esc)·**창 포커스 상실은 확정 사유 아님**(`document.hasFocus() === false` — S27 ⓒ 정밀화분)·편집기 밖 클릭·뷰 모드 전환·언마운트 안전망·저장 버퍼 합류. **초안이 조용히 사라지는 경로 0**이 불변 계약이며, 리치 표면은 확정 시점에 ⓑ의 직렬화(+3층 검사)를 거쳐 S27과 **같은 구간 치환**으로 본문에 반영된다.
  - **ⓘ 퇴로 2종**: ① **툴바 [소스로 편집] 토글 1개** — 리치 블록도 언제든 raw Markdown textarea로 볼 수 있다(문법을 직접 손보는 경로이자 ⓑ 3층 폴백·ⓕ 미지원 조작의 착지점). ② **기능 플래그 `wysiwyg?: boolean`(기본 true) prop 1개** — 끄면 **S27 동작으로 완전 복귀**(F51 `breaks?`·F52 `inlineFormat?` 전례. R31 ④·R32 ④ 공용 대응).
  - **ⓙ 소스 오프셋 → DOM 매핑(실측 근거 — 계획서 §14 F56 ⑨ 1-2)**: **react-markdown 10.1.0은 `sourcePos`·`rawSourcePos`·`includeElementIndex`를 "제거된 prop"으로 명시하고 에러 처리**하므로 쓸 수 없다. 대안 = **자체 rehype 플러그인으로 `node.position`을 data 속성에 주입**하며, 공용 `MarkdownView`에는 **기본 off 옵션**(예: `sourcePos?: boolean` — 기본 false)으로만 넣는다. 켜는 곳은 리치 표면 렌더 1곳뿐이고 **읽기 전용 화면·인쇄·임베드의 렌더 diff는 0**이다(F43 앵커 — `MarkdownView.tsx` 무변경 목표, 불가피한 prop 추가는 기본값으로 기존 사용처 diff 0).

- **에디터 v2 documents 탑재(S35 — 에디터 v2 M34, **api §4.29가 저장 계약 정본** · 지시서 `stage-35-documents-blocks.plan.md` · 2026-08-18)**: **M34는 탑재·저장 전환이 본체고 UX 마감은 M35(stage-36)다** — 이 절은 최소 계약만 둔다(두 정본에 없는 세부는 구현 재량 — 새 결정을 만들지 않는다).
  - **편집 진입 분기(이 단계의 새 편집기 진입점 = 문서 상세 `/docs/:id`의 편집 1곳)**: 퇴로 토글이 ON이고 ⓐ 전환 문서(`blocks_version` 있음)이거나 ⓑ 클라이언트 변환(메모리 — DB 무기록)이 미지원 보고 0으로 성립하면 → **새 편집기 표면**(노트 편집 §5.16 골격 재사용 — 본문·해설 2표면 + 저장 상태 4종·자동 저장·IME 조합 중 보류 규약 그대로. **제목·`choices`·`answer` 등 나머지 필드는 기존 폼 유지** — 별지 D7 대기, 편집 표면 통합은 M35). 그 외(미전환+변환 불가·토글 OFF) → **기존 편집기 그대로**(S26~S30 경로 무변경 — 완전한 편집 경로가 퇴로다).
  - **퇴로 토글**: 설정 §5.11 그룹 ⑥ 실험실 카드 확장 — "문서 편집에 새 편집기 사용", **기본 ON**(기본 OFF면 실사용이 일어나지 않아 D1 확정 실측이 나오지 않는다). OFF = 구 편집기 완전 복귀. **구 편집기(또는 DocEditor 모달 등 기존 진입점)가 전환 문서를 저장하면 전환 해제**(§4.29 ④ — 프로젝션이 소스로 승격·다음 편집 시 재전환. 알려진 동작·매뉴얼 명시).
  - **지연 전환·고지**: 열람·편집 진입은 메모리 변환만 — **블록 쌍을 처음 동반한 저장이 곧 전환**(§4.29 ③). 최초 전환 저장 시 1회 안내(저장 상태줄 — 문구는 구현 재량) + **마이크로 마크 접힘 고지**(`fromBlockNoteResult().microMarkCollapses > 0`이면 기존 배너(노트 pasteNotice 계열) 재사용 — documents는 편집기 밖에서 만들어진 본문이 들어오는 첫 표면).
  - **리더 이미지 `{w=}` 반영(M33 이월분 — "리더 무변경"의 명시 예외 1건)**: 공용 `MarkdownView` 계열(remarkStudy)이 이미지 크기 표기 `{w=}`(M32 확정 표기)를 폭으로 반영한다 — 전 화면·인쇄 자동 파급(F43 앵커 원칙). **`{w=}` 없는 이미지·기존 문서의 렌더 diff 0이 계약**이며 저장 전환 계층과 별도 커밋으로 격리한다.
  - 그 외 읽기 화면·퀴즈·인쇄·검색·백업 **무변경**(§4.29 ⑤). notes 표면은 실험실 격리 유지(§5.16 — 정식 노출 재론은 M36).
  - **상시 [저장]/[취소] · 세션 스냅샷 · 크래시 복구(S39 — 2026-08-24 추기)**: 이 분기 표면(`DocBlockEditor`)에 **§5.16 S39 확정 계약이 동일 적용**된다(스냅샷 키 `studyhub.doc.snapshot.<id>` · [취소] = 폼 필드 포함 진입 시점 복귀). **계약 정본 = §5.16 한 곳**(중복 서술 금지) — 진입점②~⑥(DocEditor 계열·S36)은 기존 폼 [저장]/[취소] 기존재로 비적용(이중 UI 금지).
  - **툴바 정비·다단(S40·S41 — 2026-08-24 편성 추기)**: 이 분기 표면과 진입점②~⑥의 새 편집기 표면(`BlockSurface` — 본문·해설 등 표면당 1개) 전부에 **§5.16 "툴바 정비 · 다단(S40·S41)" 계약이 동일 적용**된다(도킹 툴바 표면당 1개 · 블록별 필터 · 인라인 코드 버튼 · 코드 블록 외양 · `:::columns` 다단 — 리더 렌더는 공용 `MarkdownView`라 문서 상세 읽기·인쇄에 자동 파급). **계약 정본 = §5.16 한 곳**(중복 서술 금지).

- **에디터 v2 표면 통합·UX 마감(S36 — 에디터 v2 M35 전반부. 계약 확정 2026-08-19 · 지시서 `stage-36-surface-unify.plan.md`가 규약 정본 — 이 절은 최소 계약만·두 정본에 없는 세부는 구현 재량)**:
  - **편집 진입점 전수 통합(FB-5)**: S35의 "문서 상세 편집 1곳"에 더해 **나머지 진입점 전부** — `/docs/new`·`/docs/:id/edit`(DocEditPage)·탐색 create 모달·커리큘럼 create/edit 모달 — 가 새 편집기 분기를 얻는다. **개조 지점은 공용 `DocEditor`의 본문·해설 편집 위젯 분기 1곳**(진입점별 복붙 금지 — 전수표는 stage-36 규약 A). 분기 조건·퇴로 토글은 S35 그대로(토글 OFF = 전 진입점 구 편집기 완전 복귀). 신규 작성 저장 = `POST` 블록 동반(§4.29 ⑦ — 태생 전환). **제목·`choices`·`answer` 등 폼 필드는 기존 폼 유지**(D7 1차 — 문서 표면 흡수 없음).
  - **choices·오답노트 메모 인라인 리치(D7 1차 확정 — 별지 §10)**: 저장은 기존 문자열 필드 그대로(DDL 0·API 0), **렌더 지점 전수**(퀴즈 카드·문서 상세·학습·인쇄·오답노트 — 전수표는 stage-36 F-3)에서 F52 인라인 방언을 렌더. 방언 없는 기존 문서 렌더 diff 0이 계약. 채점·봉인은 서버 계약 담당(불변 규칙 1 — 서버 diff 0).
  - **문서 임베드 리치 카드(FB-3)**: 편집 표면 `docEmbed` 블록이 대상 본문을 **읽기 전용으로 카드 안에 렌더** — 데이터 = `resolve-embeds`(§4.19) **봉인 응답만**(answer·explanation·style·블록 필드 부재 — R20. 다른 API로 보충 금지), 렌더 = 공용 `MarkdownView` 재사용, **카드 안 편집 없음**(D11-ⓑ 금지) · [원문 열기]·접기 · 중첩 임베드는 1단만(안쪽은 축약 표시).
  - **UX 마감**: 슬래시 메뉴 정비(항목 전수표·한국어 라벨) · 드래그 재배열(사이드카 id 정합이 계약) · **표 열 정렬 편집 UI**(`table:align` — 열 증감 시 UI가 정렬 배열 동반 갱신) · 표면별 undo 격리(U-2 전례)·조작 단위 undo · **찾기/바꾸기 = 자체 PM 플러그인**(현재 편집 문서 안·리터럴만·편집 표면 포커스 중만 Ctrl+F 가로채기·원자 노드 제외) · 콜아웃 마감 2건(children 시각 소속·원자 서식 가드의 코어 버튼 확장).
  - 슬래시·드래그·찾기/바꾸기·콜아웃 마감은 editor2 공용 자산이라 **노트(§5.16) 표면에도 파급되는 것이 정상**(§4.28 계약은 무변). 신규 커스텀 블록(TOC·웹 임베드·이미지 크롭)은 **S37**(§4.30·별지 §5.4).

- **에디터 v2 신규 커스텀 블록 3종(S37 — 에디터 v2 M35 후반부. 착수 문서화 2026-08-21 · 지시서 `stage-37-custom-blocks.plan.md`가 규약 정본 — 이 절은 최소 계약만·두 정본에 없는 세부는 구현 재량)**:
  - **목차(TOC — `::toc`, 별지 §5.4 강등 확정)**: 저장 데이터 0(원자 블록 — 렌더 시점 파생이라 자동 갱신 공짜). 수집 범위 = **자기 표면**(본문/해설 각각). 편집 표면 = 헤딩 목록·클릭 스크롤·빈 상태 문구, 리더 = **헤딩 앵커 링크 목록**(앵커 id는 기존 `slugifyHeading` 재사용 — F43 앵커 칩과 동일 도메인·새 슬러그 규칙 금지), 인쇄 = 목록 그대로.
  - **웹 임베드(`::web{url=… title=…}` — §4.30이 메타 계약 정본)**: 삽입 = 슬래시 → URL 입력 → §4.30 1회 조회 → **메타는 블록 prop 캐시**(재조회 = [메타 새로고침] 명시 조작뿐·자동 재fetch 금지). 카드 = 저장 메타 기반 북마크 카드(실패·오프라인 = 텍스트 카드 강등 — 조용한 실패 없음·삽입은 조회 실패에도 성립). **iframe 실임베드 = 편집 표면 한정·화이트리스트 상수(1차 YouTube)·sandbox 필수 — 리더·인쇄는 항상 카드**(§4.30 ③. 별지 §5.4 비고 괄호와의 충돌은 2026-08-21 §4.30 ③ 우선으로 정돈). 메타 캐시는 프로젝션 비대상(명문화 강등 — directive에는 url·title만).
  - **이미지 크롭(§4.27 재사용 — 신규 엔드포인트 0)**: image 블록 [자르기] → canvas 자체 UI(자유 사각 1가지·신규 의존 0) → 원본 MIME 유지로 `POST /api/uploads` → **신규 해시 파일 url로 prop 교체**(undo 1단위). **원본 파일 불변**(불변 규칙 4 — 수정·삭제 호출 0·구 파일 잔존은 D8 고아 정리 대기 소관). 제공 조건 = 자체 호스팅 `/images/`만 + GIF 제외(버튼 미노출 — CORS 오염·애니 손실 방지). caption(alt)·`{w=}` 유지 — **D8 alt/크기 충족 확인이 이 단계 항목**(별지 §10 D8).

### 5.4 커리큘럼 — `/curriculum`, `/curriculum/:categoryId`
- `/curriculum`: 최상위(자격증) 카드 목록 → 시험 선택. 카드 목록에 [+ 시험 추가].
- `/curriculum/:id`: 과목→챕터 아코디언. 각 행 = 이름 + 진도바(done/total) + [이어하기|여기서 시작] 버튼. 완료 챕터는 ✓.
- **분류 관리(S4)**: 분류 편집이 탐색 §5.2 전용이 아니라 커리큘럼에서도 가능해야 한다 — 편집 모드 토글(연필 아이콘) 시 각 행/카드에 [추가(하위 분류)] [이름·시험일 수정] [이동(순서·부모 변경)] [삭제] 노출. 탐색과 동일한 공용 모달(CategoryFormModal·MoveCategoryModal·ConfirmDialog)과 기존 categories API를 재사용하며, 삭제 규칙(§4.1 — 하위·연결 문서 있으면 409)도 동일. 변경 즉시 트리·진도 invalidate.
- **챕터 파이프라인(S9, F37)** — "챕터"는 고정 계층이 아니다: **어느 깊이의 노드든 파이프라인 단위**이며 집계·출제 범위는 항상 노드+하위 트리 전체(§4.6 deep 원칙). 아코디언은 임의 깊이 중첩 렌더.
  - **노드 행**: 기존 진도바에 더해 **3단 진도**(개념 ✓ · 문제 ✓ · 기출 —, 공용 Stepper §5.9 재사용 — 완료 ✓/진행 ●/미시작 ○) + 타입별 개수 칩(개념 N·문제 N·기출 N — `tree?pipeline=1`의 `stage_progress`, §4.12) + 단계별 직접 진입 버튼 **[학습]·[문제]·[기출]**(순서 강제 아님 — 여정은 권장 동선). [학습]=`/study/:id`(개념 트랙), [문제]=`quiz/session{types:['question']}`, [기출]=`quiz/session{types:['past_question']}` — 셋 다 하위 포함.
  - **노드 펼침**: 타입별 탭(학습내용/문제/기출) 문서 목록 + 각 탭에 **[+ 문서 추가]**(현재 탭 타입이 기본값, 해당 분류 자동 연결 — DocEditor 모듈, documents POST + links 재사용) + 각 문서 행에 [편집](DocEditor 모달 — 문서 상세로 이동하지 않음). Stage 4의 "분류 편집도 커리큘럼에서" 원칙을 문서까지 확장 — **새 API 없음**.
- **API**: `categories/tree`(진도 포함, S9: `?pipeline=1` 3단 집계), `study/continue`, categories POST/PATCH/move/DELETE(S4 확장), documents POST/PATCH + links(S9 — 문서 추가·편집).

### 5.5 학습 모드 — `/study/:categoryId` (핵심 UX)
- **구성**: 상단 챕터 진행바(7/12), 본문 영역(개념=Markdown / 문제=퀴즈 카드 인라인), 하단 [◀ 이전] [다음 ▶].
- **동작**: 개념 문서에서 "다음" = `study/events{action:complete}` 후 다음 문서. 문제 문서는 풀어야(=attempts 성공 제출) "다음" 활성화. 진입·이동 시 `action:position`으로 resume 갱신.
- **챕터 완료 화면**: 정답률 요약 + 틀린 문제 목록("지금 다시 풀기" = wrong_only 미니 퀴즈) + [다음 챕터 ▶].
- **챕터 3단계 여정(S9, F37)**: ① 개념 트랙(기존 흐름 — `study-track deep=1&types=concept,question` 인터리브) 완료 화면에 **[문제 풀이로 이어가기]** → ② 챕터 연습문제 퀴즈(`quiz/session{types:['question']}`, 하위 포함) → 결과 요약에 **[기출로 마무리]** → ③ 챕터 기출 퀴즈(`types:['past_question']`) → **챕터 최종 완료 화면**: 단계별 정답률 요약(3단 Stepper) + 틀린 문제 재도전 + [다음 챕터 ▶]. 각 단계 건너뛰기 가능. 재개는 저장 없이 **next_stage 파생**(§4.12) — 커리큘럼 [이어하기]가 첫 미완료 단계로 진입, 개념 트랙 내 위치는 기존 resume_points 그대로. 해당 타입 문서 0개인 단계는 여정에서 자동 생략.
- **집중 모드(S9, F36-⑦)**: 헤더 토글로 사이드바·헤더 숨김 전체화면 — 상태는 화면 로컬(저장 안 함), Esc/토글로 복귀. 모바일은 하단 탭바 유지.
- **엣지**: 챕터에 문서 0개면 "문서를 연결하세요" 안내. 마지막 챕터 완료 시 과목 완료 화면.
- **완료 문제 재방문**: 이미 done인 문제로 돌아가면 마지막 풀이를 복원해 표시 — 내가 고른 보기(정오 색상)·정답·해설을 그대로 보여주고, [다시 풀기] 버튼으로 재제출 가능(새 attempt 누적, 이력 미니차트에 반영). 데이터는 `documents/{id}`의 `stats.last_attempt {my_answer, is_correct, created_at}`.
- **오류 신고(S6)**: 문제 카드에 [오류 신고] 버튼 → §5.3의 재생성 흐름으로 진입.
- **API**: `categories/{id}/study-track`, `study/events`, `attempts`.

### 5.6 퀴즈 — `/quiz` (설정) → `/quiz/run`
- **설정 화면**: 범위(트리 선택), 모드(순차/랜덤/오답만/북마크만), 문항 수(기본 `settings:quiz.default_count`).
- **실전 모의고사 진입(S11, F25)**: 설정 화면 상단 탭 [일반 퀴즈 | 실전 모의고사] — 모의고사는 §5.12 별도 흐름(일괄 제출 채점 — 즉시 채점인 이 화면과 경로 분리).
- **런 화면**: 문제 카드(지문 Markdown + 보기 4개 버튼), 선택 즉시 제출 → 정오 색상 + 해설 펼침 + 관련 개념 링크(S4) + [오류 신고](S6 — §5.3 재생성 흐름) → [다음]. 상단 진행바 + 경과 시간.
- **종료 요약**: 정답률, 소요 시간, 틀린 문제 리스트(각각 오답노트 메모 바로 입력).
- **키보드 단축키(S9, F36-④)**: 1~4 = 보기 선택(즉시 제출), Enter = 다음, B = 북마크 토글 — 입력 필드 포커스 중에는 무시. 해설 하단에 단축키 힌트 소표기.
- **틀린이유 원탭(S9, F36-⑤)**: 오답 직후 해설 화면에 4버튼(개념부족/실수/함정/시간부족) — attempts 응답의 `review_note_id`로 `PATCH /api/review-notes/{id}` 즉시 기록(선택 상태 토글 표시, 재탭 시 변경).
- **정답 자동 다음(S9, F36-⑥)**: `settings:quiz.auto_advance` on이면 정답 시 1.5초 후 자동 [다음](카운트다운 표시, 아무 키/탭으로 즉시 진행) — **오답은 항상 정지**(해설·틀린이유 기록 시간 보장).
- **상태**: 세션은 zustand 스토어(새로고침 시 세션 종료 확인 모달). 문항별 `time_spent`는 카드 표시~제출 시각.
- **API**: `quiz/session`, `attempts`.

### 5.7 플래시카드 — `/flashcards?category_id=` (S5) · 오늘의 복습 — `/review`
- 카드 뒤집기(탭/스페이스), 스와이프 좌="모른다"(q=1)/우="안다"(q=4). 남은 장수 + 오늘 큐 진행.
- **판정 undo 1회(S9, F36-⑧)**: 직전 판정을 [되돌리기]로 취소(오조작 SM-2 오염 방지) — 구현은 **전송 지연**(§4.12): 판정은 다음 카드 진입 시(마지막 카드는 세션 종료 시) 확정 전송, undo = 미전송 취소 후 카드 복귀. 새 API 없음.
- **복습 완료 화면(S9, F36-③)**: 오늘 큐 소화 완료 시 요약 + **"내일 N개 예정"**(`GET /api/srs/summary`) + **[이어하기로 계속]**(`study/continue` 첫 카드로 — daily_start 여정 §5.1의 후반부). **S10(F26·F36-③ 연계)**: 스트릭 배지 추가 — "N일 연속 학습 중"(`GET /api/stats/streak`), 오늘 목표 달성 시 달성 표기 — 내일 예고와 나란히 "오늘의 마무리" 블록.
- **D-Day 강화 모드(S11, F16)**: `srs/summary`의 `dday_boost` 발동 시 복습 헤더에 배지 — "『정보처리기사 필기』 D-7 · 오늘 복습 60개로 강화"(name·d_day·effective_limit). `ahead:true` 카드에는 "선행 복습" 소배지(§4.14 — 아직 due 전이지만 시험 대비로 당겨왔다는 표시). 미발동 시 기존 화면 그대로.
- **API**: `srs/today`(flashcard 타입 필터) 또는 범위 선택, `srs/answer`, `srs/summary`(S9).

### 5.8 오답노트 — `/review-notes`
- **구성**: 필터(분류 범위·틀린이유·해결여부) + 분류 계층 그룹 리스트(아래) — 카드 = 문제 요약, 내 메모 인라인 편집, 틀린이유 태그 선택, [극복] 토글, [재도전].
- **분류 범위 필터(S4)**: 평면 셀렉트가 아니라 퀴즈 설정(§5.6)의 범위 트리 선택과 동일 패턴(공용 컴포넌트화) — 선택한 범위의 **하위 전체 포함**(§4.6 하위 트리 필터).
- **계층 그룹 보기(S4)**: 리스트를 분류 경로(`category_path`) 기준 그룹 섹션으로 표시 — 상위 단위(대단위) 헤더 → 하위 단위(중/소/하위 단위) 소제목, 접기/펼치기, 그룹 헤더에 건수 배지. 여러 분류에 연결된 문서는 선택 범위 안의 경로를 우선, 없으면 첫 연결 경로 기준 1곳에만 표시. 어느 분류에도 연결 안 된 문서는 "미분류" 그룹.
- [재도전] = 해당 문서들로 `quiz/session{mode:wrong_only}`. 카드별 개별 [재도전] = `quiz/session{mode:sequential, document_ids:[해당 문서]}` — 누른 그 문제만 출제. **`wrong_only`가 아닌 `sequential`을 쓰는 이유**: wrong_only는 미해결 오답노트 조인이라 극복(resolved) 처리된 문제·오답노트 없는 문서는 0문항이 된다. 특정 문서를 지목하는 재도전(오답노트 개별·약점 위젯 §5.1)은 모두 `sequential + document_ids`.
- **API**: `review-notes`(S4: 하위 포함·경로), PATCH, `quiz/session`.

### 5.9 반입 — `/import`
- **3단계 위저드**: ① 파일 선택(JSON + 원본 선택) → ② 미리보기 표(항목별 상태 배지: 정상/중복 의심/오류. 중복은 기존 문서와 나란히 비교, 라디오: 건너뛰기/새로 추가/병합. 분류·관계 제안 체크박스) → ③ 반입 실행 → 결과 요약(생성 N, 병합 N, 건너뜀 N + 새 문서 바로가기).
- **공용 Stepper(S9, F36-⑪)**: 위저드 헤더 ①②③을 공용 Stepper 컴포넌트로 교체 — **지나온 단계(✓)는 실제 클릭으로 되돌아가기**(진행 중 결과가 버려지는 파괴적 복귀는 확인 후), 현재(●)는 강조, **미래(○)는 흐림+비클릭이 명확한 스타일·점선 연결**(눌릴 것처럼 보이지 않게). 토큰 기반 스타일. F37 3단 진도(§5.4)·챕터 최종 완료 화면(§5.5)에도 재사용.
- **엣지**: preview 만료(1h) 시 재업로드 안내. 오류 항목은 개별 오류 메시지 표시, 커밋에서 자동 제외. (**S13부터 만료는 먼저 디스크 복구를 시도한다** — §4.3.)
- **수동 반입 UX(S13, F40 — 계획서 §14 F40. 큐넷 실측 이후 **파일·URL 반입이 정본 경로**가 된 데 따른 정비)**:
  - **① 반입 대기열(F40-②)**: ①단계(선택)의 파일 입력을 **다중 선택(최대 10)** 으로 확장 → 선택한 파일 수만큼 기존 `POST /api/convert` 호출로 서버 큐에 적재(**새 API 0건 · 서버 동시 실행 1개 유지 — 병렬 금지**). 화면에는 파일 카드 목록(파일명 · 상태 배지 **대기 / 변환 중 / 검토 대기 / 반입 완료 / 실패**)이 뜨고, **진행 표시(LlmJobProgress)는 현재 처리 중 1건에만**(나머지는 배지만, 폴링 간격 완화). 완료 항목 [검토] → 기존 ②(미리보기) 단계로 진입 → 커밋 후 **[대기열로 돌아가기]**. 대기열은 ②·③ 단계에서 접힌 요약("검토 대기 N건")으로 유지. **Stepper 단계 수는 늘리지 않는다**(대기열은 ①단계 안에 산다). 잡 기록은 localStorage **배열**로 보존(기존 단건 레코드는 읽기 시 1건 큐로 승격 — 호환) → 새로고침·재접속 후 목록·상태 복원. 실패 항목은 목록에 남기고 [API로 재시도]·[건너뛰기](파일 소스는 새로고침 후 재선택 안내 — 기존 문구 재사용).
    - **`previewId`를 큐 레코드에 함께 영속(S13 구현 확정)**: 잡 완료 시 받은 `result_preview_id`를 localStorage 큐 항목에 저장한다. 잡 레코드는 서버 메모리라 만료·재시작 후 `GET /api/convert/{job_id}`가 404가 되는데, **preview_id만 남아 있으면 F40-①의 디스크 복구로 검토를 이어갈 수 있다**(잡 소실 = 검토 불가가 되지 않게). 즉 큐가 추적하는 1차 키는 잡이지만, **검토 단계의 1차 키는 preview_id**다.
    - **URL 반입도 같은 대기열로 통합(S13 구현 확정)**: URL 반입(F35-1)은 별도 단건 추적을 두지 않고 파일 항목과 같은 큐 레코드(소스 종류만 `file|url`로 구분)로 관리한다 — **폴링·재시도·새로고침 재개 코드의 이원화를 막는 것이 목적**. 화면 구성(탭 4종 · Stepper 3단계)은 불변이며, URL 탭에서 시작해도 결과는 같은 대기열 목록에 나타난다.
    - **10개 상한의 기준(S13 구현 확정)**: **"현재 대기열에 남아 있는 항목 + 이번에 새로 선택한 항목"의 합계**로 판정한다(신규 선택분만 세지 않는다) — 반입 완료·건너뛴 항목을 정리하면 다시 걸 수 있다. 초과 시 추가하지 않고 안내만 띄운다.
    - **알려진 한계(S13 — 명시)**: **처리 중인 1건은 취소·건너뛰기 불가**. 서버에 잡 취소 API가 없고(§4.10 잡 큐 계약) 이 단계에서 신설하지 않기로 했으므로(YAGNI — 동시 1개라 곧 끝난다), 취소·건너뛰기는 **아직 시작하지 않은 대기 항목에만** 노출한다. 화면·매뉴얼에 한계로 적는다. (**S22 개정 예고 — F48**: §4.24 ② 잡 취소 API로 이 한계는 해소된다 — 처리 중 1건에도 [취소]를 노출하고(실행 중 취소 = 부분 과금 확인 다이얼로그 §5.14), 이 한계 서술은 화면·매뉴얼에서 제거한다. 이 문단은 S13 시점 기준으로 보존.)
  - **② 분류 경로 지정(F40-③)**: 시작 화면(파일·URL 공통)에 접이식 **"분류 경로 지정 (선택)"** — 기존 노드는 `CategoryScopePicker` 재사용으로 고르고 뒤에 회차 등 하위 경로를 텍스트로 덧붙인다(완성 경로 미리보기 문자열 표시 — 오타를 시작 전에 드러낸다) → `POST /api/convert`의 `category_path`(§4.11). 대기열에서는 **"모든 파일에 같은 상위 경로 적용"** + 파일별 마지막 칸(회차)만 개별 입력. **제안 고정일 뿐 확정은 ②단계 승인**(R7).
  - **③ 복구·내려받기(F40-①)**: 미리보기가 디스크에서 복구된 경우 상단에 소표기("이전 미리보기를 복구했습니다 — 중복 판정은 현재 DB 기준입니다", §4.3 `recovered`). ②단계와 실패 화면에 **[변환 JSON 내려받기]**(`GET /api/import/preview/{id}/json`) — 최악의 경우에도 JSON을 손에 쥐고 "반입 JSON 파일 선택" 경로로 이어갈 수 있다. **노출 조건(S13 구현 확정)**: 보존본이 존재하는 경로 = **convert·fetch 잡에서 온 preview에만** 버튼을 렌더한다. 사용자가 직접 올린 JSON의 preview는 보존 대상이 아니라 눌러도 404이고, 애초에 파일이 사용자 손에 있으므로 버튼 자체를 두지 않는다(§4.3).
  - **④ 잘림 안내(F40-④)**: `error_info.kind:'invalid_output'` 렌더 1종 추가(§4.11) — 사람 말 메시지 + [파일 나눠서 다시 올리기](시작 화면 복귀). 원문(raw) 미노출, 색상은 토큰만.
- **엔진 선택 게이팅(S21, F47 — §4.23 ⓒ)**: 시작 화면(파일·URL)의 engine 선택과 사이트 반입 ③(예상 사용량 확인)의 엔진 표시를 **공용 EngineSelect**로 교체 — `available:false`·`enabled:false` 엔진은 **선택 불가(비활성 표시) + 사유 1줄**("설치 필요"/"로그인 필요"/"키 미등록"/"사용 안 함") + **[다시 확인]**(F34 재진단 패턴 재사용 — 진단 캐시 TTL 60초의 지연 해소). 폴백 [다시 시도] 버튼(§4.11)은 서버가 이미 available && enabled로 산출한 `fallback_engine`만 가리키므로 별도 게이팅 불요. 서버도 명시 지정 비가용·비활성 엔진을 422로 거부한다(§4.23 결정 ⑥ — 프론트만 믿지 않는다). (**S22, F48 — §4.24 ④**: EngineSelect가 **구체 엔진 선택 시 그 엔진의 모델 소목록 select**를 함께 노출한다 — 미선택 = "설정값(현재: {모델 label})" 폴백, auto·소목록 빈 엔진(codex-cli)은 모델 select 미노출, 자유 입력 없음. 사용처 7곳 공통 — 컴포넌트 1곳 확장으로 전 지점 반영, §5.14.)
- **변환 신뢰 게이트 배지(S15 예정, §4.17 ⑤·⑥)**: ②(미리보기) 표에 경고 배지 3종 — **`solved`**(정답을 LLM이 풀어 채움 — `answer_source:"solved"`) · **창작 의심**(`fabrication_suspect` — 지문·보기가 원본 추출 텍스트와 불일치) · **대조 불가**(`match_unavailable` — 원본 텍스트 추출 불가). 앞 둘은 **기본 반입 제외(체크 해제)** — 항목별 명시 승인 시에만 포함, 대조 불가는 배지 + 상단 안내 1줄(기본 포함 유지). 변환 파이프라인(convert·fetch) preview에만 나타난다(직접 업로드 JSON은 비적용 — §4.17). 색상은 토큰만.
- **사이트에서 가져오기(S10, F35-2)**: 반입 화면 진입 방식에 [파일]·[URL](S8)과 나란히 **[사이트에서 가져오기]** 추가 → 공용 Stepper(S9, F36-⑪ 재사용) 4단계 서브플로:
  - **상태(S13 → S14 갱신)**: S13 종료 시점에는 등록 어댑터가 **qnet 스텁 1개(`available:false`)** 뿐이라 이 탭이 **"준비 중" 안내 + [URL로 반입]·[파일로 반입] 대안 버튼**만 렌더했다. **S14 완료 후에는 서비스키 등록 여부가 그 자리를 결정한다** — **미등록이면 같은 안내 화면**(문구만 "설정 > 데이터에서 공공데이터포털 서비스키를 등록하세요" + 설정 링크, 대안 버튼 유지), **등록되면 아래 ①~④ 서브플로가 그대로 살아난다**. 두 상태 모두 `fetch/adapters` 응답 메타로만 갈린다(빈 화면·오류 금지).
  - ① **자격증 검색·선택** — `GET /api/fetch/certs?q=`(어댑터 병합 결과, 출처 사이트 배지 표시). **S14: 결과 0건은 오류가 아니라 안내**("이 종목의 공개문제가 없습니다 — 필기·필답형은 이 API 범위 밖입니다", 실측 근거는 계획서 §14 F35-3)
  - ② **회차 선택** — `POST /api/fetch/exams` 목록: 회차 라벨 + 어댑터 배지(**S13: 항상 큐넷 1종**, `also_on`이 비어 있으므로 대안 출처 소표기는 렌더되지 않는다 — 렌더 분기 자체는 남겨 둔다) + 문항 수(미상이면 "약 60문항 가정") + **"이미 반입됨" 배지**(`imported` — S14: 연도만/식별 불가 회차는 분류 경로를 만들지 않으므로 이 배지가 뜨지 않는 것이 정상). 한 번에 1회차 선택(라디오). **S14 안내문 토글**: 목록 하단에 **"안내문 N건 보기"** 토글 — 켜면 같은 요청을 `include_notices:true`로 다시 보내 `is_notice:true` 항목까지 표시하고, 그 항목에는 **안내문 배지**를 붙인다(§4.13). 기본은 숨김이며 **N건이 0이면 토글 자체를 렌더하지 않는다**(0건 관례). 어댑터 배지·목록은 `fetch/adapters`·`fetch/exams` 응답 메타 그대로 렌더 — 어댑터 추가·제거로 프론트 코드 변경이 없어야 정상(어댑터 격리 원칙 검증 겸용. S13 예외: 어댑터 id 유니온 타입·이름 폴백 맵만 정리).
  - ③ **예상 사용량 확인** — `estimate`(문항 수·대략 입력 토큰·가정치 여부) + 사용 엔진(auto/cli/api — §4.11 계약)과 **한도 기억 경고 배너(S8 재사용)** + **고정 고지: "개인 학습 전용 — 수집물 재배포 금지"**. 확인 없이는 실행 불가.
  - ④ **실행** — `POST /api/fetch/import`(목록 응답의 `exam_key`를 그대로 전달 — §4.13) → 기존 진행 패널 재사용(단계 스텝에 '사이트 수집' = `fetching` 추가, 경과·토큰·ETA·새로고침 안내 그대로) → 완료 시 `result_preview_id`로 **기존 위저드 ②(미리보기)에 합류** — 이후 중복 비교·분류 제안·커밋은 기존 흐름 그대로. **S14: 잡 응답의 `notes`(§4.10)가 비어 있지 않으면 미리보기 상단에 소표기로 그대로 나열**(예: 도면 묶음 ZIP 동시 저장 안내) — 문자열 가공·조건 분기 없이 출력만 한다.
  - 실패 시: `error_info` 렌더(§4.11 규칙) — `parse_failed`면 [URL로 반입]·[파일로 반입] 대안 버튼(**S13: [다른 어댑터로 재시도]는 사문화** — `also_on`이 항상 비어 있어 렌더되지 않는다). **S14**: `unsupported_format`이면 "원본은 sources/에 저장됨" 안내 + [파일 반입으로 이어가기] 버튼(ZIP은 "압축을 풀어 PDF/이미지 반입", HWP는 "한글→PDF 변환 후" — §4.13 비지원 포맷 정책). 버튼 구성은 `error_info.alternatives`(`file_import`·`url_import`, §4.11)를 따르고 **모르는 값은 무시**한다. 원문 HTML/XML 미노출. **S14 상류 실패**: 검색·목록 단계에서 쿼터 초과·키 오류가 나면 잡이 아니라 **HTTP 502 + 사람 말 `message`**(§3)로 오므로, 이 문구를 **0건 안내와 다른 오류 배너로** 렌더한다 — "결과 없음"으로 보이게 하면 안 된다.
  - **S13·S14 프론트 변경 최소 원칙**: 사설 어댑터 제거(S13)·qnet 실가동(S14) 모두 ①~④ 스텝의 렌더 로직을 바꾸지 않는다 — 응답 메타(`available`·배지·목록)를 그대로 렌더. **허용되는 프론트 변경은 딱 둘**: 어댑터 id 유니온을 `'qnet'`으로 좁히기(`FetchAdapterId`)와 이름 폴백 맵에서 제거된 어댑터 항목 삭제 — 유니온을 좁히면 타입 검사가 잔존 참조를 잡아 준다(DoD "참조 0건" 보조 수단).
- **API**: `import/preview`, `import/commit`. S6: "파일만 던지면 변환부터"(`convert`) 버튼 추가. S10: `fetch/*`(§4.13). **S13: `convert`의 `category_path?`(§4.11) · `import/preview/{id}`(복구) · `import/preview/{id}/json`(내려받기, §4.3)**. **S14: `fetch/exams`의 `include_notices?` · 잡 응답 `notes`(§4.10) — 서비스키 등록(`fetch/qnet-key`)은 설정 화면(§5.11)이 담당한다.**

### 5.10 인쇄 뷰 — `/print?type=&category_id=&options=`
- **종류 3종**(계획서 §12): 개념 정리본 / 문제집(문제 앞·해설 뒤 분리) / 오답노트.
- 옵션 패널(화면에서만 보임): 해설 포함/제외, 풀이 여백, 기간 필터(오답노트). 본문은 A4 폭 렌더 + `@media print` 규칙.
- **API**: `study-track`·`documents/batch`·`review-notes` 조합 (전용 API 없음).

### 5.11 설정 — `/settings`
- 테마(라이트/다크/시스템 — localStorage, §6), 복습 큐 상한, 기본 문항 수, D-Day 관리(S4, 아래), 백업/복원(S6), 태그 병합 도구(S6 — S9에서 태그 관리자로 승격).
- **6그룹 구성(F38 — 골격은 S8 선반영 완료, S9는 내용 완성)**: 좌측 목차(카테고리 점프, 모바일 아코디언) + ① **학습**(복습 상한·기본 문항 수 + S9: 글자 크기 `study.font_scale`·정답 자동 다음 `quiz.auto_advance` + **S10: 일일 목표** — 문제 수 `goal.daily_questions`·시간(분) `goal.daily_minutes` 숫자 입력, 비움/0 = 목표 없음, 저장 시 스트릭·히트맵 위젯 invalidate. "시간은 문제 풀이 시간 기준" 도움말 소표기 §4.13 + **S11: D-Day 복습 강화 토글** `srs.dday_boost`(기본 on) — "시험 14일 전부터 복습 상한을 늘리고 임박 시험 범위를 우선합니다" 도움말, §4.14) ② **일정**(D-Day 관리) ③ **태그·분류**(태그 규칙 + S9: **태그 관리자** — 아래) ④ **LLM 엔진**(S8 §4.11 — **S15 예정**: 카드 2장 고정 → 레지스트리 목록 렌더 + ▲▼ 우선순위 + Codex 온보딩 마법사·프라이버시 고지, §4.17 ⑦ + **S21(F47, §4.23)**: 카드마다 **활성 토글**(끄면 "사용 안 함" — auto 해석·폴백 후보에서 원천 배제, settings `llm.disabled` 배열 저장)과 **모델 선택**(레지스트리 소목록 select — status `engines[].models`·`selected_model` 렌더, **자유 텍스트 입력 없음**, 저장 = settings `llm.models` — settings PUT 재사용·신규 API 0. 꺼진 엔진 카드도 진단·온보딩은 정상 표시 — 켜면 즉시 복귀. 전 엔진을 끄면 카드 상단에 "모든 엔진이 꺼져 있습니다 — 변환·생성 기능이 동작하지 않습니다" 안내 1줄)) ⑤ **데이터**(백업/복원·CSV 내보내기 + S9: 복원 후 강제 리로드 모달 §4.12 + **S14(구현 완료 2026-07-27): 큐넷 오픈API 카드** — 공공데이터포털 서비스키 등록(입력 시 즉석 검증 — 실패 사유 표시)·등록 후 마지막 4자리 마스킹 표시·삭제, 발급 방법(공공데이터포털 활용신청) 안내 소표기 + **커버리지 한계 한 줄**(실기 공개문제 위주 — 필기·필답형은 파일·URL 반입)로 등록 전에 기대치를 맞춘다. `fetch/qnet-key` §4.13 — F34 API 키 카드 UX 미러. 배치 근거: 데이터 유입(반입 소스) 계열 — **F38 6그룹 수 불변**, 그룹 내 카드 추가만) ⑥ **화면**(테마).
- **전역 앱 테마 편집(S28 — F53 ①, §4.26 ③ 계약 정본 · 그룹 ⑥ 화면 확장 — F38 6그룹 수 불변)**: 라이트·다크 **각각** 편집(미설정 테마·항목은 기본 토큰 상속 — 테마 전환 상시 동작) — **프리셋 몇 종(세피아·고대비 등 — 목록 구현 재량) + 커스텀**. 커스텀 자유도 경계: **배경·서피스 = 자유 색 선택 + 명도 대비 자동 검증**(미달 조합 = 저장 버튼 비활성 + 사유 1줄, 서버 422가 최종 게이트 — R27 ②) · **글자색·강조색 = 팔레트/프리셋 선택만**(자유 입력 UI 없음) · 폰트 = 시스템 폰트 스택 프리셋 3종 · 기준 글자크기. 저장 = settings PUT `ui.theme_custom`(기존 API 재사용 — 신규 API 0), 적용 = `<html>` 수준 CSS 변수 런타임 주입(§7 — 저장 즉시 전 화면 파급, 불변 규칙 5의 배당금). **복구 경로(R27 ① — 계약)**: **[기본값으로 되돌리기] 상시 노출** + 토큰 무시 안전 모드 1개(URL 쿼리 등 — 구체 형태 구현 재량, 매뉴얼 명시).
- **실험실(베타) 카드(S33 — 에디터 v2 노트 표면 진입점, §5.16)**: 그룹 ⑥ **화면** 아래에 카드 1개 — 제목 "실험실(베타)" + 한 줄 설명("새 편집기를 노트에서 시험 중입니다 — 기존 문서 편집에는 영향이 없습니다") + **[노트(베타) 열기] 링크 1개**(`/notes`). **링크뿐이다** — 토글·설정 키·로직·신규 색 0(불변 규칙 5). **F38 6그룹 수 불변**(7번째 그룹이 아니라 그룹 ⑥ 안의 카드 추가 — 매뉴얼 링크 전례). 베타 표면을 사이드바·하단 탭바에 올리지 않는 이유는 §5.16 "노출 방식" 참조.
- **매뉴얼 링크(S12, F39)**: 좌측 목차 **하단**(모바일 아코디언 하단)에 "사용 설명서 열기" 링크 — `/manual` 새 탭(`target="_blank" rel="noopener"`, §4.15). **7번째 그룹이 아닌 단순 링크**(F38 6그룹 구조 불변). 모바일(<768px)에서는 이것이 유일한 매뉴얼 진입 경로(§5 공통 레이아웃 — 사이드바 없음).
- **태그 관리자(S9, F38)** — 병합 "도구"(TagMergeTool)를 관리 "화면"으로 승격:
  - **목록 테이블**: 이름 · 사용 문서 수(doc_count) · 규칙 사용 배지(rule_count>0) — 검색·정렬(이름/사용 수). 행 클릭 = 사용 문서 보기(탐색 `?tag=` 필터 링크).
  - **유사 태그 제안**: `GET /api/tags/similar` 결과를 "『정규화』↔『정규 화』 병합할까요?" 목록으로 — [병합](방향 선택 = 남길 이름 지정, 기존 `tags/merge`) / [무시](세션 내 숨김 — 저장 안 함, 과설계 방지).
  - **행 액션**: 이름 변경(`PATCH /api/tags/{id}` — 중복이면 409를 "병합을 사용하세요"로 안내) · 병합(대상 선택) · 삭제(**미사용만** — 사용 중이면 비활성+사유 툴팁).
- **D-Day 관리(S4)** — DDL 변경 없이 두 종류:
  - 시험 분류 D-Day: 트리에서 시험 분류 선택 → `exam_date` 추가/변경/제거 (`PATCH /api/categories/{id}` 재사용).
  - 임의 D-Day(접수 마감·발표일 등 분류와 무관): 라벨+날짜 추가/수정/삭제 — `settings:ddays.custom = [{id, label, date}]` (settings GET/PUT 재사용).
  - 홈 D-Day 배지는 두 종류를 병합한 `dashboard.ddays`(§4.8) 사용.
- **API**: `settings`, `categories` PATCH, `backups`(S6).

### 5.12 모의고사 — `/exam` (구성) → `/exam/run` → 결과 (S11, F25)

- **구성 화면**: 진입은 퀴즈 설정(§5.6) 탭. ① 트리에서 시험 노드 선택 → ② **직계 자식 체크리스트**로 과목 구성(문제 보유 노드는 기본 전체 선택 — 회차 노드가 섞여 있으면 사용자가 해제. **회차 노드 1개만 선택 = "회차 그대로" 모의**: 전체 문항·순차가 기본값으로 전환) → ③ 과목당 문항 수(기본 20) · 제한 시간(기본 총 문항 × 1.5분 자동 계산, 수정 가능) · 합격선(기본 40/60 표기 "과목 40점 과락 · 평균 60점 합격", 수정 가능) · 출제 순서(랜덤/순차) → [응시 시작] = `POST /api/exam/session`.
- **런 화면(`/exam/run`)**: **QuizCard의 문항 렌더(지문 Markdown + 보기 버튼)를 재사용하되 채점 상호작용은 제거** — 보기 선택 = 답안 저장만(정오 색상·해설·오답노트 없음), 답 변경·이전/다음 자유 이동. 우측(모바일 하단 시트) **문항 네비게이터**: 과목 구분 + 문항별 응답/미응답 표시, 탭 이동. 상단 **카운트다운 타이머(프론트 주도 — §4.14)**: 잔여 5분 경고색(토큰), 0초 도달 시 확인 없이 **미응답 포함 자동 일괄 제출**. [제출] 버튼 = 미응답 N개 경고 확인 모달 후 `POST /api/exam/submit`. 세션은 zustand `examSession`(제출 전엔 서버 무상태) — 새로고침·이탈 시 "시험이 사라집니다" 경고(beforeunload + 라우터 가드).
- **결과 리포트**: 총점 + 합격/불합격 배지, **과목별 점수 바**(subject_min 컷 라인 표시, 과락 과목 `--wrong` 계열 배지), 소요 시간. **문항별 리뷰**(제출 후이므로 정답·해설 공개 — results 기반): 내 답 vs 정답, 해설 펼침, 오답 문항엔 **틀린이유 원탭(F36-⑤ 재사용** — `review_note_id`로 PATCH). 하단 [틀린 문제 재도전](`quiz/session{mode:'sequential', document_ids}` — §5.8 관례) · [오답노트 가기] · **최근 응시 이력**(`GET /api/exam/history` — 점수 추이 소표기).
- **엣지**: 선택 과목 전부 0문항 → 시작 불가 안내(422 메시지 렌더). 0문항 과목은 구성 화면에서 미리 비활성(개수 칩 0). 결과 화면은 리포트 데이터가 examSession에 있는 동안만 — 이탈 후엔 이력(history)에서 요약만 확인 가능(당시 상세 리포트 재열람은 저장하지 않음 — R15).
- **API**: `exam/session`, `exam/submit`, `exam/history`(§4.14), `quiz/session`(재도전).

**응용 모의고사 (S19, F45 — §4.21. 위 F25 화면의 확장 모드, 신규 라우트 최소)**

- **진입·구성**: 모의고사 구성 화면(위)에 모드 전환 탭 **[실전(기출)] / [AI 응용]** 추가. AI 응용 탭 = ① 범위 선택(기존 분류 트리 선택 재사용 — 실전 트리에서 고른다) → ② 문항 수(1~20)·엔진 선택(**S21, F47 — §4.23 ⓒ**: 공용 EngineSelect 게이팅 — `available:false`·`enabled:false` 엔진은 선택 불가 + 사유 1줄("설치 필요"/"로그인 필요"/"키 미등록"/"사용 안 함") + [다시 확인] 재진단 연계. 명시 지정 비가용·비활성 엔진은 서버도 422로 거부 — 프론트만 믿지 않는다. **S22, F48 — §4.24 ④**: 구체 엔진 선택 시 모델 소목록 select 함께 노출 — 미선택 = 설정값 폴백, generate에 `model?` 전달. **S24, F50 — §4.21 S24 ①**: [누적(기본)/1회성] 라디오를 엔진 선택과 나란히 배치 — 고정 설명 1줄 "1회성은 태그를 만들지 않아 생성 출력이 조금 절약됩니다", 선택값을 generate `mode?`로 전달) → [생성 준비] = `POST /api/applied-exam/prepare`(LLM 0).
- **재진입 진행 복원(S22, F48 — §4.24 ⓓ)**: 생성 잡이 도는 중 화면을 떠났다 AI 응용 탭에 재진입하면 **공용 복원 훅**이 전역 잡 목록(`GET /api/llm/jobs`)에서 kind `applied_exam`의 running·queued 잡을 재발견해 `ref.gen_id`로 기존 상태 폴링·진행 패널을 복원한다("진행 중 작업을 복원했습니다" 소표기 — F40-① `recovered` 전례). **재진입 시 진행 미표시 실측 결함(계획서 §14 F48 배경 ④)의 해소 지점.** done 잡(TTL 내)은 결과 요약·[응시 시작] 복원(상태 API가 결과 보존 — §4.21).
- **사용량 확인 스텝(필수 — 확인 없이 생성 불가)**: prepare 응답의 `estimate`(approx_input_tokens·assumed) + 근거 문서 수(source_counts) + 엔진·과금형(`billing`) 표시 — FetchImportWizard 확인 스텝·고정 고지 전례. [생성 시작] = `POST /api/applied-exam/{gen_id}/generate` → 진행은 `LlmJobProgress`·`LlmErrorInfo` 재사용. 범위 과대(422 too_large 문구)·근거 0건(422)은 서버 메시지 그대로 렌더.
- **생성 완료 → 응시**: 상태 응답 result의 `generated`/`requested`/`discarded[]` 요약 표시(**미달 시 경고 배지** — "요청 20 중 16문항 생성" + 폐기 사유 건수. 조용한 축소 금지. **S24**: `result.mode` 표기 — 누적 = 고정 문구 수준("태그 부여됨 — 분류 연결 제안은 제안함에서")·**응시 전 문서 상세 일괄 조회 금지**(정답·해설이 응시 직전 브라우저로 전송되는 경로 차단 — 2026-08-04 검토 반영, "사전 미리보기 없음" 방어선의 취지)) → [응시 시작] = 기존 `POST /api/exam/session`(subjects=[런 분류]·전체 문항·`sequential` 기본) → **런 화면·타이머·네비게이터는 위 F25 `/exam/run` 그대로 재사용**(문항 content 서두의 `[AI 생성 문항]` 마커 인용구가 그대로 렌더됨 — 생성 표시 상시, 숨기지 않는다).
- **제출·리포트**: 제출만 `POST /api/applied-exam/submit`으로 분기(요청 형태는 exam/submit과 동일). 결과 리포트 = F25 리포트 재사용 + 문항별 **[근거 문서] 링크 목록(`results[].basis` — 문서 상세로 이동)** + 이상 문항 **[오류 신고] 버튼(F30 `ReportErrorButton` 재사용)** + 상단 "AI 생성 모의고사" 배지. 틀린 문제 재도전·오답노트 가기는 기존 그대로(생성 문항도 일반 문서 — quiz/session document_ids).
- **이력**: AI 응용 탭 하단에 `GET /api/applied-exam/history` 소표기(실전 이력과 분리 표시 — 혼입 없음이 §4.21 결정 ②의 목적).
- **격리 확인(화면 규약)**: 실전 탭·일반 퀴즈 설정에서 생성 문항이 보이지 않는 것은 분류 격리의 자연 결과(별도 필터 UI 없음). 생성 문항은 탐색에서 "AI 응용 모의고사" 분류 아래 + content 마커로 항상 식별된다. 색상은 전부 토큰(불변 규칙 5).
- **API**: `applied-exam/prepare`·`generate`·상태·`submit`·`history`(§4.21), `exam/session`(응시 구성 재사용), `llm/status`(엔진·과금형).

### 5.13 제안함 — `/suggestions` (S6 · **S20 — F46 수신함 2탭 일반화**)

- **구성(S20 — §4.22 결정 ①)**: 수신함 2탭 — **[분류 연결]**(기존 S6 태그 규칙 제안 §4.9 — 목록·승인/거절 UI·로직 **무변경**, 탭 전환은 로컬 상태로 조건부 렌더만) / **[반입 개선]**(신규 — `ImproveInbox` + `components/improve/` 하위 7개: `ImproveCaseList`·`ImproveGenWizard`·`ImproveProposalList`·`ImproveProposalDetailModal`·`ImproveRegressionPanel`·`ImproveRegressionWizard`·`ImproveCaseDetailModal` + `labels.ts`(kind/origin/outcome 한국어 라벨 — 배지 색상은 전부 토큰)). 사이드바 배지(`SuggestionsNavBadge`) = 두 수신함 pending 합산.
- **실패 사례 목록**(`ImproveCaseList`): kind·origin 배지 + count(반복 발생) + 최근 발생 + [제거](DELETE) — 총량 50건 상한이라 `size=50` 1회 조회·페이지네이션 UI 없음(§4.22). 사례 체크박스 선택(1~20) → [개선 제안 생성]. 근거 사례 클릭 = 상세 모달(`ImproveCaseDetailModal` — 별도 라우트 없음).
- **제안 생성 위저드**(`ImproveGenWizard` — prepare→확인→generate→진행→결과): **사용량 확인 스텝 필수(확인 없이 생성 불가)** — `estimate`(approx_input_tokens·assumed)·사례 수·엔진·과금형(`billing`) 표시(FetchImportWizard 전례) → 진행 `LlmJobProgress`·`LlmErrorInfo` 재사용 → 결과 요약 = proposal 수 + **discarded 사유 건수(조용한 축소 금지)**. 422(사례 과다·200,000자 초과)는 서버 메시지 그대로 렌더.
- **제안 카드·상세**(`ImproveProposalList`·`ImproveProposalDetailModal`): kind 배지(사례집 추가/프롬프트 수정/**코드 수정 필요**) + title·rationale·근거 사례 링크. 상세 = kind별 적용 미리보기 — casebook = 추가될 엔트리 전문 · prompt_edit = 헝크 before-after 비교 + 적용 후 전문(`preview_after`) + `policy_check` 위반 표시 · code_issue = 재현 패키지 전문 + **[복사]**(인계용). [승인 반영]/[거절] — code_issue는 [인계 완료](파일 쓰기 0). 409·422(잠금 위반·본문 변경·비 pending)는 서버 message 렌더.
- **회귀 패널**(`ImproveRegressionPanel`·`ImproveRegressionWizard`): applied 제안에서 [회귀 재검증] → 사례 선택(기본 = 근거 사례 + 같은 kind, 1~10건 — `user_report` 사례는 선택 목록에서 원천 제외) → **estimate 확인 스텝** → 실행 → `results[]` outcome(통과/여전히 실패/다른 실패/원본 없음) 표시 + 사례의 regressions 이력 소표기.
- **API**: `suggestions`·`suggestions/apply`(기존 §4.9 — 무변경), `improve/cases`(3)·`improve/gen`(3)·`improve/proposals`(4)·`improve/regression`(3)(§4.22 — 훅은 `api/improve.ts`, 폴링 키는 gen_id·reg_id — answerKey key_id 관례).

### 5.14 LLM 작업 센터 — 전역 패널 (S22, F48 — §4.24. **라우트가 아닌 전역 슬라이드 오버**)

- **열림**: §5 공통 레이아웃의 진입점(PC/태블릿 = 사이드바 하단 고정 버튼 · 모바일 = 좌측 드로어 항목 — 하단 탭바 5개 불변)에서 열린다. 어느 화면에서든 현재 상태를 떠나지 않는다(퀴즈·시험 세션 무영향 — 라우트 이동 아님). PC = 슬라이드 오버, 모바일 = 바텀 시트(구현 재량) — 색상 전부 토큰(불변 규칙 5).
- **목록**(`GET /api/llm/jobs` — §4.24 ⓐ): 진행 중(진행 표시 = `LlmJobProgress` 재사용 — 경과·토큰·ETA 그대로) → 대기(순번 표시) → 최근 완료·실패·취소(잡 TTL 1시간 내 — 그 이전 기록은 없다·영속 이력 아님). `label`·상태 문구는 서버 값 그대로 렌더(프론트 문구 조립·포맷 분기 금지 — §4.10 `notes` 관례). 빈 목록 = "진행 중인 작업이 없습니다". 각 항목 **[화면으로 이동]** = kind→라우트 매핑 상수 1곳(§4.24 ⓓ 표 — 복원 훅과 공유, 모르는 kind는 링크 미렌더). 이동한 화면에서는 복원 훅이 진행 표시를 이어받는다(중복 폴링 금지 — 쿼리 키 공유).
- **취소**(§4.24 ②): 대기 항목 = 즉시 취소(비용 0 — 확인 없음). **실행 중 항목 = 확인 다이얼로그 고정 문구 "실행 중 취소는 이미 처리된 토큰만큼 요금이 발생할 수 있습니다"** 후 `POST /api/llm/jobs/{id}/cancel`. 409(완료 경합 — "이미 완료된 작업입니다")는 서버 message 그대로 렌더 + 목록 갱신(완료로 표시 — 결과는 보존돼 있다). 취소된 잡은 "취소됨" 종료 상태(오류 색 아님 — 중립 토큰).
- **일시정지 토글**(§4.24 ③): 패널 상단 — `queue/pause`·`resume`. 안내 1줄 고정: **"실행 중인 작업은 끝까지 진행됩니다 — 다음 작업부터 보류합니다"**(정직한 경계 — LLM 호출 중간 정지 아님). paused 중에도 각 화면에서 잡 등록은 가능(대기로 쌓임 — 목록에 표시). 서버 재시작 시 해제됨.
- **폴링**: 전역 1곳(TanStack Query 키 1개 — 사이드바 배지·패널·복원 훅 공유). 진행 중 잡이 있을 때만 짧은 간격, 없으면 완만(구체 간격 구현 재량 — 인메모리 조회라 서버 비용 0). 배지 = running+queued 건수.
- **재진입 복원 공용 훅**(§4.24 ⓓ·⑤): 적용 화면 전수 — `AppliedExamPanel`(§5.12 — 원 결함 지점)·`ExplainJobPanel`·`AnswerKeyImportWizard`·`ImproveGenWizard`·`ImproveRegressionWizard`·`RegenerateJobPanel`(기존 진행 배지와 중복 렌더 금지 — 훅으로 수렴)이 **훅 1개를 공유**(화면별 개별 구현 금지). `ImportQueue`·`FetchImportWizard`의 기존 localStorage 복원은 불변(검토 단계 1차 키 = preview_id — §5.9. S22 추가분은 처리 중 1건 [취소] 노출뿐).
- **API**: `llm/jobs`·`llm/jobs/{id}/cancel`·`llm/queue/pause`·`llm/queue/resume`(§4.24 — 전부 LLM 0·DB 0).

### 5.15 분할 반입 위저드 — 반입 서브플로 (S23, F49 — §4.25. **상시 메뉴 없음 — `too_large` 대안 진입 전용**)

> 골격 출처: §4.25(계약 정본 — 결정 ㉮~㉳·신규 엔드포인트 4개·구현 앵커)와 `stage-23-llm-split-import.plan.md` §4. 두 출처에 없는 세부는 **구현 재량**으로 명기 — 이 절이 새 결정을 만들지 않는다.

- **진입**: 반입 화면(§5.9)의 convert 잡 실패 렌더에서 `error_info.kind:'too_large'`이고 `alternatives`에 `'split_import'`가 있으면 **[분할 반입]** 버튼을 렌더한다(**아는 값만 렌더** 전방 호환 관례 — §4.11, `api/types.ts` alternatives 유니온에 값 추가). **fetch 잡(FetchImportWizard) 발생분은 기존 정책 그대로 — 버튼 미추가**(§4.18 ⑥·§4.25 개정 표). too_large 안내의 원본 저장 소표기("원본은 sources/에 저장했습니다" — ㉲)를 포함한 action 문구는 서버 완성 문장 그대로 렌더. 버튼 클릭 후 원본 전달 방식(URL 프리필 / 파일 재선택 안내)은 **구현 재량** — 서버 계약은 `POST /api/import/split`에 multipart `file` 또는 `{url}` 재제출(§4.25 ⓐ)이며, 위저드의 라우트 표현(반입 화면 내 서브플로 진입 파라미터 등)도 구현 재량.
- **위저드 4스텝**(신설 `components/SplitImportWizard.tsx` — 이름 구현 재량, 공용 Stepper 재사용 §5.9 F36-⑪):
  - **① 분석**: split 시작(동기·**LLM 0**)이 휴리스틱 분할안을 즉시 반환해 표시. `confidence:'ok'`면 제안 없이 다음 스텝 진행, `'uncertain'`이면 **"LLM 정밀 분석?" 제안** 렌더 — `analyze_estimate`(approx_input_tokens·assumed) + **공용 EngineSelect**(비가용·비활성 게이팅 + 모델 소목록 — F47·F48 재사용, §5.9)와 함께, **확인 없이 호출되는 경로 없음**(F35). 실행 = `POST …/analyze`(202, kind `'split_analyze'`) → 진행 = `LlmJobProgress`·`LlmErrorInfo` 재사용, 완료 시 chunks가 갱신본으로 대체(휴리스틱안은 서버 이력 보존 — §4.25 ⓐ GET). 휴리스틱 후보 0에 정밀 분석도 실패하면 수동 분할 안내 폴백(정직 실패 — 조용한 균등 분할 없음).
  - **② 분할안 확인(핵심 게이트 — 생략 불가)**: 조각 목록(라벨 · 크기(chars) · head 발췌 200자 — 원문 부분 문자열, LLM 산출 원문·정답 미포함) + **체크박스 선택** + **인접 [합치기]만**(비인접 병합 불가 · **경계 문자 단위 편집 없음** — 과설계 기각 확정. 병합 결과 조각당 20만 자 초과 = 서버 422 메시지 렌더) + **조각별 `category_path` 초안 편집**(조각 라벨이 초안 — F40-③ 검증 규칙 동일, 분류 확정은 기존 preview 승인 경로 그대로). "무라벨 구간" 조각도 목록에 그대로 노출(부분 잘림 투입 금지 — 조용한 탈락 없음). 40개급 목록의 모바일 표시 밀도는 **구현 재량**(스텝 분리가 복잡도 억제 장치 — stage-23 리스크).
  - **③ 비용 확인**: 선택분 `estimate` 합계(chunks[].estimate의 **프론트 단순 합산**) 표시 — 확인 없이 투입 불가(F35 — 정밀 분석 ①과 조각 변환 ③의 비용 2중 확인).
  - **④ 투입**: [선택 N조각 변환 시작] = `POST …/enqueue`(`selections`·`category_paths?`) → **기존 convert 잡 N개** 등록(응답 `{jobs: [{job_id, chunk_ids, label}]}`) → **기존 ImportQueue(§5.9 ①)에 합류**(신규 진행 UI 없음). 이후 조각별 미리보기 검토·승인·커밋은 기존 ②·③ 단계 그대로(R7 — 승인 없는 커밋 경로 0).
- **재진입·복원·취소**: `split_analyze` 잡은 공용 복원 훅(`useJobRecovery` — §5.14·§4.24 ⓓ) + `utils/jobRoutes.ts` kind→라우트 매핑 추가(ref = `split_id`, 복원 화면 = 이 위저드)로 재진입 복원되고, **작업 센터(§5.14) 목록·취소에 합류**한다(실행 중 취소 = 부분 과금 확인 다이얼로그 — §5.14 고정 문구). 분할안 자체는 서버 보존(`import/split/` JSON 최근 20건 + 인메모리 TTL 1시간 — 만료 후 GET = 디스크 복구, ㉲)으로 이어받는다.
- **재진입 앵커(사용자 실사용 피드백 반영, stage-23 DoD 7)**: `useJobRecovery`는 `split_analyze` 잡이 실제로 돌 때만 잡는다 — 휴리스틱만 쓴 흔한 경로(정밀 분석 미실행)는 잡 자체가 없어 위저드를 닫거나 다른 화면에 갔다 오면 재진입 입구가 없었다("분할작업중에 다른창을 갔다오면 접근하기 어려워"). 또한 원래 `too_large` 실패 항목이 큐에 "실패"로 그대로 남아, 분할이 진행 중이어도 원본이 두 번(실패 + 분할 진행) 있는 것처럼 혼란스러웠다("기존것이 남아있어 혼란스러워"). 해소: `ImportQueue`(`utils/convertQueue.ts`)의 큐 항목에 `split_id`를 심어 **`'split_in_progress'`** 상태(중립 토큰 배지 + **[분할안 열기]** 버튼)로 전환한다 — [분할 반입] 클릭으로 `POST /api/import/split`이 성공(split_id 확보)하는 즉시 전환되고, localStorage 지속이라 새로고침·다른 화면 이동 후에도 유지된다. [분할안 열기]는 그 `split_id`로 위저드를 곧장 재오픈(§5.15 딥링크 경로 재사용). 위저드가 **enqueue를 완료**(조각 convert 잡 N개 등록)하면 앵커 항목 자체를 큐에서 제거한다(조각들이 새 항목으로 이미 합류 — 중복 앵커 잔존 금지). 위저드를 중간에 닫으면 항목은 `'split_in_progress'`로 남아 재진입 앵커 역할을 계속한다. `split_id`가 서버에서 만료(GET 404 — TTL 1시간·디스크 최근 20건 초과)된 채로 [분할안 열기]를 누르면 안내 후 앵커를 원래 실패 상태로 되돌려([분할 반입] 재시도 가능) 조용히 막히지 않게 한다.
- **같은 원본 재사용**: 같은 원본(hash12) 재분할 요청 시 서버 안내 필드 기반 **"기존 분할안 재사용(비용 0) / 새로 분석" 선택 UI**(㉳).
- **엣지 — 서버 완성 문장 그대로 렌더(§3·§4.11, 프론트 문구 조립 금지)**: 20만 자 이하("분할이 필요 없는 크기입니다 — 일반 반입을 이용하세요") · 원본 200만 자 초과 · 조각 40개 초과(수동 분할 안내) — 전부 422.
- **색상 전부 토큰**(불변 규칙 5).
- **API**: `import/split`(시작·동기)·`…/{split_id}/analyze`·`…/{split_id}`(상태)·`…/{split_id}/enqueue`(§4.25 ⓐ — 신규 4개 전부). 이후는 convert 잡·preview·commit 기존 계약 그대로(20만 자 이하 원본의 반입 경로는 완전 불변).

### 5.16 노트(베타) — `/notes`(목록) · `/notes/:id`(편집) (S33·S34, 에디터 v2 M33 — §4.28. **상시 메뉴 없음 — 격리 베타 표면**)

> 골격 출처: **api §4.28**(저장 계약 정본) + 별지 `editor-v2.plan.md` §5.2·§5.4·§12(D9) + 지시서 `stage-33-notes-surface.plan.md`(코어)·`stage-34-notes-dialect.plan.md`(방언·참조 칩·이미지·붙여넣기). 두 출처에 없는 세부는 **구현 재량** — 이 절이 새 결정을 만들지 않는다.
> **이 화면이 존재하는 이유**: 새 편집기(BlockNote 0.54.0 — D2)를 **기존 편집기·`documents`에 손대지 않고** 완성하기 위한 착지 표면이다(D9 드래프트 격리). 따라서 **기존 화면·컴포넌트는 1바이트도 바뀌지 않는다**(허용 접촉점 = App.tsx 라우트 2줄 + 설정 실험실 카드 1개 — 지시서 "격리 계약").

**노출 방식 (기존 화면 무접촉 원칙 하에서)**

- 진입은 **설정 화면(§5.11) 그룹 ⑥의 실험실(베타) 카드 링크 1개** + 직접 URL(`/notes`)뿐이다. **사이드바·모바일 하단 탭바·홈 대시보드는 무변경**(탭바 5개 불변 — F38·F36 관례).
- 사유: ① 베타 표면이 상시 메뉴에 오르면 "정식 기능"으로 읽혀 실사용 데이터가 검증 전 저장소로 몰린다 ② 격리 해제(정식 노출)는 M34에서 `documents` 탑재를 판정할 때 함께 결정한다. **→ 결정(2026-08-18, stage-35 착수 시 확정): 정식 노출하지 않는다** — documents가 새 편집기의 정식 표면을 얻으므로 노트를 승격할 사유가 소멸한다. 재론은 M36(구 편집기 퇴역 판단과 함께). **→ M36 재론 완료(2026-08-22, stage-38 G-9) · N-1 = ⓒ 확정(같은 날 — 사용자 위임 → Claude 판정)**: notes = **존치 확정·노출 현상 유지**(실험실 카드 — "베타 = 검증용 임시물" 성격은 해제, 존치 기능이다(별지 §12-5 — v2.x 캡처 파이프라인의 착지 표면)). 정식 승격(사이드바/탭바 노출 — 검색(FTS)·인쇄·백업 UX 통합 선행 필요)·documents 이관은 **v2.x 캡처 계획서 시점 재론**. 정본 = `stage-38-mobile-v2-closeout.plan.md` 규약 D + 말미 "사용자 결정 4건 확정" ①.
- 라우트 2개는 **lazy 청크**(`React.lazy` + `Suspense` — stage-31 `/editor2-poc` 전례). 편집기·Mantine 번들은 이 라우트에 들어갈 때만 로드된다(R37 — 초기 청크 증가 ≤ 5KB(min)가 DoD).

**목록 — `/notes`**

- 상단: 제목("노트 (베타)") + 한 줄 안내("새 편집기 시험용 저장소입니다 — 기존 문서와 분리돼 있습니다") + **[새 노트]**(= `POST /api/notes`로 빈 노트 생성 후 편집 화면으로 이동).
- 검색 입력(`q` — 디바운스, §4.28 ④ LIKE) · 목록 = 카드/행(제목 또는 "제목 없음" + `excerpt` + 수정일 상대 표기) · 정렬은 최근 수정순 고정(옵션 없음) · 페이지네이션은 기존 목록 관례(size 50).
- 행 액션: 열기 · **삭제**(확인 후 `DELETE` = 소프트 삭제 — "노트를 삭제할까요? 목록에서 사라집니다"). **휴지통·복구 UI는 없다**(§4.28 ⑥ — 베타 범위 밖).
- 빈 상태: "아직 노트가 없습니다 — [새 노트]로 시작하세요".
- 색상 전부 토큰(불변 규칙 5).

**편집 — `/notes/:id`**

- 레이아웃: ① 상단 바 = 제목 입력(단일 행·플레이스홀더 "제목 없음") + **저장 상태 표시** + **[미리보기] 토글**(버튼 라벨은 `미리보기`, 열리는 **패널 제목은 "Markdown 미리보기"** — 2026-08-16 구현 실측에 맞춘 문구 명확화. 좁은 상단 바에서 라벨을 짧게 두고 패널이 성격을 밝힌다) + [삭제] + [목록] ② 본문 = BlockNote 편집 표면(`BlockNoteView` — `@blocknote/mantine`, 한국어 사전 `ko`) ③ (토글 시) 미리보기 패널.
- **저장(§4.28 ③ + stage-33 규약 C)**: **디바운스 자동 저장**(유휴 1.5초·최대 대기 10초) + 명시 저장(Ctrl+S). **IME 조합 중에는 저장을 트리거하지 않는다**(S30 ⑥ 규약 계승). 상태 **4종** = `저장 대기…`(변경분이 있으나 아직 저장 주기 전) / `저장 중…` / `저장됨 (시각)` / `저장 실패 — 다시 시도`(서버 `message` 그대로 렌더 — §3) — *2026-08-16 3종에서 갱신: 자동 저장은 사용자가 명령하지 않은 저장이라 "변경분을 앱이 알고 있다"를 보여 주는 상태가 있어야 이탈 시 불안이 없다.* 실패해도 로컬 편집분을 버리지 않고 다음 주기에 재시도하며, 미저장 상태로 이탈하면 `beforeunload` 경고. ~~**상시 [저장] 버튼은 두지 않는다** — 명시 저장은 Ctrl+S이고, `지금 저장` 버튼은 **저장 실패 상태에서만** 노출한다~~(**v1.46에서 대체 — 아래 S39 확정 계약이 현행 정본**). **← 개정(2026-08-23 사용자 결정 — FB-2 채택·Design v1.45. v1.37 "상시 [저장] 버튼 없음" 확정의 번복 절차 이행 = 이 추기)**: 노트 에디터에 **[저장]/[취소] 상시 버튼을 도입**한다(필수 — N-2 구 편집기 퇴역 실행 재론 전 선행. 결정 주체 = 사용자·경위 = 별지 `editor-v2.plan.md` §13 FB-2 행). 자동저장과의 공존 방식·[취소]의 복귀 기준점·적용 표면 전수(stage-36 통합 문서 편집 표면 포함 여부)는 **`stage-39-note-save-controls.plan.md` "착수 전 결정"이 확정 지점**이며, 확정·구현 후 이 절을 실측 계약으로 재개정한다. 그 전까지 위 원문(자동저장·상태 4종·Ctrl+S·실패 시 `지금 저장`)이 현행 구현의 정본이다. **← 착수 전 결정 확정 도착(2026-08-23 사용자 — 같은 날)**: **자동저장 유지**(크래시 복구의 원천) + 편집 세션 진입 시점 **원본 스냅샷** 보관 + **[저장] = 유지 확정 / [취소] = 원본 복귀**(확인 다이얼로그 — 세션 종결은 명시적 결정) + **예기치 못한 종료 시 재진입에서 자동저장분 호출**(복구 선택 다이얼로그 — 스냅샷은 클라이언트 영속 보관·정상 종결 시 폐기). 적용 = 노트 1차(문서 편집 표면은 상시 저장/취소 부재 실측분만). 계약 상세 정본 = stage-39 규약 A~E — 이 절의 실측 재개정은 구현 완료 후(이 추기는 서술 확정 기록 — 판번 유지). **← 실측 재개정(2026-08-24 — stage-39 구현·검토 종결 · Design v1.46. 이하가 현행 확정 계약이다 · S39)**: ⓐ **상시 [저장]/[취소] 버튼** — [저장] = 명시 확정(Ctrl+S와 동일 flush 경로 · 변경분 없음/저장 중 비활성) · [취소] = **편집 세션 진입 시점 원본 스냅샷으로 복귀**(확인 다이얼로그 필수 · 복귀 후 저장 — 자동저장이 서버에 쓴 분까지 물린다). 자동저장·저장 상태 4종·IME 보류는 **무변 유지**(크래시 복구의 원천 — 종전 "실패 시에만 `지금 저장`" 조건부 버튼은 상시 [저장]에 흡수) ⓑ **세션 스냅샷** = localStorage `studyhub.note.snapshot.<id>`(표면별·대상당 1개) · 체크포인트 = 진입 시 + 명시 [저장]/[취소] 성공 시(자동저장은 체크포인트를 옮기지 않는다 · 명시 저장 체크포인트는 서버 송신 블록을 로드 경로와 같은 함수로 되읽어 확보 — 거짓 복구 다이얼로그 방지) ⓒ **크래시 복구** — 재진입 시 미폐기 스냅샷 발견 = 복구 선택 다이얼로그(**이어서 편집**(자동저장분) / **원본으로 복귀**(스냅샷)) · **결정 없이 이탈하면 다음 진입에 다시 묻는다**(조용한 자동 선택 금지) ⓓ 스냅샷 기록 실패 = [취소] 비활성 + 사유 안내(조용한 강등 금지) ⓔ **문서 상세 편집 표면(진입점① `DocBlockEditor` — §5.3 S35 분기 표면)에 동일 계약 적용**(스냅샷 키 `studyhub.doc.snapshot.<id>` · [취소] = 폼 필드 포함 복귀 · 진입점②~⑥ DocEditor 계열은 기존 폼 [저장]/[취소] 기존재로 비적용 — 이중 UI 금지). 정본 = `stage-39-note-save-controls.plan.md` 규약 A~E·완료 기록(잔여 = DoD 8 실사용·검토 경미 5건).
- **저장 파이프라인(계약)**: BlockNote 문서 → **어댑터**(앱 중립 블록, 순수 JSON 변환) → `blocksToMarkdown` 프로젝션 → `PATCH { content_blocks, content }`(**항상 동반** — §4.28 ③). 로드는 역방향. **프로젝션을 서버가 만들지 않는다**는 §4.28 원칙의 화면 측 이행 지점이다.
- **미리보기 패널("Markdown 미리보기")**: 방금 만든 프로젝션 문자열을 **공용 `MarkdownView`로 그대로 렌더**한다(import만·무수정 — D5 "전환기 리더는 MarkdownView 단독"의 이행이자, 왕복 결과를 눈으로 확인하는 검증 도구를 공짜로 얻는 장치).
- **테마**: 편집기 색은 전부 `tokens.css` 변수 경유(라이트/다크 전환이 편집 표면에도 즉시 반영 — 불변 규칙 5). 문서별 스타일(F53 `documents.style`)은 **노트에 적용하지 않는다**(노트는 `documents`가 아니다 — 범위 밖).
- **빈 문단**: 말미의 연속 빈 문단은 저장 시 1개만 남긴다(stage-33 규약 D). 본문 중간의 빈 문단은 **보존**되며, Markdown 프로젝션에서만 사라진다(정상 — 프로젝션 비대칭 계약).

**편집 표면의 기능 범위 (단계별)**

| 구분 | S33(stage-33) | S34(stage-34) |
|---|---|---|
| 블록 | 문단·헤딩(1~6)·목록 3종(글머리·번호·체크, 중첩)·인용·코드 블록(lang 드롭다운)·표·이미지 블록·구분선 | + 콜아웃(`:::note/warn/tip/fold/hide` — **고정 목록은 입력 UI에만** 건다. 목록 밖 variant(`:::노트[…]` 등 기존 데이터)는 **값 그대로 보존 + 기본 스타일로 표시** — 스키마 화이트리스트로 거부·치환하지 않는다(2026-08-16 실측 정정: 거부하면 기존 데이터가 적재에서 변형된다. 규약 E의 의도 = **새 입력 경로 차단**))·문서 임베드·수식 블록·sourceFallback(읽기 전용 보존 블록) |
| 인라인 | 굵게·기울임·취소선·밑줄·코드 스팬·링크 | + 형광펜(7색)·스포일러·글자색/바탕색/크기(`:t`)·**참조 칩 3종**·인라인 수식·인라인 이미지(보존 전용) |
| 입력 | 타이핑·슬래시 메뉴·드래그(엔진 기본값 그대로) | + **이미지 3진입점**(붙여넣기·드롭·슬래시 `/이미지` — §4.27 재사용) · **HTML 붙여넣기 화이트리스트 변환**(기존 `htmlPasteMarkdown` 재사용, `text/plain`은 평문 그대로). **붙여넣기는 미지원 서식이 섞여 있어도 안내 후 삽입**한다(2026-08-16 실측 — 로드 경로의 전부-아니면-전무 폴백과 의도적으로 다르다: 삽입 단위가 작고 사용자가 방금 한 조작이라 차단보다 안내가 맞다) |
| 미지원 서식 | 방언이 든 노트는 편집 표면에 올리지 않고 **읽기 전용 폴백 + 안내**(덮어쓰기 사고 0) | **방언 팔레트에 한해** 폴백 경로 **제거**(방언 미지원 0이 DoD) — 아래 "코어 범위 제약" 행은 예외. **신규 사유 1종 `inline:mathStyles`**(인라인 수식에 걸린 서식 — 채택 스펙의 propSchema가 비어 담을 자리가 없다. 코퍼스 표본 0건 · 툴바 입력은 아래 "원자 서식 가드"로 차단) |
| **코어 범위 제약 7종**(2026-08-16 추가 — stage-33 어댑터 실측) | 순수 CommonMark/GFM인데 **BlockNote 0.54 코어가 담지 못하는** 표현 = 느슨한 목록 · 인접 목록 경계 · 표 열 정렬 · 경성 줄바꿈 · link title · image title/height · block meta. **조용히 버리지 않고 명시적으로 미지원 보고**(폴백 대상) | **흡수 4종**(경성 줄바꿈·link title·image title/height·block meta) = **왕복 보존**(기존 데이터 수용 전용 — 새로 만드는 UI 없음). **← 2026-08-16 이행 완료**: 경성 줄바꿈 = 원자 인라인(편집기 타입명 `lineBreak`) · link title·block meta = **사이드카(블록 id 키 · 메모리 전용 — 저장분에서 매 로드마다 재유도)** · image title/height = 내장 image 스펙 prop 확장 / **판정 대기 3종**(느슨한 목록·인접 목록 경계·표 열 정렬(열 증감 시)) = **명시 보고 유지 · 손실 수용 여부는 사용자 판정(R34, 시한 = stage-35 착수 전)**. 배정 정본 = `editor-v2.plan.md` §5.4 말미 표 · 이행 규칙 = `stage-34-notes-dialect.plan.md` 규약 I. 표 열 정렬 **편집 UI**는 M35(stage-36) |

**참조 칩 UX (S34 — 별지 §5.4 ①~④ 확정분의 화면 표현)**

- **삽입**: 툴바 [참조] · 슬래시 `/참조`(링크 칩)·`/임베드`(문서 임베드 블록)·`/앵커`(문서 안 앵커). **피커 없이 삽입되는 경로는 없다.** **`[[` 같은 자동 트리거는 없다**(2026-08-16 실측 — 진입은 위 4개뿐이다. 필요가 실측되면 계획서에 먼저 등재).
- **피커**(3종 공용 인라인 팝오버): 검색 입력(**`GET /api/search?q=`** — item의 `doc_no`·`title`·`snippet`을 그대로 쓴다. 250ms 디바운스. 목록 보조는 `GET /api/documents`) + **최근 문서** + ↑↓·Enter·Esc. **앵커는 현재 노트의 헤딩 목록에서 선택**(자유 입력 없음) — 참조 대상으로 쓸 수 없는 제목은 **비활성 + 사유**로 노출한다(숨기지 않는다).
  - **"최근 문서"의 실질 = "노트에서 최근 참조·삽입한 문서"**(localStorage 최대 10건 — 2026-08-16 실측 정정). D9 격리로 **기존 문서 뷰어에 열람 훅을 걸 수 없어** 진짜 "최근 열람"이 아니다(신규 API 0 계약 유지). 격리 해제(M34) 시 재검토 대상.
- **선택 텍스트**: 드래그한 상태로 **칩**(`/참조`·`/앵커`)을 삽입하면 그 텍스트가 **칩 라벨로 승계**된다(= 지정 라벨형). 라벨에 쓸 수 없는 문자가 있으면 **삽입을 강행하지 않고** 사유 + 라벨 입력란 + [라벨 없이 삽입]/[취소]를 제시하며 **선택한 원문은 그대로 남는다** — **텍스트가 조용히 사라지는 경로 0**(2026-08-15 사용자 피드백의 해소 지점).
  - **임베드(`/임베드`)는 선택 텍스트를 소비하지 않는다**(2026-08-16 실측 — 별지 §5.4 ②는 "칩" 기준이고 임베드는 **블록**이라, 블록 삽입으로 본문 글자가 지워지는 경로를 만들지 않았다. 선택은 라벨 후보로만 쓰이고 그대로 남는다).
- **라벨**: 기본은 **대상 제목 추종**(라벨 문자열을 저장하지 않는다 — 제목이 바뀌면 따라간다). 제목 조회는 `POST /api/documents/resolve-embeds`(§4.19) 배치 + 세션 메모리 캐시. 지정 라벨은 팝오버에서 편집·[제목 추종으로 되돌리기] 가능. 미해결·삭제 대상은 **자리표시자 칩**(중립 토큰 + 사유 — F43 문구 계승). **칩 안에는 축약 문구까지만**(예: `삭제된 문서 (DOC-xxxx · 제목)`), **§4.19 ④ 전문은 `title` 속성과 칩 팝오버**에 둔다(2026-08-16 실측 정정 — 전문을 인라인 칩에 넣으면 문단이 무너진다. 문구 단일 출처 = `describeRef`).
- **클릭 동작**: **편집 표면 = 팝오버**([문서 열기]·[라벨 편집]·[대상 변경]·[삭제]) / **읽기 표면(미리보기·기존 문서 화면) = 이동**(기존 F43 동작 그대로 — 리더 무수정이라 자동 충족).

**방언 툴바 (S34 — 2026-08-16 구현 실측. 새 결정이 아니라 확정 규약의 화면 표현)**

- **마이크로 3종(밑줄·형광·스포일러)은 라디오처럼 대체**된다(별지 §5.4 ⓐ 상호 배타 — 툴바 커맨드·역방향 정규화·붙여넣기 3지점이 같은 상수 하나를 본다). 가로채는 단축키는 **기존 `Mod+U` 하나뿐**이며 형광·스포일러 전용 단축키는 두지 않는다.
- **"형광 끄기"는 `:t{bg=}`도 함께 지운다** — F52에서 형광펜과 바탕색은 **같은 시각 채널의 두 표기**이기 때문이다(`==` = 기본 노랑 / `:t{bg=색}` = 색 지정 · 둘 다 `--mark-*` 토큰). 사용자에게 둘을 다른 기능으로 가르치지 않는다.
- **원자 서식 가드**: 참조 칩·인라인 이미지·인라인 폴백·경성 줄바꿈·**인라인 수식**이 선택에 포함되면 **방언 서식 버튼을 비활성화**한다(적용해도 저장·재로드에서 사라지므로 — 눈에 보이는 비활성이 조용한 손실보다 낫다). **알려진 한계**: BlockNote **코어 버튼·코어 단축키(`Ctrl+B` 등)는 막지 않는다**(코어 키맵 감싸기는 M35 범위). 부작용으로 **수식이 든 문단을 통째로 선택하면 방언 버튼 6종이 비활성**으로 보인다(의도한 비용).
- **콜아웃 본문**은 BlockNote 중첩 그룹으로 렌더돼 상자 "바깥 아래"에 붙는다 — 좌측 보더·들여쓰기로 이어 붙였고 **한 상자로 감싸는 마감은 M35(stage-36)**.
- **미리보기 패널의 이미지 크기**: 편집기가 왕복시키는 `{w=}`를 공용 `MarkdownView`(remarkStudy)가 이제 폭(선택 높이)으로 반영한다(**stage-35 F-6 이행 완료** — §5.3 S35 계약과 동일 구현, 전 화면·인쇄 자동 파급. `{w=}` 없는 이미지·기존 문서는 렌더 diff 0).

**툴바 정비 · 다단 (S40·S41 — 2026-08-24 편성 추기 · Design v1.47. 지시서 `stage-40-editor-toolbar.plan.md`·`stage-41-flow-columns.plan.md`가 규약 정본 — 이 절은 화면 계약만 · 착수 전 결정은 권고안 채택(위임 판정 — 사용자 답변 도착 시 개정) · 구현 후 실측 재개정)**

- **적용 표면**: `BlockNoteView`가 열리는 편집 표면 전부 — 이 절의 노트 편집 + **문서 편집 진입점 ①~⑥(§5.3 S35/S36 — 본문·해설 등 표면당 1개)**. 계약 정본은 이 절 한 곳(§5.3에는 포인터 1줄 — S39 전례).
- **S40 도킹 서식 툴바(FB-9) — 부유 툴바와 병행**: 편집 표면 **상단에 상시 고정(sticky)** 되는 서식 툴바를 둔다(모바일도 상단 — 하단 배치는 실기기 불편 보고 시 재론). 기존 부유 툴바(선택 시 표시 — stage-38 모바일 3줄 규칙 포함)는 **무변 병존**. **항목 구성은 부유·도킹 단일 출처**(같은 빌더 — 도킹 전용 항목 0) · 표면마다 1개 · 판정 기준 = 그 표면 편집기의 현재 선택/커서 · 클릭이 편집기 선택을 잃게 하지 않는다 · 모바일 도킹 = **1줄 가로 스크롤**(상시 점유 높이 최소) · 도킹 on/off 설정 없음(settings 키 0).
- **S40 블록별 툴바 항목 필터(FB-6) — 부유·도킹 공통**: 선택(또는 커서) 블록 집합이 **전부 원자·미디어 블록**(`image`·`webEmbed`·`docEmbed`·`toc`·`divider`·`mathBlock`·`sourceFallback`)이거나 **전부 코드 블록**이면 **텍스트 서식군을 숨긴다**(렌더 생략 — 비활성 아님: 굵게·기울임·취소선·링크·밑줄·형광·스포일러·글자색/바탕색/크기·인라인 코드·참조 삽입) · 블록 기능군(블록 타입 변경·콜아웃·이미지 자르기·(S41) 다단)은 유지 · 텍스트 블록이 섞이면 표시(기존 **원자 인라인 서식 가드**는 별도로 그대로 — 두 규칙은 직교) · 빈 집합(미포커스)은 전체 표시. 규칙은 순수 함수 1개로 두고 단위 검사(`s40-toolbar-filter`).
- **S40 인라인 코드 버튼(FB-11 ⓒ)**: 텍스트 서식군에 **[코드]** 버튼 추가 — 코어 `code` 스타일 토글(백틱 입력 규칙·`Ctrl+E`·어댑터 `inlineCode` 왕복은 기존재 — 발견성 결함 해소일 뿐 새 서식 아님) · 원자 가드 대상.
- **S40 코드 블록·인라인 코드 외양(FB-11 ⓐ)**: 편집 표면의 코드 블록 = 토큰 배경(`--surface-raised` 계열)·테두리(`--border`)·둥근 모서리·모노 폰트·**상단 언어 라벨**(엔진 언어 드롭다운 자리 정돈) · 인라인 코드 칩 = 같은 토큰 · 다크는 토큰으로 자동(불변 규칙 5 — 색 리터럴 0). 리더(`MarkdownView`)의 코드 외양은 무접촉(편집 표면을 리더에 근접). **구문 강조는 없다**(FB-11 ⓑ — 신규 의존 0 원칙으로 제외·후속 판단) · 복사 버튼 없음(후속).
- **S41 흐름형 다단(FB-10 ⓐ — stage-40 완료 후)**: 컨테이너 블록 **`columns`(2단·3단만 — 4 이상 없음)** · 자식 블록이 내용이고 텍스트는 **Word의 단처럼 단을 넘어 흐른다**(문단 내부 흐름 허용 · 원자·표·이미지·코드 블록은 단 경계에서 잘리지 않고 다음 단으로) · **세로 구분선**(`column-rule` — 토큰 색) · **모바일(<768px) = 1단 강등**(데이터 무변) · 진입 = 슬래시 `/단나누기`(별칭 `/columns`·`/2단`·`/3단`·`/다단`) + 툴바 [다단] 메뉴(선택 블록 감싸기 / 없으면 빈 2단 삽입) · 블록 상단 컨트롤 띠 = 2/3 토글 + **[단 해제]**(자식을 제자리에 순서대로 승격 — 조용한 손실 0·undo 1단위) · 중첩 = columns 안 columns 금지 · **콜아웃 안 columns 금지**(비활성 + 사유 — 숨기지 않음) · columns 안 콜아웃·표·코드·이미지 등 모든 블록 허용 · 빈 컨테이너는 잔존하지 않는다.
  - **방언·리더**: `:::columns{n=2} … :::`(컨테이너 directive — 콜아웃 `:::note` 전례). 미리보기 패널·문서 상세·인쇄(공용 `MarkdownView`)는 **같은 CSS 다단**으로 렌더(인쇄 = 화면과 같은 단 수 · 모바일 1단) · `:::columns` 없는 기존 문서 렌더 diff 0. **Markdown 외부 소비자(remark-directive 없는 렌더러)에서는 순차 나열로 강등**(별지 `editor-v2.plan.md` §14 11번 — 명문화된 강등 손실 · 리더/편집/재전환은 무손실).
  - **알려진 위험**: 단 경계에 걸친 블록의 드래그 핸들·사이드 메뉴 위치(엔진 부유 배치 귀책) — 실측 후 강등 경로(블록 단위 흐름 또는 컨테이너 내부 드래그 비활성)는 지시서 결정 ④에 기록.
- **공통 계약**: DDL 0 · settings 키 0 · 신규 엔드포인트 0 · **신규 의존 0**(GPL `@blocknote/xl-multi-column` 금지 불변 — D10) · 백엔드 diff 0(리더는 프론트) · 저장 계약 §4.28 ③/§4.29 무변(S41은 블록 타입 1종이 실릴 뿐) · 구 편집기(DocEditor 소스 폼·`MarkdownFieldEditor`·`EditablePreview`·퇴로 토글) 무접촉(후속 stage-42 퇴역 실행 대조표 보전).

**모바일**

- 폰에서 목록·편집이 열리고 한글 입력·기본 서식이 동작하는 수준까지가 M33 범위다. **터치 선택·드래그·툴바 밀도 마감은 M36(stage-37)** — 이 절에서 약속하지 않는다.

**API**: `notes` 5종(§4.28) · `uploads`(§4.27 — 이미지 3진입점) · `search`(칩 피커 검색)·`documents` 목록(§4.2)·`documents/resolve-embeds`(§4.19) **읽기 전용 재사용**. **신규 엔드포인트는 §4.28의 5개뿐이다.**

## 6. 테마 · 디자인 토큰 (F28)

- Tailwind `darkMode: 'class'` + `styles/tokens.css`의 CSS 변수 이중 구조. 컴포넌트는 **토큰만 참조**(`bg-surface`, `text-primary` 등) — 색상 하드코딩 금지.
- 토큰(라이트/다크 각 1세트): `--bg`, `--surface`, `--surface-raised`, `--border`, `--text`, `--text-muted`, `--accent`, `--accent-soft`, `--on-accent`(accent 배경 위 텍스트 — `text-white` 대체), `--correct`, `--wrong`, `--warning`, 히트맵 5단계.
- **F52 표현 팔레트(S26 — 계획서 §14 F52 결정 ③·⑤ 확정 2026-08-09)**: 의미 이름 7색 `red·orange·yellow·green·blue·purple·gray` — 글자색 **`--ink-{색}`** 7개 + 형광펜 배경 **`--mark-{색}`** 7개, **라이트·다크 각 1세트**(사용자는 이름만 고르고 다크 대응은 토큰이 담당 — ~~자유 hex 기각~~ → **S30에서 개정: 자유 hex도 허용하되 다크 보정은 여전히 토큰이 담당**, 아래 S30 보정 토큰 항목). 설계 강제 조건(R26 ③): **mark 7색 전부 라이트에서 검정 글자(`--text`) 대비 확보**(흑백 변환 후에도 글자 가독 — 화면 라이트에도 동일 필요라 추가 비용 0), ink 7색은 각 테마 배경 위 가독 확보. `@media print` 블록에도 같은 14개를 **라이트 값으로 포함**(아래 "인쇄 = 항상 라이트" 연장 — **인쇄 전용 색 토큰 0개**). **F53(M27 — 전역 테마·문서별 스타일)이 이 명명 체계를 공유할 예정**(F52에서 F53용 토큰 선제 신설 금지 — YAGNI). 콜아웃(note/warn/tip)은 기존 토큰(`--accent`·`--warning`·`--correct` 계열) 재사용 — F52 신규 토큰은 ink/mark 14개뿐.
- **인쇄 색 유지(S26 — F52 결정 ⑤)**: `print-color-adjust: exact`(+`-webkit-print-color-adjust` 병기)를 **형광펜(`--mark-*`)·글자색(`--ink-*`) 요소에만** 적용 — 브라우저의 인쇄 배경 기본 생략과 무관하게 색이 찍힌다. 코드블록 배경·임베드 카드 등 나머지 배경은 계속 인쇄 생략(잉크 절약 — F53 문서 전체 배경색도 인쇄 무시 유지). 흑백 프린터의 색 구분 소실은 물리 한계로 수용·매뉴얼 명문화.
- **F53 문서 배경 틴트 토큰(S28 — 계획서 §14 F53 ②-3 ⓐ 확정 2026-08-09)**: 문서 배경용 **`--doc-bg-{색}`** 7개(red·orange·yellow·green·blue·purple·gray) × **라이트·다크 각 1세트**(14개). **F52 형광펜 `--mark-{색}`과 팔레트 이름 체계는 공유하되 값은 분리** — 형광펜 토큰(인라인 강조용 — 채도 높음)을 문서/전역 배경에 재사용하면 형광펜 강도의 전면 배경이 되어 눈부시다: 같은 "yellow"라도 `--doc-bg-yellow`는 **훨씬 연한 틴트**의 다른 값 쌍이다. 7색 전부 글자 토큰(`--text`) 대비 확보(R26 ③ 관례 연장). **`--doc-bg-*`에는 `print-color-adjust: exact`를 걸지 않는다** — 문서 전체 배경은 인쇄 무시 유지(F52의 exact 적용 범위는 형광펜·글자색 한정 — 결정 불변). **전역 앱 테마(F53 ①)는 신규 토큰이 아니라 기존 토큰 값의 사용자 오버라이드**(`ui.theme_custom` — `<html>` 수준 CSS 변수 주입, §4.26 ③·§7).
- **S30 자유 hex 색 보정 토큰(F56 결정 ④-2 — 계획서 F52 결정 ③ 개정. 계약 상세 = §5.3 S30 ⓓ)**: 신규 토큰 **2개** `--hex-ink-adjust`·`--hex-mark-adjust`(**라이트 = `0%`(원색 그대로) · 다크 = 양수**) — 사용자 hex는 요소 인라인 스타일의 **커스텀 프로퍼티(`--u-ink`/`--u-bg`)로만** 전달되고 실제 색은 `color-mix(in oklab, var(--u-ink), var(--text) var(--hex-ink-adjust))`·`color-mix(in oklab, var(--u-bg), var(--surface) var(--hex-mark-adjust))`로 **토큰과 섞어** 계산한다(다크에서 글자색은 밝아지고 형광펜 배경은 채도가 눌린다). **`light-dark()` 미사용**(이 앱의 다크는 `darkMode:'class'` 수동 토글 — `color-scheme` 연동 함수와 어긋난다) · `color-mix()` 미지원 시 `@supports not (…)` 폴백 = 원색. **불변 규칙 5 정합**: 코드·CSS의 색 리터럴은 0개이고(hex는 런타임 사용자 데이터) 섞는 상대·보정량은 전부 토큰이다. `@media print`에도 두 토큰의 **라이트 값(0%)**을 포함 — 인쇄는 원색으로 찍힌다(`print-color-adjust: exact` 적용 범위는 형광펜·글자색 한정 그대로, **신규 인쇄 토큰 0**).
- 테마 스토어: `theme = 'system' | 'light' | 'dark'` (localStorage `theme`). system이면 `prefers-color-scheme` 미디어쿼리 구독. `<html>`에 `class="dark"` 토글.
- 인쇄 뷰는 테마 무시하고 항상 라이트로 렌더. (S28 — 전역·문서 커스텀 **배경**도 인쇄에서 전부 무시 — 이 규약의 연장. **폰트·글자크기는 인쇄 유지**, §4.26 ④)

## 7. 프론트 상태 관리

- **서버 상태**: TanStack Query — 캐시 키 = 리소스 경로. 변경(mutation) 후 관련 쿼리 invalidate.
- **로컬 상태**: zustand 5개 — `quizSession`(문항·답안·타이머), `flashcardSession`, `examSession`(S11 — 문항·답안·카운트다운·리포트, 제출 전엔 전부 로컬(서버 무상태 §4.14), persist 없음. **S19 확장**: applied 컨텍스트(런 분류 id·applied 플래그 — 제출 엔드포인트 분기·리포트 basis 렌더용, 신규 스토어 없음 — §4.21)), `theme`, `sidebar`(S7 — `'expanded'|'collapsed'`, localStorage `sidebar` persist, 기본값 규칙은 §5 도입부).
- **전역 테마 주입(S28 — F53 ①)**: 서버 `settings:ui.theme_custom`(TanStack Query)을 읽어 **`<html>` 수준 CSS 변수로 런타임 주입**(빌드 불요 — 신규 스토어 없음, 라이트·다크 전환(`class="dark"`) 시 해당 테마 오버라이드만 적용·미설정 항목은 기본 토큰).
- 홈 레이아웃(S7)은 스토어를 두지 않는다: 저장본은 서버 `settings:home.layout`(TanStack Query), 편집 중 드래프트는 홈 컴포넌트 로컬 상태 — [완료] 시에만 PUT, 취소 시 폐기.
- 낙관적 업데이트는 북마크·진도 완료(체감 속도 중요)에만 적용, 나머지는 단순 invalidate.

