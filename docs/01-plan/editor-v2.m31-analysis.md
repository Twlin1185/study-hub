# M31 집중 분석 보고서 — BlockNote 가능성·한계 (D1·D2 위임 판정)

> 2026-08-15 · stage-31 산출물. 근거 = 병렬 리서치 2건(기능·라이선스 / 이슈·리스크, 1차 출처 URL 확인) +
> 워크트리 스파이크 실측(`frontend/src/editor2/poc/`, 대상 버전 **@blocknote 0.54.0 고정**).
> 판정 규칙 정본 = `editor-v2.plan.md` §10 D1·D2, `stage-31-blocknote-analysis.plan.md` 게이트 표.

## 1. 판정 요약

| 결정 | 판정 | 근거 요지 |
|---|---|---|
| **D2 (엔진=BlockNote)** | **확정 — Claude 위임 판단** (잔여 통과 조건 = 한글 IME 실기기 실측, §7) | 게이트 5조건 중 4건 실측 합격(커스텀 스펙 3종 성립·라이선스 무결·번들 수용·장문 반응성), IME는 "치명 이슈 없음"을 데스크톱·문헌으로 확인하되 실기기 확인만 남음. 후보 A·C 대비 **모든 축에서 우위 또는 동률**(§7) |
| **D1 (저장=블록 JSON)** | **방향 지지** — 최종 확정은 설계대로 M34 게이트 유지 | 실문서 436텍스트 전건 변환 성공(실패 0)·크기 오버헤드 중앙값 ×2.54(5천 건 외삽 +3.7MB)·6k 블록 편집 반응성 합격. 목록/검색/FTS는 Markdown 프로젝션 소비라 구조상 무영향 |

## 2. 기능 실태 — 계획서 §4 서술의 실측 검증

### 2.1 코어(MPL)에서 확인된 것
슬래시 메뉴 · 드래그 핸들+재배열 · 블록 중첩(children) · 표(내장 — 셀 병합·헤더·셀 색은 **옵션으로 활성**) ·
코드 블록(하이라이트는 무료 `@blocknote/code-block`/Shiki) · 이미지(업로드 콜백 `uploadFile`·`previewWidth`
크기 조절·캡션) · 파일/비디오/오디오 · 헤딩 h1~h6 · 토글(접기) 블록 · undo(ProseMirror history —
스파이크에서 프로그램적 문서 교체까지 역행함을 확인) · Markdown/HTML 변환기(`tryParseMarkdownToBlocks`·
`blocksToMarkdownLossy` 등, "lossy" 명시) · 헤드리스 변환(`@blocknote/server-util`, MPL — M34 배치 변환 경로).

### 2.2 계획서 정정 2건 (§4 B 서술 갱신)
- **수식(KaTeX)은 코어 미내장** — 단, **v0.54.0(2026-08-13)에서 무료 MPL 패키지 `@blocknote/math-block`**
  (블록+인라인 수식, KaTeX→MathML)이 신규 출시됐다. 초안의 "기본 내장" 표기는 과대였으나 결론적으로
  무료 MPL 범위에서 충족된다(출시 이틀째라 성숙도는 M32~M33에서 검증).
- **다이어그램도 코어 미내장** — 동일하게 v0.54.0 신규 `@blocknote/diagram-block`(mermaid ^11, MPL).

### 2.3 부재 — 자체 구현 필요 목록 (§7.1 요구와 매핑)
| 요구 | 상태 | 처리 |
|---|---|---|
| 찾기/바꾸기(§7.1-2) | **부재** | M35 자체 구현(ProseMirror 플러그인 계층) |
| 목차(TOC) 블록(§7.1-7) | **부재** | 계획대로 커스텀 블록(M35) — 예상 범위 내 |
| 콜아웃(F52) | 부재(공식 "Alert Block" 예제가 표준 경로) | 커스텀 블록 이식(M33) — 예상 범위 내 |
| 다단 레이아웃 | XL(GPL) 전용 | **D10 확정대로 도입 금지·자체 커스텀 후순위** — 변동 없음 |
| PDF/DOCX 내보내기 | XL(GPL) 전용 | 동일(D10) — 필요 시 자체 구현·후순위 |

## 3. 라이선스 — R40·D10 검증 (기계 검사)

- 패키지별(0.54.0, npm 레지스트리 확인): core/react/mantine/shadcn/ariakit/code-block/**math-block**/
  **diagram-block**/server-util = **전부 MPL-2.0**. `@blocknote/xl-*` 4종만 GPL-3.0 듀얼 — **미설치(D10)**.
- **의존 트리 전수 기계 검사**(license-checker, production 트리 278패키지): MIT 202 · MPL-2.0 3 ·
  ISC 3 · BSD 계열 3 · (MIT OR CC0) 1 · UNLICENSED 1(= private인 frontend 자기 자신). **GPL/LGPL 0건.**
- Yjs는 v0.52부터 **optional peer** — 협업 미사용이므로 프로덕션 의존에 포함되지 않는다
  (개발 전용: server-util 실행에만 devDependencies로 yjs 계열 3종 추가 — 배포 번들 무관).

## 4. 리스크 스캔 — 이슈 이력 (출처: GitHub 이슈·릴리스, 리서치 보고 원문 보존)

- **한글/CJK IME**: 2026-01~07에 수정 물결 5회 — 링크 툴바 이중 입력(→v0.47) · 슬래시/멘션 메뉴가
  조합 중 닫히는 문제(→v0.51) · 조합 확정 Enter에 입력 규칙 오발동(→~v0.53) · 안드로이드 IME 백스페이스
  무반응 계열(→v0.53/0.54). **잔존**: #1679(한글 조합 후 Enter 시 직전 음절 소실 — P4로 15개월 미해결) ·
  #2731(조합 중 제안 메뉴 유지 보완 PR 미머지). **v0.54 필수 + 실기기 실측 필수**라는 결론의 직접 근거.
  기반(ProseMirror)의 한글 처리는 자체 구현 대비 성숙하나 Chrome IME 자체가 취약층(업스트림 공지).
- **대형 문서 성능**: 블록 수 비례 지연(#2595)은 v0.48 플러그인 순회 최적화(#2600)로 해소. **가상화 없음·
  계획 없음** — 초대형 문서의 초기 마운트는 수 초(§5.4 실측과 일치). 실사용 문서(수십~수백 블록)에는 무관.
- **모바일/터치 = 최약 표면**: 터치 드래그 핸들 미동작(#1693 미해결) · iOS 메뉴 겹침(#2122) 등 장기
  미해결 다수, 단 2026년 수정 활발(모바일 툴바 재작업 PR 진행 중). **M36 모바일 마감 게이트에 직결**.
- **0.x 브레이킹**: 최근 10개월 마이너 5회에 브레이킹 동반(월 1회 릴리스). → R33 대응(정확 버전 고정 +
  앱 중립 스키마 절연 + 어댑터 1곳) 그대로 유효, 업그레이드는 의도적 일괄로.
- **undo**: 협업(Yjs) 활성 시 깨지는 미해결 이슈(#2244) — **본 앱 협업 미사용이라 비해당**. 단독 사용 undo는
  건재(스파이크 확인).
- **프로젝트 건강도**: 커밋·릴리스 활발, 크래시성 이슈 며칠 내 대응. 무료/유료 경계(XL)는 안정적 —
  기존 무료 기능의 유료화 전례 없음.

## 5. 스파이크 실측 (워크트리 `frontend/src/editor2/poc/` · PoC 라우트 `/editor2-poc`)

### 5.1 방언 커스텀 스펙 3종 — 전부 성립
| 스펙 | API | 확인 동작 |
|---|---|---|
| 형광펜(F52 `==`) | `createReactStyleSpec` propSchema:"string" — **값(색 이름) 보유 스타일 성립** | 토큰(`--mark-*`) 결선 렌더 + `toggleStyles({highlight:'yellow'})` 적용 확인 |
| 스포일러(F52 `\|\|`) | `createReactStyleSpec` propSchema:"boolean" + **React 상태 갖는 렌더** | 가림 표시 + **클릭 공개 동작 확인**(리더 뷰 동작의 에디터 내 재현 가능) |
| 참조 칩(F43) | `createReactInlineContentSpec` content:"none"(원자) | 비편집 칩 렌더·삽입·한 단위 삭제 확인 — 참조 3종·문서 임베드 칩의 일반형 |
- 함의: F52 인라인 방언·F43 칩 계열이 **전부 이 3개 API 패턴 안**이다. 콜아웃·문제 블록은
  `createReactBlockSpec`(같은 계열)로, 이식 공수는 "스펙 정의" 수준으로 수렴.

### 5.2 번들 (R32-c — 빌드 실측, min / gzip)
| 대상 | 도입 전 | 도입 후 |
|---|---|---|
| 초기 청크 index.js | 1,577.3KB | **1,577.7KB (+0.4KB / gzip +≈0.1KB)** — 지연 로드로 초기 영향 사실상 0 |
| PoC 지연 청크 | — | 924KB / **gzip 271KB** + CSS 230KB / gzip 36KB |
| 온디맨드(내부 동적 로드) | — | 419KB / gzip 81KB + 75KB / gzip 27KB |
- 편집 진입 시 1회 로드 총 **gzip ≈ 307~415KB**. 홈 네트워크 로컬 서빙 전제(R12)라 체감 무시 가능.
  PWA 캐시 대상 증가는 R37대로 기록. **수용 판정.**

### 5.3 테마 토큰 결선 — 성립
`--bn-colors-*` CSS 변수 ↔ `tokens.css` 변수 매핑 **CSS 96줄**로 라이트/다크 완전 결선(불변 규칙 5 준수,
하드코딩 0 — invariant-scan PASS). 슬래시 메뉴·툴바·에디터 표면 전부 앱 토큰을 따름을 두 테마에서 스크린샷 확인.

### 5.4 장문 편집 반응성 (혼합 블록 하네스: 헤딩·중첩 목록·서식 문단·한글 장문)
| 블록 수 | 문서 교체→페인트 | 단타 키다운→페인트(60타 평균/최대) |
|---|---|---|
| 1,000 | 163ms | — |
| 3,000 | 733ms | **15ms / 16ms** |
| 6,000 | 2,925ms* | **28.6ms / 30ms** |
- *전체 문서 일괄 교체(최악 경로) 기준 — 열람 전환(M34 지연 변환)의 상한 근사. 탭 상태에 따라 최대 6.4초까지
  관측(가상화 부재의 대가). **단타 지연은 6k 블록에서도 2프레임 이내 = 합격.**
  60자/초 수준 폭주 연속 입력 시 프레임 밀림(평균 418ms)이 있으나 인간 타이핑 속도에선 비발생 아티팩트.
- 실사용 분포(§5.5: 실문서 평균 수백 자 = 수십 블록 이하)에서는 로드·입력 모두 즉답 영역.

### 5.5 저장 크기 — D1 조건 전반부 (실데이터 실측)
- `study.db` 실문서 **436텍스트(content 224 + explanation 212) 전건** 헤드리스 변환(server-util):
  **실패 0** · Markdown 96.6KB → 블록 JSON 322.7KB · 배율 **중앙값 ×2.54 · 전체 ×3.34 · P90 ×4.85**
  (최대 ×21은 수십 자 초단문의 고정 오버헤드 — 절대량 무의미).
- 외삽: 문서 5,000건 시 블록 JSON 총 **+3.7MB** — SQLite(WAL)에 무시 가능. 6k 블록 극단 문서 1건 = 2.3MB.
- 목록/검색/FTS/백업은 **Markdown 프로젝션(`content` 존치) 소비**라 대량 문서 표시 경로에 블록 JSON이
  아예 등장하지 않는다(설계 §5.2). → **"수많은 문서 관리·표시 문제없음" 지지.**

### 5.6 한글 IME — 자동화 한계와 대체 검증
CDP 합성 입력은 실제 IME 조합(composition) 이벤트를 만들지 못해 **자동화 실측 불가**. 대체로 ① §4의
수정 이력·잔존 이슈 문헌 검증 ② 데스크톱 스모크(슬래시 메뉴 동작·undo·비조합 입력 경로) ③ **PoC 페이지에
실기기 체크리스트 6항 내장**(조합+Enter 음절 소실(이슈 1679 재현 여부)·한글 슬래시 필터·조합 중 서식·
조합 중 blur·안드로이드 백스페이스·3k 문서 중간 타이핑). **→ 사용자 이행 항목(§7).**

## 6. 한계 요약 (채택하되 알고 가는 것)
1. **한글 IME 잔존 이슈 1건(#1679) + 실기기 미검증** — 게이트 잔여 조건. M33 노트 베타 실사용이 2차 관문.
2. **모바일 터치 미성숙**(드래그 핸들 등) — M36 마감 게이트에서 재실측, 최악 시 모바일은 보기 전용+간이 입력.
3. **가상화 없음** — 수천 블록 단일 문서의 초기 마운트 수 초. 실사용 분포 밖이나 상한으로 기록.
4. **0.x 브레이킹 체인지 월례** — 정확 버전 고정·의도적 업그레이드·어댑터 절연(R33).
5. **찾기/바꾸기·TOC 부재** — M35 자체 구현 공수 추가(예상 범위 내).
6. **math/diagram 패키지가 출시 직후** — M32~M33에서 성숙도 별도 검증, 문제 시 수식은 기존 KaTeX 렌더
   경로의 커스텀 블록으로 폴백 가능(KaTeX는 이미 앱 의존).

## 7. 게이트 판정 논거

**D2 = 확정(위임 판단).** "압도적으로 유리" 판단의 근거:
- **vs A(자체 구현)**: 보류 목록(블록 조작·undo·표/수식 시각 편집·모바일)이 스파이크에서 **전부 기본
  동작으로 확인**됨. A는 이를 전부 자체 제작해야 하며 IME 리스크도 자체 부담(S30에서 이미 체감) — 열위 명백.
- **vs C(Tiptap 직접 조립)**: C는 같은 ProseMirror 기반이라 **IME 기저 리스크가 동일**하고, BlockNote가
  이미 해결한 UI 계층(슬래시·드래그·메뉴의 IME 대응 포함)을 자체 재구현해야 한다. C가 우위인 축이 없음 —
  퇴로(하강 이주 가능)로서만 유효. 저장분은 앱 중립 스키마(§5.2)로 절연되므로 퇴로 비용도 통제됨.
- 게이트 5조건: 커스텀 스펙 3종 ✅ · 라이선스 무결(GPL 0) ✅ · 번들 수용(초기 +0) ✅ · 장문 반응성 ✅ ·
  IME 치명 이슈 = 알려진 것 중 치명급 없음(문헌) + **실기기 확인 대기** ⏳.
- **잔여 통과 조건**: 사용자 실기기 IME 실측(§5.6 체크리스트)에서 치명 결함(입력 자체가 불가능한 수준)
  발견 시 이 확정은 재론한다. 경미한 결함(특정 조작 우회 가능)은 M33 베타에서 회피 구현으로 흡수.

**D1 = 방향 지지.** 성능·크기·변환 건전성(실패 0) 실측이 전제 조건("수많은 문서 관리·표시 문제없음")을
지지. 설계대로 **최종 확정은 M34 게이트**(노트 베타 실사용 통과 후 documents 연결 시점)에서 — 앞당기지 않는다.

## 8. 후속
1. **사용자 이행**: 서버 기동 후 `http://localhost:8000/editor2-poc` (또는 `npm run dev` →
   `http://localhost:5173/editor2-poc`) — §5.6 체크리스트 6항 실측(PC + 폰). 통과 보고 시 M31 완전 종결.
2. **M32 착수 가능**(변환 계층): D2 확정으로 착수 조건 충족 — 단 사용자 IME 보고 전 대규모 커밋은 자제하고
   앱 중립 블록 스키마 설계(문서 작업)부터.
3. 계획서 반영 완료: `editor-v2.plan.md` §4(정정)·§8(M31 상태)·§9(실측치)·§10(D1·D2 상태) — 본 보고서 참조.
   마스터 계획(§5·로드맵) 등재는 게이트 완전 종결 후.
