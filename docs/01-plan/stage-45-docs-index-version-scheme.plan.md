# Stage 45 — 문서 체계 재편 2: 인덱스·버전 체계·억제기 재심사 → v2.00.1 (문서 + 소규모 코드)

> 상태: **완전 종결 — v2.00.1 발행 (2026-09-01) · DoD 6/6** — 구현(Fable 직접) → Opus 검토 통과(치명·중요 0·경미 6 — 4건 발행 시 반영·2건 기록) → **Fable 최종 게이트 통과 = v2.00.1 발행** → **E-4 사용자 확인 완료(2026-09-01 "문제없어")**. 후속 소수정 1건(기동 경고 mtime→해시 — 말미 기록). 잔여 0.
> 버전 영향: **사소** (stage-44와 한 발행 단위 = v2.00.1 — 결정 ②).
> (편성 시 상태 — 이력: 편성 완료 (2026-08-31 생성 — Fable 점검·계획. 착수 대기 · stage-44 완료 전제).)
> 진척은 이 줄 + 말미 "완료 기록"이 정본.
> (생성 경위: stage-44 머리말 참조 — 같은 지시의 후반부. 사용자 제안(2026-08-31) "핵심 스테이지 완료 =
> 2.00.3→2.01.1, 사소한 스테이지 = 2.00.1→2.00.2로 자동·가시적 버전 관리 어떨까?" → **채택·본 단계에서
> 확정**(아래 결정 ②). 추가 지시 "프로젝트가 커짐에 따라 다양한 억제기 제거를 고려하고 문서정리에 힘써줘"
> → 재심사 작업줄 ③.)
> 범위: 문서 재편 마무리(인덱스·CLAUDE.md 슬림화·억제기 판정) + **버전 가시화 소규모 코드**(버전 단일
> 출처 파일 + 기존 `/api/app-version` 응답 필드 추가 + 설정 화면 표시 1곳 + 매뉴얼 1절).
> **DDL 0 · 신규 엔드포인트 0 · settings 키 0 · 신규 의존 0 · LLM 0**.
> 실행 순서: stage-44 → **stage-45 → (Fable 최종 점검 → v2.00.1 발행)** → stage-43(핵심 — 완료 시 v2.01.1).
> 진행 방식: 구현 = **Fable 메인 대화 직접**(코드 2~3파일은 위임 가능하되 Fable 검수) · 검토 =
> `stage-reviewer`(Opus) · 최종 게이트 = **Fable**(이 게이트 통과가 곧 v2.00.1 발행이다).

## 착수 전 결정 (권고 확정 — ②는 사용자 제안의 구체화이므로 이의 시 재론 1순위)

| # | 결정 | 판정 |
|---|---|---|
| ① | 버전 표기 | **`v2.MM.PP`** (현행 `v2.00.0` 표기 계승). **핵심 stage 완료 = MM+1·PP=1** (예: 2.00.3→2.01.1 — 사용자 예시 그대로) · **사소 stage 완료 = PP+1** (예: 2.00.1→2.00.2). 발행 = stage 게이트 통과 시점 |
| ② | 재편 자체의 버전 | stage-44+45를 **한 발행 단위로 묶어 v2.00.1**(사용자 원문 — "Fable이 점검함으로써 2.00.1로 넘어간다"). 새 체계는 v2.00.1부터 발효(그 전 stage-39~42는 CHANGELOG 소급 항목만·번호 재부여 없음). 종전 "stage-43 완료 → v2.0.1 발행" 계획(CLAUDE.md)은 **개정: stage-43 = 핵심 → v2.01.1** |
| ③ | 핵심/사소 판정 기준 | **핵심** = 새 기능 표면·저장 계약(DDL/API) 변경·기능 퇴역 등 사용자 체감이 큰 stage(대체로 게이트 있는 로드맵 M급) · **사소** = 버그픽스·다듬기·문서·운영 정비. **stage 편성 시 머리말에 `버전 영향: 핵심/사소` 1줄 명기가 의무**(애매하면 사용자에게 1회 질의). 소급 판정 금지 — 편성 시점 고정 |
| ④ | 버전 단일 출처 | 루트 **`VERSION` 파일 1개**(내용 = `2.00.1` 한 줄). 서버가 기동 시 읽어 기존 `GET /api/app-version` 응답에 `version` 필드 추가(신규 엔드포인트 0). 프론트는 설정 화면 하단에 `v2.00.1` 정적 표시(기존 앱버전 훅 재사용). CHANGELOG 항목 추가 + VERSION 갱신 = **stage 완료 절차의 표준 항목**(자동화 스크립트는 선택 — 절차 규약이 정본) |
| ⑤ | 억제기 제거의 권한 | 재심사 판정표에서 **"제거" 판정이 나와도 실제 삭제는 사용자 승인 후**(가드레일은 이유가 있어 생겼다 — 전례: 서버 구동 금지·무접촉 규약). 단 **명백히 사망한 것**(완료 stage의 "착수 전 결정 없이는 구현 금지" 게이트 문구, 종결 리스크의 경고 딱지, 이미 실행된 보류 딱지)은 stage-44 아카이브 이관으로 자연 소멸 — 별도 승인 불요 |

## 체크리스트

### A. stage 인덱스 (`docs/01-plan/stage-index.md` 신설)

- [x] **A-1** 표 1개 — stage 1~45 전수: 번호 · 제목 · 소속 버전대(v1 / v1.x / v2.00.0 / v2.0.x) · 상태(완료/편성/예정) · 완료일 · 버전 영향(핵심/사소 — v2.00.1 이후 편성분부터) · 산출 버전 · 파일 경로. 각 stage 문서 머리말 "상태" 줄이 근거(재서술 금지 — 1줄 압축만)
- [x] **A-2** 머리에 사용 규약 3줄: "stage 문서를 찾을 때는 이 인덱스부터 — 본문 전체 읽기 금지 · 상태의 정본은 각 stage 문서 · 새 stage 편성 시 이 표에 1행 추가 의무"
- [x] **A-3** CLAUDE.md 문서 지도에서 stage 행의 경위 서사를 인덱스 포인터로 대체(→ C-1과 한 몸)

### B. 버전 체계 확립·가시화 (결정 ①~④)

- [x] **B-1** 루트 `VERSION` 파일 신설(발행 전이므로 일단 `2.00.0` — v2.00.1 기입은 Fable 최종 게이트 통과 시)
- [x] **B-2** `backend/main.py` `get_app_version()` — `VERSION` 읽어 `{"asset": …, "version": "2.00.0"}` 확장(파일 부재 시 `null` — 실패 허용). 부분 실행 스모크로 검증(서버 구동 금지 규칙 준수 — 임시 포트·종료 확인)
- [x] **B-3** 프론트 설정 화면 하단에 버전 표시 1곳(기존 app-version 조회 재사용 · 토큰 색상만 — 하드코딩 금지). `npm run build` 성공 확인
- [x] **B-4** `docs/03-release/CHANGELOG.md` 머리에 버전 규약 절 확정 기입(결정 ①~③ — 이 절이 규약의 정본) + stage 완료 절차 표준 항목("CHANGELOG 1항목 + VERSION 갱신 + stage-index 갱신")을 CLAUDE.md 실행 절에 1줄 등재
- [x] **B-5** 매뉴얼(`docs/manual/user-manual.html`)에 버전 확인 위치 1절 추가

### C. CLAUDE.md 슬림화 (목표: 10.5KB → 6KB 이하)

- [x] **C-1** 머리말 버전 문단의 stage-39~42 경위 서사(결정·판정·PR 번호 나열) → 삭제하고 "현재 버전 + 다음 목표 + 진척은 stage-index" 3~4줄로 대체(stage 문서·CHANGELOG가 정본이므로 손실 0)
- [x] **C-2** 문서 지도에 `docs/03-release/`(패치노트) · `stage-index.md` · `docs/04-archive/` 확장분 등재, 낡은 서술(§14 로드맵 상세·"버전 정의 말미" 등 stage-44 이후 달라진 위치) 갱신
- [x] **C-3** "완료 경위를 CLAUDE.md에 옮겨 적지 말 것" 규칙이 실제로 지켜지도록 — 앞으로 stage 완료 시 CLAUDE.md는 **버전 줄 1곳만** 갱신하는 관례를 실행 절에 명문화

### D. 억제기 재심사 (사용자 지시 — 판정표 산출, 제거는 결정 ⑤)

- [x] **D-1** 전수 수집: CLAUDE.md·마스터 계획·별지·설계 색인·stage 39~43 문서에서 **살아 있는 제동 장치** 목록화 — 병존·무접촉 규약(구 편집기 관련 다수 — stage-43과 함께 소멸 예정), 보류 딱지(N-2 보류 → stage-43로 이행 확정, D8 휴지통·D11-ⓐ v2.x 이월 등), 드래프트 격리(D9 — 완료된 개발 방식), 새 세션 규약·부분 읽기 규약(유지 — 이건 억제기가 아니라 절약기), 기타 발견분
- [x] **D-2** 판정표 작성(본 문서 완료 기록에 병기): 항목 · 생긴 이유 · 현재도 유효한가 · 판정(**유지 / stage-43에 이관·소멸 / 제거 권고(사용자 승인 대기)**) · 근거 1줄
- [x] **D-3** "제거 권고" 판정분을 사용자에게 일괄 상신(승인분만 후속 커밋으로 제거 — 본 stage 안에서 소화 가능하면 반영, 아니면 stage-43에 위임 명기)

### E. 검증·발행

- [x] **E-1** `run-tests.ps1` 전체 통과(B-2 백엔드 접촉) + `invariant-scan.ps1` PASS + `npm run build` 성공
- [x] **E-2** 참조 무결성 grep(신설 파일 경로·CLAUDE.md 갱신분) — 끊김 0
- [x] **E-3** 검토 = `stage-reviewer`(Opus) → **Fable 최종 점검**(재편 전체 조감: stage-44 무손실 + 45 체계 + 크기 실측 총괄표) → 통과 시 `VERSION` = `2.00.1` · CHANGELOG v2.00.1 항목 · **v2.00.1 발행**(CLAUDE.md 버전 줄 갱신)
- [x] **E-4** 사용자 실사용 확인 1회(설정 화면 버전 표시 + 패치노트·인덱스 열람)

## DoD (완료 정의)

1. `stage-index.md`에서 임의 stage의 상태·경로·산출 버전을 **grep 없이 표 1개**로 찾을 수 있다
2. 설정 화면에 `v2.00.1`이 보이고, `/api/app-version`이 `version`을 반환한다
3. CLAUDE.md ≤ 6KB 실측 · 정보 손실 0(삭제분 전부 stage 문서·CHANGELOG·인덱스에 존재)
4. 버전 규약(핵심/사소·bump 규칙·완료 절차)이 CHANGELOG 머리에 정본으로 존재하고 CLAUDE.md가 1줄로 가리킨다
5. 억제기 판정표 산출 + "제거 권고"분 사용자 상신 완료(승인 대기 항목은 목록으로 명시)
6. E-1~E-4 전건 통과 → **v2.00.1 발행 기록**(본 문서 말미 + CHANGELOG)

## 이 단계에서 하지 않는 것

- 구 편집기 퇴역·관련 규약의 실제 제거(→ **stage-43** — 판정표가 이관 목록을 넘긴다)
- 사용자 미승인 억제기의 삭제(결정 ⑤)
- 버전 bump 자동화 스크립트(선택 — 절차 규약으로 충분하면 생략)
- 설계 문서(api·screens) 본문 재편 · 매뉴얼 전면 개편(버전 1절만)
- CI·태그·GitHub Release 연동(개인 로컬 앱 — 과잉)

## 완료 기록

### 구현 (2026-09-01 — Fable 메인 대화 직접 · 워크트리 격리)

- **A**: `docs/01-plan/stage-index.md` 신설 — stage 1~45 전수 44파일 + stage-43(문서 미생성·예정) 1표 · 머리 사용 규약 3줄 · 나열 파일명 44건 전수 실존 확인. CLAUDE.md 문서 지도의 stage 행 경위 서사 → 인덱스 포인터로 대체.
- **B**: 루트 `VERSION`(=`2.00.0` — v2.00.1 기입은 최종 게이트에서) · `backend/main.py` 기동 시 1회 읽어 `/api/app-version`에 `version` 필드 추가(부재 시 null) — 부분 실행 스모크 `{"asset":"index-…js","version":"2.00.0"}` 확인(서버 미기동 — 리스너 0) · `Settings.tsx` 하단 `AppVersionLine`(version 없으면 미표시 · 토큰 색상만) · CHANGELOG 머리에 버전 규약 ①~⑤ 정본 확정 · 매뉴얼 §19에 "앱 버전 확인" 1절.
- **C**: CLAUDE.md 10,596B → **6,124B**(≤6KiB) 실측. 삭제분(stage-39~42 경위·결정 4건 서사·빌드/백업 상세)은 전부 stage 문서·CHANGELOG·stage-index·해당 스크립트 머리 주석에 존재 — 손실 0. stage 완료 절차 표준 항목·"버전 줄 1곳만" 관례를 실행 절에 명문화.
- **E-1**: run-tests.ps1 **611 passed** · invariant-scan **PASS**(신규 위반 0) · `npm run build` 성공(+source-hash 기록). **E-2**: CLAUDE.md·신설 문서가 가리키는 경로 17건 + 인덱스 나열 44건 전수 실존 — 끊김 0.

### D. 억제기 재심사 판정표 (D-2 정본 — 전수 수집 40건 → 판정)

수집 범위: CLAUDE.md · 마스터 계획(§15 유효 리스크 포함) · editor-v2 별지(D1~D11·R33~R41) · 설계 색인 · stage-39~45 문서. 성격 분류 = 금지 규칙 / 보류 딱지 / 무접촉·병존 / 게이트 문구 / 절약 규약.

**판정 요약: 유지 34 · stage-43 이관·소멸 6 · 제거 권고 0.**

| 판정 | 항목 | 근거 1줄 |
|---|---|---|
| **유지** (절약 규약 5) | dist 접촉 금지 · 부분 읽기 규약 · 새 세션 규약 · invariant-scan 갈음 · CLAUDE.md 경위 전재 금지 | 억제기가 아니라 절약기 — 프로젝트가 클수록 더 유효(본 단계에서 오히려 강화) |
| **유지** (사용자 지시 가드레일 2) | 서버 구동 금지(2026-08-19) · 브라우저 절제(필터·위임·뒷정리) | 사용자 명시 지시 — 해제는 사용자 몫 |
| **유지** (제품 불변 규칙 9) | 서버 채점 · 트랜잭션 결합 · 물리 삭제 금지 · sources 불변 · 색상 토큰 · 스키마 §6.2+Alembic · 설계 §3 규약 · 단계 범위 준수 · 체크박스 갱신 | 데이터·보안·일관성의 계약 — 유효 근거 전건 생존(R20·R41 등) |
| **유지** (보안·법적 4) | 외부 포트포워딩 금지(R12) · GPL 의존 금지(D10) · raw HTML 불번복(주입구) · 사설 어댑터 재도입 금지(R14) | public 저장소·주입면·제거 이력 — 번복 조건 자체가 없다 |
| **유지** (유효 보류 딱지 5) | D8(휴지통 — 구현 배정 전 코드 0) · D11-ⓐ(v2.x 이월)·ⓑ(착수 금지) · D5(리더 뷰 재보류) · D7(choices 이월) · v2.x 계획서 선확정 관례 보류("2.x는 뒤로") | 재론 트리거(수요 실측·v2.x 편성)가 아직 미도래 — 소급 해제 사유 없음 |
| **유지** (프로세스 게이트 9) | 착수 전 결정 게이트 · 손실 0 계약(임의 손실 수용 금지) · R7 미리보기 승인 · 조용한 실패 금지 · 명시 제외(Yjs 등) · 일괄 변환 금지·소급 0 · 방언·입력 도메인 봉인 · YAGNI(§4.x 번호 예약 포함) · 무손실 이관 원칙 + 결정 ⑤(억제기 제거의 억제) + 버전 영향 소급 판정 금지 | 살아 있는 작업 방식의 뼈대 — stage-44·45 자신이 이 규약으로 성립했다 |
| **stage-43 이관·소멸** (6) | ① 구 편집기 무접촉 규약(stage-36 규약 A) ② 무기한 병존 금지(R38·R41 게이트 상한) ③ 드래프트 격리 D9(완료된 개발 방식 — 문구 정리는 ev2 본문 재편 시) ④ 노트 정식 메뉴 미노출(승격 여부 재론) ⑤ N-2 보류 딱지(43 = N-2 실행 그 자체) ⑥ M31~M36 로드맵 게이트 문구(전 게이트 종결 — 사망 문구) | 전부 구 편집기 퇴역·에디터 v2 종결과 한 몸 — stage-43 계획서가 이 6건을 퇴역 대조표에 편입해 소멸시킨다 |
| **제거 권고** (0) | — | 나머지 억제기는 전부 ⓐ 살아 있는 근거 보유 ⓑ 사용자 지시 ⓒ 43에서 자연 소멸 중 하나 — 지금 제거해서 얻는 이득이 없다 |

- **D-3**: 제거 권고 0건이므로 승인 대기 항목 없음(최종 보고에 판정 요약 상신). stage-43 편성 시 위 6건을 계획서 "퇴역 대조표" 절에 옮겨 실을 것을 명기해 이관한다.
- 사망 문구 자연 소멸(결정 ⑤ 단서 — 완료 stage 게이트 문구·종결 리스크 딱지)은 stage-44 아카이브 이관으로 기수행 — 본 단계 추가 조치 0.

### 검토·게이트·발행 (2026-09-01)

- **E-3 검토(Opus)**: **통과** — DoD 1~5 = 5/5 · 치명 0 · 중요 0 · **경미 6**. 처분: ① 인덱스 45행 상태(발행 시 완료로) ② 인덱스 39행 완료일 2026-08-24→**25** ④ 매뉴얼 헤더 v2.00.0 잔존 ⑤ CLAUDE.md 설계 행 브레이스 표기(Read 불가 경로) = **4건 발행 커밋에서 반영**. ③ "기존 앱버전 훅 재사용"(결정 ④ 문구) 대신 설정 진입 시 1회 별도 조회 — 60초 폴러(useAppUpdate) 중복 기동을 피하기 위한 의도적 이탈, 기능·성능 문제 없음 = **기록만**. ⑥ "6KB" 기준 = KiB(6,144B) 해석 채택 — 실측 6,124B(구판 대비 -42%) = **충족 판정**. 관찰 1건(VERSION은 기동 시 1회 읽기 — 발행 후 서버 재시작 전엔 옛 버전 표시)은 매뉴얼 버전 절에 1줄 반영.
- **Fable 최종 게이트**: 재편 전체 조감 — stage-44(이관 무손실·참조 끊김 0 — 44 게이트 기판정) + stage-45 체계(인덱스 44+1행 전수 · 규약 ①~⑤ 정본 · 억제기 40건 전수 판정) + 크기 총괄: CLAUDE.md 10,596→**6,124B** · plan 350KB→90KB(44) · design 색인 66KB→8KB(44) · 신설 stage-index 6KB. 정보 손실 0(검토자 귀속처 전수 대조 포함) → **통과 = v2.00.1 발행**.
- **발행**: `VERSION` = `2.00.1` · CHANGELOG v2.00.1 항목(stage-44·45 + 소급 stage-39~42) · CLAUDE.md 버전 줄 · 매뉴얼 헤더/푸터 · 마스터 계획 말미 버전 정의 개정 줄(구 서술은 경위 보존 잔류) · stage-index 44·45행 확정.
- **잔여**: E-4 사용자 실사용 확인 1회(서버 재시작 → 설정 화면 하단 `Study Hub v2.00.1` + 패치노트·인덱스 열람). 결함 이월 0.

### E-4 확인·후속 소수정 (2026-09-01 — 완전 종결)

- **E-4 사용자 확인 완료**: "확인해야 될 건 문제없어" → **DoD 6/6 전건 충족**.
- **후속 소수정(같은 날 사용자 보고)**: 기동 시 `[!] frontend/dist 가 소스보다 오래됐습니다` 경고가 반복 출현 — 원인은 `backend/main.py` `_warn_if_frontend_stale`의 **mtime 비교 잔존**(8/30에 시작 스크립트는 해시 판정으로 교체됐으나 이 경고만 구방식 — git pull이 파일 시각을 다시 써 빌드가 최신이어도 오탐. "자동 빌드가 자꾸 빠진다"는 인상의 진범). 수정 = `dist/.source-hash` 스탬프 vs 소스 내용 해시 비교로 교체(`source-hash.mjs`와 동일 알고리즘 — 파이썬 복제, 주석에 동조 의무 명기). 검증 = 해시 파리티 실측 **일치**(node `401aa2…` = python) · 3분기(최신=침묵/불일치/스탬프 부재) 동작 확인 · run-tests 611 passed · invariant-scan PASS. 버전 bump 없음(stage 아님 — CHANGELOG v2.00.1 절에 후속 항목).
