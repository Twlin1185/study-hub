# Stage 39 — FB-2: [저장]/[취소] 상시 버튼 (v2.0.x 후속 — N-2 재론 선행 조건)

> 상태: **구현 완료 · 검토 통과 (2026-08-24 — 1차 반려 → 수정 4건 → 표적 재검토 조건부 통과 → 잔여
> 지적 A·B 수정·검증 완료)** — F-1~F-6·G-7 전건 이행 · **DoD 1~7 충족**(백엔드 diff 0·신규 의존 0·
> 초기 청크 +46B·회귀 전건 무변·550 passed·invariant PASS). **← DoD 8 1차 충족(2026-08-25 사용자 —
> 치명 없음) · 경미 잔여 5건 판정 완료(⑤⑥⑨ 수정 · ⑦ 유지 · ⑧ 문서화) · 피드백 FB-12·FB-13 접수 →
> 후속 소수정 **완료·머지(PR #68 · 2026-08-25 — 커밋 `65f6235` · Opus 표적 검토 조건부 통과 → 중요 1 반영 · 브라우저 실측 PASS · dist 포함)** → **stage-39 완료 판정 성립**(N-2 재론 선행 조건 충족). 잔여 = 실사용 중 발견 시 보고.** 상세 = 말미 "완료 기록"·"잔여".
> (생성·확정 경위 — 착수 가능 · 착수 전 결정 ①~③ 전건 확정(2026-08-23 생성 · 같은 날 사용자 확정): 사용자
> 결정(2026-08-23) "FB-2 [저장]/[취소]는 필수 — **N-2(구 편집기 퇴역 실행) 재론 전에 반드시 구현**"의
> 이행 지시서. 착수 전 결정 ①~③은 같은 날 사용자 답변으로 **전건 확정**(아래 표 판정·원문 인용 — ①은
> 권고 ⓐ의 **상위 확장**: 세션 원본 스냅샷 + 크래시 복구 호출 → **규약 E 신설**) → **구현 착수 가능**.)
> **← 기술 정정 1건(2026-08-23 — F-1 전수 실측 도착)**: 규약 D의 접촉 파일 목록이 실제 부재 표면
> `DocBlockEditor.tsx`(진입점①) 대신 `DocEditor.tsx`를 추정 기재했던 결함 정정 + 규약 E 키 서술을
> 표면 일반형으로 정정. **결정 ③("부재 실측분만 편입") 범위 내 기술 정정 — 새 사용자 결정 아님.**
> 범위: FB-2 [저장]/[취소] 상시 버튼 — **노트 편집 표면(`/notes/:id` — screens §5.16)** 을 1차 대상으로,
> stage-36에서 통합된 **문서 편집 표면 중 상시 저장/취소 조작 부재가 실측되는 곳**까지(적용 대상 전수는
> F-1 실측 + 착수 전 결정 ③으로 확정 — 추정 편입 금지).
> 정본: 화면 계약 = **screens §5.16 개정 추기(Design v1.45 — v1.37 "상시 [저장] 버튼 없음" 확정의 번복
> 절차 이행. 본 지시서 확정·구현 후 §5.16을 실측 계약으로 재개정)** · 배경·경위 = 별지 `editor-v2.plan.md`
> §13 FB-2 행(2026-08-19 접수 → 2026-08-23 채택 확정) · 저장 계약 = **api §4.28 ③·§4.29 무개정 계승**
> (동반 규칙·프로젝션 클라이언트 생성·얕은 검증 그대로 — 이 단계는 저장 API를 건드리지 않는다).
> **DDL 0 · Alembic 0 · settings 키 0 · LLM 0 · 신규 엔드포인트 0 · 신규 npm/파이썬 의존 0 ·
> 백엔드 diff 0**(위반이 필요해지면 착수 중단·사용자 보고 — 이 단계의 성격상 프론트 전용이 계약이다).

## 착수 전 결정 (✅ 전건 확정 2026-08-23 사용자 — 원문 선택지 보존·판정 추기)

> 관례(D1·D7 전례): 권고안을 명기하되 확정은 사용자(또는 사용자의 명시 위임 판정)만 한다.
> 확정 시 이 표에 판정·일자·주체를 추기하고, 그 내용이 규약 A~D의 빈칸을 채운다.
> **← 전건 확정(2026-08-23 사용자 답변) — 각 행 말미에 판정 추기 완료. 확정분의 규약 반영 = A~E(E 신설 —
> ① 확정의 스냅샷·크래시 복구분).**

| # | 결정 | 선택지 · 권고안 (임의 확정 금지) |
|---|---|---|
| ① | **자동저장과 [저장]의 공존 방식** | ⓐ **자동저장 유지 + [저장] = 즉시 확정 flush**(현행 Ctrl+S와 동일 경로 재사용 — 신규 저장 경로 0. §5.16 저장 상태 4종·디바운스 계약 무변) — **권고**: 이탈 손실 방어(자동저장)를 유지한 채 사용자가 원한 "명시적 확정 조작"만 더한다·구현 폭 최소 / ⓑ 자동저장 제거 + 명시 저장 전용 — 비권고: 이탈 손실 표면이 다시 열리고 저장 상태 4종·beforeunload 계약 전면 재설계가 필요(번복 폭 최대). **← 확정(2026-08-23 사용자 — ⓐ의 상위 확장·직접 서술)**: 원문 = "자동저장기능 유지, 원본을 기억하다가 취소, 저장을 통해 유지할지 원본으로 돌릴지 명시적으로 결정, 만약 예기치못한 종료가 발생하면 자동저장된 데이터 호출가능". 해석 = **자동저장 유지**(서버 기록 지속 — 데이터 유실 방지·크래시 복구의 원천) + 편집 세션 진입 시점의 **원본 스냅샷을 기억** + **[저장] = 현재 편집분 유지 확정 / [취소] = 원본 스냅샷 복귀**(세션 종결 = 사용자의 명시적 결정) + **예기치 못한 종료**([저장]/[취소] 없는 종결 — 크래시·탭 닫힘 등) 시 재진입에서 **자동저장분 호출 가능**(복구 선택 다이얼로그 — 이행 규약 = **E 신설**. 스냅샷은 크래시를 견디는 클라이언트 영속 보관 — 백엔드 diff 0 계약 유지, 저장 수단 세부는 구현 재량) |
| ② | **[취소]의 복귀 기준점** | 전제: ①-ⓐ(자동저장 유지)에서 "마지막 저장분 복귀"는 실효가 거의 없다(유휴 1.5초마다 저장되므로 마지막 저장분 ≈ 방금 편집분 — 취소의 기대 의미와 어긋남). ⓐ 마지막 저장분 복귀(단순·실효 낮음 — ①-ⓑ 채택 시에만 자연스러움) / ⓑ **편집 세션 진입 시점 스냅샷 복귀** — **권고**: 진입 시점 블록 상태를 로컬 보관하고 [취소] = 스냅샷으로 되돌린 뒤 저장(자동저장이 이미 서버에 쓴 분까지 물리는 효과 — "이번 편집을 물린다"는 기대와 부합). **파괴적 조작이라 확인 다이얼로그 필수**(규약 C) / ⓒ 절충: [취소] 대신 [변경 전으로 되돌리기] 라벨로 ⓑ 의미를 명시 — 라벨은 구현 재량으로 두되 의미는 ⓑ와 동일. **← 확정(2026-08-23 사용자): ⓑ 채택** — 편집 세션 진입 시점 스냅샷 복귀 + 확인 다이얼로그 필수. **①의 "원본 스냅샷"과 동일 개념·동일 실체로 일원화**(규약 C·E — 스냅샷 이원화 금지. 라벨 문구는 구현 재량) |
| ③ | **적용 표면 범위** | ⓐ **노트 편집(`/notes/:id`)만 1차** — **권고**: FB-2 원문이 노트 대상이고, 문서 편집(DocEditor·stage-36 진입점 ①~⑥)은 F-1 실측에서 **상시 저장/취소 조작이 이미 있는 표면은 대상 아님**(이중 UI 금지 — 부재가 실측되는 표면만 편입 재판단) / ⓑ 노트 + 문서 편집 전 표면 일괄 — F-1 실측 결과 부재 표면이 있을 때만 의미 있는 선택지. **← 확정(2026-08-23 사용자): ⓐ 채택** — 노트 편집 1차 · 문서 편집 표면은 F-1 전수 실측에서 상시 저장/취소 UI 부재가 확인되는 곳만 편입(이중 UI 금지) |

## 확정 규약 (착수 전 결정 ①~③ 확정(2026-08-23)으로 **전건 유효**. 이 문서 + screens §5.16 재개정분이 정본)

### A. 버튼 배치·상태

- 편집 표면 상단 바에 **[저장]·[취소] 상시 노출**(§5.16 기존 상단 바 구성에 가산 — 저장 상태 표시
  4종(`저장 대기…`/`저장 중…`/`저장됨`/`저장 실패`)은 **문구·의미 무변 병존**. 기존 "실패 시에만
  `지금 저장`" 조건부 버튼은 상시 [저장]으로 흡수).
- [저장] 비활성 조건 = 변경분 없음 또는 저장 중(중복 발사 금지). [취소]는 변경분 없으면 비활성.
- 모바일(390px)에서 상단 바 밀도 확인 — 줄바꿈·오버플로 없이 배치(stage-38 마감 수준 유지).
- 색·상태 표현은 전부 토큰(불변 규칙 5 — 신규 색 0).
- **← FB-13 추기(2026-08-25)**: [저장] = 채움 accent 버튼(`bg-accent text-on-accent text-sm`) · [취소] =
  테두리 버튼 · 모바일 최소 높이 **36px**(stage-38 D7 수준 — 검토 경미 ⑨ 터치 타깃과 합쳐 처리).

### B. 저장 경로 단일 (신규 파이프라인 금지)

- [저장] = **기존 명시 저장(Ctrl+S) flush 경로 재사용**: 어댑터 → 프로젝션 → `PATCH { content_blocks,
  content }` 동반 규칙 그대로(§4.28 ③ / 문서 표면이면 §4.29). **저장 파이프라인·API·검증 로직 신설 0.**
- IME 조합 중 클릭 = 조합 확정 후 이행(S30 ⑥ 저장 보류 규약 계승 — 조합 중 트리거 금지).

### C. [취소] 안전 규약 (조용한 손실 경로 0)

- **확인 없이 폐기 금지** — 확인 다이얼로그 필수(변경분 요지 고지: "편집 내용을 되돌릴까요?" 수준.
  파괴적 조작 관례).
- 복귀 기준점 = **편집 세션 진입 시점 원본 스냅샷**(② 확정 — 규약 E의 스냅샷과 **동일 실체·이원화 금지**).
  복귀 = 스냅샷 복원 후 저장(자동저장이 이미 서버에 쓴 분까지 물린다) + **스냅샷 폐기**(정상 종결).
  복귀 실행 후 저장 상태 표시·dirty 플래그·beforeunload 등록 상태를 일관 갱신(취소 직후 "저장 대기"
  잔존 금지).
- **← FB-12 추기(2026-08-25)**: **문서 표면(`/docs/:id` [편집] → `DocBlockEditor`)에 한해 [취소] =
  스냅샷 복귀 + 편집 종료(닫기 — **복귀분 저장 성공 후**에만 닫는다 · 실패 시 열린 채 "저장 실패"+자동 재시도·스냅샷 보존 — 표적 검토 중요 1 반영)**. 결정 ②(복귀 기준점·확인 다이얼로그)는 유지하고 "닫기"만 가산 —
  문서 표면엔 [편집 종료](저장+닫기)가 따로 있어 [취소]가 편집을 유지하면 기대와 어긋난다. 복구
  다이얼로그의 "원본으로 되돌리기"는 편집 유지(닫지 않음) · IME 조합 중 재시도 분기에서는 닫지 않음
  (닫으면 되돌리기 유실) · 노트 표면(`/notes/:id`)은 편집 모드 개념이 없어 무변.
- **스냅샷 확보 실패 시 [취소] 비활성 + 사유 안내**(규약 E 상한 조항 — 조용한 강등 금지).
- 편집기 undo 히스토리와의 관계를 구현에서 명시(복귀를 undo 1단위로 되돌릴 수 있게 할지는 구현
  재량이되, **어느 쪽이든 본문이 조용히 사라지는 경로 0**이 계약).
  **← 명시(2026-08-25 — 검토 경미 ⑧ 이행·코드 무변경)**: [취소] 복귀는 편집기 히스토리에 **한
  트랜잭션**으로 남는다 — **노트 표면 및 문서 표면의 복구 다이얼로그 경로(편집 유지)**에 한해 직후 Ctrl+Z로
  되돌리기 이전 **본문(포커스된 표면의 블록)**이 돌아오고 dirty → 자동저장된다("취소의 취소" — 조용한 손실
  경로 없음). 문서 표면 [취소] 버튼 경로는 저장 성공 후 닫히므로 Ctrl+Z 경로 없음 · 해설 표면은 별도 히스토리 ·
  제목/보기/정답/난이도(React state)는 undo 대상 밖(표적 검토 정정 2026-08-25).

### D. 격리·검증

- 접촉 파일은 프론트 한정 — 노트 편집 표면(`NoteEditorPage` 계열)·**`DocBlockEditor.tsx`(진입점①
  문서 상세 편집 — F-1 실측 편입 확정분)**·공용 상단 바 컴포넌트.
  **← 정정(2026-08-23 F-1 실측 도착)**: 작성 시점 추정 기재였던 `DocEditor.tsx`를 실측으로 교체 —
  **`DocEditor.tsx`(진입점②~⑥ 공용)는 폼 제출 [저장] + `attemptClose` [취소]가 기존재 → 결정 ③
  "이중 UI 금지"로 비편입·무접촉 종결**. 부재 표면 = `DocBlockEditor.tsx`(상시 [저장] 없음 — 자동저장 +
  실패 시만 "지금 저장"·"편집 종료"는 저장+닫기 결합이라 별개 성격 · 상시 [취소](스냅샷 복귀) 없음).
  **변환기(`editor2/transform/**`)·어댑터·스키마·`MarkdownView`·백엔드 무접촉**
  (접촉이 필요해지면 설계 어긋남 — 사유 기록 후 보고).
- 회귀 세트 전건 건수 무변·실패 0(s32~s37 계열·`run-tests.ps1`·invariant PASS) · 초기 청크 증가
  ≤ **2KB(min)**(버튼·다이얼로그 수준 — 큰 UI는 없다).

### E. 세션 원본 스냅샷 · 크래시 복구 (착수 전 결정 ① 확정 이행 — 2026-08-23 신설)

- **스냅샷 = 편집 세션 진입 시점의 블록 상태(제목 포함)**. ②의 [취소] 복귀 기준점과 **동일 실체**
  (이원화 금지 — 저장소·키·수명주기 하나). **표면별 대상 id 키(노트 id / 문서 id) · 표면당·대상당
  1개**(세션 갱신 시 대체 — 누적 금지. 2026-08-23 정정: F-1 실측으로 문서 표면(`DocBlockEditor.tsx`)이
  편입되면서 노트 한정 서술을 일반화 — 스냅샷 실체·수명주기·크래시 복구 규약은 **문서 표면에도 동일
  적용**되며, 문서 표면의 저장 계약 = §4.29(동반 규칙 무변) 조항은 그대로다).
- **클라이언트 영속 보관**(localStorage 등 브라우저 크래시를 견디는 저장소 — 백엔드 diff 0 계약의
  귀결. 저장 수단·직렬화 세부는 구현 재량이되 "크래시를 견딘다"가 계약).
- **수명주기**: [저장](유지 확정)·[취소](원본 복귀) = **정상 종결 → 스냅샷 폐기**. 명시 버튼 없는
  정상 이탈(목록 이동 등)은 **유지 확정으로 간주·스냅샷 폐기**(자동저장이 이미 서버에 반영 — 종전
  UX와 동일. 세부는 구현 재량이되 **미폐기 스냅샷이 남아 다음 진입에 거짓 복구 다이얼로그가 뜨는
  경로 0**이 계약).
- **크래시 복구**: 재진입 시 미폐기 스냅샷 발견 = 예기치 못한 종료로 판정 → **복구 선택 다이얼로그**
  ("이전 편집이 정상 종결되지 않았습니다" 수준 고지 + ⓐ **자동저장분 이어서 편집**(서버 저장분 그대로 —
  스냅샷은 세션 계속 승계) / ⓑ **원본으로 복귀**(스냅샷 복원 후 저장) 택일). **조용한 자동 선택 금지.**
- **스냅샷 부재·손상 시**(구버전 이월·localStorage 소실 등) = 다이얼로그 생략·자동저장분으로 진입
  (현행 동작과 동일 — 복구 선택지가 없을 뿐, 조용한 손실 아님).
- **상한·실패 강등**: 스냅샷 기록 실패(용량 상한 등) 시 편집은 그대로 진행하되 **[취소] 비활성 +
  사유 안내**(조용한 강등 금지 — 문구는 구현 재량).

## 체크리스트

### 백엔드 묶음

- 없음 — **백엔드 diff 0이 이 단계의 계약이다**(B 묶음 부재 자체가 검증 항목 · DoD 5).

### 프론트 묶음 (담당 `frontend-dev` · Sonnet)

- [x] F-1. **적용 표면 전수 실측** — 노트 편집 + stage-36 진입점 전수표 6곳 대조: 각 표면의 상시
      저장/취소 조작 유무 목록화(**착수 전 결정 ③의 입력** — 목록은 완료 기록에 남긴다).
      **← 실측 결과 도착(2026-08-23)**: **편입 = 노트 편집(`NoteEditPage.tsx` — 부재·구현 완료) +
      진입점① `DocBlockEditor.tsx`(상시 [저장]·[취소] 부재 — "편집 종료"는 저장+닫기 결합이라 별개)** ·
      **비편입 = 진입점②~⑥ `DocEditor.tsx` 계열**(폼 [저장] + `attemptClose` [취소] 기존재 —
      이중 UI 금지·무접촉 종결).
- [x] F-2. **[저장] 상시 버튼**(규약 A·B) — 기존 flush 경로 재사용 · 비활성 조건 · 저장 상태 표시
      병존 · IME 보류. **← 이행**(노트 + `DocBlockEditor` 양 표면 — 검토·재검토 확인).
- [x] F-3. **[취소] 버튼**(규약 C — ② 확정 = 세션 진입 시점 원본 스냅샷 복귀) — 확인 다이얼로그 ·
      스냅샷 복원 정확성(복원 후 저장) · 복귀 후 상태 일관 갱신 · 정상 종결 스냅샷 폐기 ·
      스냅샷 확보 실패 시 비활성 + 안내. **← 이행**(1차 검토 치명 1 = 문서 표면 폼 4필드 미복귀 →
      수정·표적 재검토 해소 · 잔여 A(문서 폼 정규화 비대칭)도 수정·검증 완료).
- [x] F-4. **세션 원본 스냅샷·크래시 복구**(규약 E — ① 확정 이행) — 진입 시 스냅샷 영속 보관
      (노트당 1개·대체) · 정상 종결([저장]/[취소]/정상 이탈) 시 폐기(**거짓 복구 다이얼로그 경로 0**) ·
      재진입에서 미폐기 스냅샷 발견 = 복구 선택 다이얼로그(이어서 편집 / 원본 복귀 — 자동 선택 금지) ·
      부재·손상 시 생략 진입 · 기록 실패 시 [취소] 비활성 강등. **← 이행**(키 2종
      `studyhub.note.snapshot.<id>`/`studyhub.doc.snapshot.<id>` · 명시 저장 체크포인트 = 서버 송신
      블록을 로드 경로와 같은 함수(`toBlockNoteBlocks`)로 되읽어 확보 — 거짓 다이얼로그(중요 1) 실측
      해소·코퍼스 182건+표본 10종 equal 전건 · 잔여 B(자동저장의 취소 재시도 타이머 제거)도 수정 완료).
- [x] F-5. **모바일·실기기 표면**(규약 A) — 390px 상단 바 배치 · 터치 오조작 방지(버튼 간격) ·
      한글 IME 조합 중 클릭 처리 · 복구 다이얼로그 모바일 표시. **← 이행**(경미 3 StrictMode·경미 4
      IME 타이머 수정 포함 — 실기기 최종 확인은 DoD 8·터치 타깃 크기는 경미 잔여 ⑨).
- [x] F-6. **검증 일괄**(규약 D) — 회귀 세트 건수 무변·실패 0 · `tsc -b`·`npm run build` · 초기 청크
      ≤ +2KB(min) · `invariant-scan.ps1` PASS · `run-tests.ps1` 무회귀 · 백엔드 diff 0 확인.
      **← 이행**(수치 전건 = 완료 기록 — 초기 청크 +46B·550 passed).

### 공통

- [x] G-7. **문서 반영** — 본 문서 체크박스·완료 기록 · **screens §5.16 재개정**(확정·실측 계약으로 —
      Design 판번 +1) · 별지 `editor-v2.plan.md` §13 FB-2 행 완료 추기 · 마스터 머리말 이력 1줄 ·
      `docs/manual/user-manual.html` 갱신([저장]/[취소]·자동저장 관계·크래시 복구 안내).
      **← 이행(2026-08-24)**(§5.16 실측 재개정 + §5.3 포인터 1줄 = **Design v1.46** · 별지 §13·머리말 ·
      마스터 **v0.54** · CLAUDE.md · 매뉴얼 — 복구 재질문 한 줄 포함).

## 이 단계에서 하지 않는 것 (착수 금지)

- **노트 복제** — 보류 유지(2026-08-23 확정 범위 밖. 부기: 종전 보류 사유였던 notes 거취는 N-1 ⓒ
  존치(2026-08-22)로 해소됐고, 남은 근거는 실수요 시점 미도래 — 재론 시 별지 §13에서).
- **D8 고아 이미지 정리(휴지통)** — 방식은 확정(사용자 주도 휴지통 — 별지 §10 D8)됐으나 **구현 배정 =
  v2.x 계획 편성 시**. 이 단계 관련 코드 0(불변 규칙 4 개정 이행도 그 시점).
- **D11-ⓐ 참조로 붙여넣기** — v2.x 이월 확정(2026-08-23) · **D11-ⓑ** — 보류·착수 금지 불변.
- **N-2 구 편집기 퇴역 실행** — 보류(2026-08-23). 이 단계 완료는 그 **재론의 선행 조건일 뿐**이며
  퇴역 실행·퇴로 토글 제거 코드는 일절 만들지 않는다(구 편집기 무접촉 — stage-36 규약 A 계승).
- **자동저장 파라미터 튜닝**(디바운스 1.5초·최대 10초 변경) — 착수 전 결정 ①이 ⓐ인 한 무변.
- **저장 API·동반 규칙·프로젝션 생성 위치 변경** — §4.28/§4.29 무개정 계승(영구 금지 조항 포함).
- **notes 정식 노출·이관** — v2.x 캡처 계획서 시점 재론(N-1 ⓒ 기록 불변).
- **서버측 스냅샷·세션 저장 API** — 백엔드 diff 0 계약(스냅샷은 클라이언트 영속 보관 — 규약 E).
  자동저장의 기존 서버 기록 외 신규 서버 상태 0 · 스냅샷 이력 다중 보관(버전 히스토리化) 금지
  (표면당·대상당 1개 — 규약 E. 2026-08-23 정정으로 "노트당" 서술 일반화).

## DoD

**자동 검증(구현 사이클 내 확인)**

1. **착수 전 결정 ①~③ 확정 기록 존재**(이 문서 표에 판정·일자·주체 추기 — 없으면 착수 자체가 무효).
   **← 충족(2026-08-23 — 위 표 판정·원문 추기 완료).**
2. **적용 표면 전건 동작** — F-1 확정 목록의 각 표면에서 [저장](flush·비활성 조건) · [취소](확인 →
   스냅샷 복귀 → 상태 일관·폐기) 계약 충족.
3. **저장 경로 단일** — [저장]이 기존 flush 경로만 호출(§4.28 ③/§4.29 동반 규칙 위반 0 · 신규 요청
   패턴 0 — 네트워크 실측).
4. **조용한 손실 0** — [취소] 확인 없이 폐기되는 경로 0 · 복귀 후 재열람 왕복 무손실 · IME 조합 중
   클릭으로 본문이 잘리는 경로 0.
5. **크래시 복구 동작**(규약 E) — 강제 종료(탭 종료·프로세스 킬) 후 재진입 = 복구 다이얼로그 노출 +
   양 선택지(이어서 편집 / 원본 복귀) 각각 정확 동작 · **정상 종결([저장]/[취소]/정상 이탈) 후 재진입 =
   다이얼로그 미노출**(거짓 다이얼로그 0) · 스냅샷 부재 시 생략 진입 · 기록 실패 시 [취소] 비활성 강등.
6. **검증 일괄 통과**(F-6 수치 전건 — 회귀 무변·청크 한도·invariant·run-tests·**백엔드 diff 0**).
7. **문서 반영 완료**(G-7 — §5.16 재개정·매뉴얼 포함).

**사용자 이행**

8. **실사용 확인** — PC + 폰: ⓐ [저장] 즉시 확정 체감 ⓑ [취소] 복귀가 기대(결정 ② — 세션 진입 시점)와
   일치 ⓒ 한글 IME 입력 중 조작 이상 없음 ⓓ 모바일 상단 바 배치 ⓔ **강제 종료 후 재진입 복구 1회**
   (다이얼로그·양 선택지). 발견 결함 보고 — 치명이면 완료 보류.
   **← 1차 충족(2026-08-25 사용자 — "나머지는 사용에 문제 없어 보인다" · 치명 없음)** + 피드백 2건
   접수 = **FB-12**(문서 표면 [취소]가 편집을 종료하지 않음) · **FB-13**([저장]/[취소] 가시성 낮음) →
   별지 §13 등재 · 후속 소수정 브랜치 `fix/stage-39-followup`. 경미 잔여 판정은 사용자 위임 → Fable 직접
   코드 대조(말미 "잔여").

## 게이트/판정 규칙

- **이 단계는 게이트가 아니다** — 완료 = DoD 전건 충족(8 포함) + stage-reviewer 검토 통과.
- 이 단계의 완료가 **N-2(구 편집기 퇴역 실행) 재론의 선행 조건**이다(2026-08-23 사용자 조건) —
  완료 기록 전에는 N-2 재론을 열지 않는다. 완료 후에도 N-2 재론 시점은 **v2.x 첫 로드맵 편성 시**
  (별지 §9 R38 — 이 단계가 재론을 앞당기지 않는다).

## 구현 순서·분배 힌트

- 순서: (착수 전 결정 ①~③ 확정 완료 — 2026-08-23) F-1(실측 — ③ 확정의 "부재 표면" 판별 입력이므로
  최우선) → F-2·F-3·F-4(직렬 — 같은 상단 바·스냅샷 수명주기가 셋에 걸친다) → F-5 → F-6 → 검토 → G-7.
- 서브에이전트 프롬프트에 담을 것: 확정된 결정 ①~③ 원문 · 규약 A~D 발췌 · 접촉 파일 목록(규약 D) ·
  **백엔드 무접촉·`frontend/dist` 금지** · 서버 검증이 필요하면 임시 포트 + 종료·리스너 부재 확인 의무.
- 완료 후 리뷰 = `stage-reviewer`(Opus). 말미에 "완료 기록"을 추가한다(15단계 이후 관례 — 머리말
  "상태" 줄 갱신 + 말미 완료 기록).

## 완료 기록 (2026-08-23~24 구현·검토 — G-7 반영 2026-08-24)

**구현 경위**: frontend-dev(Sonnet) 단독 · 접촉 = 프론트 7파일(수정 3: `NoteEditPage.tsx`·
`DocBlockEditor.tsx`·`BlockSurface.tsx` / 신규 4: `sessionSnapshotStorage`·`noteSessionSnapshot`(리팩터)·
`docBlockSnapshot`·`blockTreeNormalize`) · **백엔드 diff 0·신규 의존 0·신규 색 0**(규약 D 계약 충족).

### F-1 — 적용 표면 전수 실측 표 (2026-08-23)

| 표면 | 실측 | 판정 |
|---|---|---|
| 노트 편집 `/notes/:id` (`NoteEditPage.tsx`) | 상시 저장/취소 없음 | **편입 — 구현 완료** |
| 진입점① 문서 상세 `/docs/:id` [편집] (`DocBlockEditor.tsx`) | 상시 [저장] 없음(자동저장 + 실패 시만 "지금 저장" · "편집 종료"는 저장+닫기 결합이라 별개 성격) · 상시 [취소](스냅샷 복귀) 없음 | **편입 — 구현 완료**(결정 ③ "부재 실측분" 기준) |
| 진입점②~⑥ (`DocEditor.tsx` 공용 — 신규 작성·모달 등) | 폼 제출 [저장] + `attemptClose` [취소] 기존재 | **비편입 — 이중 UI 금지·무접촉 종결** |

### 스냅샷 설계 실측 (규약 E 이행)

- localStorage 키 = `studyhub.note.snapshot.<id>` / `studyhub.doc.snapshot.<id>`(표면별·대상당 1개).
- **체크포인트 = 진입 시 + 명시 [저장]/[취소] 성공 시**(자동저장은 체크포인트를 옮기지 않는다 —
  [취소]가 자동저장분까지 물린다는 ② 확정의 이행 지점).
- 명시 저장 체크포인트는 **방금 서버에 보낸 앱 블록을 로드 경로와 같은 함수(`toBlockNoteBlocks`)로
  되읽어** 확보 — 거짓 복구 다이얼로그 방지(구현자·검토자 코퍼스 182건 + 표본 10종 equal=true 전건 실측).
- 실패 강등 = [취소] 비활성 + 사유 · 크래시 복구 = 재진입 시 미폐기 스냅샷 → 선택 다이얼로그(이어서
  편집 / 원본 복귀).
- **특기(검토자 판정 = 타당)**: 복구 다이얼로그를 결정 없이 이탈하면 **다음 진입에 다시 묻는다**
  (조용한 자동 선택 금지 우선 해석 — 매뉴얼에 한 줄 반영 완료).

### 검토 (stage-reviewer · Opus)

- **1차 = 반려**: 치명 1(문서 표면 [취소]가 폼 4필드 미복귀) · 중요 1(명시 저장 후 거짓 복구
  다이얼로그 — 실측) · 경미 8건.
- **수정 4건**(치명 1 · 중요 2 · 경미 3 StrictMode · 경미 4 IME 타이머) → **표적 재검토 = 조건부 통과**
  (반려 지적 전건 해소 · 신규 경미 A(문서 폼 정규화 비대칭)·B(자동저장이 취소 재시도 타이머 제거) 발견)
  → **A·B 수정·검증 완료**(오케스트레이터 수정 지점 재확인).
- **경미 잔여 5건(항목 5~9 — 수정 여부 = 사용자 판단 대기)**: ⑤ 체크포인트 폼 필드 비행 후 값
  ⑥ 재시도 시 explicit 플래그 소실 ⑦ 복구 다이얼로그 Esc = "이어서 편집" 간주 ⑧ undo 관계 명시
  미이행(규약 C의 명시 조항) ⑨ 터치 타깃 크기.

### 최종 검증 수치 (전 수정 반영 후 1회전)

- `tsc` 0에러 · `npm run build` 성공 · 초기 청크 **1,617,258B**(착수 전 1,617,212B — **+46B**, 한도 2KB).
- 프론트 회귀 s30~s37 = **365 / 751 / 999 / 193 / 99 / 40 / 191 전건 무변·실패 0**.
- `run-tests.ps1` **550 passed**(+기지 실패 2 = applied_exam CLI PATH 의존 — 무회귀) ·
  `invariant-scan.ps1` **PASS** · **백엔드 diff 0**.

### 잔여 (사용자 이행·판단) — ← 판정 완료(2026-08-25)

1. **DoD 8 실사용** — **1차 충족**(2026-08-25 사용자 — 치명 없음 · 피드백 FB-12·FB-13 접수).
2. **검토 경미 잔여 5건(⑤~⑨)** — 사용자 위임 → Fable 직접 코드 대조 판정:
   - ⑤ **수정** — 문서 표면 체크포인트가 요청 성공 시점의 `formRef.current`(비행 후 값)를 사용 → 비행 중
     폼 수정 시 저장된 적 없는 값이 체크포인트가 됨. 요청 전 캡처 값 사용.
   - ⑥ **수정** — 저장 실패 재시도 `setTimeout(saveNow, RETRY_MS)`가 `explicit` 소실 → 명시 저장의
     재시도 성공이 체크포인트를 안 옮김. `() => saveNow(opts)`(양 표면).
   - ⑦ **현상 유지** — 복구 다이얼로그 X·Esc·오버레이 닫기 = "이어서 편집" 간주(비파괴 기본값 · 세션
     내 [취소]로 원본 복귀 가능 · 손실 경로 없음).
   - ⑧ **문서화만** — 규약 C undo 조항에 명시 추기(코드 무변경).
   - ⑨ **수정** — [저장]/[취소] 높이 약 22px(`text-xs py-0.5`) → FB-13과 합쳐 처리(규약 A 추기).
3. **후속 소수정** = ⑤·⑥·⑨(+FB-13) + FB-12 → 브랜치 `fix/stage-39-followup`(별도 stage 번호 없음 ·
   머지 후 머리말 상태 줄 갱신).

### G-7 — 문서 반영 내역 (2026-08-24)

본 문서(머리말·체크박스·완료 기록) · screens **§5.16 실측 재개정 + §5.3 포인터 1줄 = Design v1.46** ·
별지 `editor-v2.plan.md` §13 FB-2 행·머리말 종결 추기 · 마스터 `study-app.plan.md` **v0.54** 이력 1줄 ·
`CLAUDE.md` 머리말·문서 지도 · `docs/manual/user-manual.html`([저장]/[취소]·자동저장 관계·크래시 복구·
복구 재질문 안내).
